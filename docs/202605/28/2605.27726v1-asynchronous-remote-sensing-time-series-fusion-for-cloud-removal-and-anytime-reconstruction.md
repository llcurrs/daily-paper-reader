---
title: Asynchronous Remote Sensing Time-Series Fusion for Cloud Removal and Anytime Reconstruction
title_zh: 异步遥感时间序列融合用于去云和任意时刻重建
authors: "Forouzan Fallah, Chia Yu Hsu, Wenwen Li, Anna Liljedahl, Yezhou Yang"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27726v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 融合S1 SAR和S2光学传感器进行云去除和任意时刻重建
tldr: "频繁云覆盖限制了Sentinel-2光学时序的使用，Sentinel-1 SAR可提供全天候补充观测，但二者采集异步且不规则，现有方法需预对齐且仅重建观测时刻，难以应对长间隙和按需合成。提出AGFlow，一种时空流匹配模型，通过时间戳条件内部对齐融合异步S1和含云S2，无需预处理配对；联合时空上下文去噪模拟空间结构与时间动态；支持任意时刻查询，在监测窗口内生成无云帧。在RESTORE-DiT基准上，全缺失帧重建MAE和RMSE降低16-19%，去云性能持平，并实现可靠的长间隙重建与灵活时间查询，助力密集植被监测等下游任务。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27726v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1809, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27726v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 864, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27726v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1802, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27726v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1803, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27726v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1524, \"height\": 763, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27726v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 870, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27726v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 872, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27726v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1133, \"height\": 264, \"label\": \"Table\"}]"
motivation: 现有S1/S2融合方法需预对齐且只恢复观测时刻，无法处理长间隙和按需合成。
method: 提出AGFlow，利用时间戳条件内部对齐融合异步数据，联合时空去噪建模空间-时间动态，支持任意时刻查询。
result: "RESTORE-DiT基准上，全缺失帧重建MAE和RMSE降低16-19%，去云性能竞争，长间隙重建可靠。"
conclusion: AGFlow实现高效异步融合与灵活时间查询，适用于密集植被监测等场景。
---

## 摘要
频繁云层覆盖严重限制了Sentinel-2（S2）光学时间序列在地表监测中的可用性。Sentinel-1（S1）SAR提供了全天候的补充观测，但由于数据采集不规则且异步，实际的S1/S2融合仍然困难。许多现有方法假设时间对齐的输入（或需要外部最近日期匹配），且通常仅重建观测到的时间戳，限制了长间隙下的重建能力，并阻碍了按需合成。我们提出了AGFlow（时间对齐生成流匹配），一种用于S1/S2去云和时间序列重建的时空流匹配模型，具有三个能力：（1）时间戳条件内部对齐，融合异步S1和有云S2观测，无需基于预处理的配对；（2）时空上下文感知去噪，联合建模空间结构与时间动态（而非独立的逐像素时间序列）；（3）任意时刻查询，能够在监测窗口内的观测时间戳和用户指定时间戳生成无云S2帧。我们在RESTORE-DiT基准协议上进行评估，包括定量指标、定性比较和组件消融。AGFlow在完全缺失帧重建方面取得了显著改进（MAE和RMSE相较于RESTORE-DiT降低16-19%），并在持续间隙下提供可靠重建，同时具有竞争性的去云性能和灵活的时间查询能力，适用于密集植被监测等下游任务。

## Abstract
Frequent cloud cover severely limits the usability of Sentinel-2 (S2) optical time series for Earth surface monitoring. Sentinel-1 (S1) SAR provides all-weather complementary observations, but practical S1/S2 fusion remains difficult because acquisitions are irregular and asynchronous. Many existing approaches assume temporally aligned inputs (or require external nearest-date matching) and typically restore only observed timestamps, limiting reconstruction under long gaps and preventing on-demand synthesis. We propose AGFlow (Time Aligned Generative Flow Matching), a spatiotemporal flow-matching model for S1/S2 cloud removal and time-series reconstruction with three capabilities: (1) timestamp-conditioned internal alignment that fuses asynchronous S1 and cloudy S2 observations without preprocessing-based pairing; (2) spatiotemporal, context-aware denoising that models spatial structure jointly with temporal dynamics (rather than independent per-pixel time series); and (3) anytime querying, enabling generation of cloud-free S2 frames at both observed and user-specified timestamps within the monitoring window. We evaluate on the RESTORE-DiT benchmark protocol with quantitative metrics, qualitative comparisons, and component ablations. AGFlow notably improves fully missing-frame reconstruction (MAE and RMSE reduce by 16-19% over RESTORE-DiT) and provides reliable reconstructions under persistent gaps, while also yielding competitive cloud removal performance and flexible temporal querying for downstream tasks such as dense vegetation monitoring.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文核心问题与整体含义（研究动机和背景）
- **核心问题**：频繁云覆盖严重限制了Sentinel-2（S2）光学遥感时间序列的可用性，Sentinel-1（S1）SAR虽能提供全天候互补观测，但二者采集时间异步且间隔不规则。现有方法大多假设输入已时间对齐（或依赖外部最近日期匹配），且仅能重建观测时刻的帧，无法处理长期间隙，也不能按需合成用户指定时刻的无云图像。
- **研究动机**：解决异步、不规则采样的多模态遥感时间序列融合难题，实现云去除与任意时刻重建，支持密集植被监测等下游任务。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出AGFlow（Time-Aligned Generative Flow Matching），一种基于时间戳条件内部对齐的时空流匹配模型，将云去除和任意时刻生成统一为掩码重建问题。
- **关键技术细节**：
  - **统一掩码重建框架**：对于云/传感器间隙去除，使用像素级掩码（m=1表示缺失）；对于任意时刻生成，在训练时对查询帧设置全帧掩码（mq=1），模型需从剩余上下文推断完整帧。输出时观测像素被固定（m=0对应零速度）。
  - **内部时间戳条件对齐**：通过日期编码（绝对日期与查询相对日期）和相对时间偏差（B_Δd）在注意力机制中嵌入时间信息，无需预处理配对。
  - **时间对齐交叉注意力（TA-CA）**：包含空间交叉注意力和时间交叉注意力。空间交叉注意力中光学块注意力对准SAR块（同网格）；时间交叉注意力沿时间轴重塑补丁序列，并加入跨传感器日差学习偏置B_S2←S1_Δd，以匹配最相关的SAR时刻。
  - **掩码流匹配**：定义线性路径 y_τ = (1-τ)y + τϵ，目标速度 v* = ϵ - y。损失函数仅计算掩码像素上的MSE：L = E[∥m⊙(v_θ(z_τ, τ, cond) - (ϵ-y))∥²_F]。推理时从噪声开始沿ODE反向积分，观测像素冻结。
  - **序列去噪Transformer（SDT）**：将时空补丁嵌入为token，加入2D空间正弦编码、时间偏置和RoPE，使用AdaLN条件化流时间τ。

## 3. 实验设计
- **数据集**：采用RESTORE-DiT基准的PASTIS-R数据集（法国区域），包含2433个非重叠128×128补丁，时间跨度2018年9月至2019年11月，每个序列38~61个S2观测和约70个S1观测。使用官方5折划分（4折训练，1折测试）。
- **模拟设置**：训练时从真实云掩码池采样并叠加到无云序列上，模拟真实缺失模式。
- **评估任务**：
  - 缺失帧重建（随机移除一整帧，从上下文重建）
  - 云/传感器间隙去除（像素级掩码）
  - 任意时刻生成（查询未观测时刻，采用RapidAI4EO的NDVI区域统计评估）
- **对比方法**：Linear、U-TILISE、U-TILISE-SAR、RESTORE-DiT（扩散基线）。所有方法在相同分割上从零训练。
- **评估指标**：MAE、RMSE、SAM、PSNR、SSIM（仅对缺失/遮挡像素计算）。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量、训练时长等具体算力信息。仅提到训练使用Adam优化器（lr=2×10⁻⁴，batch size=16），训练3000个epoch，采用MultiStepLR调度。训练平台为Google Cloud Platform和Arizona State University研究计算资源。

## 5. 实验数量与充分性
- **实验组数**：
  - 缺失帧重建对比表（Table 1）：对比RESTORE-DiT与AGFlow（2种方法）
  - 云去除对比表（Table 2）：共5种方法（Linear、U-TILISE、U-TILISE-SAR、RESTORE-DiT、AGFlow）
  - 任意时刻定性图（Fig.4）+ NDVI区域趋势图（Fig.5）
  - 消融实验（Table 3）：4个变体（空间仅SAR融合、去除相对时间偏置、去除查询相对时间、完整模型）
- **充分性与客观性**：
  - 采用标准基准（RESTORE-DiT协议）和官方数据划分，确保公平比较。
  - 消融实验系统隔离各组件贡献，结论清晰。
  - 但任意时刻生成缺乏直接定量指标（因无真实参考），采用间接NDVI区域统计评估，有一定合理性但不够严格。
  - 仅在法国一个区域评估，未覆盖其他气候/植被带，泛化性有待验证。

## 6. 主要结论与发现
- AGFlow在缺失帧重建上显著优于RESTORE-DiT：MAE降低16.4%，RMSE降低18.9%，PSNR和SSIM也提升。
- 云去除任务上，AGFlow在所有指标上达到最佳（MAE 0.0133 vs 0.0140，SAM 2.45 vs 2.64），但提升幅度小于缺失帧任务。
- 消融实验表明时间对齐的SAR融合（TA-CA）最重要，去除后MAE上升0.0011，SAM上升0.62；相对时间偏置次之；查询相对时间带来额外收益。
- 任意时刻生成在区域NDVI趋势上与辅助参考RapidAI4EO一致，证明模型能产生合理的季节动态。

## 7. 优点
- **创新性**：首次将流匹配与时间戳内部对齐结合用于异步多模态遥感序列融合，统一了云去除与任意时刻生成。
- **实用性**：无需预处理配对，降低工作流复杂度；支持按需合成，增强下游灵活性（如密集植被监测）。
- **建模能力**：时空联合建模（非逐像素）保留空间结构，扩散采样提供不确定性表征。
- **实验严谨**：使用标准基准、公平对比、系统消融，结果可靠。

## 8. 不足与局限
- **空间配准假设**：依赖S1与S2的精确配准，实际中配准误差可能降低融合质量。
- **固定时间窗口**：使用固定长度窗口（T=15），超长时序依赖可能截断。
- **地理泛化性**：仅在法国区域训练和测试，不同气候/地表类型需重新训练和标定。
- **任意时刻评估不足**：缺乏像素级定量验证（如查询时刻的真实S2参考），依赖区域NDVI趋势，证据强度有限。
- **计算资源细节缺失**：未报告GPU型号、数量、训练时间，可复现性受影响。
- **仅对比了单个扩散基线（RESTORE-DiT）**，未包括更多近期方法（如EMRDM、AnytimeFormer等），对比广度可提升。

（完）
