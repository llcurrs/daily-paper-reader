Title: Leveraging Previous-Traversal Point Cloud Map Priors for Camera-Based 3D Object Detection and Tracking

URL Source: https://arxiv.org/pdf/2604.25405v1

Published Time: Wed, 29 Apr 2026 00:52:16 GMT

Number of Pages: 9

Markdown Content:
# Leveraging Previous-Traversal Point Cloud Map Priors for Camera-Based 3D Object Detection and Tracking 

Markus Käppeler 1, Özgün Çiçek 2, Yakov Miron 2,3, and Abhinav Valada 1

Abstract — Camera-based 3D object detection and tracking are central to autonomous driving, yet precise 3D object localization remains fundamentally constrained by depth ambiguity when no expensive, depth-rich online LiDAR is available at inference. In many deployments, however, vehicles repeatedly traverse the same environments, making static point cloud maps from prior traversals a practical source of geometric priors. We propose DualViewMapDet , a camera-only inference framework that retrieves such map priors online and leverages them to mitigate the absence of a LiDAR sensor during deployment. The key idea is a dual-space camera–map fusion strategy that avoids one-sided view conversion. Specifically, we (i) project the map into perspec-tive view (PV) and encode multi-channel geometric cues to enrich image features and support BEV lifting, and (ii) encode the map directly in bird’s-eye view (BEV) with a sparse voxel backbone and fuse it with lifted camera features in a shared metric space. Extensive evaluations on nuScenes and Argoverse 2 demonstrate consistent improvements over strong camera-only baselines, with particularly strong gains in object localization. Ablations further validate the contributions of PV/BEV fusion and prior-map coverage. We make the code and pre-trained models available at https://dualviewmapdet.cs.uni-freiburg.de .

I. I NTRODUCTION 

Camera-based 3D object detection and tracking is a core perception capability for autonomous driving, enabling the localization and tracking of dynamic agents for downstream prediction and planning. Many recent driving systems rely on object-centric scene representations, often combined with sparse maps, as an interface for interaction modeling and decision making [1]. While LiDAR-only [2], [3] and camera-LiDAR fusion methods [4]–[7] achieve impressive performance leveraging precise metric depth and 3D geom-etry, they require expensive LiDARs at inference that limit scalability. This motivates camera-only 3D detection [8], [9], which uses inexpensive multi-view cameras that provide rich semantic cues, but remains fundamentally challenged by depth ambiguity. Errors in depth estimation directly degrade 3D box localization [10]. This limitation affects both methods operating in the perspective view (PV) [8], [11]–[14] and approaches that lift image features into a bird’s-eye view 

(BEV) [10], [15]–[17], since reliable BEV lifting requires accurate depth estimates. A promising direction to reduce depth ambiguity is to incorporate geometric priors beyond the current observa-tion. In many real-world deployments, vehicles operate in  

> 1

Department of Computer Science, University of Freiburg, Germany.  

> 2

Bosch Research, Robert Bosch GmbH, Renningen, Germany.  

> 3

University of Haifa, Israel This research was funded by Bosch Research as part of a collaboration between Bosch Research and the University of Freiburg on AI-based automated driving. Encoding &  

> Fusion
> Detection & Tracking
> Transformer Head
> Query at
> ego location
> LiDAR scans from previous
> traversals of the same area
> 3D Object
> Detection &
> Tracking
> Multi-View
> RGB Images
> Global Point Cloud Map

Fig. 1: Overview of our camera-map fusion framework. Given multi-view camera images and the current ego location, we retrieve a local static LiDAR point cloud map constructed from previous mapping traversals of the same area. The map is encoded in both perspective view (PV) and bird’s-eye view (BEV) and fused with the corresponding image features in each representation. A transformer-based head then predicts 3D bounding boxes and object tracks from the fused PV/BEV features. 

environments that have been traversed before by a mapping vehicle, making prior maps available. In particular, 3D LiDAR point cloud maps constructed from earlier traversals provide accurate geometry of the static surroundings at the same metric scale as the detection targets. Such priors can anchor object depth relative to nearby static structure (e.g., road surface, curbs, signs) and provide spatial context that helps disambiguate object localization even when objects are far away or partially occluded (Fig. 2). Importantly, unlike camera-LiDAR fusion, these maps encode only the static 

environment and thus must be exploited as a prior rather than as a source of current dynamic measurements. In this paper, we show that a prior point cloud map of the static environment, which is retrieved at the ego location, can substantially improve camera-based 3D object detection and tracking while preserving camera-only inference. Fig. 1 shows an overview of our camera–map fusion framework. We address the representation gap between sparse 3D geometry and dense multi-view image features with a dual-space camera-map fusion architecture. First, we project the retrieved map onto each camera and construct multi-channel PV map encodings that include sparse depth, depth positional embeddings, and ego-frame point coordinates. This makes image features explicitly depth-aware for the head and eases depth estimation for BEV lifting. Second, we encode the map directly in BEV using a sparse voxel backbone and fuse it with lifted camera features in a shared metric BEV space, which we find crucial for fully exploiting map priors to achieve accurate object localization. Our approach, DualViewMapDet ,  

> arXiv:2604.25405v1 [cs.CV] 28 Apr 2026 Without Point Cloud Map Prior With Point Cloud Map Prior
> Scene 1 Scene 2 Fig. 2: Map priors reduce depth ambiguity. Compared to purely image-based 3D object detection (left), incorporating a static point cloud map provides accurate geometric anchors in the scene, improving 3D box localization for dynamic objects (right). Top-down BEV detections of cars are shown in light blue, detections of pedestrians in dark blue, and ground truth in gray. The ego car is at the center of the circles. In the background, we visualize BEV activations from a single channel that responds strongly to cars. Activations with a map prior are less distorted along the depth.

consistently improves strong camera-only methods on large-scale benchmarks such as nuScenes [18] and Argoverse 2 [19], with particularly strong improvements in object translation accuracy, highlighting the value of map priors for precise 3D perception without requiring LiDAR at test time. To summarize, our main contributions are: 1) A camera-only inference framework that leverages prior point cloud maps from previous traversals to reduce depth ambiguity and improve 3D object localization, without requiring LiDAR at test time. 2) We propose multi-channel perspective-view map encod-ings of the projected point cloud prior (e.g., sparse depth with validity masks, depth positional embeddings, and ego-frame geometry cues) to inject geometric priors into image features, supporting depth estimation and detection. 3) We design, for the first time, a dual-space fusion mechanism that combines camera and map information in PV, and additionally fuses lifted camera BEV features with a sparse voxel-encoded BEV map prior in a shared metric BEV space. This enables effective exploitation of map geometry for accurate 3D object detection and resolving design decisions in autonomous driving architectures, i.e., how to use map priors and how the data flow should be from PV and BEV views. 4) A image–map grid masking strategy, which further improves robustness to missing or imperfect map priors. II. R ELATED WORK 

We review prior work on 3D object detection and the use of geometric priors from maps of previous traversals, focusing on camera-based detection, multi-modal fusion, and map priors to improve 3D perception tasks. 

A. 3D Object Detection 

3D object detection predicts 3D bounding boxes from sensor data such as multi-view cameras, LiDAR, or radar. Key challenges include multi-view fusion, temporal modeling, and multi-modal fusion across heterogeneous data representations. 

LiDAR-Based Detection: LiDAR provides accurate geometry and depth, but is sparse at long range. Voxel-based detectors encode point clouds using sparse 3D convolutions, as popular-ized by SECOND [20], and many approaches detect objects from BEV features such as CenterPoint [2]. These designs highlight the effectiveness of BEV as a metric representation for detection and motivate our use of a sparse voxel-based BEV encoder to encode the point cloud map prior. 

Camera-Based Detection: Camera-only methods provide rich semantic and long-range cues, but suffer from depth ambiguity [21]. Most approaches fall into dense BEV-based methods that lift image features into a unified BEV grid [10], [15]–[17] and sparse query-based methods that refine a set of 3D object queries by cross-attending to multi-view image features [8], [11]–[14]. BEV-based methods use attention-based view transformation (e.g., BEVFormer [15]) or LSS-style lifting [16], [17], where depth estimation is the bottleneck. Thus, supervising depth with LiDAR improves lifting and accuracy [10]. Query-based approaches avoid dense BEV features by directly aggregating image features at projected 3D reference points [11], [13], [14] and often perform temporal query propagation [8], [12]. We build on the strong query-based head design of Sparse4Dv3 [8], but explicitly inject geometric priors from a point cloud map to address depth ambiguity, and fuse these map priors in both PV and BEV to leverage complementary representations. 

Camera–LiDAR Fusion: Camera-LiDAR fusion combines complementary strengths [22] where cameras provide seman-tics and high-resolution cues, while LiDAR provides metric geometry. Many methods fuse in BEV as a shared metric rep-resentation. TransFusion [23] injects image cues into LiDAR query-proposals via cross-attention, while BEVFusion [4] lifts images to BEV and fuses them with LiDAR BEV features before box decoding. Subsequent work improves view transformation and fusion robustness, often using LiDAR to guide lifting and alignment [5], [6], [24], [25]. While highly effective, these methods require LiDAR sensor data at inference. In contrast, we target camera-only inference while leveraging a prior point cloud map from previous traversals. We enhance PV image features with multiple map-derived encodings to support both the detection head and the LSS depth network, and additionally fuse map features with camera features in BEV to better exploit metric structure. 

3D Multi-Object Tracking: Most modern 3D multi-object tracking approaches are built on top of strong 3D detectors, often by reusing their box predictions and adding temporal association or propagation modules [8], [12], [26]. B. Geometric Prior Maps for 3D Perception Prior Maps for 3D Occupancy: Several works use priors from previous traversals for occupancy prediction, e.g., NeRF-based maps [27] and global occupancy maps [28]. However, occupancy is voxelized and coarse, whereas 3D object detection requires precise continuous object localization. 

Multi-Task Geometry for Detection: Other approaches couple detection and occupancy estimation to enrich geometric super-vision and fine-grained spatial structure [29], [30]. Although beneficial, these methods are not primarily optimized for state-of-the-art 3D detection accuracy. 

Map Priors for Detection: Beyond occupancy, prior traversals have been exploited to build maps that support detection, including multi-traversal consensus to detect dynamic content in reconstructed scene representations [31] and reusing LiDAR maps for LiDAR-based online detection [32]. Most closely related, AsyncDepth [33] improves camera-based 3D detection by projecting an aggregated historical point cloud into per-camera depth maps and concatenating them with image features in PV. However, PV-only fusion is limited by the representational gap between sparse 3D geometry and image features (see Tab. V), and does not explicitly leverage BEV as a metric fusion space. III. T ECHNICAL APPROACH 

We propose DualViewMapDet , a camera-only framework for 3D object detection and tracking augmented with an offline static point cloud prior map from previous traversals (Fig. 3). At inference, DualViewMapDet requires only multi-view RGB images, calibrated cameras, a globally localized ego pose, and an online retrieval of a local static map patch. No live LiDAR sensor is used. The central challenge is to exploit a 3D geometric map prior for precise 3D box localization while preserving the semantic richness of image features. A static point cloud map can anchor depth and provide metric context, but using it effectively is non-trivial due to the mismatch between sparse 3D geometry and dense image features. PV-only fusion lacks an explicit metric space where nearby pixels may correspond to distant 3D locations, limiting geometric context aggregation, particularly under occlusions. BEV-only fusion enables metric reasoning but can lose fine PV semantics during view transformation and does not directly assist the depth estimation required by LSS-based lifting [16]. We therefore adopt a dual-space camera-map fusion architecture: (i) we project the map into PV to build multi-channel geometric encodings aligned with each image, making PV features explicitly depth-aware and easing depth estimation for lifting; (ii) we also encode the map in BEV using a sparse voxel backbone to exploit metric structure for localization. Then, we fuse the map features projected to PV with camera features in PV, and lift it to BEV. (iii) Finally, we fuse the lifted fused features in BEV with the map encoded in BEV, and decode boxes with a transformer head whose queries sequentially attend to fused BEV and fused PV features. 

A. Problem Setup 

At time t, we observe synchronized surround-view images 

{Ict }Nc=1 with known intrinsics Kc and extrinsics TE→Cc (ego-to-camera). We assume global ego localization T GEt ∈ SE (3) .We are given an offline-built global static point cloud map 

MG = {pG} from previous traversals that contains only static structure. The goal is to predict a set of oriented 3D boxes in the ego frame: 

Bt = {bti}Mi=1 , bti = ( x, y, z, w, h, l, yaw , v), (1) where (x, y, z ) is the center, (w, h, l ) are dimensions, and v

denotes velocity. 

B. Prior Map Construction and Online Retrieval 

We build MG offline by accumulating LiDAR sweeps from other traversals and removing dynamic points using 3D boxes. To enable efficient retrieval, we store the map in spatial tiles of size L × L meters. At inference, given ego translation tGEt and perception range R, we retrieve a local patch 

PGt = pG ∈ M G ∥(pGx , p Gy ) − (tGx , t Gy )∥∞ ≤ R . (2) To avoid leakage, we exclude points originating from the current traversal and traversals too close in time. We merge re-trieved points, remove noise via statistical outlier removal [34], and downsample by average voxel pooling with voxel size 

0.4 m for efficiency. Finally, we transform the patch into the current ego frame: 

PEt = pE = ( T GEt )−1pG pG ∈ P Gt . (3) This ego-frame static map prior PEt is the only map input to DualViewMapDet at test time. 

C. Dual-Space Map Encoding 

We bridge sparse 3D map geometry and dense image features by encoding the retrieved map in both PV and BEV. 

1) Multi-view Image Features: We extract multi-scale PV features from each camera image using an image backbone: 

Fc,s  

> img

= ϕs

> img

(Ict ), s ∈ S , (4) where S denotes backbone feature scales. 

2) PV Map Rasterization and Multi-Channel Encodings: 

We project the retrieved ego-frame map patch PEt into each camera using the known intrinsics and extrinsics to obtain a sparse depth map and additional geometric cues aligned with the image plane. We rasterize a sparse depth image Dct ∈

RH×W using a z-buffer rule: if multiple projected points fall into the same pixel, we keep the closest depth. We also build a validity mask M ct ∈ { 0, 1}H×W , where M ct (u, v ) = 1 iff at least one map point projects to pixel (u, v ). Since the prior map is sparse, this mask enables the network to distinguish reliable geometric anchors from uncovered pixels. PV Map      

> Encodings
> Grid Mask Fusion
> Image
> Encoder
> Detection &
> Tracking
> Transformer
> Head 3D Object
> Detection &
> Tracking
> Multi-Scale
> PV Fusion
> Point Cloud Map
> at ego location
> PV Point
> Cloud
> Encoder
> BEV Point
> Cloud
> Encoder
> BEV
> Fusion
> Lifting to
> BEV
> FPN
> BEV
> Encoder
> Encoding
> Multi-View
> RGB Images
> Global Point Cloud Map
> Depth
> XYZ
> Spread
> Projection
> to PV

Fig. 3: Overview of DualViewMapDet . Given multi-view RGB images and a local static point cloud map patch retrieved at the current ego location, we employ image-map grid masking during training to improve robustness (omitted in PV map encodings). The images are encoded into multi-scale PV features, while the map is encoded in perspective view by projecting it into each camera to form multi-channel PV map encodings and in bird’s-eye view into a BEV feature grid. We then fuse camera and map features in PV, lift the fused PV features to BEV, and fuse them with the BEV map features in a shared metric space. Finally, a transformer-based head with BEV-PV deformable aggregation decodes 3D bounding boxes from the fused PV and BEV representations. 

Ego-frame coordinate channels: For pixels with a valid point, we store its ego-frame coordinates pE = ( x, y, z )

as three additional channels normalized by the perception range. This makes PV features explicitly aware of metric 3D structure and the ego-centric coordinate system in which boxes are regressed, providing a direct cue for localization beyond depth alone (e.g., height via z). 

Depth positional embedding: We embed normalized depth dnorm = d/d max , where d = Dct (u, v ), via a sinusoidal mapping (positional encoding) [35], [36] into Φ( dnorm ).

Nearest-valid depth spread and distance-to-valid: Sparse depth leaves large uncovered regions. We therefore add two channels that (i) spread the nearest valid depth within a bounded pixel radius r and (ii) encode uncertainty via the (clipped and normalized) distance to the nearest valid pixel. Concretely, for each pixel we copy the depth value dnorm of the nearest valid pixel to dnear if it lies within radius r (otherwise we set the spread depth to zero), and we additionally store the normalized distance-to-valid δnorm ∈ [0 , 1] .

Final PV map tensor and encoder: We concatenate all cues to form a multi-channel PV map tensor: 

Xct (u, v ) = 

h

dnorm , M ct , Φ( dnorm ), pE

> norm

, d near , δ norm 

i

∈ RCm ,

(5) and encode it with a lightweight CNN (ResNet-18 [37]) into multi-scale PV map features: 

Fc,s  

> map

= ϕs

> pvmap

(Xct ). (6) 

3) BEV Map Encoding via Sparse Voxels: We additionally encode the retrieved map PEt directly in BEV using a sparse voxel backbone [20]. We use a voxel size of 0.2 m and compute per-voxel features as the mean 3D coordinate (mean 

x, y, z ) of points within the voxel. A sparse 3D convolutional encoder then produces a dense BEV feature grid: 

Fmap BEV = ϕbevmap 

 Voxelize( PEt ) ∈ RY ×X×Cb . (7) 

D. Dual-Space Camera-Map Fusion 

We fuse camera and map information in PV to support depth reasoning and PV decoding, then fuse again in BEV to exploit metric structure. 

1) PV Fusion and Post-Fusion FPN: At each backbone scale s, we fuse image and PV map features by concatenating along the channel dimension and applying a 3 × 3 Conv (with BN and ReLU) to reduce channels: 

Fc,s  

> PV

= ψs

> PV

h 

Fc,s  

> img

∥ Fc,s 

> map

i 

, (8) where [·∥· ] denotes channel-wise concatenation and ψs

> PV

denotes the 3 × 3 Conv block. We then employ an FPN on top of {Fc,s  

> PV

}s∈S to fuse multi-scale information and obtain the final fused PV pyramid used by the head and the BEV lifting module. 

2) Depth-Aware BEV Lifting: We lift fused PV features into BEV using an LSS-style module [16], [17]. The module predicts a per-pixel discrete depth distribution Pc

> depth

(u, v ) ∈

[0 , 1] D and forms frustum features via an outer product: 

Fc

> frustum

(u, v ) = Pc

> depth

(u, v ) ⊗ Fc

> PV

(u, v ) ∈ RD×C . (9) Unprojected frustum features from all cameras are pooled into a BEV grid, yielding Fcam BEV . Using fused PV features makes depth estimation easier as the map provides accurate geometric anchors in static regions, thereby reducing lifting ambiguity for nearby dynamic objects (via surrounding context) and improving BEV alignment. 

3) BEV Fusion: We fuse lifted camera BEV features with BEV map prior features in the shared metric space by concatenating along the channel dimension and applying a 3 × 3 Conv (with BN and ReLU): 

FBEV = ψBEV 

  Fcam BEV ∥ Fmap BEV 

 , (10) followed by a lightweight BEV encoder (ResNet blocks + small FPN) to refine and distribute fused context. This BEV fusion is critical for precise 3D object localization because it enables reasoning in a metric space where distances and neigh-borhood relations correspond to 3D geometry—something PV-only fusion cannot provide. 

E. Detection Head with BEV-PV Deformable Aggregation 

We adopt the detection head from DualViewDistill [9], which builds upon Sparse4Dv3 [8]. The head maintains Mobject queries consisting of (i) 3D anchors and (ii) instance features: 

A ∈ RM ×11 , F ∈ RM ×C , (11) where each anchor encodes center, dimensions, yaw (as 

sin / cos ), and velocity:             

> ai= ( x, y, z, ln w, ln h, ln l, sin yaw ,cos yaw , v x, v y, v z).(12)

Sequential BEV then PV aggregation.: In each decoder layer, queries fi interact with both BEV and PV features via deformable aggregation over a set of 3D keypoints sampled on the anchor box. Each keypoint is (i) projected into the BEV grid to bilinearly sample FBEV , and (ii) projected into each camera to bilinearly sample multi-scale fused PV features 

{Fc,s  

> PV

}. We fuse modalities sequentially: 

f ′ 

> i

= fi + BEV-DeformAgg (fi, ai, FBEV ) ,

f ′′  

> i

= f ′ 

> i

+ PV-DeformAgg (f ′ 

> i

, ai, {Fc,s  

> PV

}) , (13) where BEV aggregation injects metric context (including the BEV map prior) and PV aggregation injects fine-grained semantic details from fused PV features. This ordering aligns with our motivation: BEV provides spatially grounded cues for localization, while PV refines object-centric semantics and details that may be attenuated in BEV. 

F. Image–Map Grid Masking 

To improve robustness to missing or imperfect priors and avoid overfitting to map cues, we employ grid masking augmentation [38] to (i) RGB images, (ii) PV map tensors 

Xct , and (iii) the BEV map prior by removing all map points whose BEV coordinates fall into masked ground regions. This improves robustness to missing or imperfect map priors and avoids overfitting to the map. 

G. Training Objective 

We train DualViewMapDet end-to-end. The map prior is learned to be exploited only through detection gradients . The total loss is 

Ltotal = λdet Ldet + λdepth Ldepth , (14) where Ldet is the standard detection loss of the Sparse4D head (classification + box regression + orientation/auxiliary terms) [8]. Ldepth supervises the LSS depth estimator with LiDAR depth labels [10], [17] during training, while inference remains camera-only sensing augmented with the retrieved static prior map. IV. E XPERIMENTAL EVALUATION 

We evaluate DualViewMapDet on large-scale autonomous driving datasets to quantify the benefit of prior point cloud maps for camera-based 3D object detection and tracking. Our experiments are designed to (i) compare against strong camera-only baselines under standard dataset evaluation protocols, and (ii) isolate key design choices in map encoding, fusion location (PV vs. BEV), and robustness to incomplete or missing map priors. Unless noted otherwise, we report the official dataset metrics and follow common training and evaluation conventions to ensure comparability. 

A. Implementation and Training Details 

Both the image backbone and the PV map encoder produce multi-scale features at scales {4, 8, 16 , 32 }. For the BEV branch, we use a BEV grid of 128 × 128 . BEV features from the map encoder and the lifted PV features share the same spatial resolution. For BEV lifting, we lift stride-8

PV features for smaller input resolutions (nuScenes and Argoverse 2 ablations) and stride-16 PV features for the higher-resolution Argoverse 2 model. On Argoverse 2, we use VoVNet-99 [39] initialized from FCOS3D pretraining on nuScenes, and ResNet-50 [37] initialized from ImageNet, following our baselines. The PV map encoder is a lightweight ResNet-18 initialized from ImageNet. For optimization, we train all models for 80 epochs with AdamW, using cosine learning-rate decay with a 500-iteration warm-up and a base learning rate of 2 × 10 −4. We use a batch size of 24 and train with a sequential iteration strategy. We apply both image-level and BEV/3D augmentations as in [9], and supervise the LSS depth estimator with LiDAR depth during training while keeping inference camera-only. For Argoverse 2 long-range evaluation at 150 m , we increase the number of object queries from 900 to 1800 to cover the larger range. To reduce memory usage, we keep the BEV grid range at 50 m and rely on PV features beyond the BEV extent. 

B. Datasets 

We evaluate on nuScenes [18] and Argoverse 2 [19], which provide multi-view surround camera rigs and LiDAR sensor data. Both datasets contain repeated coverage of urban areas across many sequences, enabling the construction of prior point cloud maps from other sequences. To avoid any leakage from evaluation data, we construct training maps exclusively from the training split. For evaluation, we use maps from the training and validation splits, but exclude the map of the current traversal. For nuScenes , we follow the official protocol and report NDS and mAP as the primary detection metrics, together with the true-positive errors (mATE, mASE, mAOE, mAVE, mAAE). For multi-object tracking, we report AMOTA as the primary metric and the standard tracking metrics. For Argoverse 2 , we report the official Composite Detection Score (CDS) and mAP, together with the standard true-positive errors (mATE, mASE, mAOE) across the 26 object categories. 

C. Global Ego Pose Correction 

Accurate global alignment across traversals is crucial for map retrieval. Even small cross-sequence offsets can corrupt the projected PV depth cues and degrade BEV fusion. Argoverse 2 provides 6-DoF ego poses within each sequence, but we observe cross-sequence global misalignments of up to a few meters. We therefore refine global poses by performing point cloud matching between overlapping sequences using KISS-Matcher [40] and solving a pose graph optimization to obtain a globally geometrically consistent map prior. We will provide the corrected poses for future research. In contrast, nuScenes provides global poses with a fixed height component TABLE I: Long-range 3D object detection results on Argoverse 2 val set.                                                                                                                                                

> Method CDS ↑mAP ↑mATE ↓mASE ↓mAOE ↓
> PETR *[11] 0.122 0.176 0.911 0.339 0.819 StreamPETR *[12] 0.146 0.203 0.843 0.321 0.650 RayDN *[14] 0.161 0.223 0.825 0.325 0.629 Far3D *[13] 0.181 0.244 0.796 0.304 0.538 Sparse4Dv3 *‡ [8] 0.186 0.247 0.692 0.302 0.558 Sparse4Dv3 §‡ [8] 0.182 0.244 0.726 0.307 0.548 DualViewMapDet §0.202 0.263 0.667 0.291 0.515
> We evaluate within a range of 150 meters. TABLE II: Near-range 3D object detection results on Argoverse 2 val set.
> Method CDS ↑mAP ↑mATE ↓mASE ↓mAOE ↓
> Far3D *[13] 0.281 0.367 0.730 0.300 0.531 Sparse4Dv3 *‡ [8] 0.294 0.381 0.706 0.314 0.666 Sparse4Dv3 §‡ [8] 0.288 0.376 0.694 0.298 0.547 DualViewMapDet §0.311 0.397 0.622 0.279 0.527
> We evaluate within a range of 50 meters. Tab. I and Tab. II: All methods use VoVNet-99 as the image encoder with an input image resolution of 640 ×960 .*: Training uses the 10 Hz Argoverse 2 sequences by splitting each sequence into five offset subsequences, yielding
> ≈5×more (but redundant) training samples than strict 2 Hz subsampling.
> §: Training with strict 2 Hz subsampling. ‡: Baselines trained by us with the code provided by the authors. TABLE III: 3D object detection results on the nuScenes val set.
> Method NDS ↑mAP ↑mATE ↓mASE ↓mAOE ↓mAVE ↓mAAE ↓
> BEVFormerv2 [15] †0.529 0.423 0.618 0.273 0.413 0.333 0.188 VideoBEV [41] 0.535 0.422 0.564 0.276 0.440 0.286 0.198 StreamPETR [12] 0.540 0.432 0.581 0.272 0.413 0.295 0.196 Sparse4Dv2 0.539 0.439 0.598 0.270 0.475 0.282 0.179 BEVNeXt [17] 0.548 0.437 0.550 0.265 0.427 0.260 0.208 Sparse4Dv3 *[8] 0.561 0.469 0.553 0.274 0.476 0.227 0.200 Sparse4Dv3 ‡[8] 0.553 0.447 0.541 0.262 0.500 0.225 0.179 DualViewMapDet 0.588 0.481 0.484 0.262 0.393 0.212 0.176
> All methods use ResNet-50 as the image encoder with an input image resolution of 256 ×704 . †: Uses pre-trained weights from the nuImage dataset. ‡: Baselines trained with the code provided by the authors. *: Results reported in the original paper but not reproducible.

(effectively 5-DoF), which introduces errors when projecting map points into PV on non-flat roads. Since this limitation is difficult to fix, we use the provided poses as-is for nuScenes and emphasize Argoverse 2 as our primary testbed. 

D. 3D Object Detection and Tracking 

We benchmark DualViewMapDet against strong camera-only baselines across datasets and ranges to assess the impact of augmenting camera-only inference with a retrieved static point cloud prior. On Argoverse 2, we compare at 150 m 

and 50 m ranges (Tab. I and Tab. II), and on nuScenes we report detection results in Tab. III and tracking results in Tab. IV. For fairness, we match backbones and image resolutions to the respective baselines. Across all settings, DualViewMapDet consistently improves the primary detec-tion metrics (CDS/NDS and mAP) over our main baseline Sparse4Dv3, and we observe high gains in box localization accuracy (lower mATE). These results align with our motiva-tion: prior static geometry provides metric anchors that reduce depth ambiguity and directly translate into improved 3D object localization. On nuScenes tracking, DualViewMapDet also                                          

> TABLE IV: 3D multi-object tracking results on the nuScenes val set.
> Method AMOTA ↑AMOTP ↓IDS ↓Recall ↑MOTA ↑MOTP ↓
> QTrack [42] 0.347 1.347 944 0.426 0.309 0.722 DORT [43] 0.424 1.264 -0.492 --Sparse4Dv3 *[8] 0.490 1.164 430 0.574 0.436 0.660 Sparse4Dv3 ‡[8] 0.440 1.184 403 0.525 0.401 0.622 DualViewMapDet 0.513 1.109 473 0.595 0.457 0.583
> All methods use ResNet-50 as the image encoder with an input image resolution of 256 ×704 . ‡: Baselines trained with the code provided by the authors. *: Results reported in the original paper but not reproducible.

improves AMOTA and reduces localization error along tracks (lower AMOTP), indicating that geometric priors can benefit not only detection but also object tracking. 

E. Ablation Studies 

We perform controlled ablations on Argoverse 2 to isolate which components are essential for effectively exploiting map priors in Tab. V and to assess robustness under imperfect map availability. Unless stated otherwise, all ablations use VoVNet-99 [39] pretraining with FCOS3D with image size 

480 × 704 and are evaluated on the Argoverse 2 validation split within a range of 50 meters. We highlight our final configuration in Tab. V and Tab. VII in gray. 

PV map encodings: We first study PV-only fusion by projecting the map onto each camera and concatenating the PV map features with image features, as proposed in [33]. A naïve PV depth input (depth + mask) does not yield a benefit on our used dataset, revealing that simply concatenating sparse depth is insufficient. Adding depth positional embeddings provides a small improvement, while introducing nearest-valid depth spreading substantially improves CDS/mAP and reduces mATE, indicating that making sparse geometry spatially usable in PV is critical. Augmenting the PV encoding with ego-frame coordinate channels (x, y, z ) further improves box localization. 

BEV map encoding: We evaluate BEV-only map usage by encoding the retrieved point cloud in BEV and attending to it in the head. Both pillar-based [27] and sparse-voxel [20] encoders are effective, but sparse voxel encoding achieves stronger results. 

Where to fuse: PV vs. BEV vs. both: Combining PV and BEV branches is not a trivial union of the best single-branch variants. Simply enabling both can underperform due to repre-sentation mismatch and training dynamics. In contrast, fusing in PV and explicitly lifting the PV-fused features to BEV for BEV fusion yields the strongest improvements, showing that BEV fusion in a shared metric space is essential to fully exploit map priors. This dual-space design also distinguishes our approach from PV-only prior-map fusion [33] by enabling metric neighborhood reasoning in BEV while retaining PV object-centric details via PV deformable aggregation. 

Map coverage and robustness: Not all validation sequences have a map prior. To quantify the dependence on coverage, we evaluate subsets of sequences with higher map overlap in Tab. VI, while also evaluating the map-free baseline on the same subsets to isolate the effect of the prior. Performance (a) Sparse4Dv3 (VoVNet-99) 

(b) DualViewMapDet (VoVNet-99) Fig. 4: Visualization of 3D object detection results on the Argoverse 2 val set. Our method shows substantially more accurate object box localization (orange) compared to Sparse4Dv3. Ground truths are colored in gray while predictions are colored by class: regular vehicle, pedestrian, box truck. TABLE V: Ablation study: Map encoding, fusion, and image-map grid masking. 

PV Map Encodings BEV Map Encoding Camera-Map Fusion Depth Depth Embed. Spread XYZ Pillar Voxel PV Fusion BEV Fusion Grid Mask CDS ↑ mAP ↑ mATE ↓

0.289 0.378 0.698 

✓ ✓ 0.282 0.374 0.681 

✓ ✓ ✓ 0.284 0.373 0.680 

✓ ✓ ✓ 0.293 0.382 0.675 

✓ ✓ ✓ ✓ ✓ 0.294 0.385 0.666 

✓ 0.297 0.386 0.660 

✓ 0.301 0.394 0.645 

✓ ✓ ✓ ✓ ✓ ✓ 0.287 0.378 0.658 

✓ ✓ ✓ ✓ ✓ ✓ ✓ 0.309 0.401 0.648 

✓ ✓ ✓ ✓ ✓ ✓ ✓ ✓ 0.309 0.401 0.645 The baseline is Sparse4Dv3 with an image resolution of 480 × 704 , VoVNet-99 pretrained with FCOS3D as the backbone, and with 3D/BEV data augmentations. TABLE VI: Results on subsets with higher prior-map coverage. 

Subset DualViewMapDet (map prior) Sparse4Dv3 (no map) CDS ↑ mAP ↑ mATE ↓ CDS ↑ mAP ↑ mATE ↓

All 0.309 (+ 2.0 pp) 0.401 0.645 0.289 0.378 0.698 Overlap k=1 0.345 (+ 3.0 pp) 0.443 0.586 0.315 0.408 0.636 Overlap k=2 0.336 (+ 3.7 pp) 0.429 0.607 0.299 0.388 0.670 Overlap k: a sequence is included iff every 50 m × 50 m tile it covers is also covered by at least k other sequences. The map-free baseline is evaluated on the same subsets to isolate the effect of the prior. Gains (green) are in percentage points (pp) for CDS. TABLE VII: Robustness to complete map drop at test time. 

Method CDS ↑ mAP ↑ mATE ↓ mASE ↓ mAOE ↓

DualViewMapDet – Grid Mask 0.278 0.368 0.710 0.293 0.622 DualViewMapDet 0.286 0.375 0.721 0.293 0.534 Sparse4Dv3 (map-free) 0.289 0.378 0.698 0.290 0.556 For DualViewMapDet variants, the prior-map input is removed at test time. The map-free baseline is unaffected. Grid Mask denotes applying grid-masking augmentation to the map input during training to discourage overreliance on the prior map. 

gains grow with coverage, confirming that DualViewMapDet is most effective when reliable priors are available. Finally, we evaluate robustness to complete map drop at test time in Tab. VII. Without map-specific regularization, performance can fall below the map-free baseline, indicating overreliance on priors. Applying image-map grid masking during training makes our DualViewMapDet competitive with the map-free baseline even when the prior is entirely missing at test time, while preserving the benefits when the map is available. 

F. Qualitative Results 

Fig. 4 presents qualitative comparisons of our DualViewMapDet with Sparse4Dv3, in both PV and BEV on Argoverse 2. In challenging scenarios with clutter and many occlusions, we observe that DualViewMapDet yields tighter 3D box localization and more precise box predictions along the depth for both distant and close objects. We also observe that objects such as cars align more closely with the lane layout. This supports our quantitative finding that leveraging prior static geometry reduces depth ambiguity and improves 3D object localization. V. C ONCLUSION 

In this paper, we presented DualViewMapDet, a novel method that improves camera-based 3D object detection and tracking on nuScenes and Argoverse 2 datasets by leveraging a clever data flow and eliminating the need for precise LiDAR depth information at inference time. For data flow, we proposed enriching both PV and BEV features with a PV-projected and BEV-encoded prior map. By fusing the map-enriched PV features lifted to BEV with map-enriched BEV features, we achieved state-of-the-art performance on both tasks without access to LiDAR-precise depth during the drive. We believe that overcoming the limitations of one-sided data flows and encoding precise 3D geometry into such systems will increase their performance and reliability. REFERENCES [1] W. Sun, X. Lin, Y. Shi, C. Zhang, H. Wu, and S. Zheng, “Sparsedrive: End-to-end autonomous driving via sparse scene representation,” arXiv preprint arXiv:2405.19620 , 2024. [2] T. Yin, X. Zhou, and P. Krahenbuhl, “Center-based 3d object detection and tracking,” in IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2021, pp. 11 784–11 793. [3] C. Lang, A. Braun, L. Schillingmann, and A. Valada, “A point-based approach to efficient lidar multi-task perception,” in IEEE/RSJ International Conference on Intelligent Robots and Systems , 2024. [4] Z. Liu, H. Tang, A. Amini, X. Yang, H. Mao, D. Rus, and S. Han, “Bevfusion: Multi-task multi-sensor fusion with unified bird’s-eye view representation,” arXiv preprint arXiv:2205.13542 , 2022. [5] H. Cai, Z. Zhang, Z. Zhou, et al. , “Bevfusion4d: Learning lidar-camera fusion under bird’s-eye-view via cross-modality guidance and temporal aggregation,” arXiv preprint arXiv:2303.17099 , 2023. [6] J. Yin, J. Shen, R. Chen, W. Li, R. Yang, P. Frossard, and W. Wang, “Is-fusion: Instance-scene collaborative fusion for multimodal 3d object detection,” in IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2024, pp. 14 905–14 915. [7] R. Mohan, D. Cattaneo, F. Drews, and A. Valada, “Progressive multi-modal fusion for robust 3d object detection,” in Conference on Robot Learning , 2024. [8] X. Lin, Z. Pei, T. Lin, et al. , “Sparse4d v3: Advancing end-to-end 3d detection and tracking,” arXiv preprint arXiv:2311.11722 , 2023. [9] M. Käppeler, Ö. Çiçek, D. Cattaneo, C. Gläser, Y. Miron, and A. Valada, “Bridging perspectives: Foundation model guided bev maps for 3d object detection and tracking,” arXiv preprint arXiv:2510.10287 , 2025. [10] Y. Li, Z. Ge, G. Yu, J. Yang, Z. Wang, Y. Shi, J. Sun, and Z. Li, “Bevdepth: Acquisition of reliable depth for multi-view 3d object detection,” in AAAI Conference on Artificial Intelligence , 2023. [11] Y. Liu, T. Wang, X. Zhang, and J. Sun, “Petr: Position embedding transformation for multi-view 3d object detection,” in European Conference on Computer Vision , 2022, pp. 531–548. [12] S. Wang, Y. Liu, T. Wang, Y. Li, and X. Zhang, “Exploring object-centric temporal modeling for efficient multi-view 3d object detection,” in International Conference on Computer Vision , 2023, pp. 3621–3631. [13] “Far3d: Expanding the horizon for surround-view 3d object detection,” in AAAI Conference on Artificial Intelligence , 2024. [14] F. Liu, T. Huang, Q. Zhang, H. Yao, C. Zhang, F. Wan, Q. Ye, and Y. Zhou, “Ray denoising: Depth-aware hard negative sampling for multi-view 3d object detection,” in European Conference on Computer Vision , 2024, pp. 200–217. [15] C. Yang, Y. Chen, H. Tian, C. Tao, X. Zhu, Z. Zhang, et al. , “Bevformer v2: Adapting modern image backbones to bird’s-eye-view recognition via perspective supervision,” in IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2023, pp. 17 830–17 839. [16] J. Philion and S. Fidler, “Lift, splat, shoot: Encoding images from arbitrary camera rigs by implicitly unprojecting to 3d,” in European Conference on Computer Vision , 2020, pp. 194–210. [17] Z. Li, S. Lan, J. M. Alvarez, and Z. Wu, “Bevnext: Reviving dense bev frameworks for 3d object detection,” in IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2024, pp. 20 113–20 123. [18] W. K. Fong, R. Mohan, J. V. Hurtado, L. Zhou, H. Caesar, O. Beijbom, and A. Valada, “Panoptic nuscenes: A large-scale benchmark for lidar panoptic segmentation and tracking,” Robotics and Automation Letters ,vol. 7, no. 2, pp. 3795–3802, 2022. [19] B. Wilson, W. Qi, T. Agarwal, J. Lambert, J. Singh, S. Khandelwal, 

et al. , “Argoverse 2: Next generation datasets for self-driving perception and forecasting,” in Proc. of the Neural Information Processing Systems Track on Datasets and Benchmarks , 2021. [20] Y. Yan, Y. Mao, and B. Li, “Second: Sparsely embedded convolutional detection,” Sensors , vol. 18, no. 10, p. 3337, 2018. [21] M. R. Nallapareddy, K. Sirohi, P. L. Drews, W. Burgard, C.-H. Cheng, and A. Valada, “Evcenternet: Uncertainty estimation for object detection using evidential learning,” in IEEE/RSJ International Conference on Intelligent Robots and Systems , 2023, pp. 5699–5706. [22] M. Büchner and A. Valada, “3d multi-object tracking using graph neural networks with cross-edge modality attention,” Robotics and Automation Letters , vol. 7, no. 4, pp. 9707–9714, 2022. [23] X. Bai, Z. Hu, X. Zhu, Q. Huang, Y. Chen, H. Fu, and C.-L. Tai, “Transfusion: Robust lidar-camera fusion for 3d object detection with transformers,” in Proceedings of the IEEE/CVF conference on computer vision and pattern recognition , 2022, pp. 1090–1099. [24] H. Hu, F. Wang, J. Su, Y. Wang, L. Hu, W. Fang, J. Xu, and Z. Zhang, “Ea-lss: Edge-aware lift-splat-shot framework for 3d bev object detection,” arXiv preprint arXiv:2303.17895 , 2023. [25] Z. Song, L. Yang, S. Xu, L. Liu, D. Xu, C. Jia, F. Jia, and L. Wang, “Graphbev: Towards robust bev feature alignment for multi-modal 3d object detection,” in European Conference on Computer Vision , 2024. [26] N. Gosala, B. R. Kiran, S. Yogamani, and A. Valada, “Sparse3dtrack: Monocular 3d object tracking using sparse supervision,” arXiv preprint arXiv:2603.18298 , 2026. [27] T. Yuan, Y. Mao, J. Yang, Y. Liu, Y. Wang, and H. Zhao, “Presight: Enhancing autonomous vehicle perception with city-scale nerf priors,” in European Conference on Computer Vision , 2024, pp. 323–339. [28] S. Yuan, J. Wei, M. Tie, X. Ren, Z. Gan, and W. Ding, “Lmpocc: 3d semantic occupancy prediction utilizing long-term memory prior from historical traversals,” arXiv preprint arXiv:2504.13596 , 2025. [29] J. Yuan, M. Nguyen-Duc, Q. Liu, M. Hauswirth, and D. Le-Phuoc, “Collaborative perceiver: Elevating vision-based 3d object detection via local density-aware dense spatial occupancy,” in International Conference on Neural Information Processing , 2025, pp. 48–62. [30] Z. Yu and C. Shu, “Ultimatedo: An efficient framework to marry occupancy prediction with 3d object detection via channel2height,” 

arXiv preprint arXiv:2409.11160 , 2024. [31] Y. Li, Z. Wang, Y. Wang, Z. Yu, Z. Gojcic, M. Pavone, C. Feng, and J. M. Alvarez, “Memorize what matters: Emergent scene decomposition from multitraverse,” Advances in Neural Information Processing Systems , vol. 37, pp. 108 389–108 438, 2024. [32] Y. You, K. Z. Luo, X. Chen, J. Chen, W.-L. Chao, W. Sun, B. Hariharan, M. Campbell, and K. Q. Weinberger, “Hindsight is 20/20: Leveraging past traversals to aid 3d perception,” in International Conference on Learning Representations , 2022. [33] Y. You, C. P. Phoo, C. A. Diaz-Ruiz, K. Z. Luo, W.-L. Chao, M. Campbell, B. Hariharan, and K. Q. Weinberger, “Better monocular 3d detectors with lidar from the past,” in IEEE International Conference on Robotics and Automation , 2024, pp. 6634–6641. [34] Q.-Y. Zhou, J. Park, and V. Koltun, “Open3d: A modern library for 3d data processing,” arXiv preprint arXiv:1801.09847 , 2018. [35] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez, Ł. Kaiser, and I. Polosukhin, “Attention is all you need,” Advances in Neural Information Processing Systems , vol. 30, 2017. [36] D. Cattaneo and A. Valada, “Cmrnext: Camera to lidar matching in the wild for localization and extrinsic calibration,” IEEE Transactions on Robotics , 2025. [37] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image recognition,” in IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2016, pp. 770–778. [38] P. Chen, S. Liu, H. Zhao, X. Wang, and J. Jia, “Gridmask data augmentation,” arXiv preprint arXiv:2001.04086 , 2020. [39] Y. Lee, J.-w. Hwang, S. Lee, Y. Bae, and J. Park, “An energy and gpu-computation efficient backbone network for real-time object detection,” in Proc. of the IEEE/CVF conf. on computer vision and pattern recognition workshops , 2019. [40] H. Lim, D. Kim, G. Shin, J. Shi, I. Vizzo, H. Myung, J. Park, and L. Carlone, “Kiss-matcher: Fast and robust point cloud registration re-visited,” in IEEE International Conference on Robotics and Automation ,2025, pp. 11 104–11 111. [41] C. Han, J. Yang, J. Sun, Z. Ge, R. Dong, H. Zhou, W. Mao, Y. Peng, and X. Zhang, “Exploring recurrent long-term temporal fusion for multi-view 3d perception,” Robotics and Automation Letters , vol. 9, no. 7, pp. 6544–6551, 2024. [42] J. Yang, E. Yu, Z. Li, X. Li, and W. Tao, “Quality matters: Embracing quality clues for robust 3d multi-object tracking,” arXiv preprint arXiv:2208.10976 , 2022. [43] L. Qing, T. Wang, D. Lin, and J. Pang, “Dort: Modeling dynamic objects in recurrent for multi-camera 3d object detection and tracking,” in Conference on Robot Learning , 2023, pp. 3749–3765.