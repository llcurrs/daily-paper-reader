---
title: "WeatherOcc3D: VLM-Assisted Adverse Weather Aware 3D Semantic Occupancy Prediction"
title_zh: WeatherOcc3D：基于视觉语言模型辅助的恶劣天气感知三维语义占据预测
authors: "A. Enes Doruk, Abdelaziz Hussein, Hasan F. Ates"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16127v1"
tags: ["query:q6"]
score: 9.0
evidence: 当特定传感器变得不可靠时进行自适应重新加权
tldr: 本文提出WeatherOcc3D框架，通过语言模型辅助利用天气文本线索自适应调节相机和LiDAR融合比例，应对光照弱与雨雪干扰等复杂环境，在多种主流架构上显著提升3D语义占据预测精度与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16127v1/fig-001.webp\", \"caption\": \"TABLE I: Quantitative comparisons on the nuScenes-OpenOccupancy validation set. C, D, L denote camera, depth and LiDAR, respectively. The best and second-best are in bold and underlined, respectively.\", \"page\": 3, \"index\": 1, \"width\": 804, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2605.16127v1/fig-002.webp\", \"caption\": \"Figure 2: Qualitative results under adverse weather and lighting conditions using OccMamba baseline model on the nuScenesOpenOccupancy validation set.\", \"page\": 3, \"index\": 2, \"width\": 893, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2605.16127v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of our proposed model architecture.\", \"page\": 2, \"index\": 3, \"width\": 857, \"height\": 396}]"
motivation: 现有多模态融合方法在恶劣天气下无法动态调整不同传感器的信任度，导致预测性能下降。
method: 通过利用预训练的CLIP语言-视觉嵌入空间，引入参数高效的适配器和环境不确定性门控策略，自适应调整相机与LiDAR的融合权重。
result: 在nuScenes数据集上，该方法在OccMamba和M-CONet架构上分别取得26.3和21.1的mIoU，显著优于传统基线。
conclusion: 该研究证明了引入语言模型辅助的环境感知机制能有效提升多模态3D语义占据预测在恶劣天气条件下的鲁棒性。
---

## 摘要
尽管多模态三维语义占据预测通常通过融合相机与激光雷达（LiDAR）输入以增强鲁棒性，但其有效性从根本上受环境变化的限制。具体而言，相机传感器在低光环境下会出现严重的退化，而激光雷达传感器在强降水条件下则会遭受显著的后向散射噪声。这些不利条件引发了模态信任问题，因为静态融合策略无法在特定传感器失效时自适应地重新加权输入。为此，我们提出一种由视觉语言模型（VLM）辅助的框架，利用预训练的 CLIP 潜空间，通过语言化的环境线索来引导多传感器融合。我们采用参数高效的适配器将特定天气的文本嵌入与传感器特征对齐，并结合一种门控策略，将环境不确定性分解为两个因素：可见度与照明。这使模型能够动态调节融合比例——在晴朗白天优先利用语义丰富的相机特征，而在雨夜则转向几何驱动的激光雷达先验。在 nuScenes 数据集上的评估表明，我们的方法具有极强的通用性：当在 OccMamba 与 M-CONet 架构上实现所提框架时，分别获得了 26.3 和 21.1 的 mIoU 得分，显著优于其传统基线方法。

## Abstract
While multi-modal 3D semantic occupancy prediction typically enhances robustness by fusing camera and LiDAR inputs, its effectiveness is fundamentally constrained by environmental variability. Specifically, camera sensors suffer from severe low-light degradation, while LiDAR sensors encounter significant backscatter noise during heavy precipitation. These adverse conditions create a modality trust problem, as static fusion strategies fail to adaptively re-weight inputs when a specific sensor becomes unreliable. To address this, we propose a VLM-assisted framework leveraging the pre-trained CLIP latent space to guide multi-sensor integration via linguistic environmental cues. We utilize a parameter-efficient adapter to align weather-specific text embeddings with sensor features, coupled with a gating strategy that decomposes environmental uncertainty into two factors: visibility and illumination. This enables the model to dynamically modulate the fusion ratio - prioritizing semantic camera features in clear daylight and shifting to geometric LiDAR priors during rainy nights. Evaluations on the nuScenes dataset demonstrate the versatility of our approach, as implementing our proposed framework on the OccMamba and M-CONet architectures achieves mIoU scores of 26.3 and 21.1, respectively, significantly outperforming their traditional baselines.

---

## 论文详细总结（自动生成）

# WeatherOcc3D：基于视觉语言模型辅助的恶劣天气感知三维语义占据预测论文总结

---

## 一、研究动机与背景

- **核心问题**：传统的多模态三维语义占据预测方法通常融合相机与 LiDAR 数据，以获取鲁棒的环境感知能力。然而，恶劣天气和复杂光照条件下两个传感器的可靠性差异显著：
  - 相机在低光和强降雨中严重退化（可见度下降、模糊、反光干扰）。
  - LiDAR 在雨雪环境中出现强烈的后向散射和噪声。
- **问题本质**：在这种动态环境下，“模态信任度”变化频繁，而目前主流的融合方法（如 BEVFusion、OccFormer）多采用静态或固定权重，无法灵活调节传感器贡献，导致预测误差增大。
- **研究目标**：本论文致力于构建一种环境自适应的多模态融合机制，使系统能够根据天气和光照条件自动调整 Camera 与 LiDAR 的融合比例，从而提升 3D 占据预测的鲁棒性与精度。
- **创新背景**：作者提出用 **视觉语言模型（VLM, 如 CLIP）** 的语义空间来表征天气状态，通过文字描述（如“雨夜”“晴天”等）引导动态感知调整，从语言层面引入“环境理解能力”。

---

## 二、方法论：核心思想与技术流程

### 1. 整体框架

- 方法名：**WeatherOcc3D**
- 核心思想：利用预训练的 **CLIP** 语言-视觉嵌入空间，通过文本描述生成环境语义嵌入，以此指导 Camera–LiDAR 融合比例。
- 主体组成：
  1. **2D 图像编码器** → 基于 ResNet + FPN 提取多尺度视觉特征。
  2. **3D LiDAR 编码器** → 提取空间几何特征。
  3. **Lift-Splat-Shoot（LSS）投影模块** → 将 2D 特征投射到统一的 3D voxel 空间。
  4. **天气预测子模块** → 从视觉特征中预测两类环境因素：
     - 可见性（Clear / Rainy）
     - 光照（Day / Night）
  5. **CLIP 文本编码器 + LoRA 适配器** → 使用冻结的 CLIP 模型，辅以低秩适配层（LoRA）学习天气文本嵌入。
  6. **门控机制（Gating Mask）** → 基于环境嵌入，对各通道特征进行选择性抑制。
  7. **全局融合权重（wₑₙᵥ）** → 通过 MLP + Sigmoid 学习全局模态信任因子。

### 2. 融合公式说明

- **全局权重计算**：
  \[
  w_{env} = \sigma(\text{MLP}(f_{env}))
  \]
  其中 \(f_{env}\) 为经过 CLIP + LoRA 得到的环境嵌入。

- **融合策略**：
  \[
  V_{fused} = w_{env}(G_{cam}V_{cam}) + (1 - w_{env})(G_{pts}V_{pts})
  \]
  该式实现基于天气自适应的特征融合，动态调整视觉与几何模态的贡献。

- **损失函数**：
  \[
  L_{total} = \lambda_{occ}L_{occ} + \lambda_{weather}L_{weather}
  \]
  - \(L_{occ}\)：用于占据预测的交叉熵 + Lovász-Softmax 组合损失。
  - \(L_{weather}\)：用于天气与光照二分类任务的二元交叉熵损失。

### 3. 特点总结

- 通过语言模型嵌入实现“语义化的环境感知”。
- 利用 LoRA 实现 **参数高效微调**，避免 CLIP 全量训练。
- 环境分解为两个独立因素（visibility + illumination），实现细粒度调控。

---

## 三、实验设计与评估

### 1. 数据集与场景

- **数据集**：nuScenes-OpenOccupancy（基于 nuScenes）
  - 包含 6 个环视相机、1 个 LiDAR、多个雷达。
  - 体素范围： X/Y ∈ [-40m, 40m]，Z ∈ [-1m, 5.4m]，体素尺寸为 0.4m。
- **评价指标**：Mean Intersection over Union（mIoU）
- **任务类型**：单一数据集的多模态占据预测。

### 2. 对比基线方法

与多种主流方法比较，包括：
- **单模态**：TPVFormer、SparseOcc、LMSCNet、JS3C-Net。
- **多模态融合**：BEVFusion、OccFormer、Co-Occ、M-CONet、OccMamba。
- **VLM相关扩展**：LanguageOcc、VEON、GaussianOcc3D。

同时，与不同融合策略比较（加法、拼接、ACL F等）。

### 3. WeatherOcc3D 的嵌入方式

- 模块集成到 **OccMamba (2025)** 与 **M-CONet (2023)** 两个基线架构中。
- 均视为 **“即插即用”** 模块，无需修改原网络结构。

---

## 四、资源与算力配置

- 项目使用 **PyTorch** 框架训练。
- **GPU配置**：
  - 4 张 NVIDIA RTX 6000。
  - 总批量大小为 1。
  - 训练时长为 20 个 epoch。
- **优化器**：AdamW，初始学习率 2×10⁻⁴。
- **CLIP模型** 在训练中保持冻结，仅微调 LoRA 层。

> ✅ 文中明确说明了硬件配置与训练时长，算力资源较充足。

---

## 五、实验数量与充分性分析

- **主要实验**：
  1. 与 10+ 主流方法对比（Table I）。
  2. 与不同融合策略对比（Table II）——加法、拼接、ACL F、本文方法。
  3. 在不同天气与光照条件下的鲁棒性测试（Table III）。
  4. 集成至两种不同框架验证可迁移性（OccMamba / M-CONet）。
- **实验充分性**：
  - 包含定量、定性可视化（雨天、夜间、晴天）。
  - 不涉及跨数据集验证，但在单一 benchmark 上进行了全面分析。
- **公平性**：
  - 使用官方 nuScenes 数据与评测标准；
  - 同样训练 epoch 与参数设定，比较客观。

---

## 六、主要结论与发现

- WeatherOcc3D 有效提升恶劣天气下的多模态占据预测性能。
- 与基线相比：
  - 在 OccMamba 上提升 mIoU 从 **25.2 → 26.3（+1.1）**。
  - 在 M-CONet 上提升 mIoU 从 **20.1 → 21.1（+1.0）**。
- 环境分解策略（Visibility / Illumination）显著提升夜间和雨天鲁棒性：
  - 雨天提升 +3.2 mIoU。
  - 夜间提升 +3.9 mIoU。
- 平均推理延迟仅增加 2.14ms（低于其他动态融合方法），说明实用性强。

---

## 七、优点与创新亮点

- ✅ **语言模型辅助的环境语义理解**：将天气描述转为可学习的融合指令。
- ✅ **参数高效 LoRA 微调**：无需重训练大型 CLIP，计算成本低。
- ✅ **双层融合机制**：通道级门控 + 全局加权结合，提高可解释性。
- ✅ **广泛兼容性**：在不同架构上均带来一致提升，可作为独立模块部署。
- ✅ **鲁棒性验证充分**：在雨夜等极端场景下性能提升明显。

---

## 八、不足与局限性

- ⚠ **数据集单一**：仅在 nuScenes-OpenOccupancy 上验证，缺乏跨域或跨城市评估。
- ⚠ **环境标签依赖**：训练阶段需明确天气与光照分类标签，实际应用时可能缺乏精确标注。
- ⚠ **未涉及雷达或其他模态融合**：当前仅限 Camera + LiDAR。
- ⚠ **实验规模仍有限**：训练 batch size 极小（=1），对大规模场景泛化仍需验证。
- ⚠ **CLIP文本嵌入的抽象性**：在极端气候下文本描述可能不足以捕捉复杂物理特性。

---

（完）
