---
title: "UniTriGen: Unified Triplet Generation of Aligned Visible-Infrared-Label for Few-Shot RGB-T Semantic Segmentation"
title_zh: UniTriGen：用于少样本RGB-T语义分割的对齐可见光-红外-标签统一三元组生成
authors: "Ping Zhou, Haoyu Wang, Mengmeng Zheng, Lei Zhang, Wei Wei, Chen Ding, Fei Zhou"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14626v1"
tags: ["query:q6"]
score: 9.0
evidence: 为模态缺失场景生成对齐的可见光-红外-标签三元组
tldr: 本文提出UniTriGen，一个统一的可见光-红外-标签三元组生成框架，以解决真实场景中缺乏严格对齐的RGB-T数据问题。UniTriGen通过将三模态联合编码到共享潜空间并引入扩散建模，实现跨模态一致的图像生成，并结合轻量模态残差适配器与平衡采样策略，有效缓解数据不平衡，提升了生成质量与分割效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14626v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison between existing paradigms and the proposed UniTriGen. (a) Cascaded Generation Paradigms that progressively assemble the target triplet through intermediate outputs. (b) Our UniTriGen directly generates spatially aligned, semantically consistent, and modality complementary VIS-IR-Label triplets from text prompts in an end-to-end manner.\", \"page\": 2, \"index\": 1, \"width\": 829, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.14626v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative comparison of different methods on SemanticRT and PST900 datasets. The best results are highlighted in bold, while the second-best outcomes are denoted by underlined italic.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.14626v1/fig-003.webp\", \"caption\": \"Figure 3: Visualization of UniTriGen generation results on the SemanticRT dataset and PST900 dataset. The text prompt consists of scene and cotegories.\", \"page\": 8, \"index\": 3, \"width\": 829, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2605.14626v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of the UniTriGen framework. The framework consists of two main components: Unified Triplet Generation Mechanism and SBCA Few-Shot Sampling.\", \"page\": 4, \"index\": 4, \"width\": 824, \"height\": 333}]"
motivation: 当前RGB-T语义分割缺乏严格对齐的多模态配对数据，现有生成方法无法保证跨模态一致性和语义连贯性。
method: UniTriGen将可见光、红外和标签联合编码至共享潜空间，通过扩散机制和模态特定适配器实现一致的三元组生成，并采用类平衡采样策略。
result: 实验表明，UniTriGen 能利用有限的配对数据生成高质量、对齐且一致的三元组，从而显著提升多种RGB-T语义分割模型的性能。
conclusion: UniTriGen能够在少样本条件下统一生成高质量的RGB-T三元组，为RGB-T语义分割提供更可靠的数据支持。
---

## 摘要
RGB-T语义分割需要严格对齐的VIS-IR-Label三元组；然而，在现实场景中这种对齐的三元组数据往往十分稀缺。现有的生成式数据增强方法通常采用级联式生成范式，将联合三元组生成分解为局部条件过程。因此，VIS、IR和Label在空间结构、语义内容以及跨模态细节上的一致性无法得到可靠保持。为解决这一问题，我们提出了UniTriGen，一种统一的三元组生成框架，可在文本提示的引导下直接生成空间对齐、语义一致且模态互补的VIS-IR-Label三元组。UniTriGen首先引入统一的三元组生成机制，将VIS、IR和Label共同编码到共享的潜在空间中，并通过扩散过程建模以强化全局跨模态一致性。为了适应不同模态的成像特性和输出格式，该机制进一步集成了轻量级的模态特定残差适配器。针对有限配对三元组中场景与类别分布不均导致的生成偏差，UniTriGen还采用场景平衡与类别感知的少样本采样策略，从而引导更均衡的采样分布，增强生成三元组的场景与类别多样性。实验结果表明，UniTriGen能从有限的真实配对数据中生成高质量的对齐三元组，从而在多种RGB-T语义分割模型上实现一致的性能提升。

## Abstract
RGB-T semantic segmentation requires strictly aligned VIS-IR-Label triplets; however, such aligned triplet data are often scarce in real-world scenarios. Existing generative augmentation methods usually adopt cascaded generation paradigms, decomposing joint triplet generation into local conditional processes. As a result, consistency among VIS, IR, and Label in spatial structure, semantic content, and cross-modal details cannot be reliably maintained. To address this issue, we propose UniTriGen, a unified triplet generation framework that directly generates spatially aligned, semantically consistent, and modality complementary VIS-IR-Label triplets under the guidance of text prompts. UniTriGen first introduces a unified triplet generation mechanism, where VIS, IR, and Label are jointly encoded into a shared latent space and modeled with a diffusion process to enforce global cross-modal consistency. Lightweight modality-specific residual adapters are further integrated into this mechanism to accommodate modality-specific imaging characteristics and output formats. To mitigate generation bias caused by imbalanced scene and class distributions in limited paired triplets, UniTriGen also employs a scene-balanced and class-aware few-shot sampling strategy, which induces a more balanced sampling distribution and enhances the scene and class diversity of generated triplets. Experiments show that UniTriGen generates high-quality aligned triplets from limited real paired data, thereby achieving consistent performance improvements across various RGB-T semantic segmentation models.

---

## 论文详细总结（自动生成）

## 一、研究动机与核心问题

- **研究背景**：  
  RGB-T（可见光–红外）语义分割需要严格对齐的三模态数据（VIS-IR-Label）。然而，现实中由于多传感器标定难度大、场景配准误差高、像素级标注成本高，导致可用的对齐数据集极少。  
- **问题实质**：  
  现有的生成式增强或翻译方法（如可见光到红外翻译、图像-标签联合生成）多采用“级联式”生成流程，即依次生成可见光图像、再翻译为红外或生成标签。这类逐步条件生成的方式缺乏统一约束，结果常出现：
  - 模态间空间错位（layout shift）；  
  - 类别语义不一致；  
  - 红外特征与可见光外观不匹配。  
- **核心目标**：  
  论文旨在提出一个**统一的端到端生成框架**，能够直接从文本描述生成**空间对齐、语义一致、模态互补**的可见光–红外–语义标签三元组，用于增强少样本RGB-T语义分割训练。

---

## 二、方法论：UniTriGen 框架

### 1. 总体思想
- **理念**：不同于分阶段生成，UniTriGen在共享潜空间内**联合建模三种模态**（Visible、Infrared、Label），利用扩散过程在全局范围内保持跨模态一致性。
- **输入与输出**：
  - 输入：文本提示（text prompt），包含场景与类别信息；
  - 输出：三个模态——VIS图像、IR图像、以及对应语义分割标签。

### 2. 框架组成
论文设计了两个核心模块：

#### (1) Unified Triplet Generation Mechanism （统一三元组生成机制）
- **关键结构**：基于 **UniDiffuser** 的多模态扩散框架。
- **步骤流程**：
  1. 使用各自的VAE编码器 \(E_V, E_I, E_L\) 将可见光图像、红外图像和标签分别编码为潜空间变量 \(z_V, z_I, z_L\)；
  2. 拼接为联合潜变量 \(z_0 = [z_V; z_I; z_L]\)；
  3. 在扩散正向过程中加入高斯噪声形成 \(z_t\)，反向扩散利用文本嵌入 \(z_T\) 作为全局条件；
  4. 在Transformer自注意力机制下捕捉文本-模态间的交互；
  5. 利用残差预测噪声的方式进行去噪学习，优化目标为噪声重构误差 \(L_{denoise}\)；
  6. 去噪后通过三个轻量“模态残差适配器”（V/I/L adapters）进行潜向量校准 \( \tilde{z}_m = \hat{z}_m + A_m(\hat{z}_m)\)，再分别解码得到三模态输出。
- **优化目标**：联合损失 \( L = L_{denoise} + \lambda L_{calib} \)，保证全局一致与模态特定校准兼顾。
- **优势**：一次性联合生成三种模态，避免级联误差传播。

#### (2) SBCA Few-Shot Sampling （场景平衡与类别感知采样）
- **动机**：少样本场景下数据分布常极不均衡（部分场景或类过多、部分稀缺）。
- **设计策略**：
  - 将文本提示结构化为“场景描述 + 类别集合”；
  - 利用 Places365 提取场景特征并聚类成 \(K\) 个组；
  - 在文本嵌入中添加“scene token”；
  - 实现两级再采样策略：  
    - *场景级平衡*：在各场景组间均衡抽样；  
    - *类别级加权*：稀有类别样本提升权重 \(w_c \propto 1/N_c^\alpha\)。  
- **效果**：使训练集在场景与类别维度上更均衡，提高长尾类与低频场景生成质量。

---

## 三、实验设计

- **数据集**：
  - **SemanticRT**：大规模RGB-T语义分割数据集；
  - **PST900**：红外-可见配准数据集。
- **少样本设定**：
  - SemanticRT：仅取5%样本（342对训练数据）；
  - PST900：取50%样本（299对训练数据）。
- **基线对比**：
  - 三种典型**级联式生成**管线：
    1. **Seq-Cascade**：逐模态顺序生成；
    2. **JointLabel-Cascade**：先生成可见光-标签对；
    3. **TriCond-Cascade**：三条件组合生成。  
  - **下游分割模型**（评估生成数据有效性）：
    - M-SpecGene (UperNet backbone)
    - Sigma (Mamba)
    - MilNet (Segformer)
    - 传统 ResNet152-based 模型。
- **评测指标**：mIoU（平均交并比），比较 *纯合成* 与 *真实+合成* 两种训练情况。

---

## 四、算力与资源

- 论文**未明确**说明使用的GPU型号、数量或训练时长。  
  推测因模型基于UniDiffuser与VAE–Transformer组合，训练涉及多模态扩散过程，需较大算力（至少多GPU环境）。但文中未提供具体硬件细节。

---

## 五、实验数量与充分性

- **主要实验**：
  - 两个公开数据集（SemanticRT、PST900）；
  - 四种下游模型架构；
  - 对比三种基线生成方法；
  - 同时报告合成-only与real+syn两种训练组合；
  - 并进行多数据比例（5%、10%、25%等）测试。
- **补充实验**：
  - 消融实验：验证统一生成机制、SBCA采样的独立与叠加效果；
  - 扩展实验：考察合成-真实比例（1:1～1:20）放大后的性能变化。
- **评估充分性**：  
  实验覆盖面广，验证了可扩展性、跨模型迁移性与采样均衡性。指标客观且对比公平（相同训练集划分与架构使用）。

---

## 六、主要结论与发现

- UniTriGen 能够在**少量真实三元组数据**下生成**高质量、对齐、一致**的可见光–红外–标签数据；
- 相比级联方案，**mIoU最高提升达5–6个百分点**；
- 与所有下游架构均产生**正向增益**，表现出模型无关性；
- SBCA采样可缓解长尾类、低频场景生成偏差；
- 扩展比例实验证明，随着合成数据增多，性能稳步提升，呈可扩展趋势；
- 生成结果在边界结构、热信号表现、稀有类别识别上更自然一致。

---

## 七、方法优点与创新点

- **创新性**：
  - 首次提出**文本驱动的可见光–红外–标签联合生成框架**；
  - 将三模态扩散建模融为单一统一过程；
  - 引入**轻量模态残差适配器**，在保持统一一致性的同时增强模态特征逼真度；
  - 设计**类平衡场景采样策略**，针对性地解决少样本下的数据偏分布。
- **实用性**：
  - 可直接输出语义标签，可为分割任务直接使用；
  - 合成数据显著提升多个下游网络的性能；
  - 框架通用，可拓展到其他多模态任务。

---

## 八、不足与局限

- **资源细节未公开**：缺乏算力、参数规模、训练时长描述，不利于复现；
- **实验范围**：仅验证两数据集，尚缺跨领域泛化测试；
- **质量评估偏单一**：主要基于下游分割性能，缺少人类感知或多模态相似性客观指标（如FID、SSIM）；
- **合成上限未探讨**：未系统分析合成数据在数量极大时的性能边界与现实应用稳定性；
- **文本生成依赖**：输入prompt质量对生成效果可能有显著影响，但文中未细化自动生成或控制方式。

---

（完）
