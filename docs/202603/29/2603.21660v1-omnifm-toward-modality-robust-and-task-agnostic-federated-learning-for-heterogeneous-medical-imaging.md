---
title: "OmniFM: Toward Modality-Robust and Task-Agnostic Federated Learning for Heterogeneous Medical Imaging"
title_zh: OmniFM：面向异构医学影像的模态鲁棒与任务无关联邦学习
authors: "Meilin Liu, Jiaying Wang, Jing Shan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21660v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 用于医学影像多模态融合的模态鲁棒联邦学习
tldr: 针对医疗影像联邦学习中任务耦合度高及模态异构性强的问题，本文提出OmniFM框架。该框架利用频域中低频成分的跨模态一致性，通过全局频谱知识检索、交叉注意力融合及频谱提示技术，实现了对分类、分割、超分辨率等多种任务的统一支持。实验证明，OmniFM在处理异构模态数据时表现优异，显著提升了联邦学习的鲁棒性与泛化能力。
selection_source: fresh_fetch
motivation: 现有的联邦学习框架通常受限于特定任务且对异构影像模态鲁棒性不足，难以满足实际医疗场景的多样化需求。
method: 提出OmniFM框架，利用低频频谱的一致性，通过全局频谱检索、交叉注意力融合和频谱提示技术来对齐并增强异构模态特征。
result: 实验结果显示，OmniFM在跨模态异构场景下的多种医疗影像任务中均取得了超越现有最先进联邦学习方法的性能。
conclusion: OmniFM通过频域对齐实现了模态鲁棒且任务无关的联邦学习，为异构医疗影像的协同分析提供了新范式。
---

## 摘要
联邦学习（FL）已成为协作式医学图像分析中一种极具前景的范式，然而现有框架仍与特定任务的主干网络紧密耦合，且在异构成像模态下表现脆弱。这些限制阻碍了实际部署，因为不同机构的模态分布差异巨大，且必须支持多种下游任务。为解决这一局限性，我们提出了 OmniFM，这是一个模态无关且任务无关的联邦学习框架，它在无需重新设计优化流水线的情况下，统一了分类、分割、超分辨率、视觉问答和多模态融合的训练。OmniFM 基于一个关键的频域见解：低频光谱分量表现出强大的跨模态一致性，并编码了模态不变的解剖结构。据此，OmniFM 集成了 (i) 全局光谱知识检索以注入全局频率先验，(ii) 嵌入级交叉注意力融合以对齐表示，以及 (iii) 前缀-后缀光谱提示以共同调节全局和个性化线索，并由稳定聚合的光谱近端对齐（Spectral-Proximal Alignment）目标进行联合正则化。在真实世界数据集上的实验表明，OmniFM 在模态内和跨模态异构性方面始终优于最先进的联邦学习基准，在微调和从头训练设置下均取得了卓越的结果。

## Abstract
Federated learning (FL) has become a promising paradigm for collaborative medical image analysis, yet existing frameworks remain tightly coupled to task-specific backbones and are fragile under heterogeneous imaging modalities. Such constraints hinder real-world deployment, where institutions vary widely in modality distributions and must support diverse downstream tasks. To address this limitation, we propose OmniFM, a modality- and task-agnostic FL framework that unifies training across classification, segmentation, super-resolution, visual question answering, and multimodal fusion without re-engineering the optimization pipeline. OmniFM builds on a key frequency-domain insight: low-frequency spectral components exhibit strong cross-modality consistency and encode modality-invariant anatomical structures. Accordingly, OmniFM integrates (i) Global Spectral Knowledge Retrieval to inject global frequency priors, (ii) Embedding-wise Cross-Attention Fusion to align representations, and (iii) Prefix-Suffix Spectral Prompting to jointly condition global and personalized cues, together regularized by a Spectral-Proximal Alignment objective that stabilizes aggregation. Experiments on real-world datasets show that OmniFM consistently surpasses state-of-the-art FL baselines across intra- and cross-modality heterogeneity, achieving superior results under both fine-tuning and training-from-scratch setups.

---

## 论文详细总结（自动生成）

以下是对论文《OmniFM: Toward Modality-Robust and Task-Agnostic Federated Learning for Heterogeneous Medical Imaging》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的医疗影像联邦学习（FL）框架存在两个主要瓶颈：
    1.  **任务耦合度高（Task-Specific）**：通常针对特定任务（如分类或分割）设计，缺乏通用性，切换任务需重新设计优化流水线。
    2.  **模态鲁棒性差（Modality-Fragile）**：不同医疗机构使用的设备（MRI、CT、PET等）导致影像模态异构，产生严重的非独立同分布（Non-IID）问题，导致全局模型聚合时难以收敛。
*   **研究动机**：旨在构建一个**任务无关（Task-Agnostic）**且**模态鲁棒（Modality-Robust）**的统一联邦学习框架，使不同机构能协作处理多种下游任务，同时克服模态差异带来的优化漂移。

### 2. 方法论：OmniFM 框架
OmniFM 的核心思想是利用**频域（Frequency-domain）**特性：低频光谱分量在不同模态间具有高度一致性，编码了通用的解剖结构。
*   **关键技术组件**：
    1.  **全局光谱知识检索 (GSKR)**：服务器维护一个全局光谱知识库，存储各客户端上传的光谱嵌入。客户端通过余弦相似度检索最相关的 Top-k 全局原型，以增强本地表示。
    2.  **嵌入级交叉注意力融合 (ECA)**：将检索到的全局光谱上下文通过交叉注意力机制注入到主干网络的特征表示中，使模型更关注模态不变的解剖结构而非模态相关的纹理。
    3.  **前缀-后缀光谱提示 (PSP)**：在 Token 序列中，前缀加入全局光谱 Token 以引导模态一致性，后缀加入本地学习的 CLS Token 以保留个性化特征。
    4.  **光谱近端对齐 (SPAlign)**：在优化目标中加入正则化项，强制本地光谱嵌入靠近全局原型中心，从而抑制由模态差异引起的优化漂移。
*   **算法流程**：客户端提取影像特征并进行快速傅里叶变换（FFT），提取低频分量生成光谱嵌入；通过与服务器交互检索全局知识并进行融合训练；最后仅上传光谱嵌入和部分参数进行聚合。

### 3. 实验设计
*   **任务场景**：涵盖了五大主流医疗影像任务：
    *   **分类**：使用 MedMNIST-v2（包含病理、皮肤镜、血细胞显微镜等异构模态）。
    *   **超分辨率**：使用 BreaKHis 乳腺癌组织病理数据集。
    *   **视觉问答 (VQA)**：使用 SLAKE、VQA-RAD、VQA-Med (2019-2021) 等多源数据集。
    *   **分割**：使用 FeTS2022 脑肿瘤多模态 MRI 数据。
    *   **多模态融合**：使用哈佛医学院的 MRI、PET、CT、SPECT 融合数据。
*   **对比基准 (Benchmarks)**：
    *   通用 FL 方法：FedAvg, FedProx, SCAFFOLD, FedDyn。
    *   个性化 FL 方法：Ditto, MOON, FedRep, FedPer。
    *   特定任务方法：FedDAT, PromptFL, F3OCUS 等。

### 4. 资源与算力
*   **模型配置**：分类使用 ResNet-18；分割使用 U-Net；VQA 任务使用了大模型底座（LLaVA-1.5, CLIP ViT-B/32 视觉编码器, LLaMA-3.2-3B 文本主干），并采用 LoRA 进行参数高效微调。
*   **算力说明**：文中提到了训练轮数（如分类 100 轮、融合 600 轮）和本地 Epoch 数，但**未明确列出具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 种不同的医疗任务上进行了广泛实验，每种任务都设置了不同的异构场景（如 Hard/Medium/Extra 场景，或 IID/Non-IID 划分）。
*   **消融实验**：针对 GSKR、ECA、PSP 和 SPAlign 四个核心组件分别进行了消融研究，验证了各部分的必要性。
*   **参数敏感性**：分析了对齐系数 $\lambda$ 和检索数量 $k$ 的影响。
*   **充分性评价**：实验覆盖了从“从头训练”到“大模型微调”的多种模式，对比方法涵盖了近几年的顶会成果，实验设计较为客观、全面且具有说服力。

### 6. 主要结论与发现
*   **性能卓越**：OmniFM 在所有测试任务中均显著优于现有最先进（SOTA）的联邦学习方法，尤其在极端模态异构的情况下表现稳健。
*   **频域价值**：验证了低频光谱信息作为跨模态“通用语言”的有效性，能够有效缓解 Non-IID 带来的负面影响。
*   **任务通用性**：证明了单一的联邦优化流水线可以复用于多种完全不同的医疗影像任务，无需针对任务特性做大幅修改。

### 7. 优点与亮点
*   **创新视角**：从频域角度切入解决模态异构问题，避开了复杂的空间域对齐。
*   **高复用性**：解耦了联邦协议与具体任务/主干网络，极大地降低了 FL 在实际医疗场景中的部署难度。
*   **隐私保护**：通过仅交换光谱嵌入和部分参数，在提升性能的同时维持了较高的隐私安全性。

### 8. 不足与局限
*   **计算开销**：虽然光谱编码器是轻量级的，但在每一轮中进行全局知识检索和交叉注意力计算会增加额外的通信和计算开销。
*   **超参数依赖**：性能对对齐系数 $\lambda$ 和检索数 $k$ 有一定敏感性，实际应用中可能需要针对新任务进行调优。
*   **硬件细节缺失**：缺乏具体的算力消耗报告，难以评估其在资源受限的医疗机构终端上的运行效率。
*   **长尾模态风险**：如果全局知识库中缺乏某些罕见模态的样本，检索机制的有效性可能会下降。

（完）
