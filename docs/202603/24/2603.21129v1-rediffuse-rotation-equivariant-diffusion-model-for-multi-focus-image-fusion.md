---
title: "ReDiffuse: Rotation Equivariant Diffusion Model for Multi-focus Image Fusion"
title_zh: ReDiffuse：用于多聚焦图像融合的旋转等变扩散模型
authors: "Bo Li, Tingting Bao, Lingling Zhang, Weiping Fu, Yaxian Wang, Jun Liu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21129v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 提出了一种用于多聚焦图像融合的旋转等变扩散模型。
tldr: 针对多聚焦图像融合（MFIF）中散焦模糊导致几何结构变形并产生伪影的问题，本文提出ReDiffuse模型。该模型首次将旋转等变性引入扩散模型架构，通过构建端到端的等变网络，确保融合结果能保持原始图像的结构一致性。在四个主流数据集上的实验表明，ReDiffuse在六项指标上均有提升，有效解决了传统扩散模型在处理对称几何特征时的局限性。
selection_source: fresh_fetch
motivation: 扩散模型在处理多聚焦融合时，散焦模糊易导致几何结构扭曲并产生伪影，需通过旋转等变性来保持结构一致性。
method: 提出ReDiffuse模型，通过精心设计的端到端旋转等变扩散架构，并结合严谨的理论分析来评估其内在等变误差。
result: "在Lytro、MFFW等四个数据集上，ReDiffuse在六个评估指标上实现了0.28%至6.64%的性能提升。"
conclusion: 引入旋转等变性显著增强了扩散模型在多聚焦图像融合中的鲁棒性，为处理复杂几何纹理提供了有效方案。
---

## 摘要
扩散模型在多聚焦图像融合（MFIF）中取得了显著的性能。然而，将扩散模型应用于病态的 MFIF 问题时面临的一个关键挑战是，离焦模糊会导致常见的对称几何结构（如纹理和边缘）发生扭曲和变形，从而在融合图像中产生意外的伪影。因此，在扩散网络中嵌入旋转等变性至关重要，因为它能使融合结果忠实地保留输入图像底层几何模式的原始方向和结构一致性。受此启发，我们提出了 ReDiffuse，一种用于 MFIF 的旋转等变扩散模型。具体而言，我们精心构建了基础扩散架构以实现端到端的旋转等变性。我们还提供了严谨的理论分析来评估其内在等变误差，证明了嵌入等变结构的有效性。我们在四个数据集（Lytro、MFFW、MFI-WHU 和 Road-MF）上对 ReDiffuse 与各种 MFIF 方法进行了全面评估。结果表明，ReDiffuse 取得了具有竞争力的性能，在六项评估指标上提升了 0.28-6.64%。代码可在 https://github.com/MorvanLi/ReDiffuse 获取。

## Abstract
Diffusion models have achieved impressive performance on multi-focus image fusion (MFIF). However, a key challenge in applying diffusion models to the ill-posed MFIF problem is that defocus blur can make common symmetric geometric structures (e.g., textures and edges) appear warped and deformed, often leading to unexpected artifacts in the fused images. Therefore, embedding rotation equivariance into diffusion networks is essential, as it enables the fusion results to faithfully preserve the original orientation and structural consistency of geometric patterns underlying the input images. Motivated by this, we propose ReDiffuse, a rotation-equivariant diffusion model for MFIF. Specifically, we carefully construct the basic diffusion architectures to achieve end-to-end rotation equivariance. We also provide a rigorous theoretical analysis to evaluate its intrinsic equivariance error, demonstrating the validity of embedding equivariance structures. ReDiffuse is comprehensively evaluated against various MFIF methods across four datasets (Lytro, MFFW, MFI-WHU, and Road-MF). Results demonstrate that ReDiffuse achieves competitive performance, with improvements of 0.28-6.64\% across six evaluation metrics. The code is available at https://github.com/MorvanLi/ReDiffuse.

---

## 论文详细总结（自动生成）

以下是对论文《ReDiffuse: Rotation Equivariant Diffusion Model for Multi-focus Image Fusion》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多聚焦图像融合（MFIF）旨在将不同焦距拍摄的图像合成为全焦图像。虽然扩散模型（Diffusion Models）在生成高质量图像方面表现出色，但在处理 MFIF 时存在一个关键挑战：**离焦模糊（Defocus Blur）**会使图像中的边缘、纹理等几何结构发生扭曲。现有的扩散模型在去噪过程中难以保持这些结构的几何一致性，常导致融合图像出现伪影（如边缘变形、纹理失真）。
本文的研究动机是引入**旋转等变性（Rotation Equivariance）**作为几何先验，确保网络对不同方向的相同结构产生一致响应，从而在去噪过程中忠实地保留原始图像的方向性和结构完整性。

### 2. 方法论：核心思想与关键技术
论文提出了 **ReDiffuse**，一个端到端的旋转等变扩散框架。
*   **核心思想**：通过重新设计扩散模型的 U-Net 骨干网络，使其所有组件（卷积、上下采样、归一化）均满足旋转等变性，从而在扩散的逆过程中强制执行结构一致性约束。
*   **关键技术细节**：
    *   **旋转等变卷积（Rot-E Conv）**：采用基于傅里叶级数参数化的 B-Conv，在连续域实现严格的旋转等变性。
    *   **等变架构设计**：构建了包含 Rot-E DownBlock（下采样）、MidBlock 和 UpBlock（上采样）的 U-Net。
    *   **理论证明**：论文对最大池化（下采样）、双线性插值（上采样）和组归一化（GN）进行了严谨的数学分析，证明了在离散网格下，当网格尺寸趋于零时，等变误差趋于零；特别是在 $m=4$（90度旋转组）时，理论误差为零。
    *   **轻量化**：由于旋转等变模型在不同方向上共享参数，参数量从基准模型的 26.91M 大幅减少至 7.55M。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用真实世界数据集 Real-MFF（600对训练，110对验证）。
    *   **测试集**：在四个主流 benchmark 上评估：Lytro（20对）、MFFW（13对，含复杂散焦）、MFI-WHU（120对）、Road-MF（80对，道路场景）。
*   **对比方法**：对比了 11 种代表性方法，涵盖 GAN（MFF-GAN）、CNN（IFCNN 等）、Transformer（SwinFusion）以及最新的扩散模型（FusionDiff, VDMUFuse, Mask-DiFuser 等）。
*   **评估指标**：采用 $Q_{abf}$、$Q_{MI}$、$Q_G$、$Q_P$、$Q_E$ 和 MS-SSIM 六项定量指标。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA V100 GPU**。
*   **训练设置**：训练 10,000 个 epoch，batch size 为 32，使用 Adam 优化器。
*   **时长**：文中未明确给出具体的训练总时长，但提到旋转等变性对推理速度的影响微乎其微。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同分布的数据集上进行了测试，涵盖了从实验室环境到复杂道路场景的多种情况。
*   **消融实验**：
    *   验证了旋转等变模块的有效性（对比无等变性的模型）。
    *   对比了不同的滤波器参数化方法（B-Conv vs G-Conv vs F-Conv）。
    *   **泛化性验证**：将旋转等变设计嵌入到其他扩散模型（如 FusionDiff）中，均观察到了性能提升。
*   **评价**：实验设计全面，包含了定量对比、定性视觉对比、差异图（Difference Image）分析以及等变误差图的可视化，能够客观、公平地证明方法的优越性。

### 6. 主要结论与发现
*   **性能领先**：ReDiffuse 在所有四个数据集的几乎所有指标上均排名第一，平均性能提升达 0.28%–6.64%。
*   **结构保持**：视觉效果显示，ReDiffuse 能更自然地恢复物体轮廓（如海豚边缘）和线性结构，有效消除了伪影。
*   **参数高效**：在提升性能的同时，参数量减少了约 72%，证明了旋转等变性带来的归纳偏置（Inductive Bias）非常高效。
*   **理论一致性**：实验测得的等变误差图与论文的理论推导高度吻合。

### 7. 优点
*   **理论支撑深厚**：不仅提出了模型，还为扩散模型中的各个组件提供了详尽的旋转等变误差分析。
*   **轻量且强大**：通过参数共享实现了模型压缩与性能提升的双赢。
*   **即插即用**：展示了该旋转等变架构可以作为通用模块提升其他扩散模型的融合质量。

### 8. 不足与局限
*   **极端散焦失效**：在“散焦扩散效应（Defocus Spread Effect）”极强的区域，模型可能会出现错误的结构选择，无法从极度模糊中提取正确纹理。
*   **局部性限制**：目前的等变性主要基于局部特征，对于需要长程依赖（Non-local）的严重模糊区域，仅靠旋转等变性可能不足。
*   **应用范围**：目前仅针对多聚焦图像融合，尚未探讨在多模态（如红外-可见光）融合中的表现，尽管其几何保持特性可能同样适用。

（完）
