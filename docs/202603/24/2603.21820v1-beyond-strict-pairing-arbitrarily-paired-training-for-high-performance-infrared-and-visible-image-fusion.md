---
title: "Beyond Strict Pairing: Arbitrarily Paired Training for High-Performance Infrared and Visible Image Fusion"
title_zh: 超越严格配对：面向高性能红外与可见光图像融合的任意配对训练
authors: "Yanglin Deng, Tianyang Xu, Chunyang Cheng, Hui Li, Xiao-jun Wu, Josef Kittler"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21820v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 红外与可见光图像融合及非对齐训练范式
tldr: 该研究针对红外与可见光图像融合中严格配对数据获取难的问题，提出了非配对（UPTP）和任意配对（APTP）训练范式。通过设计涵盖CNN、Transformer和GAN的轻量级框架，该方法能在极少量且未对齐的数据上实现高性能融合。实验证明，其在小规模数据集上的表现可媲美规模大百倍的配对数据集，显著降低了数据采集成本并增强了模型鲁棒性。
selection_source: fresh_fetch
motivation: 解决红外与可见光图像融合中严格配对数据采集成本高昂且限制跨模态关系学习的问题。
method: 提出任意配对训练范式（APTP），并构建了包含三种主流架构的轻量级基准模型及配套损失函数。
result: 在极少量且内容不一致的数据集上训练，达到了与规模大100倍的配对数据集相当的融合性能。
conclusion: 证明了非配对和任意配对训练在图像融合中的可行性，为低成本、高性能的IVIF研究提供了新路径。
---

## 摘要
红外与可见光图像融合（IVIF）在保留自然纹理和显著热特征的同时，结合了互补的模态信息。现有的解决方案主要依赖于大量严格对齐的图像对进行训练。然而，由于对齐过程成本高昂且耗费人力，获取此类数据通常是不切实际的。此外，在训练过程中维持严格的配对设置限制了跨模态关系的丰富度，从而限制了泛化性能。为此，本研究通过系统地探讨用于高性能 IVIF 的非配对训练范式（UPTP）和任意配对训练范式（APTP），挑战了严格配对训练范式（SPTP）的必要性。我们建立了 APTP 的理论目标，反映了 UPTP 和 SPTP 之间的互补性。更重要的是，我们开发了一个实用的框架，即使在训练数据严重受限且未对齐的情况下，也能显著丰富跨模态关系。为了验证我们的主张，我们设计了三个端到端的轻量级基准模型以及一套创新的损失函数，涵盖了三种经典框架（CNN、Transformer、GAN）。综合实验表明，所提出的 APTP 和 UPTP 是可行的，并且能够在内容不一致且严重受限的红外与可见光数据集上训练模型，其性能可与在 SPTP 下规模大 100 倍的数据集相媲美。这一发现从根本上缓解了数据收集的成本和难度，同时从数据角度增强了模型的鲁棒性，为 IVIF 研究提供了一个可行的解决方案。代码可在 https://github.com/yanglinDeng/IVIF_unpair 获取。

## Abstract
Infrared and visible image fusion(IVIF) combines complementary modalities while preserving natural textures and salient thermal signatures. Existing solutions predominantly rely on extensive sets of rigidly aligned image pairs for training. However, acquiring such data is often impractical due to the costly and labour-intensive alignment process. Besides, maintaining a rigid pairing setting during training restricts the volume of cross-modal relationships, thereby limiting generalisation performance. To this end, this work challenges the necessity of Strictly Paired Training Paradigm (SPTP) by systematically investigating UnPaired and Arbitrarily Paired Training Paradigms (UPTP and APTP) for high-performance IVIF. We establish a theoretical objective of APTP, reflecting the complementary nature between UPTP and SPTP. More importantly, we develop a practical framework capable of significantly enriching cross-modal relationships even with severely limited and unaligned training data. To validate our propositions, three end-to-end lightweight baselines, alongside a set of innovative loss functions, are designed to cover three classic frameworks (CNN, Transformer, GAN). Comprehensive experiments demonstrate that the proposed APTP and UPTP are feasible and capable of training models on a severely limited and content-inconsistent infrared and visible dataset, achieving performance comparable to that of a dataset 100$\times$ larger in SPTP. This finding fundamentally alleviates the cost and difficulty of data collection while enhancing model robustness from the data perspective, delivering a feasible solution for IVIF studies. The code is available at \href{https://github.com/yanglinDeng/IVIF_unpair}{\textcolor{blue}{https://github.com/yanglinDeng/IVIF\_unpair}}.

---

## 论文详细总结（自动生成）

这是一篇关于红外与可见光图像融合（IVIF）训练范式创新的资深学术论文总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 IVIF 模型高度依赖**严格配对训练范式（SPTP）**，即需要空间和时间上精确对齐的红外与可见光图像对。
*   **研究动机**：
    *   **数据获取成本高**：多模态传感器的校准、对齐和同步过程极其耗时耗力。
    *   **泛化能力受限**：严格配对限制了模型学习跨模态关系的样板数量，导致模型在面对未见过的场景或分布差异时表现脆弱。
    *   **理论挑战**：作者质疑“严格配对”是否为训练高性能融合模型的必要条件，并探索利用非对齐数据提升性能的可能性。

### 2. 方法论
*   **核心思想**：提出**任意配对训练范式（APTP）**。其核心逻辑是：图像融合本质上是像素级的特征提取与组合，模型应该学习的是跨模态的通用特征关系，而非特定的场景内容一致性。
*   **关键技术细节**：
    *   **范式定义**：将训练集从 $N$ 个对齐对扩展到 $N^2$ 个任意组合对。APTP 被定义为 SPTP（对齐）和 UPTP（完全不对齐）的并集。
    *   **自适应加权损失函数**：设计了一套通用的线性加权函数 $W(a; a, b) = a / (a+b)$，用于自动决定每个像素在强度（Intensity）、梯度（Gradient）和结构（SSIM）维度上的贡献比例。
    *   **三大基准架构**：为了证明通用性，作者构建了三个轻量级端到端基准模型：
        1.  **CNN**：侧重局部上下文特征。
        2.  **Transformer**：侧重长距离依赖建模。
        3.  **GAN**：采用生成-验证模式增强模态特征。
*   **算法流程**：通过最大化对数似然估计，将优化目标从“内容一致性”转向“跨模态独立概率分布的组合”，使模型即使输入内容不相关的图像也能收敛。

### 3. 实验设计
*   **数据集**：涵盖了 MSRS、M3FD、LLVIP、RoadScene 和 TNO 五个主流基准数据集。
*   **对比实验**：
    *   **内部对比**：对比了 SPTP、UPTP 和 APTP 在相同架构下的表现。
    *   **数据缩放实验**：测试了从 15 对基础数据扩展到 22,500 对任意组合数据的性能增益。
    *   **SOTA 对比**：与 DeF、LRRNet、MetaFusion、DDFM、CrossFuse 等 10 余种最先进方法进行定量和定性对比。
*   **评价指标**：使用信息熵（EN）、互信息（MI）、视觉信息保真度（VIF）、边缘保持度（Qabf）和结构相似性（SSIM）。

### 4. 资源与算力
*   **硬件平台**：使用了一台配备 **NVIDIA GeForce RTX 3090 GPU** 的 Linux 服务器。
*   **模型规模**：强调“轻量级”，其中一个基准模型的大小仅为 **0.88MB**。
*   **训练细节**：文中给出了超参数设置（$\alpha=1, \beta=0.2, \lambda=0.01$），但未明确说明具体的总训练时长（小时数）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的充分验证，包括：
    *   收敛性分析（证明了 UPTP 也能稳定收敛）。
    *   跨数据集训练验证（如用 MSRS 的可见光配对 M3FD 的红外光）。
    *   消融实验（针对不同数据量级和配对策略）。
*   **客观性**：实验涵盖了三种完全不同的网络架构（CNN/Trans/GAN），证明了结论的普适性而非偶然性。对比 SOTA 方法时使用了多个未见过的测试集，验证了强悍的泛化能力。

### 6. 主要结论与发现
*   **可行性确认**：非配对训练（UPTP）是完全可行的，模型能从视觉混乱的中间状态最终学习到正确的融合逻辑。
*   **以小博大**：在仅有 150 对原始数据的基础上进行任意配对训练，其效果可媲美规模大 **100 倍**的严格配对数据集。
*   **鲁棒性增强**：APTP 通过丰富跨模态关系的样板，显著提升了模型在不同数据集上的泛化表现。

### 7. 优点
*   **范式突破**：打破了 IVIF 领域对对齐数据的长期依赖，极大地降低了数据采集门槛。
*   **理论完备**：从贝叶斯优化和概率独立性的角度为非配对训练提供了数学支撑。
*   **高效轻量**：提出的基准模型极小，易于部署，且在性能上超越了许多复杂的重型模型。

### 8. 不足与局限
*   **噪声敏感度**：在某些极端非配对情况下，信息熵（EN）会出现波动，表明模型在过滤无关噪声方面仍有优化空间。
*   **容量瓶颈**：实验发现当基础数据量极小时，过度增加配对组合可能导致轻量级模型达到容量上限，性能提升边际递减。
*   **语义对齐缺失**：虽然像素级融合表现优异，但在需要极高语义一致性的下游任务（如精确的目标分割）中，完全随机配对是否会引入细微的伪影尚需进一步验证。

（完）
