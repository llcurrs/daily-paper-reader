---
title: "Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens"
title_zh: Le MuMo JEPA：基于可学习融合 Token 的多模态自监督表示学习
authors: "Ciem Cornelissen, Sam Leroux, Pieter Simoens"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24327v1"
tags: ["query:mm-fus"]
score: 8.0
evidence: 带有可学习融合令牌的多模态自监督表示学习
tldr: 本研究针对自监督学习多局限于单一模态的问题，提出了Le MuMo JEPA框架，旨在从RGB图像及其对齐的辅助模态（如LiDAR深度或热成像）中学习统一表示。该方法在共享Transformer中引入可学习的融合标记作为潜在瓶颈，并采用剪枝融合策略以提升效率。实验表明，该模型在Waymo、nuScenes和FLIR等自动驾驶数据集上的下游任务中，实现了性能与计算效率的最佳平衡。
selection_source: fresh_fetch
motivation: 旨在解决当前自监督学习方法大多仅处理单一模态，从而无法利用异构传感器提供的互补信息的问题。
method: 采用可学习的融合标记作为模态间的潜在瓶颈，并结合剪枝融合策略与SIGReg正则化来学习多模态联合表示。
result: 在多个自动驾驶基准测试中，该方法在保持较低计算成本的同时，显著提升了目标检测、深度估计和分割任务的性能。
conclusion: Le MuMo JEPA通过高效的跨模态注意力机制和瓶颈设计，为多模态自监督表示学习提供了一个高性能且低功耗的解决方案。
---

## 摘要
自监督学习已成为一种无需手动标注即可学习视觉表示的强大范式，但大多数方法仍运行在单一模态上，因此错失了异构传感器提供的互补结构。我们提出了 Le MuMo JEPA，这是一个从 RGB 图像和对齐的伴随模态中学习统一表示的自监督框架。在我们的驾驶实验中，第二种模态是与相机对齐的 LiDAR 深度；我们还在 Teledyne FLIR ADAS 基准测试中评估了 RGB-热成像训练和迁移。我们的方法通过学习融合 Token 将 LeJEPA 扩展到多模态设置，这些 Token 在共享 Transformer 内部的模态特定补丁主干（patch stems）之间充当潜瓶颈（latent bottleneck）。我们的默认模型采用了一种剪枝融合策略：在初始跨模态注意力层之后，丢弃模态特定的 Token，迫使跨模态信息进入共享的融合 Token 网格，作为一种高效的潜瓶颈，随后对联合多模态 CLS 嵌入应用草图各向同性高斯正则化（SIGReg）。在 Waymo 数据集上，Le MuMo JEPA 在从零开始训练的多模态基准模型中，在下游补丁探测（patch probes）上表现出最强的性能-效率权衡，改进了 CenterNet 检测和稠密深度，同时在分割任务上保持了竞争力。在 nuScenes 的从零开始训练中，Le MuMo JEPA 依然是最强的模型，并且在 FLIR 上也取得了最佳结果，尤其是在经过 Waymo 初始化的微调之后。在我们的研究中，它还以显著更低的计算量、内存占用和估计训练时间，保持了最佳的整体准确率-效率平衡。

## Abstract
Self-supervised learning has emerged as a powerful paradigm for learning visual representations without manual annotations, yet most methods still operate on a single modality and therefore miss the complementary structure available from heterogeneous sensors. We present Le MuMo JEPA, a self-supervised framework that learns unified representations from RGB images and aligned companion modalities. In our driving experiments, the second modality is camera-aligned LiDAR depth; we also evaluate RGB-thermal training and transfer on the Teledyne FLIR ADAS benchmark. Our approach extends LeJEPA to the multi-modal setting by learning fusion tokens that act as a latent bottleneck between modality-specific patch stems inside a shared transformer. Our default model employs a pruned fusion strategy: after an initial cross-modal attention layer, modality-specific tokens are dropped, forcing cross-modal information into the shared fusion-token grid as an efficient latent bottleneck before Sketched Isotropic Gaussian Regularization (SIGReg) is applied to the joint multimodal CLS embedding. On Waymo, Le MuMo JEPA gives the strongest performance-efficiency trade-off on downstream patch probes among the from-scratch multimodal baselines, improving CenterNet detection and dense depth while remaining competitive on segmentation. Under from-scratch training on nuScenes, Le MuMo JEPA remains the strongest model, and it also gives the best FLIR results, especially after Waymo-initialized fine-tuning. It also retains the best overall accuracy-efficiency balance in our study at substantially lower compute, memory, and estimated training time.

---

## 论文详细总结（自动生成）

以下是对论文《Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的自监督学习（SSL）方法（如 DINO, MAE, I-JEPA）大多局限于单一模态（通常是 RGB 图像），忽略了自动驾驶等现实场景中异构传感器（如 LiDAR、热成像）提供的互补信息。
*   **核心问题**：如何在不依赖昂贵人工标注的前提下，有效地融合结构差异巨大的多模态信号，并解决全注意力机制带来的高计算成本问题。
*   **研究背景**：论文基于 LeJEPA 框架，引入了“可学习融合 Token”和“潜瓶颈（Latent Bottleneck）”概念，旨在构建一个既能捕捉跨模态关联又具备极高计算效率的多模态表示学习模型。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 RGB 和辅助模态（深度图或热图）映射到统一的 2D 空间网格，通过一组专门的“融合 Token”在 Transformer 内部充当信息中转站。
*   **关键技术细节**：
    *   **输入处理**：RGB 图像和对齐的辅助模态（如投影后的 LiDAR 深度图）分别通过各自的 Patch Stem 进行分块（14x14）。
    *   **可学习融合 Token (FT)**：引入与空间位置对应的 $N$ 个融合 Token。在 Transformer 的第 0 层，融合 Token 通过交叉注意力同时聚合来自 RGB 和辅助模态对应位置的信息。
    *   **剪枝融合策略 (FT-Pruned)**：这是效率提升的关键。在第 0 层之后，模型会丢弃所有的 RGB 和辅助模态 Token，仅保留融合 Token 和 CLS Token 进入后续层。这形成了一个“潜瓶颈”，迫使模型在早期就完成跨模态信息的压缩与整合。
    *   **SIGReg 正则化**：采用草图各向同性高斯正则化（SIGReg），将联合多模态的 CLS 嵌入推向标准正态分布，从而防止表示崩溃，且无需负采样或动量教师网络。
    *   **训练目标**：结合不变性损失（Invariance Loss）和 SIGReg，使模型学习到对增强变换不敏感且分布均匀的特征。

### 3. 实验设计
*   **数据集与场景**：
    *   **Waymo Open Dataset**：主要驾驶基准，用于评估 3D 检测、深度估计和语义分割。
    *   **nuScenes**：用于验证从零开始训练（From-scratch）的泛化能力。
    *   **Teledyne FLIR ADAS**：用于评估 RGB-热成像模态的迁移学习和微调性能。
*   **Benchmark 与任务**：使用冻结的补丁探测器（Frozen Patch Probes）测试特征质量，包括 CenterNet 风格的 3D 检测（XY/XZ mAP）、稠密深度估计（MAE）和语义分割（mIoU）。
*   **对比方法**：
    *   单模态基准：LeJEPA (RGB/LiDAR)、DINOv3。
    *   多模态基准：MultiMAE (自监督与多任务版)、ImageBind (对比学习类)。
    *   架构消融：早期融合（Early Fusion）、后期融合（Late Fusion）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **AMD EPYC 9275F 24核处理器** 和 **NVIDIA H200 GPU** 的系统上运行。
*   **训练时长与开销**：
    *   默认模型（Le MuMo JEPA）在 Waymo 子集上训练 5 个 Epoch 约需 **55.09 分钟**。
    *   总算力消耗约为 **86.0 PFLOPs**，峰值显存占用为 **12.11 GB**。
    *   相比于不剪枝的“持久路由（Persistent Routing）”方案，训练时间缩短了约 3 倍，显存占用降低了约 66%。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了三个主流自动驾驶/多模态数据集，进行了从零训练、跨数据集迁移和下游微调等多维度测试。
*   **消融实验**：针对融合策略（剪枝 vs 持久）、损失函数（SIGReg vs VICReg）、监督方式（单次前向 vs 三次前向）进行了详细对比。
*   **充分性评价**：实验设计较为充分且客观。通过在相同的硬件和代码框架下重新训练所有基准模型（如 ImageBind 和 MultiMAE），确保了对比的公平性（Matched from-scratch protocol），避免了直接引用预训练权重带来的偏差。

### 6. 主要结论与发现
*   **性能领先**：Le MuMo JEPA 在 Waymo 和 nuScenes 的所有空间指标上均优于单模态和现有的多模态自监督方法。
*   **效率极高**：通过 Token 剪枝，模型在保持高性能的同时，计算成本远低于全注意力模型。
*   **正则化优势**：SIGReg 在处理多模态联合嵌入时比传统的对比学习或方差正则化（VICReg）更有效，能更好地对齐异构特征空间。
*   **迁移能力**：在 Waymo 上预训练的模型在 FLIR 热成像任务上表现出极强的零样本迁移和微调潜力。

### 7. 优点：亮点总结
*   **创新的瓶颈设计**：通过第 0 层后的 Token 剪枝，巧妙解决了多模态输入导致的计算量激增问题（注意力计算量减少约 9 倍）。
*   **无需标签的对齐**：利用 JEPA 架构和 SIGReg，实现了无需显式配对标签或负采样的多模态特征对齐。
*   **通用性强**：该框架不局限于 LiDAR，可直接扩展至热成像等其他对齐模态。

### 8. 不足与局限
*   **空间对齐依赖**：目前模型高度依赖于像素级的空间对齐（如将 LiDAR 投影为 2D 深度图），这可能会丢失原始 3D 点云的部分几何结构信息。
*   **模型规模限制**：实验主要集中在 ViT-Small 规模，尚未在大规模模型（如 ViT-Huge）上验证其扩展性。
*   **鲁棒性未知**：对于传感器校准误差、非重叠视野或极端天气下的模态缺失，论文尚未给出深入的鲁棒性分析。
*   **应用限制**：目前仅在驾驶场景验证，对于非空间对齐的多模态数据（如文本-图像）适用性有待研究。

（完）
