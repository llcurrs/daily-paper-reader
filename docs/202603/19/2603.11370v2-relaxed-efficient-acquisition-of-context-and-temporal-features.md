---
title: Relaxed Efficient Acquisition of Context and Temporal Features
title_zh: 松弛化的高效上下文与时间特征采集
authors: "Yunni Qu, Dzung Dinh, Grant King, Whitney Ringwald, Bing Cai Kok, Kathleen Gates, Aiden Wright, Junier Oliva"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11370v2"
tags: ["query:mm-fus"]
score: 8.0
evidence: 缺失成像模态的自适应选择
tldr: 本研究针对生物医学应用中测量成本高昂的问题，提出了REACT框架。该框架首次将初始背景信息（如人口统计学）的选择与随后的纵向自适应特征采集进行联合优化。通过引入Gumbel-Sigmoid松弛技术，REACT实现了端到端的梯度优化，在满足成本约束的同时，能够自适应地选择最有价值的测量项。在多个真实健康数据集中，该方法以更低的成本实现了更优的预测性能。
selection_source: fresh_fetch
motivation: 现有的纵向特征采集研究忽略了初始背景信息选择与后续动态采集之间的协同优化，导致临床工作流中的资源分配不够高效。
method: 提出REACT框架，利用Gumbel-Sigmoid松弛和直通估计器，对离散的采集掩码进行端到端的可微优化。
result: 在真实世界的健康和行为数据集中，REACT在更低的采集成本下取得了优于现有基准模型的预测准确率。
conclusion: 将初始背景选择与时间耦合的采集计划纳入统一优化框架，能显著提升受限资源下的纵向预测效率。
---

## 摘要
在许多生物医学应用中，测量值在推理阶段并非免费获取：每项实验室检查、成像模态或评估都会产生经济成本、时间负担或患者风险。纵向主动特征采集 (LAFA) 旨在通过随时间自适应地选择测量值，在这些约束下优化预测性能，但由于时间耦合决策（错过的早期测量无法重新获取，且采集选择会影响所有下游预测），该问题本质上仍具有挑战性。此外，现实世界的临床工作流程通常始于初始入组阶段，在此期间会一次性收集相对稳定的上下文描述符（如人口统计学或基线特征），并随后作为纵向决策的条件。尽管具有实际重要性，但入组上下文的高效选择尚未与时间自适应采集进行联合研究。因此，我们提出了 REACT（Relaxed Efficient Acquisition of Context and Temporal features），这是一个端到端可微框架，可同时优化 (i) 入组上下文描述符的选择，以及 (ii) 成本约束下纵向测量的自适应特征-时间采集计划。REACT 采用带有直通估计 (straight-through estimation) 的 Gumbel-Sigmoid 松弛，以实现对离散采集掩码的基于梯度的优化，从而允许从预测损失和采集成本进行直接反向传播。在现实世界的纵向健康和行为数据集上，与现有的纵向采集基准相比，REACT 以更低的采集成本实现了更高的预测性能，证明了在统一优化框架内对入组和时间耦合采集进行建模的优势。

## Abstract
In many biomedical applications, measurements are not freely available at inference time: each laboratory test, imaging modality, or assessment incurs financial cost, time burden, or patient risk. Longitudinal active feature acquisition (LAFA) seeks to optimize predictive performance under such constraints by adaptively selecting measurements over time, yet the problem remains inherently challenging due to temporally coupled decisions (missed early measurements cannot be revisited, and acquisition choices influence all downstream predictions). Moreover, real-world clinical workflows typically begin with an initial onboarding phase, during which relatively stable contextual descriptors (e.g., demographics or baseline characteristics) are collected once and subsequently condition longitudinal decision-making. Despite its practical importance, the efficient selection of onboarding context has not been studied jointly with temporally adaptive acquisition. We therefore propose REACT (Relaxed Efficient Acquisition of Context and Temporal features), an end-to-end differentiable framework that simultaneously optimizes (i) selection of onboarding contextual descriptors and (ii) adaptive feature--time acquisition plans for longitudinal measurements under cost constraints. REACT employs a Gumbel--Sigmoid relaxation with straight-through estimation to enable gradient-based optimization over discrete acquisition masks, allowing direct backpropagation from prediction loss and acquisition cost. Across real-world longitudinal health and behavioral datasets, REACT achieves improved predictive performance at lower acquisition costs compared to existing longitudinal acquisition baselines, demonstrating the benefit of modeling onboarding and temporally coupled acquisition within a unified optimization framework.

---

## 论文详细总结（自动生成）

以下是对论文《Relaxed Efficient Acquisition of Context and Temporal Features》（REACT）的结构化深度总结：

---

### 1. 核心问题与整体含义
该论文针对**纵向主动特征采集（Longitudinal Active Feature Acquisition, LAFA）**问题展开研究。在临床医疗和行为监测等实际场景中，获取数据（如实验室检查、影像扫描、问卷评估）并非免费，而是伴随着经济成本、时间负担或患者风险。
*   **研究动机**：现有的 LAFA 方法通常将所有特征视为同质化，忽略了现实中“入组（Onboarding）+ 随访（Follow-up）”的二阶段结构。即：在开始时一次性收集稳定的上下文信息（如人口统计学），随后根据这些信息自适应地决定何时、采集哪些随访特征。
*   **核心挑战**：决策在时间上是耦合的（错过的早期测量无法弥补），且离散的采集决策（采或不采）导致目标函数不可微，传统强化学习（RL）方法在处理此类组合动作空间时往往训练不稳定。

### 2. 方法论
论文提出了 **REACT** 框架，其核心思想是将离散的采集决策转化为端到端可微的优化问题。
*   **核心组件**：
    1.  **上下文选择器（Context Selector, $\alpha$）**：在 $t=0$ 时刻决定采集哪些初始背景特征。
    2.  **纵向规划器（Longitudinal Planner, $\pi_\theta$）**：基于已观测的历史和上下文，动态预测未来的采集掩码（Mask）。
    3.  **预测器（Predictor, $f_\phi$）**：利用已采集的稀疏特征进行下游任务预测。
*   **关键技术细节**：
    *   **Gumbel-Sigmoid 松弛**：使用 Gumbel-Sigmoid 分布替代离散采样，使二值掩码（0/1）变得连续可微。
    *   **直通估计器（Straight-Through Estimator, STE）**：在前向传播时使用离散阈值，在反向传播时通过连续松弛变量传递梯度，从而实现端到端训练。
    *   **自迭代训练（Self-Iterative Training）**：类似于 DAgger 算法，规划器通过在自身生成的轨迹（On-policy）上进行训练，以增强对测试时状态的泛化能力。
*   **优化目标**：最小化“预测损失 + $\lambda \times$ 采集成本”的加权和，其中 $\lambda$ 用于调节准确率与成本的权衡。

### 3. 实验设计
*   **数据集**：
    *   **行为类**：CHEEARS（大学生饮酒行为监测）、ILIADD（心理健康评估）。
    *   **临床类**：OAI（膝关节炎进展，包含 KLG 和 WOMAC 两个任务）、ADNI（阿尔茨海默症进展）。
*   **Benchmark 与对比方法**：
    *   **ASAC**：基于 Actor-Critic 的主动传感方法。
    *   **RAS / AS**：针对连续时间的风险规避主动传感方法。
    *   **DIME**：一种非纵向的动态特征选择基准（经修改以适应纵向场景）。
    *   **全特征基准**：假设所有特征均免费获取时的预测上限。

### 4. 资源与算力
*   **硬件环境**：使用了单张 **NVIDIA L40S GPU** 和 **Intel Xeon Gold 6526Y CPU**。
*   **训练细节**：模型在所有数据集上统一训练 1000 个 Batch（Batch Size 为 64），其中前 50 个 Batch 为预热阶段，后 950 个为自迭代训练阶段。
*   **效率表现**：论文通过实验证明，REACT 的训练和推理时间与 DIME、RAS 等深度学习基准方法相当或更快，具有较好的工程实用性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同的预测任务，跨越行为科学和临床医学两个领域。
*   **消融实验**：
    *   验证了**上下文选择器**的有效性（对比“全选”和“不选”上下文）。
    *   验证了**自迭代训练**的必要性（对比仅使用离线参考状态训练）。
*   **客观性**：实验通过调节 $\lambda$ 绘制了完整的“成本-性能曲线”（AUROC/AUPRC vs. Cost），并提供了特征采集轨迹的可视化分析。对比基准均使用了官方实现并进行了超参数微调，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：在所有数据集上，REACT 在相同的成本预算下均取得了比现有 LAFA 基准更高的预测准确率。
*   **自适应性**：REACT 能够学习到数据集特有的采集策略。例如，在 ADNI 数据集中，它倾向于早期采集高价值影像指标后迅速停止，以节省后续成本。
*   **上下文的重要性**：证明了联合优化初始上下文选择与后续动态采集，比单纯进行动态采集更有效。

### 7. 优点
*   **实用性强**：首次形式化了临床工作流中“入组+随访”的二阶段采集模式，更贴近真实应用。
*   **优化高效**：通过可微松弛规避了强化学习中复杂的信用分配（Credit Assignment）问题，训练更稳定。
*   **可解释性**：通过可视化采集轨迹，能够清晰观察到模型在不同阶段对不同特征的偏好。

### 8. 不足与局限
*   **成本假设**：实验中假设特征成本是静态且预定义的，但在现实中，某些检查的成本可能会随时间或医疗资源波动。
*   **离散时间限制**：虽然 REACT 在离散时间步上表现优异，但对于某些需要极高频率、连续时间决策的重症监护场景，其表现尚未验证。
*   **超参数敏感性**：权衡系数 $\lambda$ 的选择对最终策略影响较大，实际部署时可能需要针对特定预算进行多次调优。

（完）
