---
title: "Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data"
title_zh: 用于多模态生物医学数据可解释少样本阿尔茨海默病预测的表格大语言模型
authors: "Sophie Kearney, Shu Yang, Zixuan Wen, Weimin Lyu, Bojian Hou, Duy Duong-Tran, Tianlong Chen, Jason H. Moore, Marylyn D. Ritchie, Chao Chen, Li Shen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17191v1"
tags: ["query:mm-fus"]
score: 8.0
evidence: 处理少量且不完整的医学多模态数据
tldr: 针对阿尔茨海默病（AD）诊断中表格数据规模小且不完整的挑战，本文提出了TAP-GPT框架。该框架基于TableGPT2进行领域适配，利用表格提示词而非纯文本进行微调，实现了多模态生物标志物数据的少样本分类。实验表明，TAP-GPT在少样本场景下优于传统机器学习模型，且在数据缺失情况下表现稳定，并能提供符合生物学逻辑的可解释性推理，为临床决策支持系统奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决阿尔茨海默病诊断中表格生物标志物数据量小、缺失严重且传统深度学习模型表现不佳的问题。
method: 提出了基于TableGPT2微调的TAP-GPT框架，利用表格化提示词对多模态生物标志物和影像数据进行少样本分类。
result: TAP-GPT在少样本设置下优于传统机器学习模型，且在无需插值的情况下对真实及模拟的缺失数据表现出极高的稳定性。
conclusion: 本研究首次系统地将表格专用大模型应用于多模态AD预测，证明了其在处理结构化临床预测任务和提供可解释性推理方面的显著优势。
---

## 摘要
阿尔茨海默病 (AD) 的准确诊断需要处理表格形式的生物标志物数据，然而这类数据通常规模较小且不完整，深度学习模型在这些数据上的表现往往难以超越经典方法。预训练大语言模型 (LLMs) 提供了少样本泛化、结构化推理和可解释的输出，为临床预测带来了强有力的范式转变。我们提出了 TAP-GPT (Tabular Alzheimer's Prediction GPT)，这是一个基于 TableGPT2 构建的领域自适应表格 LLM 框架，并针对少样本 AD 分类进行了微调，使用表格提示词而非纯文本。我们在四个源自 ADNI 的数据集上评估了 TAP-GPT，包括用于二分类 AD 预测的 QT-PAD 生物标志物以及区域级结构化 MRI、淀粉样蛋白 PET 和 tau 蛋白 PET。在多模态和单模态设置下，TAP-GPT 相比其骨干模型有所改进，在少样本设置中优于传统机器学习基准，同时与最先进的通用 LLMs 保持竞争力。我们展示了特征选择可以减轻高维输入带来的性能下降，并且 TAP-GPT 在模拟和真实世界的缺失情况下无需插补即可保持稳定的性能。此外，TAP-GPT 产生了与已确立的 AD 生物学相一致的结构化、模态感知的推理，并在自我反思下表现出更高的稳定性，支持其在迭代多智能体系统中的应用。据我们所知，这是表格专用 LLM 在基于多模态生物标志物的 AD 预测中的首次系统应用，证明了此类预训练模型可以有效处理结构化临床预测任务，并为表格 LLM 驱动的多智能体临床决策支持系统奠定了基础。源代码可在 GitHub 上公开获取：https://github.com/sophie-kearney/TAP-GPT。

## Abstract
Accurate diagnosis of Alzheimer's disease (AD) requires handling tabular biomarker data, yet such data are often small and incomplete, where deep learning models frequently fail to outperform classical methods. Pretrained large language models (LLMs) offer few-shot generalization, structured reasoning, and interpretable outputs, providing a powerful paradigm shift for clinical prediction. We propose TAP-GPT Tabular Alzheimer's Prediction GPT, a domain-adapted tabular LLM framework built on TableGPT2 and fine-tuned for few-shot AD classification using tabular prompts rather than plain texts. We evaluate TAP-GPT across four ADNI-derived datasets, including QT-PAD biomarkers and region-level structural MRI, amyloid PET, and tau PET for binary AD classification. Across multimodal and unimodal settings, TAP-GPT improves upon its backbone models and outperforms traditional machine learning baselines in the few-shot setting while remaining competitive with state-of-the-art general-purpose LLMs. We show that feature selection mitigates degradation in high-dimensional inputs and that TAP-GPT maintains stable performance under simulated and real-world missingness without imputation. Additionally, TAP-GPT produces structured, modality-aware reasoning aligned with established AD biology and shows greater stability under self-reflection, supporting its use in iterative multi-agent systems. To our knowledge, this is the first systematic application of a tabular-specialized LLM to multimodal biomarker-based AD prediction, demonstrating that such pretrained models can effectively address structured clinical prediction tasks and laying the foundation for tabular LLM-driven multi-agent clinical decision-support systems. The source code is publicly available on GitHub: https://github.com/sophie-kearney/TAP-GPT.

---

## 论文详细总结（自动生成）

以下是对论文《Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：阿尔茨海默病（AD）的诊断依赖于表格形式的生物标志物数据（如 AT[N] 系统）。然而，这类临床数据通常具有**样本量小、维度高、缺失值多**的特点。
*   **研究背景**：传统的深度学习模型在小样本表格数据上往往表现不如随机森林或 XGBoost 等经典机器学习方法，且缺乏临床解释性。
*   **整体含义**：本文旨在探索预训练大语言模型（LLM）在处理结构化医疗表格数据方面的潜力，通过引入领域自适应的表格专用 LLM（TAP-GPT），解决少样本场景下的 AD 预测问题，并提供符合生物学逻辑的推理过程。

### 2. 论文提出的方法论
*   **核心思想**：构建 **TAP-GPT (Tabular Alzheimer’s Prediction GPT)** 框架，基于 **TableGPT2**（结合了语义表格编码器和 Qwen2.5 解码器）进行领域微调。
*   **关键技术细节**：
    *   **架构**：采用编码器-解码器结构。语义表格编码器保持固定，以保留通用的表格结构理解能力；解码器部分使用 **QLoRA** 进行参数高效微调。
    *   **提示词工程**：设计了四种提示格式（零样本/少样本 × 表格化/序列化）。其中“表格化提示”直接保留列对齐结构，而非将其转换为纯叙述性文本。
    *   **上下文学习 (ICL)**：在提示词中加入 $k$ 个已标注的示例病患数据，引导模型对目标病患进行推理。
    *   **可解释性增强**：通过思维链（CoT）引导模型输出 JSON 格式的结果，包含预测标签、推理逻辑和置信度分数。
    *   **特征选择**：针对高维影像数据，引入 LASSO 或 LLM 引导的特征排名，以应对 LLM 的上下文长度限制。

### 3. 实验设计
*   **数据集**：全部源自 ADNI 数据库，分为四类：
    1.  **QT-PAD**：包含 15 种临床标志物（CSF、MRI 衍生指标、APOE4 等）的多模态数据集。
    2.  **淀粉样蛋白 PET**、**Tau 蛋白 PET**、**结构化 MRI**：三个基于区域（ROI）汇总的单模态影像数据集。
*   **Benchmark（基准任务）**：AD vs. 认知正常（CN）的二分类任务。
*   **对比方法**：
    *   **传统机器学习**：逻辑回归 (LR)、随机森林 (RF)、XGBoost、SVM。
    *   **表格基础模型**：TabPFN（专门针对小样本表格设计的 Transformer）。
    *   **通用 LLMs**：Qwen2.5-7B-Instruct、Qwen3-8B、GPT-4o-mini。
    *   **消融对比**：未微调的 TableGPT2 和 TableGPT-R1。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算集群，配备 **NVIDIA A100 GPU (80GB HBM)**。
*   **算力分配**：每个训练任务分配 1 个 GPU、4 个 CPU 核心和 160GB 系统内存。
*   **实验规模**：共执行了 **335 个实验变体**（涵盖不同模态、种子、提示格式和超参数组合），产生约 0.25 TB 的模型存储数据。

### 5. 实验数量与充分性
*   **实验组数**：所有实验均在 **10 个随机种子**下重复，以确保统计显著性。
*   **消融实验**：
    *   对示例数量 $k$ 和特征数量 $p$ 进行了详细的消融研究（$k$-ablation 和 $p$-ablation）。
    *   进行了**模拟缺失值**（10%-50% 遮盖）和**真实世界缺失值**的鲁棒性测试。
    *   引入了**自我反思（Self-Reflection）**实验，测试模型在多轮对话中的稳定性。
*   **充分性评价**：实验设计非常全面，不仅对比了性能，还深入探讨了特征维度、数据缺失和迭代推理对模型的影响，实验流程客观且公平。

### 6. 论文的主要结论与发现
*   **性能领先**：在少样本（如 8-shot）场景下，TAP-GPT 在多模态 QT-PAD 数据集上的 F1 分数（0.89）显著优于传统机器学习模型和 TabPFN。
*   **结构化优势**：表格化提示词在少样本设置下通常优于序列化文本，证明了 TableGPT2 架构处理结构化数据的有效性。
*   **鲁棒性强**：在无需数据插补的情况下，TAP-GPT 在缺失率高达 40% 时仍能保持稳定性能，优于传统模型。
*   **推理一致性**：TAP-GPT 生成的推理过程符合 AD 生物学逻辑（如关注海马体体积、APOE4 状态等），且在自我反思步骤中比 GPT-4o-mini 更稳定。

### 7. 优点
*   **范式创新**：首次系统地将表格专用 LLM 应用于多模态 AD 预测，弥补了传统模型缺乏解释性的短板。
*   **无需预处理**：能够直接处理含有缺失值的原始表格，省去了复杂的特征工程和插值步骤。
*   **多智能体潜力**：模型在自我反思中的稳定性及其生成的结构化输出，为未来构建多智能体临床协作诊断系统奠定了基础。

### 8. 不足与局限
*   **架构限制**：由于 TableGPT2 的架构限制，表格编码器是固定的，无法随解码器同步更新，可能限制了特征表示的深度优化。
*   **推理错误风险**：定性分析发现模型偶尔会误解特征方向（如误将高 FDG 值视为 AD 迹象）或引用错误的行数据，存在“幻觉”风险。
*   **高维瓶颈**：尽管有特征选择，但在处理极高维度的表格（如全脑体素级数据）时，LLM 的上下文窗口仍是主要限制。
*   **模型依赖**：性能高度依赖于底座 LLM 的能力，且微调过程对算力有一定要求。

（完）
