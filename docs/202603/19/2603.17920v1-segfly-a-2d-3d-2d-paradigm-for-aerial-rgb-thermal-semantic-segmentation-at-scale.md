---
title: "SegFly: A 2D-3D-2D Paradigm for Aerial RGB-Thermal Semantic Segmentation at Scale"
title_zh: SegFly：一种用于大规模航空 RGB-热红外语义分割的 2D-3D-2D 范式
authors: "Markus Gross, Sai Bharadhwaj Matha, Rui Song, Viswanathan Muthuveerappan, Conrad Christoph, Julius Huber, Daniel Cremers"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17920v1"
tags: ["query:mm-fus"]
score: 8.0
evidence: 统一框架内的红外与可见光模态
tldr: "本研究针对无人机RGB-T语义分割数据集规模小、标注成本高及对齐难的问题，提出了SegFly框架。该框架采用2D-3D-2D范式，利用多视图几何冗余，仅需标注不到3%的RGB图像即可自动生成大规模RGB和热成像伪标签，并实现高精度跨模态对齐。基于此构建了包含2万余张图像的SegFly数据集，显著提升了多模态场景理解模型的性能。"
selection_source: fresh_fetch
motivation: 现有的无人机RGB-T数据集受限于高昂的手动标注成本和难以实现的跨模态精确对齐，导致数据规模和多样性不足。
method: 提出一种几何驱动的2D-3D-2D范式，通过将少量标注的2D图像提升至3D语义点云再重投影，实现标签自动传播和跨模态像素级对齐。
result: "该方法实现了91%的标注准确率和87%的对齐准确率，并成功构建了包含2万张RGB图像和1.5万对RGB-T图像的大规模SegFly数据集。"
conclusion: 几何驱动的2D-3D-2D流水线为大规模多模态场景理解提供了高效且可扩展的解决方案，显著增强了视觉基础模型的监督效果。
---

## 摘要
无人机（UAV）语义分割是航空场景理解的基础，但由于人工标注成本高昂以及现成无人机上 RGB-T 精确对齐困难，现有的 RGB 和 RGB-T 数据集在规模、多样性和标注效率方面仍然有限。为了应对这些挑战，我们提出了一种可扩展的几何驱动 2D-3D-2D 范式，该范式利用高重叠航空图像中的多视图冗余，在统一框架内将标签从一小部分人工标注的 RGB 图像自动传播到 RGB 和热红外模态。通过将不到 3% 的 RGB 图像提升为语义 3D 点云并将其重投影到所有视图中，我们的方法能够在大型图像集中生成密集的伪真值（pseudo ground-truth），自动生成 97% 的 RGB 标签和 100% 的热红外标签，在无需任何 2D 手动细化的情况下分别实现了 91% 和 88% 的标注准确率。我们进一步将这种 2D-3D-2D 范式扩展到跨模态图像配准，利用 3D 几何作为中间对齐空间，在无需硬件级同步的情况下，实现了全自动、强像素级的 RGB-T 对齐，配准准确率达到 87%。将我们的框架应用于现有的地理参考航空图像，我们构建了 SegFly，这是一个包含超过 20,000 张高分辨率 RGB 图像和超过 15,000 对几何对齐的 RGB-T 图像对的大规模基准测试集，涵盖了跨越多个高度和季节的各种城市、工业和农村环境。在 SegFly 上，我们建立了用于 RGB 和热红外语义分割的 Firefly 基准模型，并证明了传统架构和视觉基础模型都能从 SegFly 的监督中显著获益，突显了几何驱动的 2D-3D-2D 流水线在可扩展多模态场景理解方面的潜力。数据和代码可在 https://github.com/markus-42/SegFly 获取。

## Abstract
Semantic segmentation for uncrewed aerial vehicles (UAVs) is fundamental for aerial scene understanding, yet existing RGB and RGB-T datasets remain limited in scale, diversity, and annotation efficiency due to the high cost of manual labeling and the difficulties of accurate RGB-T alignment on off-the-shelf UAVs. To address these challenges, we propose a scalable geometry-driven 2D-3D-2D paradigm that leverages multi-view redundancy in high-overlap aerial imagery to automatically propagate labels from a small subset of manually annotated RGB images to both RGB and thermal modalities within a unified framework. By lifting less than 3% of RGB images into a semantic 3D point cloud and reprojecting it into all views, our approach enables dense pseudo ground-truth generation across large image collections, automatically producing 97% of RGB labels and 100% of thermal labels while achieving 91% and 88% annotation accuracy without any 2D manual refinement. We further extend this 2D-3D-2D paradigm to cross-modal image registration, using 3D geometry as an intermediate alignment space to obtain fully automatic, strong pixel-level RGB-T alignment with 87% registration accuracy and no hardware-level synchronization. Applying our framework to existing geo-referenced aerial imagery, we construct SegFly, a large-scale benchmark with over 20,000 high-resolution RGB images and more than 15,000 geometrically aligned RGB-T pairs spanning diverse urban, industrial, and rural environments across multiple altitudes and seasons. On SegFly, we establish the Firefly baseline for RGB and thermal semantic segmentation and show that both conventional architectures and vision foundation models benefit substantially from SegFly supervision, highlighting the potential of geometry-driven 2D-3D-2D pipelines for scalable multi-modal scene understanding. Data and Code available at https://github.com/markus-42/SegFly.

---

## 论文详细总结（自动生成）

以下是对论文《SegFly: A 2D-3D-2D Paradigm for Aerial RGB-Thermal Semantic Segmentation at Scale》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：无人机（UAV）航空场景理解中，RGB-热红外（RGB-T）语义分割面临三大瓶颈：
    1.  **标注成本极高**：高分辨率航空图像结构复杂，手动进行像素级标注耗时耗力。
    2.  **数据规模有限**：现有数据集通常仅包含数千个样本，难以支撑现代深度学习模型（如视觉基础模型）的训练。
    3.  **跨模态对齐困难**：现成无人机往往缺乏硬件级的传感器同步，导致 RGB 和热红外图像难以实现精确的像素级配准。
*   **整体含义**：本文提出了一种几何驱动的 **2D-3D-2D 范式**，旨在通过极少量的人工标注（<3%）和纯软件算法，实现大规模、多模态、高精度的语义标签自动生成与图像配准。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用航空拍摄中高重叠度图像的多视图几何冗余，将 2D 标注提升到 3D 空间进行融合，再重投影回所有 2D 视图（包括不同模态和高度）。
*   **关键技术流程**：
    1.  **2D-to-3D 标签提升**：利用 SfM（运动恢复结构）和 MVS（多视图立体视觉）重建 3D 点云。仅手动标注不到 3% 的 RGB 图像，通过多视图投票和 kNN 去噪将标签赋予 3D 点云。
    2.  **3D-to-2D 伪标签生成**：将语义点云投影回所有 RGB 和热红外视图。包含四个关键步骤：Z-buffering（深度排序）、遮挡过滤、基于 Splatting 的稠密化、深度引导的标签传播。
    3.  **几何驱动的跨模态配准**：独立重建热红外 3D 点云，通过 ICP 算法将其与 RGB 点云对齐，最后通过透镜畸变转移技术实现像素级配准，无需硬件同步。
    4.  **Firefly 基准模型**：基于 DINOv3 编码器，引入 Rein 适配器（Adapters）进行参数高效的领域自适应，实现从 RGB 到热红外模态的知识迁移。

### 3. 实验设计
*   **数据集与场景**：构建了 **SegFly** 数据集，包含 9 个场景（城市、工业、农村），涵盖四季和三个飞行高度（30m, 40m, 50m）。
*   **Benchmark 规模**：
    *   **SegFly-RGB**：20,606 张高分辨率图像。
    *   **SegFly-RGB-T**：15,007 对几何对齐的图像对。
*   **对比方法**：
    *   **传统架构**：UPerNet, SegFormer。
    *   **视觉基础模型（VFM）**：CatSeg（开集分割）、AnyThermal（热红外基础模型）。
*   **评估指标**：准确率（Accuracy）、频率加权平均交并比（FWmIoU）。

### 4. 资源与算力
*   **硬件设备**：文中提到了使用 DJI Phantom 4 RTK 和 Mavic 3 Enterprise 采集数据。
*   **算力说明**：论文**未明确说明**具体的训练 GPU 型号、数量及具体训练时长。但提到 Firefly 模型使用了“参数高效（parameter-efficient）”的微调方法，暗示其对算力需求相对优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 2D-3D-2D 标签转移的准确性验证（对比手动真值）。
    *   对渲染流水线的四个阶段进行了消融实验。
    *   对比了不同监督模式（手动标注 vs. SegFly 自动标注）对模型性能的影响。
    *   测试了视觉基础模型在 SegFly 上的零样本（Zero-shot）与微调性能。
*   **充分性与客观性**：实验设计较为全面，通过引入一组未参与训练的独立手动标注真值进行验证，确保了评估的客观性。涵盖了多高度、多季节场景，证明了范式的泛化能力。

### 6. 主要结论与发现
*   **高效性**：仅需标注 2.84% 的数据，即可生成准确率达 91%（RGB）和 88%（热红外）的伪标签。
*   **监督增益**：使用 SegFly 自动生成的伪标签训练模型，其性能优于仅使用少量手动标注训练的模型。
*   **配准精度**：纯软件几何配准达到了 87% 的像素级准确率，证明了摆脱硬件同步限制的可行性。
*   **基础模型受益**：CatSeg 和 AnyThermal 等基础模型在 SegFly 上微调后，性能大幅提升（如 CatSeg 的 FWmIoU 从 28.6% 提升至 55.4%）。

### 7. 优点
*   **极高的可扩展性**：通过几何关系将标注成本降低了两个数量级，适合构建超大规模数据集。
*   **硬件兼容性强**：解决了消费级无人机 RGB-T 对齐的难题，降低了数据采集门槛。
*   **多高度复用**：在一个高度标注，可自动传播至其他高度的图像，极大提高了数据利用率。

### 8. 不足与局限
*   **静态场景假设**：基于 SfM/MVS 的重建依赖静态场景，对于行人、车辆等动态物体的处理能力有限（文中将其设为 unlabeled）。
*   **细小结构挑战**：电缆、细杆等极细结构在 3D 重建中容易丢失，导致投影标签不准确。
*   **环境限制**：目前主要针对白天热红外数据，对于夜间或极端天气下的表现尚需进一步研究。
*   **计算开销**：虽然节省了人工，但大规模 SfM 重建和点云处理需要较大的离线计算资源。

（完）
