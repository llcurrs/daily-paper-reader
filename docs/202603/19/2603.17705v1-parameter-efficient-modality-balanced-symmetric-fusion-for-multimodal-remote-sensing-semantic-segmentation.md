---
title: Parameter-Efficient Modality-Balanced Symmetric Fusion for Multimodal Remote Sensing Semantic Segmentation
title_zh: 面向多模态遥感语义分割的参数高效、模态平衡对称融合框架
authors: "Haocheng Li, Juepeng Zheng, Shuangxi Miao, Ruibo Lu, Guosheng Cai, Haohuan Fu, Jianxi Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17705v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 遥感中的模态平衡对称融合
tldr: 本研究针对多模态遥感语义分割中预训练视觉大模型微调开销大及模态不平衡问题，提出了MoBaNet框架。该框架采用对称双流架构，通过冻结大部分骨干网络参数实现高效微调。核心包含跨模态提示注入适配器（CPIA）促进深层交互，差异引导门控融合模块（DGFM）优化特征融合，以及模态条件随机掩码（MCRM）策略缓解模态竞争。实验证明，MoBaNet在ISPRS数据集上以极少的可训练参数实现了最先进的性能。
selection_source: fresh_fetch
motivation: 旨在解决预训练视觉大模型在多模态遥感任务中微调成本高昂以及辅助模态贡献被抑制的模态不平衡挑战。
method: 提出了包含CPIA交互适配器、DGFM融合模块和MCRM掩码策略的对称双流参数高效微调框架。
result: 在ISPRS Vaihingen和Potsdam基准测试中，该方法以显著减少的可训练参数量达到了当前最先进的分割精度。
conclusion: MoBaNet证明了通过参数高效的对称融合与针对性的模态平衡策略，可以有效提升多模态遥感图像的语义分割性能。
---

## 摘要
多模态遥感语义分割通过利用异构数据中的互补物理线索来增强场景解释。尽管预训练视觉基础模型（VFMs）提供了强大的通用表示，但将其应用于多模态任务通常会产生巨大的计算开销，且容易出现模态不平衡问题，即辅助模态的贡献在优化过程中被抑制。为了应对这些挑战，我们提出了 MoBaNet，一种参数高效且模态平衡的对称融合框架。MoBaNet 基于基本冻结的 VFM 主干网络，采用对称双流架构，在最小化可训练参数数量的同时保留了泛化表示。具体而言，我们设计了一个跨模态提示注入适配器（CPIA），通过生成共享提示并将其注入冻结主干下的瓶颈适配器中，实现深层语义交互。为了获得用于解码的紧凑且具有辨别力的多模态表示，我们进一步引入了差异引导门控融合模块（DGFM），该模块通过显式利用跨模态差异来引导特征选择，从而自适应地融合成对的阶段特征。此外，我们提出了一种模态条件随机掩码（MCRM）策略，通过在训练期间仅对一种模态进行掩码，并对特定模态分支施加硬像素辅助监督，来缓解模态不平衡。在 ISPRS Vaihingen 和 Potsdam 基准测试上的大量实验表明，MoBaNet 以显著少于全量微调的可训练参数实现了最先进的性能，验证了其在鲁棒且平衡的多模态融合方面的有效性。本工作的源代码可在 https://github.com/sauryeo/MoBaNet 获取。

## Abstract
Multimodal remote sensing semantic segmentation enhances scene interpretation by exploiting complementary physical cues from heterogeneous data. Although pretrained Vision Foundation Models (VFMs) provide strong general-purpose representations, adapting them to multimodal tasks often incurs substantial computational overhead and is prone to modality imbalance, where the contribution of auxiliary modalities is suppressed during optimization. To address these challenges, we propose MoBaNet, a parameter-efficient and modality-balanced symmetric fusion framework. Built upon a largely frozen VFM backbone, MoBaNet adopts a symmetric dual-stream architecture to preserve generalizable representations while minimizing the number of trainable parameters. Specifically, we design a Cross-modal Prompt-Injected Adapter (CPIA) to enable deep semantic interaction by generating shared prompts and injecting them into bottleneck adapters under the frozen backbone. To obtain compact and discriminative multimodal representations for decoding, we further introduce a Difference-Guided Gated Fusion Module (DGFM), which adaptively fuses paired stage features by explicitly leveraging cross-modal discrepancy to guide feature selection. Furthermore, we propose a Modality-Conditional Random Masking (MCRM) strategy to mitigate modality imbalance by masking one modality only during training and imposing hard-pixel auxiliary supervision on modality-specific branches. Extensive experiments on the ISPRS Vaihingen and Potsdam benchmarks demonstrate that MoBaNet achieves state-of-the-art performance with significantly fewer trainable parameters than full fine-tuning, validating its effectiveness for robust and balanced multimodal fusion. The source code in this work is available at https://github.com/sauryeo/MoBaNet.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MoBaNet** 的新型框架，旨在解决多模态遥感图像语义分割中的参数效率与模态不平衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：遥感图像（RS）受光照、阴影和季节变化影响大，仅靠 RGB 图像难以实现高精度分割。引入辅助模态（如 DSM 高程数据）可提供互补信息。
*   **核心挑战**：
    1.  **计算开销**：现有的视觉大模型（VFM）参数量巨大，全量微调成本极高。
    2.  **模态不平衡**：在优化过程中，模型往往过度依赖 RGB 信息（主导模态），导致辅助模态的贡献被抑制，无法充分利用互补特征。
    3.  **交互不足**：在冻结骨干网络的情况下，不同模态间的深层语义交互难以实现。

### 2. 核心方法论
MoBaNet 采用对称双流架构，基于冻结的 VFM（如 DINOv2 或 SAM）骨干网络，通过以下三个关键组件实现高效、平衡的融合：
*   **跨模态提示注入适配器 (CPIA)**：
    *   在每个阶段前插入。它从双模态 Token 中提取共享语义，生成“提示（Prompts）”，并将其注入到轻量级瓶颈适配器（Adapter）中。
    *   这种方式在不破坏预训练特征空间的前提下，实现了模态间的深层语义调制。
*   **差异引导门控融合模块 (DGFM)**：
    *   在每个阶段后执行特征融合。它显式计算两模态特征的**差异线索（Discrepancy Cue）**。
    *   利用该差异引导门控网络生成空间和通道维度的权重，自适应地选择有效特征并抑制噪声。
*   **模态条件随机掩码 (MCRM) 策略**：
    *   **训练阶段专用**：随机对其中一种模态进行局部掩码（Masking），强迫模型在主导模态缺失时寻找辅助模态的线索。
    *   **硬像素辅助监督**：仅对融合分支预测错误的“硬像素”施加模态特定分支的辅助损失，进一步强化各模态的独立表达能力。

### 3. 实验设计
*   **数据集**：使用了遥感领域权威的 **ISPRS Vaihingen**（小城镇场景，NIR-R-G + DSM）和 **ISPRS Potsdam**（历史城区场景，RGB-IR + DSM）基准数据集。
*   **对比方法**：
    *   **单模态基准**：U-Net, SegNet, PSPNet, ABCNet, DC-Swin, UNetFormer 等。
    *   **多模态融合方法**：FuseNet, CMFNet, CMGFNet, TMFNet, FTransUNet, MANet 等。
*   **评价指标**：总体精度 (OA)、平均 F1 分数 (mF1) 和平均交并比 (mIoU)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU。
*   **训练细节**：采用 AdamW 优化器，初始学习率 $3 \times 10^{-4}$，Batch Size 为 12（ViT-L 减小至 6），训练 50 个 Epoch，每个 Epoch 1000 次迭代。
*   **效率**：MoBaNet (ViT-B) 的可训练参数仅为 **6.18M**，推理延迟约 13.97ms (71.60 FPS)。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型公开数据集上进行了详尽测试，涵盖了从 ViT-B 到 ViT-L 不同规模的骨干网络。
*   **消融实验**：系统验证了 CPIA、DGFM 和 MCRM 三个组件的增量贡献，证明了每个模块的必要性。
*   **鲁棒性测试**：专门设计了“模态缺失”推理实验，验证了 MCRM 策略在缓解模态依赖方面的显著效果。
*   **客观性**：对比了多种 SOTA 方法，并提供了特征响应图的可视化分析，实验设计严谨、公平。

### 6. 主要结论
*   **性能领先**：MoBaNet 在两个数据集上均刷新了 SOTA 记录，尤其在 Potsdam 数据集上 mIoU 达到 88.03%。
*   **参数高效**：仅需极少的可训练参数（约占总量的很小比例）即可超越全量微调的效果。
*   **平衡性**：通过 MCRM 策略，模型不再盲目依赖 RGB，在辅助模态（DSM）缺失时表现出极强的鲁棒性（性能下降远小于基准模型）。

### 7. 优点与亮点
*   **对称性设计**：通过对称架构赋予辅助模态与 RGB 同等的地位，从结构上缓解不平衡。
*   **差异引导思想**：DGFM 利用“差异”来引导融合，比简单的拼接或加权更具物理意义。
*   **即插即用**：该框架具有骨干网络无关性（Backbone-agnostic），可轻松适配 DINOv2、SAM 等不同的视觉大模型。

### 8. 不足与局限
*   **类别挑战**：尽管整体精度高，但在“低矮植被”、“树木”和“小汽车”等细碎或视觉相似类别上，提升空间仍存。
*   **模态局限**：目前主要验证了 RGB+DSM 组合，对于 SAR（合成孔径雷达）或多光谱等差异更大的模态组合，其泛化能力有待进一步验证。
*   **场景覆盖**：实验集中在城市建筑场景，对于森林、湿地等自然景观的适用性尚未探讨。

（完）
