---
title: "EgoAdapt: Enhancing Robustness in Egocentric Interactive Speaker Detection Under Missing Modalities"
title_zh: EgoAdapt：增强模态缺失下第一视角交互式说话人检测的鲁棒性
authors: "Xinyuan Qian, Xinjia Zhu, Alessio Brutti, Dong Liang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18082v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 模态缺失下的鲁棒检测
tldr: 本研究针对第一视角交互中“谁在跟我说话”（TTM）任务，提出了EgoAdapt框架。针对现实场景中视觉缺失、背景噪声及头部姿态被忽视等挑战，该框架集成了视觉目标识别、并行共享权重音频编码及视觉缺失感知模块。通过融合言语与非言语信号，EgoAdapt在Ego4D数据集上显著提升了检测精度和鲁棒性，超越了现有最先进方法。
selection_source: fresh_fetch
motivation: 传统TTM模型在处理视觉缺失、背景噪声及忽略头部姿态等现实复杂场景时表现不佳。
method: 提出了包含视觉目标识别、并行音频编码及模态缺失感知模块的自适应框架EgoAdapt。
result: "在Ego4D数据集的TTM基准测试中，该方法在准确率和平均精度上分别比现有最优方法提升了4.96%和1.56%。"
conclusion: EgoAdapt通过动态感知模态缺失并融合多维交互信号，有效增强了第一视角交互说话人检测的鲁棒性。
---

## 摘要
TTM（Talking to Me，跟我说话）任务是理解人类社交互动的关键组成部分，旨在确定谁正在与摄像机佩戴者进行对话。传统模型在现实场景中经常面临视觉数据缺失、忽视头部朝向的作用以及背景噪声等挑战。本研究通过引入 EgoAdapt 解决了这些局限性，这是一个专为模态缺失下鲁棒的第一视角“跟我说话”说话人检测而设计的自适应框架。具体而言，EgoAdapt 包含三个关键模块：(1) 视觉说话人目标识别（VSTR）模块，它将头部朝向作为非语言线索，将唇部运动作为语言线索，从而实现对语言和非语言信号的全面解释以处理 TTM 任务，这使其区别于仅关注检测说话状态的任务；(2) 并行权值共享音频（PSA）编码器，用于在噪声环境中增强音频特征提取；以及 (3) 视觉模态缺失感知（VMMA）模块，该模块估计每一帧中各模态的存在或缺失，以动态调整系统响应。在 Ego4D 数据集的 TTM 基准测试上的全面评估表明，EgoAdapt 实现了 67.39% 的平均精度均值（mAP）和 62.01% 的准确率（Acc），在准确率和 mAP 上分别显著超过现有最先进方法 4.96% 和 1.56%。

## Abstract
TTM (Talking to Me) task is a pivotal component in understanding human social interactions, aiming to determine who is engaged in conversation with the camera-wearer. Traditional models often face challenges in real-world scenarios due to missing visual data, neglecting the role of head orientation, and background noise. This study addresses these limitations by introducing EgoAdapt, an adaptive framework designed for robust egocentric "Talking to Me" speaker detection under missing modalities. Specifically, EgoAdapt incorporates three key modules: (1) a Visual Speaker Target Recognition (VSTR) module that captures head orientation as a non-verbal cue and lip movement as a verbal cue, allowing a comprehensive interpretation of both verbal and non-verbal signals to address TTM, setting it apart from tasks focused solely on detecting speaking status; (2) a Parallel Shared-weight Audio (PSA) encoder for enhanced audio feature extraction in noisy environments; and (3) a Visual Modality Missing Awareness (VMMA) module that estimates the presence or absence of each modality at each frame to adjust the system response dynamically.Comprehensive evaluations on the TTM benchmark of the Ego4D dataset demonstrate that EgoAdapt achieves a mean Average Precision (mAP) of 67.39% and an Accuracy (Acc) of 62.01%, significantly outperforming the state-of-the-art method by 4.96% in Accuracy and 1.56% in mAP.

---

## 论文详细总结（自动生成）

### 论文总结：EgoAdapt —— 增强模态缺失下第一视角交互式说话人检测的鲁棒性

#### 1. 核心问题与整体含义（研究动机和背景）
本文关注的是第一视角（Egocentric）视频理解中的 **“跟我说话”（Talking to Me, TTM）** 任务。该任务旨在从摄像机佩戴者的视角出发，识别场景中哪个参与者正在与其进行对话。
*   **研究动机**：在现实社交场景中，TTM 任务面临三大挑战：
    1.  **视觉模态缺失**：由于相机运动或遮挡，说话人的头部经常出画或被遮挡。
    2.  **背景噪声干扰**：环境噪音使得仅凭音频难以准确判断说话状态。
    3.  **忽视非言语线索**：传统方法多关注“是否在说话”（Speaking Status），而忽视了“说话目标”（Speaking Target），即头部朝向等关键社交信号。
*   **核心目标**：开发一个在模态不完整和高噪声环境下依然鲁棒的自适应检测框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **EgoAdapt** 框架，由三个核心模块组成：
*   **视觉说话人目标识别（VSTR）模块**：
    *   **头部姿态提取**：利用 RepVGG 骨干网络和 6D 旋转表示法估计头部的偏航（Yaw）、俯仰（Pitch）和翻滚（Roll）角，捕捉“面向佩戴者”这一非言语线索。
    *   **唇部特征提取**：使用 CLIP ViT-B/32 提取唇部运动特征，作为言语线索。
*   **并行共享权重音频（PSA）编码器**：
    *   基于 Whisper-small 模型，采用双支路结构同时输入干净音频和人工合成的带噪音频。
    *   通过 **均方误差（MSE）损失函数** 约束两者的潜在嵌入（Latent Embeddings）趋于一致，迫使编码器提取抗噪的鲁棒特征。
*   **视觉模态缺失感知（VMMA）模块**：
    *   **细粒度感知**：在帧级别标记视觉数据是否存在。
    *   **粗粒度感知**：计算片段内缺失帧的比例 $\Delta_c$，并结合自适应阈值 $\beta_T$ 生成视觉掩码矩阵（Visual Mask Matrix），动态调整模型对不同模态的依赖权重。
*   **多模态融合**：采用两两交互的 **交叉注意力机制**（Head-Lip, Lip-Audio, Audio-Head）进行特征对齐与增强，最后通过自注意力层输出预测结果。

#### 3. 实验设计
*   **数据集**：
    *   **Ego4D (Social Interaction Benchmark)**：主要评估数据集，包含 32.4 小时训练集、4.2 小时验证集和 11.1 小时测试集。
    *   **WHAM!**：用于提供现实世界的背景噪声，增强音频分支的鲁棒性训练。
*   **Benchmark 与对比方法**：
    *   对比了 **Random Guess**、**ResNet-18 Bi-LSTM**（Ego4D 基准）、**EgoT2**（任务翻译方法）以及当时的 SOTA 方法 **QuAVF**（质量感知融合）。
*   **评估指标**：平均精度均值（mAP）和 Top-1 准确率（Acc）。

#### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率为 $1 \times 10^{-5}$，采用 Focal Loss 处理类别不平衡，训练 40 个 epoch，包含早停机制（Patience=100）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量以及总训练时长。文中仅提到 Batch Size 为 1，并使用了 4 个数据加载线程。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Ego4D 验证集和测试集上进行了完整的性能对比。
    *   **消融实验**：详细验证了 VSTR、PSA、VMMA 三个模块各自及组合的贡献。
    *   **参数分析**：探讨了 VMMA 不同阈值设置（固定 vs 自适应）的影响。
    *   **鲁棒性测试**：在 -10dB 到 10dB 的不同信噪比（SNR）下测试了 PSA 的抗噪能力。
*   **充分性评价**：实验设计较为充分且客观。通过定量指标和定性可视化（如多说话人、高噪声、视觉缺失场景的 Case Study）全面证明了方法的有效性。

#### 6. 主要结论与发现
*   **性能突破**：EgoAdapt 在测试集上达到了 **62.01% Acc** 和 **67.39% mAP**，分别比之前的 SOTA（QuAVF）提升了 **4.96%** 和 **1.56%**。
*   **模态互补**：实验发现，单纯增加视觉模态有时会因数据缺失反而降低性能，但通过 VMMA 模块的动态感知，可以有效利用视觉线索并超越单音频性能。
*   **非言语线索的重要性**：引入头部姿态（VSTR）显著提升了在多说话人场景下区分“谁在跟我说话”的能力。

#### 7. 优点（亮点）
*   **动态自适应性**：VMMA 模块解决了多模态学习中常见的“模态缺失”痛点，使模型不再死板地依赖完整输入。
*   **抗噪设计**：PSA 编码器通过共享权重和一致性约束，在极低信噪比环境下表现优异。
*   **社交维度建模**：不同于传统的说话人检测，该方法通过头部朝向建模了社交意图，更符合第一视角交互的本质。

#### 8. 不足与局限
*   **同步假设**：模型假设音视频流是严格时间同步的，但在实际廉价设备或网络传输中，音视频对齐偏差可能影响性能。
*   **计算复杂度**：多级交叉注意力机制虽然效果好，但计算开销较大，可能限制其在资源受限的嵌入式设备（如智能眼镜）上的实时运行。
*   **场景局限**：目前主要在 Ego4D 数据集上验证，对于更极端的运动模糊或完全黑暗环境的泛化能力尚待进一步探索。

（完）
