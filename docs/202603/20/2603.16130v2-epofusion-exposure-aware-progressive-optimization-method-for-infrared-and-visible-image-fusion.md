---
title: "EPOFusion: Exposure aware Progressive Optimization Method for Infrared and Visible Image Fusion"
title_zh: EPOFusion：一种用于红外与可见光图像融合的曝光感知渐进优化方法
authors: "Zhiwei Wang, Yayu Zheng, Defeng He, Li Zhao, Xiaoqin Zhang, Yuxing Li, Edmund Y. Lam"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16130v2"
tags: ["query:mm-fus"]
score: 10.0
evidence: 具有曝光感知的红外与可见光图像融合
tldr: 针对可见光图像在过曝场景下信息丢失的问题，本文提出了一种曝光感知的红外与可见光图像融合模型EPOFusion。该模型通过引导模块提取过曝区域的红外特征，并利用迭代解码器逐步优化融合效果。此外，研究者构建了首个红外与可见光过曝数据集IVOE。实验证明，该方法在保持过曝区域红外细节的同时，提升了整体视觉质量和下游任务表现。
selection_source: fresh_fetch
motivation: 现有的红外与可见光融合方法在处理可见光图像的高亮度过曝区域时表现不佳，导致关键视觉信息丢失。
method: 提出了一种包含引导模块、迭代解码器和自适应损失函数的曝光感知融合框架，并构建了专门的IVOE数据集。
result: 实验结果表明，EPOFusion在处理过曝区域的红外线索保留和非过曝区域的视觉保真度方面均优于现有方法。
conclusion: 该研究通过引入曝光感知机制和高质量数据集，有效解决了复杂光照条件下的图像融合难题，提升了视觉效果和实用价值。
---

## 摘要
过曝光在实际场景中频繁发生，导致关键视觉信息的丢失。然而，现有的红外与可见光融合方法在高亮度区域的表现仍不尽如人意。为此，我们提出了 EPOFusion，一种曝光感知融合模型。具体而言，引入了一个引导模块，以辅助编码器从过曝光区域提取细粒度的红外特征。同时，设计了一个结合多尺度上下文融合模块的迭代解码器，以逐步增强融合图像，确保细节的一致性和卓越的视觉质量。最后，通过自适应损失函数动态约束融合过程，实现在不同曝光条件下各模态之间的有效平衡。为了实现更好的曝光感知，我们构建了首个红外与可见光过曝光数据集（IVOE），并为过曝光区域提供了高质量的红外引导标注。大量实验表明，EPOFusion 优于现有方法。它在保留过曝光区域红外线索的同时，在非过曝光区域实现了视觉上真实的融合，从而提升了视觉保真度和下游任务的性能。代码、融合结果和 IVOE 数据集将在 https://github.com/warren-wzw/EPOFusion.git 上发布。

## Abstract
Overexposure frequently occurs in practical scenarios, causing the loss of critical visual information. However, existing infrared and visible fusion methods still exhibit unsatisfactory performance in highly bright regions. To address this, we propose EPOFusion, an exposure-aware fusion model. Specifically, a guidance module is introduced to facilitate the encoder in extracting fine-grained infrared features from overexposed regions. Meanwhile, an iterative decoder incorporating a multiscale context fusion module is designed to progressively enhance the fused image, ensuring consistent details and superior visual quality. Finally, an adaptive loss function dynamically constrains the fusion process, enabling an effective balance between the modalities under varying exposure conditions. To achieve better exposure awareness, we construct the first infrared and visible overexposure dataset (IVOE) with high quality infrared guided annotations for overexposed regions. Extensive experiments show that EPOFusion outperforms existing methods. It maintains infrared cues in overexposed regions while achieving visually faithful fusion in non-overexposed areas, thereby enhancing both visual fidelity and downstream task performance. Code, fusion results and IVOE dataset will be made available at https://github.com/warren-wzw/EPOFusion.git.

---

## 论文详细总结（自动生成）

以下是对论文《EPOFusion: Exposure-aware Progressive Optimization Method for Infrared and Visible Image Fusion》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶和智能监控等实际应用中，**过曝光（Overexposure）**是一个常见且棘手的挑战（如白天的强光或夜间的远光灯）。
*   **研究动机**：当可见光传感器发生像素饱和时，图像会丢失有效的梯度和纹理。现有的融合方法在处理此类场景时，要么被饱和的亮度主导而丢失红外热细节，要么在试图保留红外信息时引入过多的传感器噪声，导致视觉质量下降。
*   **核心问题**：如何在极端曝光条件下，既能保留红外图像中的关键目标信息，又能维持非过曝区域的视觉保真度。

### 2. 方法论：核心思想与关键技术
EPOFusion 提出了一种**曝光感知的渐进优化框架**，主要包含以下三个核心组件：

*   **引导模块（Guidance Module）**：
    *   通过预测过曝区域的红外掩码（Mask），显式引导编码器关注过曝区域的红外特征，从而在特征提取阶段就强化对细粒度热目标的捕捉。
*   **迭代解码融合模块（Iterative Decoding Fusion Module）**：
    *   **核心思想**：借鉴去噪扩散模型（DDPM/DDIM）的逐步优化机制，将融合过程建模为特征的逐步精炼。
    *   **技术细节**：利用一个去噪网络（Denoising Net）在多个时间步内迭代处理特征，纠正潜在的模态不一致性。
    *   **多尺度上下文融合（MSCF）**：设计了包含“上下文融合分支（CFB）”和“高保真分支（HFB）”的双路径结构，前者捕捉长程依赖，后者保留源图像的底层细节（如边缘）。
*   **自适应损失函数（Adaptive Loss Function）**：
    *   根据曝光掩码动态调整权重。在正常区域，平衡可见光亮度和红外纹理；在过曝区域，通过增加红外梯度的权重（$\gamma$ 因子）和参考伪标签（Pseudo GT），强制模型提取红外细节。
*   **IVOE 数据集构建**：
    *   构建了首个红外-可见光过曝光数据集。利用 SAM（Segment Anything Model）辅助标注，包含 2315 对合成训练样本和 176 对真实世界的测试样本。

### 3. 实验设计
*   **数据集**：在公共数据集 **MSRS**、**FMB** 以及作者自建的 **IVOE** 上进行了测试。
*   **对比方法（Benchmark）**：对比了 13 种最先进（SOTA）的方法，涵盖了：
    *   生成对抗网络类：GANMcC, FusionGAN 等。
    *   扩散模型类：DDFM, Dif-Fusion。
    *   手工损失/任务驱动类：SDNet, TarDal, SegMif, TDFusion, MaeFuse 等。
*   **下游任务验证**：使用 SegFormer 进行语义分割实验，使用 YOLOv11 进行目标检测实验，验证融合图像对机器视觉任务的提升。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-13700KF CPU，32GB RAM，**NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **软件环境**：Ubuntu 20.04, PyTorch 2.2.0。
*   **训练细节**：使用 AdamW 优化器，学习率 $1 \times 10^{-5}$，Batch Size 为 8，训练迭代次数为 $1 \times 10^5$ 次。
*   **推理效率**：在 480x640 分辨率下，推理时间约为 **74.80ms**，参数量为 33.87M。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同特性的数据集上进行了定量对比（涵盖 EN, MI, VIF, QAB/F, SSIM 五项指标）。
    *   展示了四种典型场景（白天街景、夜间街景、过曝驾驶、强热辐射）的定性视觉对比。
    *   针对引导模块（GM）、自适应损失（AL）、迭代机制（DDM）和 MSCF 模块进行了详尽的**消融实验**。
    *   探讨了迭代步数（Iteration Steps）对性能与效率的平衡影响。
*   **评价**：实验设计非常充分且客观。通过引入真实世界的过曝测试集，避免了仅在合成数据上表现良好的偏差；同时通过下游任务的精度提升，证明了该方法的实用价值。

### 6. 主要结论与发现
*   **性能领先**：EPOFusion 在多个数据集的 MI（互信息）和 VIF（视觉信息保真度）等关键指标上均排名第一或第二。
*   **过曝处理能力**：在可见光完全饱和的区域（如车灯、烈日下的屋顶），该方法能清晰还原红外图像中的物体轮廓和纹理，且不引入背景噪声。
*   **下游任务增益**：在过曝场景下，使用 EPOFusion 融合后的图像进行语义分割，mIoU 达到了 73.89%（比第二名高出 2.1%）；目标检测 mAP@50 达到 0.851（提升 1.8%）。

### 7. 优点与亮点
*   **针对性强**：专门解决了 IVIF 领域长期存在的“过曝失效”痛点。
*   **数据集贡献**：IVOE 数据集的发布填补了该细分领域的空白，且利用 SAM 进行标注保证了高质量。
*   **渐进式优化**：将扩散模型的迭代思想引入解码阶段，比传统的单次卷积解码更能精炼跨模态特征。
*   **自适应性**：损失函数能够根据图像内容自动切换侧重点，实现了视觉自然度与信息完整性的平衡。

### 8. 不足与局限
*   **计算开销**：虽然推理时间（74.8ms）在可接受范围内，但由于采用了迭代解码机制，其速度仍慢于一些轻量级的单次前向网络（如 SDNet 仅需 122ms，但其性能较低）。
*   **合成数据偏差**：训练集主要基于合成的过曝效果（高斯光斑），虽然在真实测试集上表现良好，但在面对极其复杂的非线性传感器饱和或镜头光晕时，可能仍存在泛化风险。
*   **迭代步数依赖**：性能高度依赖于迭代步数的选择，步数过少影响质量，步数过多则无法满足实时性需求。

（完）
