---
title: Missing-Aware Multimodal Fusion for Unified Microservice Incident Management
title_zh: 面向统一微服务故障管理的缺失感知多模态融合
authors: "Wenzhuo Qian, Hailiang Zhao, Ziqi Wang, Zhipeng Gao, Jiayi Chen, Zhiwei Ling, Shuiguang Deng"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25538v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 针对模态缺失场景设计的鲁棒门控融合框架
tldr: 针对微服务故障管理中多模态数据（指标、日志、追踪）经常缺失的问题，本文提出ARMOR框架。该框架通过非对称编码器隔离分布差异，并利用缺失感知的门控融合机制和可学习占位符来减少缺失数据带来的干扰。ARMOR采用自监督学习，在异常检测、故障分类和根因定位任务上均表现出色，即使在严重数据缺失下也能保持高诊断精度。
selection_source: fresh_fetch
motivation: 现有的多模态微服务故障管理方法假设数据完整，但在实际场景中网络波动常导致模态缺失，使用静态占位符会引入噪声并降低性能。
method: 提出ARMOR框架，采用非对称编码器处理不同模态分布，并结合缺失感知的门控融合与掩码引导的自监督重构技术。
result: 实验表明ARMOR在完整数据下达到SOTA水平，且在模态严重缺失的情况下仍能保持稳健的诊断准确率。
conclusion: ARMOR通过缺失感知的融合机制有效解决了微服务监控数据不完整带来的挑战，实现了统一且鲁棒的故障管理。
---

## 摘要
自动化故障管理对于微服务的可靠性至关重要。虽然最近的统一框架利用多模态数据进行联合优化，但它们不切实际地假设了数据的完美完整性。在实践中，网络波动和代理故障经常导致模态缺失。现有依赖静态占位符的方法会引入插值噪声，从而掩盖异常并降低性能。为解决这一问题，我们提出了 ARMOR，这是一个专为模态缺失场景设计的鲁棒自监督框架。ARMOR 的特点包括：(i) 一种模态特定的非对称编码器，用于隔离指标（metrics）、日志（logs）和追踪（traces）之间的分布差异；(ii) 一种缺失感知的门控融合机制，利用可学习的占位符和动态偏差补偿来防止不完整输入产生的跨模态干扰。通过采用带有掩码引导重构的自监督自回归，ARMOR 联合优化了异常检测 (AD)、故障分类 (FT) 和根因定位 (RCL)。AD 和 RCL 不需要故障标签，而 FT 仅依赖于下游分类器的故障类型标注。广泛的实验表明，ARMOR 在数据完整的情况下达到了最先进的性能，并在严重的模态缺失下仍能保持鲁棒的诊断准确性。

## Abstract
Automated incident management is critical for microservice reliability. While recent unified frameworks leverage multimodal data for joint optimization, they unrealistically assume perfect data completeness. In practice, network fluctuations and agent failures frequently cause missing modalities. Existing approaches relying on static placeholders introduce imputation noise that masks anomalies and degrades performance. To address this, we propose ARMOR, a robust self-supervised framework designed for missing modality scenarios. ARMOR features: (i) a modality-specific asymmetric encoder that isolates distribution disparities among metrics, logs, and traces; and (ii) a missing-aware gated fusion mechanism utilizing learnable placeholders and dynamic bias compensation to prevent cross-modal interference from incomplete inputs. By employing self-supervised auto-regression with mask-guided reconstruction, ARMOR jointly optimizes anomaly detection (AD), failure triage (FT), and root cause localization (RCL). AD and RCL require no fault labels, while FT relies solely on failure-type annotations for the downstream classifier. Extensive experiments demonstrate that ARMOR achieves state-of-the-art performance under complete data conditions and maintains robust diagnostic accuracy even with severe modality loss.

---

## 论文详细总结（自动生成）

这是一份关于论文《Missing-Aware Multimodal Fusion for Unified Microservice Incident Management》（面向统一微服务故障管理的缺失感知多模态融合）的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：在微服务架构中，自动化的故障管理（异常检测 AD、故障分类 FT、根因定位 RCL）依赖于指标（Metrics）、日志（Logs）和追踪（Traces）的多模态数据。然而，现实生产环境中由于网络波动、采集代理崩溃等原因，经常出现**模态缺失**。
*   **研究动机**：
    *   **现有框架的局限性**：现有的统一框架假设数据是完美的。当数据缺失时，通常使用静态占位符（如补零），这会引入“伪正常”噪声，掩盖真实的异常信号。
    *   **数据分布不对称**：连续的指标数据与稀疏的日志/追踪数据在结构上高度不对称，使用统一的编码器会导致相互干扰。
    *   **标签稀缺**：工业界故障标签极度匮乏，需要一种不依赖大量标注的自监督鲁棒方案。

### 2. 方法论（ARMOR 框架）
ARMOR 提出了一个两阶段的自监督框架，核心思想是“隔离差异、感知缺失、统一表示”：
*   **模态特定非对称编码器**：针对指标（高维连续）和日志/追踪（低维稀疏）设计了不同容量的 ModernTCN 块，独立提取特征，防止缺失模态的噪声污染幸存模态。
*   **缺失感知门控融合机制**：
    *   **可学习占位符**：使用可学习的向量 $E_{miss}$ 替代静态补零，向网络明确传递“数据丢失”而非“系统正常”的信号。
    *   **动态偏差补偿**：在注意力机制中引入负向偏差 $b_{miss}$，当检测到模态缺失时，自动降低该模态在融合中的权重。
*   **自监督重构任务**：通过掩码引导的自回归任务（预测下一时刻数据），在无标签的情况下学习系统正常运行的基准分布。
*   **统一故障特征表示**：将重构误差（显式偏差）与潜在嵌入（隐式上下文）拼接，形成统一的故障签名，同时支持 AD、FT 和 RCL 三个下游任务。

### 3. 实验设计
*   **数据集**：
    *   **$D_1$ (电商模拟系统)**：包含 46 个节点，210 个故障案例，涵盖 CPU、内存、网络等故障。
    *   **$D_2$ (某大型商业银行管理系统)**：包含 18 个异构实例，133 个故障案例，涉及 JVM 和节点级异常。
*   **对比方法（Baselines）**：共 9 种，包括多任务框架（ART, TrioXpert, Eadro 等）和单任务方法（Hades, DeepHunt 等）。
*   **缺失场景模拟**：设计了 6 种缺失组合（如仅缺失日志、同时缺失指标和追踪等），全面评估鲁棒性。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **20-vCPU Intel Xeon Platinum 8470Q 处理器**和 **单张 NVIDIA RTX 4090 GPU (24GB)** 的服务器。
*   **软件环境**：Python 3.9, PyTorch 1.12.1, DGL 0.9.0。
*   **推理效率**：文中提到 ARMOR 的端到端诊断延迟低于最强基线 ART，证明了其在线部署的可行性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **总体性能对比**：在两个数据集上对比了 9 种方法在 3 个任务上的表现。
    *   **鲁棒性测试**：在 6 种不同的模态缺失场景下进行了压力测试。
    *   **消融实验**：设计了 **12 种变体**（A1-A9, B1-B3），分别验证编码器、融合机制、图网络及特征表示的有效性。
    *   **敏感性分析**：对模型深度、图网络层数、滑动窗口大小等核心超参数进行了分析。
*   **评价**：实验设计非常充分，不仅涵盖了标准场景，还深入探讨了工业界最关心的“数据不完整”问题，对比实验客观且具有针对性。

### 6. 主要结论与发现
*   **性能领先**：ARMOR 在完整数据下全面超越 SOTA，尤其在故障分类（FT）上 F1 分数提升了 8.4%~15.5%。
*   **极强鲁棒性**：在指标（Metrics）缺失的极端情况下，传统模型性能会发生断崖式下跌，而 ARMOR 仍能保持较高的诊断精度（RCL 准确率比基线高出 60% 以上）。
*   **偏差学习**：可视化结果证实，模型能自动为缺失模态学习到显著的负向偏差，从而有效抑制插值噪声。

### 7. 优点与亮点
*   **首创性**：这是首个专门针对微服务故障管理中“模态缺失”问题设计的自监督统一框架。
*   **架构优雅**：通过非对称编码和门控融合，巧妙解决了多模态数据在物理含义和数据结构上的不对称性。
*   **实用性强**：AD 和 RCL 任务完全无需标签，FT 仅需少量类型标注，非常适合标注成本高昂的工业环境。

### 8. 不足与局限
*   **训练依赖**：离线训练阶段仍假设历史存档在结构上是相对完整的，以便进行掩码重构训练。如果历史数据本身长期缺失某一模态，模型可能难以学习该模态的重构。
*   **拓扑演进**：依赖于预定义的调用图拓扑，对于实例频繁销毁/创建的极动态环境，可能需要更频繁的图更新机制。
*   **细粒度缺失**：目前的实验主要针对“整个通道丢失”，对于更细粒度的部分数据丢失（如某个指标项丢失）的讨论较少。

（完）
