---
title: "DriveXQA: Cross-modal Visual Question Answering for Adverse Driving Scene Understanding"
title_zh: DriveXQA：用于不利驾驶场景理解的跨模态视觉问答
authors: "Mingzhe Tao, Ruiping Liu, Junwei Zheng, Yufan Chen, Kedi Ying, M. Saquib Sarfraz, Kailun Yang, Jiaming Zhang, Rainer Stiefelhagen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11380v1"
tags: ["query:mm-fus"]
score: 8.0
evidence: 融合包含传感器失效和恶劣环境的传感器数据
tldr: 本研究针对自动驾驶在恶劣场景下的理解难题，提出了DriveXQA数据集，包含多种视觉模态、传感器故障及天气状况下的10万余个问答对。同时设计了MVX-LLM架构，利用双交叉注意力（DCA）投影器高效融合多模态信息并减少冗余。该方法显著提升了模型在雾天等极端环境下的理解能力，为多模态大模型在自动驾驶领域的应用提供了重要基准和技术方案。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在利用多传感器信息理解恶劣驾驶场景方面研究不足，缺乏相关数据集和有效的融合机制。
method: 提出了包含10万余个QA对的DriveXQA数据集，并设计了带有双交叉注意力投影器的MVX-LLM架构以实现高效的多模态融合。
result: 实验表明DCA投影器在雾天等挑战性条件下表现优异，GPT评分从基准的25.1提升至53.5。
conclusion: 该研究通过新数据集和高效架构，有效提升了自动驾驶系统在复杂及恶劣环境下的跨模态视觉问答与场景理解能力。
---

## 摘要
融合具有互补模态的传感器对于维持对异常驾驶场景稳定且全面的理解至关重要。然而，多模态大语言模型（MLLMs）在利用多传感器信息理解自动驾驶车辆中的不利驾驶场景方面尚未得到充分探索。为了填补这一空白，我们提出了 DriveXQA，这是一个用于自动驾驶视觉问答（VQA）的多模态数据集。除了四种视觉模态、五种传感器故障情况和五种天气条件外，该数据集还包含 102,505 个问答对，分为三类：全局场景级、他心视角级（allocentric level）和本车中心级（ego-vehicle centric level）。由于现有的 MLLM 框架均未采用多种互补的视觉模态作为输入，我们设计了 MVX-LLM，这是一种具有双交叉注意力（DCA）投影器的令牌高效架构，通过融合模态来缓解信息冗余。实验表明，我们的 DCA 在雾天等挑战性条件下实现了性能提升（GPTScore：53.5，而基准模型为 25.1）。所建立的数据集和源代码将公开发布。

## Abstract
Fusing sensors with complementary modalities is crucial for maintaining a stable and comprehensive understanding of abnormal driving scenes. However, Multimodal Large Language Models (MLLMs) are underexplored for leveraging multi-sensor information to understand adverse driving scenarios in autonomous vehicles. To address this gap, we propose the DriveXQA, a multimodal dataset for autonomous driving VQA. In addition to four visual modalities, five sensor failure cases, and five weather conditions, it includes $102,505$ QA pairs categorized into three types: global scene level, allocentric level, and ego-vehicle centric level. Since no existing MLLM framework adopts multiple complementary visual modalities as input, we design MVX-LLM, a token-efficient architecture with a Dual Cross-Attention (DCA) projector that fuses the modalities to alleviate information redundancy. Experiments demonstrate that our DCA achieves improved performance under challenging conditions such as foggy (GPTScore: $53.5$ vs. $25.1$ for the baseline). The established dataset and source code will be made publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《DriveXQA: Cross-modal Visual Question Answering for Adverse Driving Scene Understanding》的结构化深度总结：

### 1. 核心问题与研究背景
*   **研究动机**：自动驾驶系统在复杂环境下（如大雾、暴雨、传感器故障）需要极高的可靠性。虽然多模态大语言模型（MLLM）在通用视觉任务上表现优异，但在自动驾驶领域，现有模型大多依赖单一的 RGB 图像输入，缺乏对多传感器（如激光雷达、事件相机、深度相机）互补信息的深度利用。
*   **核心问题**：现有的自动驾驶 VQA 数据集缺乏对恶劣天气和传感器失效场景的系统性覆盖，且缺乏能高效融合多种异构传感器信息并处理冗余的 MLLM 架构。

### 2. 方法论
*   **核心思想**：提出 **MVX-LLM** 框架，通过专门设计的投影器实现多模态特征的高效融合与压缩，使其能够理解包含传感器故障在内的极端驾驶场景。
*   **关键技术细节**：
    *   **多模态编码**：使用共享的 CLIP-ViT 编码 RGB、深度和事件相机数据；使用 PointNet++ 独立编码激光雷达（LiDAR）点云。
    *   **双交叉注意力（DCA）机制**：包含**空间交叉注意力**（建立不同模态间的空间对应关系）和**通道交叉注意力**（增强跨通道的语义特征），用于在传感器退化时实现跨模态补偿。
    *   **令牌聚合（Token Aggregation）**：对比了 GAP、QAttn（查询注意力）、Spectral QAttn 等多种策略，最终采用 **QAttn** 将海量视觉令牌压缩为语言模型可接受的紧凑嵌入。
    *   **分层问答体系（Hierarchical XQA）**：将问题分为全局场景级（天气/宏观交通）、他心视角级（物体间空间关系）和本车中心级（车道位置/传感器状态）。

### 3. 实验设计
*   **数据集**：自建 **DriveXQA** 数据集。
    *   **规模**：7,885 帧场景，102,505 个问答对。
    *   **覆盖范围**：5 种天气（晴、阴、雨、雾、夜）、5 种传感器故障（运动模糊、过曝、欠曝、激光雷达抖动、事件相机低分辨率）。
*   **对比基准（Benchmarks）**：
    *   **融合策略**：对比了简单拼接（prepend）、Self-query、TokenFusion 等。
    *   **投影器架构**：对比了 Honeybee 和 ParGo 等先进的 MLLM 投影器。
*   **评价指标**：传统 NLP 指标（BLEU-4, CIDEr 等）+ GPTScore（利用 GPT-4o-mini 作为裁判进行 1-5 分评测并归一化）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100-40GB GPU**。
*   **训练细节**：采用分布式训练和混合精度优化；使用 LoRA（Rank=16）对语言模型进行参数高效微调；优化器为 Adam，学习率 1e-4。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了全量数据集的对比实验，涵盖了所有天气和故障组合。
    *   **消融实验**：针对 DCA 的各个组件（空间 vs 通道注意力）以及不同传感器模态的组合（如 RGB+Depth, RGB+LiDAR 等）进行了详尽测试。
*   **客观性与公平性**：引入了 4 名人类评估员对 360 个样本进行独立评分，结果显示人类评分与 GPTScore 的相关系数超过 0.83，证明了自动评估指标的客观性。

### 6. 主要结论与发现
*   **性能提升**：MVX-LLM 在恶劣条件下表现强劲，尤其在雾天场景下，GPTScore 从基准的 25.1 提升至 **53.5**。
*   **模态互补性**：实验证明 RGB 是基础，但加入 LiDAR 和深度信息能显著提升空间推理能力；在视觉传感器受损时，几何模态（LiDAR）能提供关键补偿。
*   **架构优势**：DCA 机制优于传统的简单拼接或单一注意力机制，能更有效地处理异构传感器间的冗余信息。

### 7. 优点与亮点
*   **数据集填补空白**：首次系统性地将“传感器失效”引入自动驾驶 VQA 任务，具有极强的工程实践意义。
*   **高效融合**：DCA 投影器在保持令牌效率（Token-efficient）的同时，实现了深度的跨模态交互。
*   **分层设计**：QA 体系的设计符合自动驾驶的逻辑认知过程（从宏观环境到微观操作）。

### 8. 不足与局限
*   **数据来源**：DriveXQA 主要基于仿真数据构建，虽然模拟了多种故障，但仍存在 **Sim-to-Real（仿真到现实）** 的领域鸿沟。
*   **时序缺失**：目前的模型基于单帧图像理解，缺乏对视频序列的时间相干性推理，而这对于预测他车行为至关重要。
*   **应用限制**：虽然使用了 LoRA 优化，但大模型的实时性在车载端侧部署仍面临挑战。

（完）
