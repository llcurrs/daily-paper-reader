---
title: "EPOFusion: Exposure aware Progressive Optimization Method for Infrared and Visible Image Fusion"
title_zh: EPOFusion：一种用于红外与可见光图像融合的曝光感知渐进优化方法
authors: "Zhiwei Wang, Yayu Zheng, Defeng He, Li Zhao, Xiaoqin Zhang, Yuxing Li, Edmund Y. Lam"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16130v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 针对过曝（信息缺失）场景的红外与可见光图像融合
tldr: 针对可见光图像在过曝场景下信息丢失的问题，本文提出了一种曝光感知的红外与可见光图像融合模型EPOFusion。该模型通过引导模块提取过曝区域的红外特征，并利用迭代解码器逐步优化融合效果。此外，作者构建了首个红外与可见光过曝数据集IVOE。实验证明，该方法在保持过曝区域红外细节的同时，提升了整体视觉质量和下游任务性能。
selection_source: fresh_fetch
motivation: 现有的红外与可见光融合方法在处理可见光图像的高亮度过曝区域时表现不佳，导致关键视觉信息丢失。
method: 提出包含引导模块、多尺度上下文融合迭代解码器及自适应损失函数的EPOFusion模型，并构建了IVOE数据集。
result: 实验结果表明，EPOFusion在过曝区域能有效保留红外线索，且在非过曝区域保持了极高的视觉保真度。
conclusion: 该方法通过曝光感知和逐步优化策略，显著提升了复杂光照条件下的图像融合质量及下游任务的适用性。
---

## 摘要
过曝光在实际场景中频繁发生，导致关键视觉信息的丢失。然而，现有的红外与可见光融合方法在高亮度区域的表现仍不尽如人意。为此，我们提出了 EPOFusion，一种曝光感知融合模型。具体而言，引入了一个引导模块，以辅助编码器从过曝光区域提取细粒度的红外特征。同时，设计了一个结合多尺度上下文融合模块的迭代解码器，以逐步增强融合图像，确保细节的一致性和卓越的视觉质量。最后，通过自适应损失函数动态约束融合过程，实现在不同曝光条件下各模态之间的有效平衡。为了实现更好的曝光感知，我们构建了首个红外与可见光过曝光数据集（IVOE），并为过曝光区域提供了高质量的红外引导标注。大量实验表明，EPOFusion 优于现有方法。它在保留过曝光区域红外线索的同时，在非过曝光区域实现了视觉上真实的融合，从而提升了视觉保真度和下游任务的性能。代码、融合结果和 IVOE 数据集将在 https://github.com/warren-wzw/EPOFusion.git 上发布。

## Abstract
Overexposure frequently occurs in practical scenarios, causing the loss of critical visual information. However, existing infrared and visible fusion methods still exhibit unsatisfactory performance in highly bright regions. To address this, we propose EPOFusion, an exposure-aware fusion model. Specifically, a guidance module is introduced to facilitate the encoder in extracting fine-grained infrared features from overexposed regions. Meanwhile, an iterative decoder incorporating a multiscale context fusion module is designed to progressively enhance the fused image, ensuring consistent details and superior visual quality. Finally, an adaptive loss function dynamically constrains the fusion process, enabling an effective balance between the modalities under varying exposure conditions. To achieve better exposure awareness, we construct the first infrared and visible overexposure dataset (IVOE) with high quality infrared guided annotations for overexposed regions. Extensive experiments show that EPOFusion outperforms existing methods. It maintains infrared cues in overexposed regions while achieving visually faithful fusion in non-overexposed areas, thereby enhancing both visual fidelity and downstream task performance. Code, fusion results and IVOE dataset will be made available at https://github.com/warren-wzw/EPOFusion.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《EPOFusion: Exposure aware Progressive Optimization Method for Infrared and Visible Image Fusion》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：在实际场景（如强日光、夜间远光灯）中，可见光传感器常因**过度曝光（Overexposure）**导致像素饱和，丢失关键的纹理和梯度信息。
*   **研究动机**：现有的红外与可见光融合（IVIF）方法在处理高亮度区域时表现不佳：要么被饱和的可见光强度主导而丢失红外热目标细节，要么在试图保留红外信息时放大了传感器噪声。
*   **整体含义**：本文旨在开发一种能够感知曝光状态、通过迭代优化逐步恢复过曝区域细节，并能显著提升下游任务（检测、分割）性能的融合框架。

### 2. 方法论
EPOFusion 采用了一种曝光感知的渐进式优化架构，核心组件包括：
*   **引导模块（Guidance Module）**：通过预测过曝区域的红外掩码（Mask），显式引导编码器关注过曝区域的红外特征，确保热目标在视觉缺失区被准确提取。
*   **迭代解码融合模块（Iterative Decoding Fusion Module）**：
    *   借鉴了**去噪扩散模型（DDPM/DDIM）**的思想，将融合过程建模为特征的逐步精炼。
    *   通过多步迭代（推理时默认3步）修正潜在的特征不一致性，避免单次解码带来的偏差。
*   **多尺度上下文融合模块（MSCF）**：包含上下文融合分支（CFB，提取全局上下文）和高保真分支（HFB，保留底层细节），用于最终重建高质量图像。
*   **自适应损失函数（Adaptive Loss）**：
    *   根据曝光掩码动态调整权重。在正常区域平衡两模态强度；在过曝区域，强制融合结果趋向于红外引导的伪标签，并放大红外梯度的权重。
*   **IVOE 数据集**：构建了首个红外-可见光过曝数据集，利用 SAM（Segment Anything Model）辅助标注，包含 2315 对合成训练样本和 176 对真实世界测试样本。

### 3. 实验设计
*   **数据集**：在 **MSRS**、**FMB** 以及自建的 **IVOE** 数据集上进行了测试。
*   **对比方法**：涵盖了 13 种最先进（SOTA）的方法，包括基于 GAN 的（GANMcC）、基于任务驱动的（TarDal, SegMif, TDFusion）、基于扩散模型的（DDFM, Dif-Fusion）以及基于手工损失的（SDNet, U2Fusion）等。
*   **评价指标**：使用信息熵（EN）、互信息（MI）、视觉信息保真度（VIF）、结构相似度（SSIM）等 5 项客观指标。
*   **下游任务**：使用 SegFormer 进行语义分割实验，使用 YOLOv11 进行目标检测实验。

### 4. 资源与算力
*   **硬件环境**：Intel i7-13700KF CPU，32GB RAM，**NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **训练细节**：基于 PyTorch 2.2.0，使用 AdamW 优化器，训练 100,000 次迭代，Batch Size 为 8。
*   **推理效率**：在 480x640 分辨率下，推理时间约为 **74.80ms**，参数量为 33.87M。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流数据集上进行了大规模定量对比，并在 4 种典型场景（白天街景、夜间街景、过曝驾驶、强热辐射）下进行了定性展示。
*   **消融实验**：针对引导模块（GM）、自适应损失（AL）、迭代机制（DDM）、多尺度模块（MSCF）以及迭代步数（1-10步）分别做了详细的消融研究。
*   **充分性评价**：实验设计非常充分且客观。不仅验证了融合质量，还通过下游任务（分割和检测）的精度提升（mIoU 提升 2.1%，mAP@50 提升 1.8%）证明了该方法的实用价值。

### 6. 主要结论与发现
*   **曝光感知至关重要**：引入显式的曝光引导能有效解决可见光信息失效时的“盲目融合”问题。
*   **迭代优化的优势**：相比单次前向传播，基于扩散思想的迭代解码能显著减少噪声并增强结构清晰度。
*   **下游任务增益**：高质量的融合图像能直接转化为更高的检测和分割精度，尤其是在极端光照条件下。

### 7. 优点与亮点
*   **针对性强**：专门解决了 IVIF 领域中长期存在的“过曝”痛点。
*   **数据集贡献**：填补了过曝场景红外-可见光融合缺乏高质量标注数据集的空白。
*   **性能平衡**：虽然采用了迭代机制，但通过优化采样步数（3步），在保持高性能的同时兼顾了推理速度（约 13 FPS）。
*   **自适应性**：自适应损失函数解决了不同曝光区域优化目标冲突的问题。

### 8. 不足与局限
*   **合成数据偏差**：训练集主要基于合成的过曝效果（高斯光斑），虽然在真实测试集上表现良好，但与极其复杂的真实过曝（如动态眩光、传感器拖尾）可能仍存在分布差异。
*   **计算开销**：尽管 74ms 在 4090 上较快，但对于嵌入式端或实时性要求极高的自动驾驶场景（通常要求 <30ms），迭代机制仍存在一定的计算负担。
*   **参数量**：33.87M 的参数量相比于一些轻量化模型（如 SDNet 的 0.07M）显得较大。

（完）
