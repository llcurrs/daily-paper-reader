---
title: "BALM: A Model-Agnostic Framework for Balanced Multimodal Learning under Imbalanced Missing Rates"
title_zh: BALM：一种针对不平衡缺失率下平衡多模态学习的模型无关框架
authors: "Phuong-Anh Nguyen, Tien Anh Pham, Duc-Trong Le, Cam-Van Thi Nguyen"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19718v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 不平衡缺失率下的平衡多模态学习
tldr: 针对多模态学习中因缺失率不均衡（IMR）导致的优化失衡问题，本文提出了一种模型无关的插件式框架BALM。该框架通过特征校准模块（FCM）利用全局上下文统一异构缺失模式下的表示基础，并利用梯度重平衡模块（GRM）从分布和空间维度调节梯度动态。实验证明，BALM能显著提升多模态情感识别模型在复杂缺失场景下的鲁棒性与性能。
selection_source: fresh_fetch
motivation: 旨在解决多模态学习中由于各模态缺失率不一致导致的表示学习扭曲和梯度动力学失衡问题。
method: 提出了包含特征校准模块（FCM）和梯度重平衡模块（GRM）的通用框架，分别从特征表示和梯度优化两个层面进行调节。
result: 在多个多模态情感识别基准测试中，BALM在各种缺失和不平衡设置下均显著提升了模型的鲁棒性和准确率。
conclusion: BALM作为一种灵活的插件框架，为处理现实场景中不均衡缺失数据的多模态学习提供了有效的解决方案。
---

## 摘要
从多个模态中学习通常面临不平衡问题，其中信息丰富的模态主导了优化过程，而较弱或部分缺失的模态贡献较少。在具有不平衡缺失率（IMR）的现实场景中，这种不平衡变得更加严重，即每个模态以不同的概率缺失，从而扭曲了表示学习和梯度动力学。我们从训练过程的角度重新审视这一问题，并提出了 BALM，这是一个模型无关的插件式框架，旨在实现 IMR 下的平衡多模态学习。该框架包含两个互补模块：特征校准模块（FCM），利用全局上下文重新校准单模态特征，以在异构缺失模式之间建立共享的表示基础；梯度重平衡模块（GRM），通过从分布和空间两个维度调节梯度的大小和方向，来平衡各模态之间的学习动力学。BALM 可以无缝集成到各种骨干网络中，包括多模态情感识别（MER）模型，而无需更改其架构。在多个 MER 基准测试上的实验结果证实，BALM 在各种缺失和不平衡设置下都能持续增强鲁棒性并提高性能。代码可在以下地址获取：https://github.com/np4s/BALM_CVPR2026.git

## Abstract
Learning from multiple modalities often suffers from imbalance, where information-rich modalities dominate optimization while weaker or partially missing modalities contribute less. This imbalance becomes severe in realistic settings with imbalanced missing rates (IMR), where each modality is absent with different probabilities, distorting representation learning and gradient dynamics. We revisit this issue from a training-process perspective and propose BALM, a model-agnostic plug-in framework to achieve balanced multimodal learning under IMR. The framework comprises two complementary modules: the Feature Calibration Module (FCM), which recalibrates unimodal features using global context to establish a shared representation basis across heterogeneous missing patterns; the Gradient Rebalancing Module (GRM), which balances learning dynamics across modalities by modulating gradient magnitudes and directions from both distributional and spatial perspectives. BALM can be seamlessly integrated into diverse backbones, including multimodal emotion recognition (MER) models, without altering their architectures. Experimental results across multiple MER benchmarks confirm that BALM consistently enhances robustness and improves performance under diverse missing and imbalance settings. Code available at: https://github.com/np4s/BALM_CVPR2026.git

---

## 论文详细总结（自动生成）

以下是对论文《BALM: A Model-Agnostic Framework for Balanced Multimodal Learning under Imbalanced Missing Rates》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多模态学习在现实应用中常面临“模态缺失”问题。现有研究多假设**共享缺失率（SMR）**，即各模态缺失概率相同。然而，现实中不同传感器的可靠性、噪声和成本不同，导致**不平衡缺失率（IMR）**。
*   **核心问题**：IMR 会引发两个层面的失衡：
    1.  **表示失衡**：异构的缺失模式扭曲了单模态特征分布，阻碍了一致的跨模态融合。
    2.  **学习失衡**：缺失率低的模态（出现频率高）会主导梯度更新，导致缺失率高的模态学习不足，模型陷入偏见平衡。
*   **整体含义**：本文提出了 **BALM** 框架，旨在不改变现有模型架构的前提下，通过插件式模块解决 IMR 下的表示与优化双重失衡问题。

### 2. 方法论：核心思想与关键技术
BALM 由两个互补的模块组成，采用模型无关（Model-Agnostic）的设计：
*   **特征校准模块 (FCM - 表示层重平衡)**：
    *   **核心思想**：利用所有可用模态的全局上下文信息来校准受损的单模态特征。
    *   **技术细节**：首先通过平均池化获取可用特征的全局描述符，经全连接层提取跨模态上下文，生成模态特定的校准权重（Calibration Weights），最后通过门控机制（Sigmoid）对原始特征进行重缩放，以建立统一的表示基础。
*   **梯度重平衡模块 (GRM - 优化层重平衡)**：
    *   **核心思想**：从分布和空间两个维度动态调节梯度，确保各模态获得公平监督。
    *   **分布驱动调节**：引入轻量级单模态预测头，计算单模态预测与多模态参考预测之间的 KL 散度。根据学习进度的快慢动态调整梯度缩放系数 $\mu_m$（学习快的模态减小梯度，学习慢的增加）。
    *   **空间驱动调节**：测量单模态梯度与多模态梯度之间的余弦相似度，通过辅助损失函数（Modulation Loss）将各模态的优化方向引导至与多模态目标一致的方向。

### 3. 实验设计
*   **数据集**：
    *   **IEMOCAP**：交互式情感对话捕捉数据集（6类情感）。
    *   **CMU-MOSEI**：大规模多模态情感分析数据集（情感极性回归/分类）。
*   **Benchmark 与对比方法**：
    *   **缺失/不平衡专用模型**：MMIN, SDR-GNN, Mi-CGA, MoMKE, GCNet, Ada2I, RedCore, MCE。
    *   **标准多模态骨干网络**：MMGCN, MMDFN。
*   **实验场景**：设置了多种 IMR 配置，缺失率 $(r_A, r_L, r_V)$ 在 $\{0.3, 0.5, 0.7\}$ 之间排列组合，并测试了极端缺失（如 90% 缺失）和标准 SMR 场景。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 PyTorch 框架和 Comet.ml 进行实验追踪。超参数设置显示：IEMOCAP Batch Size 为 16，训练 80 Epochs；CMU-MOSEI Batch Size 为 32，训练 25 Epochs。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个数据集分别进行了 6 组不同的 IMR 配置测试。
    *   进行了完整的消融实验（FCM vs GRM）。
    *   进行了模态组合分析（单模态、双模态、三模态性能对比）。
    *   进行了超参数敏感性分析和极端缺失率（0.9）下的鲁棒性测试。
*   **充分性评价**：实验设计非常**充分且客观**。通过将 BALM 集成到多种不同类型的基准模型（包括专门处理缺失的算法和通用融合算法）中，验证了其广泛的适用性。每组 SMR 实验均运行 5 次取平均值，保证了结果的可靠性。

### 6. 主要结论与发现
*   **性能提升**：BALM 在所有 IMR 和 SMR 设置下均显著提升了现有骨干网络的性能（如在 IEMOCAP 上最高提升了 7.51% 的 F1 值）。
*   **模态敏感性**：文本（Lexical）模态是最具判别力但也最脆弱的模态，其高缺失率会导致性能剧降，而 BALM 能有效缓解这种退化。
*   **双层调节的必要性**：消融实验证明，仅靠特征校准或梯度重平衡都无法达到最优，两者结合能显著稳定优化动力学。
*   **鲁棒性**：即使在某一模态缺失率高达 90% 的极端情况下，BALM 依然能保持优于基准模型的稳定性。

### 7. 优点
*   **模型无关性**：作为插件使用，无需重新设计复杂的融合架构，易于部署到现有系统。
*   **现实针对性**：首次深入探讨了比 SMR 更符合实际的 IMR（不平衡缺失率）场景。
*   **双重平衡机制**：同时从特征表示和梯度优化两个根本层面解决失衡问题，逻辑自洽且互补。

### 8. 不足与局限
*   **模态质量依赖**：在 IEMOCAP 的视觉模态上，由于原始特征质量较低，BALM 在纯视觉场景下的提升有限，甚至在某些极端组合下出现轻微波动。
*   **任务覆盖面**：目前主要集中在情感识别（MER）任务，尚未在视觉问答（VQA）或多模态检索等更广泛的任务上验证。
*   **算力成本未详述**：虽然 FCM 和单模态预测头很轻量，但增加的辅助损失计算和梯度监控对训练时间的具体影响未给出量化数据。

（完）
