---
title: "CCF: Complementary Collaborative Fusion for Domain Generalized Multi-Modal 3D Object Detection"
title_zh: CCF：面向领域泛化多模态 3D 目标检测的互补协作融合
authors: "Yuchen Wu, Kun Wang, Yining Pan, Na Zhao"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23276v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 当某一模态严重退化时的鲁棒融合
tldr: 针对多模态3D目标检测在跨域场景（如雨天、夜间）中性能下降的问题，本文提出了一种互补协作融合框架（CCF）。该框架通过查询解耦损失平衡模态梯度，利用LiDAR引导的深度先验增强2D查询的空间感知，并采用互补跨模态掩码促进模态间的自适应融合。实验证明，CCF在保持源域性能的同时，显著提升了目标域的泛化能力。
selection_source: fresh_fetch
motivation: 现有多模态检测器在恶劣环境下易受单模态退化影响，且存在LiDAR主导导致的视觉信息利用不足和鲁棒性差的问题。
method: 提出了查询解耦损失以平衡梯度、LiDAR引导深度先验以增强空间初始化，以及互补跨模态掩码以促进模态间的竞争与协作。
result: 在多个跨域基准测试中显著超越了现有最先进方法，同时在源域保持了极具竞争力的检测性能。
conclusion: CCF通过优化模态间的交互与监督机制，有效解决了多模态3D检测中的领域泛化难题。
---

## 摘要
多模态融合已成为实现精确 3D 目标检测的一种极具前景的范式。然而，当部署在与训练域不同的目标域时，性能会大幅下降。在这项工作中，我们专注于双分支候选区域级检测器，并确定了限制鲁棒跨域泛化的两个因素：1) 在雨天或夜间等具有挑战性的领域，单一模态可能会发生严重退化；2) LiDAR 分支通常在检测过程中占据主导地位，导致视觉线索被系统性地低估，且在点云受损时表现脆弱。为了应对这些挑战，我们提出了三个组件。首先，查询解耦损失（Query-Decoupled Loss）为纯 2D、纯 3D 和融合查询提供独立监督，从而重新平衡跨模态的梯度流。其次，LiDAR 引导的深度先验（LiDAR-Guided Depth Prior）通过对图像预测和 LiDAR 衍生的深度分布进行概率融合，为 2D 查询增强了实例感知的几何先验，从而改善了其空间初始化。第三，互补跨模态掩码（Complementary Cross-Modal Masking）对图像和点云应用互补的空间掩码，鼓励来自两种模态的查询在融合解码器中进行竞争，从而促进自适应融合。大量实验表明，该方法在保持源域性能的同时，相比现有最先进的基准模型取得了显著提升。代码和模型已在 https://github.com/IMPL-Lab/CCF 公开。

## Abstract
Multi-modal fusion has emerged as a promising paradigm for accurate 3D object detection. However, performance degrades substantially when deployed in target domains different from training. In this work, focusing on dual-branch proposal-level detectors, we identify two factors that limit robust cross-domain generalization: 1) in challenging domains such as rain or nighttime, one modality may undergo severe degradation; 2) the LiDAR branch often dominates the detection process, leading to systematic underutilization of visual cues and vulnerability when point clouds are compromised. To address these challenges, we propose three components. First, Query-Decoupled Loss provides independent supervision for 2D-only, 3D-only, and fused queries, rebalancing gradient flow across modalities. Second, LiDAR-Guided Depth Prior augments 2D queries with instance-aware geometric priors through probabilistic fusion of image-predicted and LiDAR-derived depth distributions, improving their spatial initialization. Third, Complementary Cross-Modal Masking applies complementary spatial masks to the image and point cloud, encouraging queries from both modalities to compete within the fused decoder and thereby promoting adaptive fusion. Extensive experiments demonstrate substantial gains over state-of-the-art baselines while preserving source-domain performance. Code and models are publicly available at https://github.com/IMPL-Lab/CCF.

---

## 论文详细总结（自动生成）

### 论文总结：CCF（互补协作融合）

#### 1. 核心问题与研究动机
*   **核心问题**：多模态 3D 目标检测在跨领域部署（如从晴天到雨天、从白天到黑夜）时性能大幅下降。
*   **研究动机**：作者通过初步研究发现两个关键限制因素：
    *   **模态退化不对称**：在恶劣天气下，LiDAR 和相机受损程度不同（如雨天 LiDAR 散射，夜间相机曝光不足），但模型往往无法自适应调整。
    *   **模态利用不平衡（LiDAR 主导）**：在双分支检测器中，3D 查询（Query）在匈牙利匹配中占据主导地位（匹配比例高达 37.5:1），导致 2D 分支（相机）得不到充分训练，且 2D 查询的深度初始化极不准确。

#### 2. 方法论
CCF 框架由三个核心组件组成，旨在平衡模态利用并增强鲁棒性：
*   **查询解耦损失（Query-Decoupled Loss, DL）**：
    *   **核心思想**：为 2D、3D 和融合查询提供独立的监督路径。
    *   **技术细节**：在训练时执行三次并行的解码器传递（权重共享）：仅 2D 查询、仅 3D 查询、以及融合查询。通过独立的匈牙利匹配和损失计算，确保图像分支获得足够的梯度流，防止其被 3D 分支掩盖。
*   **LiDAR 引导的深度先验（LiDAR-Guided Depth Prior, DP）**：
    *   **核心思想**：利用 LiDAR 的几何信息辅助 2D 查询的深度初始化。
    *   **技术细节**：将图像预测的深度分布与 2D 框内 LiDAR 点生成的直方图分布进行概率融合。引入一个轻量化置信度网络预测融合权重 $\lambda$，采用类似“专家乘积（PoE）”的形式生成最终深度分布，从而提高 2D 查询的空间定位精度。
*   **互补跨模态掩码（Complementary Cross-Modal Masking, CM）**：
    *   **核心思想**：通过模拟局部传感器失效，强制模型进行自适应融合。
    *   **技术细节**：在图像和点云上应用互补的空间掩码（如 GridMask）。当某一区域的图像被遮蔽时，保留其对应的 LiDAR 信息，反之亦然。这种策略促使两类查询在解码器中竞争，减少对单一模态的过度依赖。

#### 3. 实验设计
*   **数据集与场景**：基于 **nuScenes** 构建跨域基准。
    *   **源域（Source）**：新加坡晴天场景（226 个场景）。
    *   **目标域（Target）**：雨天（Rain）、夜间（Night）、波士顿（Boston，地理迁移）。
*   **Benchmark 与对比方法**：
    *   对比了 CMT、ISFusion、MoME、MEFormer 等最先进的多模态检测器。
    *   包含 LiDAR-only 方法（如 FSDv2）作为参考。
    *   设置了 **Oracle** 实验（在目标域直接训练）以展示性能上限。

#### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，初始学习率为 $4 \times 10^{-4}$，权重衰减 0.01，采用余弦退火调度。
*   **训练规模**：Batch size 为 16，训练 24 个 epoch。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和数量，但提到推理阶段仅使用融合分支，不增加额外计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比（4 个领域，8 种以上方法）。
    *   详尽的消融实验（验证 DL、DP、CM 各组件的贡献）。
    *   针对掩码策略（Modal Mask vs. Consistent Mask vs. Complementary Mask）的专项分析。
    *   提供了 Oracle 性能对比和定性可视化结果。
*   **充分性与公正性**：实验覆盖了天气、光照和地理位置三种主要的领域偏移，对比基准统一，且通过 Oracle 实验证明了方法的有效区间，实验设计较为客观、充分。

#### 6. 主要结论与发现
*   **性能提升**：CCF 在目标域（雨天/夜间/波士顿）的 mAP 分别提升了 **2.8%、1.3% 和 3.2%**，同时在源域保持了极具竞争力的性能。
*   **平衡效应**：通过查询解耦，2D 查询的性能显著提升，缩小了与 3D 查询的差距，增强了系统在 LiDAR 退化时的生存能力。
*   **深度初始化至关重要**：引入 LiDAR 几何先验后，2D 查询的定位误差显著降低，是提升融合效果的关键。

#### 7. 优点
*   **深入的诊断分析**：论文通过 Pilot Study 量化了模态不平衡问题（37.5:1 的匹配比），为后续方法提供了坚实的理论依据。
*   **训练与推理分离**：查询解耦损失仅在训练阶段增加开销，推理时保持原有的单次传递，兼顾了性能与效率。
*   **即插即用**：提出的组件具有通用性，可应用于其他双分支候选区域级检测框架。

#### 8. 不足与局限
*   **对 LiDAR 的依赖**：虽然旨在解决 LiDAR 退化问题，但“深度先验”组件仍需部分 LiDAR 点。在 LiDAR 完全失效（如传感器断开）的极端情况下，该组件的增益可能受限。
*   **超参数敏感性**：掩码概率 $p$ 和课程学习（Curriculum Learning）的进度可能需要针对不同数据集进行精细调优。
*   **场景覆盖**：实验主要集中在城市道路，对于更复杂的越野或极端浓雾等极端环境的泛化能力尚待验证。

（完）
