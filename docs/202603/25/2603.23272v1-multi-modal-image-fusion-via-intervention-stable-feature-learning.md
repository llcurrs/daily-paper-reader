---
title: Multi-Modal Image Fusion via Intervention-Stable Feature Learning
title_zh: 基于干预稳定特征学习的多模态图像融合
authors: "Xue Wang, Zheng Guan, Wenhua Qian, Chengchao Wang, Runzhuo Ma"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23272v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 通过掩码测试缺失信息补偿的多模态图像融合
tldr: 针对多模态图像融合中现有方法易受虚假统计关联影响的问题，本文提出一种基于因果干预的特征学习框架。通过设计互补掩码、随机掩码和模态丢弃三种干预策略，结合因果特征整合器（CFI）及自适应不变性门控，旨在识别并提取在不同扰动下保持稳定的鲁棒跨模态依赖。该方法在多个基准测试和下游视觉任务中达到了SOTA水平，显著提升了融合图像的质量和泛化能力。
selection_source: fresh_fetch
motivation: 传统方法过度依赖统计相关性，容易捕获数据集中的虚假关联，导致在分布偏移时性能下降。
method: 提出基于因果干预的框架，通过三种掩码策略探测模态关系，并利用Causal Feature Integrator提取干预稳定特征。
result: 实验证明该方法在公开基准测试和下游高级视觉任务上均取得了当前最先进的性能。
conclusion: 引入因果干预能有效识别鲁棒的跨模态依赖，为提升多模态图像融合的稳定性和泛化性提供了新思路。
---

## 摘要
多模态图像融合将来自不同模态的互补信息整合到统一的表示中。现有方法主要优化模态间的统计相关性，往往会捕捉到由数据集引起的伪相关，而这些相关性在分布偏移下会发生退化。在本文中，我们受因果原理启发，提出了一种基于干预的框架，用于识别鲁棒的跨模态依赖关系。借鉴 Pearl 的因果层级理论，我们设计了三种原则性的干预策略来探测模态关系的不同方面：i) 具有空间不相交扰动的互补掩码，用于测试各模态是否能真正补偿彼此缺失的信息；ii) 相同区域的随机掩码，用于识别在部分可观测性下仍保持信息量的特征子集；iii) 模态丢弃，用于评估每个模态不可替代的贡献。基于这些干预，我们引入了一种因果特征整合器（CFI），它通过自适应不变性门控学习识别并优先处理在不同扰动模式下保持重要性的干预稳定特征，从而捕捉鲁棒的模态依赖关系而非伪相关。大量实验表明，我们的方法在公共基准测试和下游高级视觉任务上均达到了 SOTA 性能。

## Abstract
Multi-modal image fusion integrates complementary information from different modalities into a unified representation. Current methods predominantly optimize statistical correlations between modalities, often capturing dataset-induced spurious associations that degrade under distribution shifts. In this paper, we propose an intervention-based framework inspired by causal principles to identify robust cross-modal dependencies. Drawing insights from Pearl's causal hierarchy, we design three principled intervention strategies to probe different aspects of modal relationships: i) complementary masking with spatially disjoint perturbations tests whether modalities can genuinely compensate for each other's missing information, ii) random masking of identical regions identifies feature subsets that remain informative under partial observability, and iii) modality dropout evaluates the irreplaceable contribution of each modality. Based on these interventions, we introduce a Causal Feature Integrator (CFI) that learns to identify and prioritize intervention-stable features maintaining importance across different perturbation patterns through adaptive invariance gating, thereby capturing robust modal dependencies rather than spurious correlations. Extensive experiments demonstrate that our method achieves SOTA performance on both public benchmarks and downstream high-level vision tasks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于因果干预的多模态图像融合框架，旨在解决现有方法过度依赖统计相关性而忽略因果鲁棒性的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的多模态图像融合（MMIF）方法主要基于“关联（Association）”层面的统计相关性进行优化。这种范式容易捕获数据集中的**虚假关联（Spurious Correlations）**（如特定光照与热信号的共现），导致模型在面对分布偏移（Distribution Shifts）或训练集之外的场景时性能退化。
*   **研究动机**：借鉴 Pearl 的因果层级理论，作者认为真正的模态互补性应在“干预（Intervention）”下保持稳定。通过主动对输入进行扰动（干预），可以迫使模型识别出那些真正具有因果支撑的、鲁棒的跨模态依赖特征。

### 2. 方法论
核心思想是通过**原则性干预策略**探测模态关系，并利用**因果特征整合器（CFI）**提取稳定特征。

*   **三种干预策略**：
    1.  **互补掩码（Complementary Masking）**：对不同模态应用空间不相交的遮挡，测试一模态是否能补偿另一模态缺失的信息，从而识别真正的跨模态补偿能力。
    2.  **随机掩码（Random Masking）**：对双模态相同区域进行随机遮挡，识别在局部信息受损时依然稳健的特征子集。
    3.  **模态丢弃（Modality Dropout）**：完全移除某一模态，评估各模态的不可替代性，防止模型过度依赖单一模态。
*   **关键技术：因果特征整合器（CFI）**：
    *   **双向跨模态注意力**：交换模态信息，捕捉互补关系。
    *   **自适应不变性门控（Invariance Gating）**：通过学习一个门控权重 $G_k$，识别在上述干预扰动下依然保持重要性的特征（稳定特征），并抑制受扰动影响大的虚假特征。
*   **训练目标函数**：
    *   包含融合保真度损失（$L_f$）、**干预一致性损失（$L_{inv}$）**（强制干预前后的特征在关键区域保持一致）以及模态必要性损失（$L_{nec}$）。

### 3. 实验设计
*   **数据集与场景**：
    *   **红外与可见光融合（IVIF）**：使用 MSRS、TNO、M3FD 三个主流基准数据集。
    *   **医学图像融合（MIF）**：使用 Harvard Medical 数据库进行跨域泛化测试。
    *   **下游任务**：在 M3FD 上进行目标检测（YOLOv5），在 MSRS 上进行语义分割。
*   **对比方法**：与 9 种 SOTA 方法对比，包括 TIMFusion、SAGE、CDDFuse、LRRNet、DCEvo、A2RNet 等。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：在 MSRS 数据集上训练，图像裁剪为 $256 \times 256$ 块，Batch Size 为 16，训练 50 个 Epoch。
*   **优化器**：Adam 优化器，学习率为 1e-4。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个 IVIF 数据集的定量与定性对比。
    *   进行了**零样本（Zero-shot）跨域迁移实验**（IVIF 模型直接用于医学图像融合），验证了泛化性。
    *   **消融实验**：分别验证了 CFI 模块、一致性损失、必要性损失以及干预机制本身的有效性。
    *   **因果分析**：引入了**平均处理效应（ATE）分析**，定量评估了不同干预手段对融合质量的具体影响。
*   **充分性评价**：实验设计非常全面，不仅关注静态指标（PSNR, CC, AG 等），还通过下游视觉任务和跨传感器迁移实验证明了方法的实用价值，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **干预稳定性是关键**：通过干预学习到的特征比单纯基于相关性学习的特征更具鲁棒性，尤其在烟雾、过曝等极端条件下表现更优。
*   **性能领先**：在所有测试基准上，该方法在边缘保持（AG, SF）和结构一致性（CC）等指标上均达到或接近最优。
*   **泛化能力强**：无需微调即可在医学图像上取得良好效果，证明模型捕获的是通用的融合原理而非特定数据集的偏见。

### 7. 优点
*   **理论创新**：首次将 Pearl 的因果干预思想系统地引入多模态图像融合领域，突破了传统相关性学习的局限。
*   **可解释性**：通过不变性门控的可视化，清晰展示了模型如何聚焦于鲁棒的特征区域。
*   **任务友好**：在目标检测和语义分割任务中表现出色，证明融合图像保留了关键的语义细节。

### 8. 不足与局限
*   **计算开销**：虽然推理速度较快（FPS 较高），但训练阶段由于需要多次前向传播（处理四种干预输出），训练成本可能高于普通模型。
*   **超参数依赖**：干预掩码的大小、数量以及损失函数的平衡系数（$\alpha, \beta$）仍需经验性设置，对不同模态组合的敏感度有待进一步研究。
*   **模态局限**：目前主要验证了红外-可见光和医学图像，对于 SAR、深度图等其他模态的适用性尚未探讨。

（完）
