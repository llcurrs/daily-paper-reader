---
title: "Gau-Occ: Geometry-Completed Gaussians for Multi-Modal 3D Occupancy Prediction"
title_zh: Gau-Occ：用于多模态 3D 占用预测的几何补全高斯
authors: "Chengxin Lv, Yihui Li, Hongyu Yang, YunHong Wang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22852v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 从稀疏LiDAR中恢复缺失结构以进行多模态融合
tldr: 针对自动驾驶中3D语义占据预测计算开销大的问题，本文提出Gau-Occ框架。该方法摒弃了昂贵的密集体素处理，转而使用紧凑的语义3D高斯建模场景。通过LiDAR补全扩散器（LCD）修复稀疏点云以初始化高斯锚点，并利用高斯锚点融合（GAF）高效集成多视图图像语义。Gau-Occ在保持空间一致性的同时提升了语义辨别力，在多个基准测试中达到了SOTA性能且计算效率显著。
selection_source: fresh_fetch
motivation: 现有的多模态3D占据预测方法依赖高成本的密集体素或BEV张量，难以平衡精度与计算效率。
method: 提出以3D高斯为核心的框架，结合LiDAR补全扩散器修复几何结构，并通过几何对齐的2D采样融合多视图图像语义。
result: 在多个挑战性基准测试中取得了最先进的性能，并显著降低了计算开销。
conclusion: Gau-Occ证明了利用紧凑的3D高斯表示结合跨模态几何补全，可以高效且准确地实现自动驾驶场景的3D占据预测。
---

## 摘要
3D 语义占用预测对于自动驾驶至关重要。虽然多模态融合相比纯视觉方法提升了准确性，但它通常依赖于计算开销巨大的稠密体素或 BEV（鸟瞰图）张量。我们提出了 Gau-Occ，这是一个多模态框架，通过将场景建模为语义 3D 高斯的紧凑集合，绕过了稠密的体积处理。为了确保几何完整性，我们提出了一种 LiDAR 补全扩散器（LCD），从稀疏的 LiDAR 数据中恢复缺失结构，以初始化鲁棒的高斯锚点。此外，我们引入了高斯锚点融合（GAF），通过几何对齐的 2D 采样和跨模态对齐，高效地整合了多视图图像语义。通过细化这些紧凑的高斯描述符，Gau-Occ 同时捕捉了空间一致性和语义可区分性。在具有挑战性的基准测试上进行的大量实验表明，Gau-Occ 在实现最先进性能的同时，具有显著的计算效率。

## Abstract
3D semantic occupancy prediction is crucial for autonomous driving. While multi-modal fusion improves accuracy over vision-only methods, it typically relies on computationally expensive dense voxel or BEV tensors. We present Gau-Occ, a multi-modal framework that bypasses dense volumetric processing by modeling the scene as a compact collection of semantic 3D Gaussians. To ensure geometric completeness, we propose a LiDAR Completion Diffuser (LCD) that recovers missing structures from sparse LiDAR to initialize robust Gaussian anchors. Furthermore, we introduce Gaussian Anchor Fusion (GAF), which efficiently integrates multi-view image semantics via geometry-aligned 2D sampling and cross-modal alignment. By refining these compact Gaussian descriptors, Gau-Occ captures both spatial consistency and semantic discriminability. Extensive experiments across challenging benchmarks demonstrate that Gau-Occ achieves state-of-the-art performance with significant computational efficiency.

---

## 论文详细总结（自动生成）

### Gau-Occ: 用于多模态 3D 占用预测的几何补全高斯 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在保证 3D 语义占用预测（Semantic Occupancy Prediction）高精度的同时，降低多模态融合带来的巨大计算开销。
*   **研究背景**：
    *   **纯视觉方法**在远距离和遮挡区域几何线索弱，导致预测不完整。
    *   **现有融合方法**（如 LiDAR+Camera）通常依赖稠密的体素（Voxel）或 BEV 张量，内存和计算需求随分辨率呈立方级增长，难以扩展。
    *   **稀疏 LiDAR** 存在遮挡偏差和稀疏性，限制了对未观测区域的推理能力。

#### 2. 提出的方法论
Gau-Occ 抛弃了稠密的体素处理，采用**语义 3D 高斯（3D Gaussians）**作为场景的紧凑表示。
*   **核心思想**：将场景建模为一组可学习的 3D 高斯锚点，通过补全的 LiDAR 几何引导图像特征的融合，最后通过高斯泼溅（Splatting）生成占据网格。
*   **关键技术细节**：
    1.  **LiDAR 补全扩散器 (LCD)**：针对稀疏 LiDAR，提出局部扩散模型。它不进行全局缩放以保持度量几何，通过学习时间聚合的稠密点云先验，预测并补全遮挡区域的结构。
    2.  **混合高斯初始化**：结合“基于密度的选择”和“随机覆盖采样”，从补全的点云中初始化高斯中心，确保几何覆盖的完整性。
    3.  **高斯锚点融合 (GAF)**：
        *   **几何引导采样**：利用 LiDAR 特征预测图像平面的 2D 偏移量，实现非局部特征采样。
        *   **Geo-VLAD 重采样**：借鉴 VLAD 思想，将多视图采样特征聚类为紧凑描述符，并使用 FiLM（特征线性调制）进行几何增强。
        *   **跨模态对齐**：通过单层交叉注意力机制，将图像语义注入到以 LiDAR 为中心的 3D 锚点中。
    4.  **局部高斯泼溅**：将细化后的高斯属性累加到体素空间，输出最终的语义标签。

#### 3. 实验设计
*   **数据集**：
    *   **SurroundOcc-nuScenes**：大规模自动驾驶场景，提供稠密占据标注。
    *   **Occ3D-nuScenes**：另一种占据预测协议，具有不同的体素范围和类别定义。
    *   **KITTI-360**：长距离城市驾驶场景，测试单目+LiDAR 的性能。
*   **对比方法**：
    *   **纯视觉**：MonoScene, BEVFormer, TPVFormer, GaussianFormer 等。
    *   **纯 LiDAR**：LMSCNet, L-CONet, L2COcc。
    *   **多模态**：Co-Occ, OccMamba, SDGOcc, DAOcc（当前最强基准）。

#### 4. 资源与算力
*   **硬件环境**：
    *   **nuScenes 实验**：在 NVIDIA **RTX 4090 (24GB)** 上进行训练和推理。
    *   **KITTI-360 实验**：在 NVIDIA **A100 (40GB)** 上进行。
*   **训练细节**：
    *   nuScenes 训练 20 个 epoch，KITTI-360 训练 25 个 epoch。
    *   使用 AdamW 优化器，学习率采用线性预热加余弦退火策略。
*   **效率表现**：在 12.8k 查询设置下，推理延迟仅为 **124ms**，显存占用 **3.3GB**，比基于 BEV 的多模态方法（如 Co-Occ）快约 5 倍，显存节省约 70%。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准上进行了定量评估，并提供了大量的定性可视化结果（包括恶劣天气和拥堵交通场景）。
*   **消融实验**：针对 LCD 补全模块、高斯初始化策略、GAF 内部组件（GGS 采样、GVR 重采样）分别做了详细对比。
*   **充分性评价**：实验设计全面，涵盖了精度、效率、鲁棒性等多个维度。对比对象包含了最新的 SOTA 模型，且在相同硬件条件下测试了延迟和内存，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先**：Gau-Occ 在所有基准上均刷新了记录。在 SurroundOcc-nuScenes 上比 DAOcc 提升了 1.5 IoU 和 0.6 mIoU。
*   **几何补全的重要性**：LCD 模块能显著提升远距离和遮挡区域的预测精度，证明了扩散模型在处理稀疏传感器数据中的潜力。
*   **高斯表示的优越性**：相比稠密网格，稀疏的高斯锚点能以更低的计算成本捕捉精细的几何边界和语义细节。

#### 7. 优点与亮点
*   **高效性**：通过稀疏高斯锚点避开了密集体素计算的“维度诅咒”，实现了极高的推理速度。
*   **几何对齐**：GAF 模块通过 LiDAR 特征引导图像采样，解决了跨模态对齐中的空间一致性问题。
*   **鲁棒性**：在单摄像头（KITTI-360）和恶劣天气下表现稳健，证明了多模态融合的有效性。

#### 8. 不足与局限
*   **预训练依赖**：LCD 模块需要预先在聚合的稠密点云上训练，增加了流水线的复杂性。
*   **静态假设**：虽然能处理动态物体，但 LCD 补全主要基于多帧聚合，对于高速运动物体的即时补全可能存在滞后或伪影。
*   **高斯数量固定**：实验中使用了固定的高斯数量（如 25.6k），在极端复杂的超大规模场景下，可能需要动态调整锚点数量以平衡精度与开销。

（完）
