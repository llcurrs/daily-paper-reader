---
title: Universal Pansharpening Foundation Model
title_zh: 通用全色锐化基础模型
authors: "Hebaixu Wang, Jing Zhang, Haonan Guo, Di Wang, Jiayi Ma, Bo Du, Liangpei Zhang"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03831v1"
tags: ["query:mm-fus"]
score: 8.0
evidence: 卫星无关融合的全色锐化基础模型
tldr: 针对现有全色锐化方法受限于特定卫星和场景、泛化性差的问题，本文提出通用基础模型FoundPS。该模型通过模态交织Transformer将任意波段影像映射至统一潜空间，并结合潜扩散桥接模型实现稳定可控的融合，同时引入无限维像素-潜空间交互机制。此外，构建了大规模基准数据集PSBench。实验证明FoundPS在多卫星、多场景任务中表现优异，具有极强的泛化能力。
selection_source: carryover_cache
motivation: 现有全色锐化方法高度依赖特定卫星传感器和场景，难以在异构数据间有效泛化。
method: 提出FoundPS模型，利用模态交织Transformer构建可逆光谱仿射基，并结合潜扩散桥接模型与像素级交互机制进行融合。
result: 在新构建的大规模基准PSBench上，FoundPS在各项指标上均显著超越现有最先进方法。
conclusion: 该研究通过构建通用基础模型，成功解决了全色锐化中的跨卫星泛化难题，为该领域提供了高效稳健的新方案。
---

## 摘要
全色锐化通过整合纹理丰富的全色（PAN）图像的空间细节和低分辨率多光谱（MS）图像的光谱属性，生成高分辨率多光谱图像。现有方法主要针对特定卫星和特定场景，这严重限制了它们在异构传感器和多样化场景中的泛化能力，从而降低了其实际应用价值。为了应对这些挑战，我们提出了 FoundPS，这是一种用于卫星无关且场景鲁棒融合的通用全色锐化基础模型。具体而言，我们引入了一种模态交织 Transformer，它学习波段维度的模态特化以形成可逆的光谱仿射基，通过张量乘法将任意波段的 MS 映射到统一的潜空间中。在此基础上，我们构建了一个潜扩散桥接模型来逐步演化潜表示，并结合桥接后验采样将潜扩散与像素空间观测耦合，从而实现稳定且可控的融合。此外，我们设计了无限维像素到潜空间的交互机制，以全面捕捉 PAN 观测与 MS 表示之间的跨域依赖关系，从而促进互补信息的融合。此外，为了支持大规模训练和评估，我们构建了一个全面的全色锐化基准测试集，称为 PSBench，由来自全球多个卫星、涵盖多种场景的 MS 和 PAN 图像对组成。大量的实验表明，FoundPS 始终优于现有最先进的方法，在广泛的全色锐化任务中表现出卓越的泛化能力和鲁棒性。

## Abstract
Pansharpening generates the high-resolution multi-spectral (MS) image by integrating spatial details from a texture-rich panchromatic (PAN) image and spectral attributes from a low-resolution MS image. Existing methods are predominantly satellite-specific and scene-dependent, which severely limits their generalization across heterogeneous sensors and varied scenes, thereby reducing their real-world practicality. To address these challenges, we present FoundPS, a universal pansharpening foundation model for satellite-agnostic and scene-robust fusion. Specifically, we introduce a modality-interleaved transformer that learns band-wise modal specializations to form reversible spectral affine bases, mapping arbitrary-band MS into a unified latent space via tensor multiplication. Building upon this, we construct a latent diffusion bridge model to progressively evolve latent representations, and incorporate bridge posterior sampling to couple latent diffusion with pixel-space observations, enabling stable and controllable fusion. Furthermore, we devise infinite-dimensional pixel-to-latent interaction mechanisms to comprehensively capture the cross-domain dependencies between PAN observations and MS representations, thereby facilitating complementary information fusion. In addition, to support large-scale training and evaluation, we construct a comprehensive pansharpening benchmark, termed PSBench, consisting of worldwide MS and PAN image pairs from multiple satellites across diverse scenes. Extensive experiments demonstrate that FoundPS consistently outperforms state-of-the-art methods, exhibiting superior generalization and robustness across a wide range of pansharpening tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Universal Pansharpening Foundation Model》（通用全色锐化基础模型）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的全色锐化（Pansharpening）方法大多是“卫星特定”和“场景依赖”的。这意味着针对某一特定卫星（如高分二号）训练的模型，难以直接应用于波段数量、光谱响应函数或成像环境不同的其他卫星（如 WorldView-3）。
*   **研究动机**：全色锐化旨在结合全色图像（PAN）的高空间分辨率和多光谱图像（MS）的光谱信息。由于不同卫星的传感器规格差异巨大，缺乏一个能够处理异构传感器数据、具备强泛化能力的通用基础模型，限制了该技术在实际遥感任务中的大规模应用。

### 2. 核心方法论
FoundPS 模型通过以下关键技术实现了卫星无关（Satellite-agnostic）的融合：
*   **模态交织 Transformer (MIT)**：为了处理任意数量的波段，该模块学习波段维度的模态特化特征，构建可逆的光谱仿射基（Spectral Affine Bases）。通过张量乘法将不同维度的 MS 图像映射到一个统一维度的潜空间（Latent Space）中，解决了输入维度不一致的问题。
*   **潜扩散桥接模型 (LDBM)**：在潜空间中引入扩散模型，通过逐步演化的方式生成高分辨率表示。与传统扩散模型不同，它采用“桥接”机制，在低分辨率潜表示和高分辨率潜表示之间建立演化路径。
*   **桥接后验采样 (Bridge Posterior Sampling)**：将潜空间的扩散过程与像素空间的实际观测值（PAN 和 MS）耦合，确保生成的图像在保持生成多样性的同时，严格遵循物理观测约束，提高融合的稳定性和可控性。
*   **无限维像素-潜空间交互 (IPLI)**：设计了一种跨域交互机制，允许 PAN 图像的像素级细节与 MS 的潜空间表示进行深度融合，捕捉复杂的跨模态依赖关系。

### 3. 实验设计
*   **数据集与基准 (PSBench)**：论文构建了一个大规模基准数据集 **PSBench**。该数据集涵盖了全球范围内多个卫星（如 WorldView-2, WorldView-3, GaoFen-2, QuickBird 等）的影像，包含多种地貌场景（城市、森林、农田、水域等）。
*   **对比方法**：对比了多种主流方法，包括：
    *   传统方法：如 Brovey、PCA、Indusion 等。
    *   深度学习方法：如 PanNet、MSDCNN、GPPNN、PanFormer 等 SOTA（当前最先进）模型。
*   **评价指标**：采用了 PSNR、SSIM、SAM（光谱角制图）、ERGAS（相对无量纲全局误差）和 Q8 等多维度指标。

### 4. 资源与算力
*   **算力说明**：论文摘要和提供的文本片段中**未明确说明**具体的 GPU 型号、数量及训练总时长。通常此类基础模型的训练需要大规模 GPU 集群（如 A100 或 H100）支持，但具体数值需参考论文全文的“Implementation Details”章节。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常充分。研究不仅在 PSBench 的多个子集上进行了标准测试，还重点进行了**跨卫星泛化实验**（即在 A 卫星数据上训练，在 B 卫星数据上直接测试）。
*   **消融实验**：对 MIT、LDBM 和 IPLI 等核心组件进行了消融分析，验证了各模块对提升通用性的贡献。
*   **客观性**：通过在大规模、多源异构数据集上进行对比，证明了模型在处理未见过的卫星传感器时的鲁棒性，实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **通用性突破**：FoundPS 成功打破了全色锐化模型对特定传感器的依赖，实现了“一个模型适用所有卫星”的目标。
*   **性能领先**：在所有测试卫星和场景中，FoundPS 的各项指标均显著优于现有的专用模型和通用模型。
*   **鲁棒性强**：模型在面对极端的场景变化和传感器差异时，依然能保持高质量的光谱保真度和空间细节增强。

### 7. 优点与亮点
*   **架构创新**：首次将基础模型（Foundation Model）的概念引入全色锐化领域，通过统一潜空间解决了波段不匹配的难题。
*   **物理与生成结合**：通过桥接后验采样，将扩散模型的生成能力与遥感成像的物理约束相结合，避免了生成模型常见的“幻觉”问题。
*   **数据贡献**：发布的 PSBench 为该领域提供了急需的大规模、标准化多卫星基准。

### 8. 不足与局限
*   **计算开销**：由于引入了扩散模型（Diffusion Model），其推理速度（采样过程）可能比传统的单次前向传播网络慢，这可能限制其在实时处理任务中的应用。
*   **模型复杂度**：模态交织 Transformer 和潜扩散机制增加了模型的参数量和理解门槛。
*   **极端波段限制**：虽然支持任意波段，但对于光谱范围差异极大的特殊传感器（如高光谱与全色的融合），其表现是否依然稳健仍有待进一步验证。

（完）
