---
title: "BiEvLight: Bi-level Learning of Task-Aware Event Refinement for Low-Light Image Enhancement"
title_zh: BiEvLight：面向低光照图像增强的任务感知事件细化双层学习
authors: "Zishu Yao, Xiang-Xiang Su, Shengning Zhou, Guang-Yong Chen, Guodong Fan, Xing Chen"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04975v1"
tags: ["query:mm-fus"]
score: 8.0
evidence: 事件相机与低光图像的模态融合策略
tldr: 本研究针对低光照图像增强中事件相机背景噪声与图像低信噪比导致的噪声耦合难题，提出了BiEvLight框架。该框架通过双层优化策略将事件去噪与图像增强协同处理，利用梯度引导的去噪先验，使去噪过程能根据增强任务的需求自适应调整。实验证明，该方法在真实数据集上显著提升了增强质量，解决了传统静态去噪带来的性能瓶颈。
selection_source: carryover_cache
motivation: 事件相机在低光增强中面临背景噪声与图像噪声耦合的挑战，且传统的静态去噪预处理难以兼顾不同增强目标的需求。
method: 提出一种基于双层优化的任务感知框架，利用图像与事件的梯度相关性构建去噪先验，并使去噪过程受增强任务约束进行协同优化。
result: 在SDE数据集上的实验结果显示，该方法在PSNR上平均提升了1.30dB，在SSIM上提升了0.047，显著优于现有最先进方法。
conclusion: 通过将事件去噪重构为受增强任务约束的双层优化问题，BiEvLight成功释放了事件相机在低光图像增强中的潜力。
---

## 摘要
事件相机凭借其高动态范围，在低光照图像增强（LLIE）领域展现出巨大潜力。现有工作主要集中在设计有效的模态融合策略。然而，一个关键挑战在于事件中固有的背景活动（BA）噪声与图像中低信噪比（SNR）带来的双重退化，这导致模态融合过程中出现严重的噪声耦合，从而形成关键的性能瓶颈。因此，我们认为精确的事件去噪是释放基于事件融合潜力的前提。为此，我们提出了 BiEvLight，这是一个分层且任务感知的框架，通过利用增强与去噪之间的内在相互依赖性来协同优化两者。具体而言，BiEvLight 利用图像与事件之间强大的梯度相关性，构建了一个梯度引导的事件去噪先验，以缓解重噪声区域去噪不足的问题。此外，我们没有将事件去噪视为静态的预处理阶段（这不可避免地会导致过度去噪与去噪不足之间的权衡，且无法适应特定增强目标的需求），而是将其重新表述为受增强任务约束的双层优化问题。通过跨任务交互，上层去噪问题学习为下层增强目标量身定制的事件表示，从而显著提高整体增强质量。在真实噪声数据集 SDE 上的广泛实验表明，我们的方法显著优于现有最先进（SOTA）方法，在 PSNR、PSNR* 和 SSIM 上分别平均提升了 1.30dB、2.03dB 和 0.047。代码将公开于 https://github.com/iijjlk/BiEvlight。

## Abstract
Event cameras, with their high dynamic range, show great promise for Low-light Image Enhancement (LLIE). Existing works primarily focus on designing effective modal fusion strategies. However, a key challenge is the dual degradation from intrinsic background activity (BA) noise in events and low signal-to-noise ratio (SNR) in images, which causes severe noise coupling during modal fusion, creating a critical performance bottleneck. We therefore posit that precise event denoising is the prerequisite to unlocking the full potential of event-based fusion. To this end, we propose BiEvLight, a hierarchical and task-aware framework that collaboratively optimizes enhancement and denoising by exploiting their intrinsic interdependence. Specifically, BiEvLight exploits the strong gradient correlation between images and events to build a gradient-guided event denoising prior that alleviates insufficient denoising in heavily noisy regions. Moreover, instead of treating event denoising as a static pre-processing stage-which inevitably incurs a trade-off between over- and under-denoising and cannot adapt to the requirements of a specific enhancement objective-we recast it as a bilevel optimization problem constrained by the enhancement task. Through cross-task interaction, the upper-level denoising problem learns event representations tailored to the lower-level enhancement objective, thereby substantially improving overall enhancement quality. Extensive experiments on the Real-world noise Dataset SDE demonstrate that our method significantly outperforms state-of-the-art (SOTA) approaches, with average improvements of 1.30dB in PSNR, 2.03dB in PSNR* and 0.047 in SSIM, respectively. The code will be publicly available at https://github.com/iijjlk/BiEvlight.

---

## 论文详细总结（自动生成）

以下是对论文《BiEvLight: Bi-level Learning of Task-Aware Event Refinement for Low-Light Image Enhancement》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在低光照环境下，传统相机图像存在低信噪比（SNR）和运动模糊，而事件相机虽然具有高动态范围，但会产生严重的背景活动（BA）噪声。
*   **研究动机**：
    *   **噪声耦合瓶颈**：现有方法多关注模态融合策略，忽略了图像噪声与事件噪声在融合时的相互干扰。
    *   **静态预处理缺陷**：传统的事件去噪被视为独立的预处理步骤，缺乏与后续增强任务的交互，容易导致“过度去噪（丢失细节）”或“去噪不足（残留噪声）”的矛盾。
    *   **目标**：通过建立任务感知的协同优化机制，使事件去噪能够根据增强任务的需求进行自适应调整。

### 2. 方法论
BiEvLight 提出了一个分层且任务感知的双层学习框架，核心思想是将去噪与增强视为相互依赖的整体。
*   **空间自适应梯度引导去噪（Spatially-adaptive Gradient-guided Denoising）**：
    *   利用图像与事件之间的强梯度相关性，使用反射分量的梯度作为先验，引导事件流去噪。
    *   引入空间自适应阈值 $q$，根据局部梯度分布自动调整去噪强度，在平滑区域保留稀疏事件，在纹理区域抑制噪声。
*   **双层优化框架（Bi-level Learning）**：
    *   **上层问题（去噪）**：优化去噪网络参数 $w$，目标是在增强任务约束下获得最佳事件表示。
    *   **下层问题（增强）**：优化增强网络参数 $\theta$，利用去噪后的事件辅助图像重建。
    *   **交互机制**：增强任务的性能增益通过梯度回传给去噪模块（反馈），而去噪后的高质量事件则为增强模块提供清晰的结构引导（前馈）。
*   **算法流程**：采用一阶截断迭代微分（ITD）策略解决双层优化中的高阶 Hessian 矩阵计算难题，通过链式法则更新参数。

### 3. 实验设计
*   **数据集**：
    *   **SDE 数据集**：包含 91 个真实世界的图像-事件序列（分为室内 SDE-in 和室外 SDE-out）。
    *   **SDSD 数据集**：包含 150 个序列，用于验证模型在不同场景下的泛化能力。
*   **对比方法（Benchmark）**：
    *   **纯图像方法**：SNRNet, FourLLIE, Retinexformer, NeRCo, SCI++, URWKV。
    *   **图像+事件融合方法**：eSLNet, ELIE, EvLight (CVPR 2024)。
*   **评价指标**：PSNR、SSIM 以及针对低光增强优化的 PSNR*。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练细节**：
    *   Batch size 为 8，迭代次数为 150k 次。
    *   使用 Adam 优化器，初始学习率 2e-4，采用余弦退火策略。
*   **训练时长**：文中表 5 显示，BiEvLight 的训练耗时约为 **48 小时**（相比交替学习策略增加了约 23% 的时间成本，但推理延迟未增加）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（SDE 和 SDSD）的室内外子集上进行了详尽的定量对比。
    *   **消融实验**：分别验证了“去噪策略（Base vs. 梯度引导）”和“学习范式（联合学习 vs. 交替学习 vs. 双层学习）”的有效性。
    *   **参数敏感性分析**：对梯度监督水平 $\mu$、缩放因子 $m$、窗口大小 $W$ 等超参数进行了鲁棒性测试。
*   **充分性评价**：实验设计全面，涵盖了从定量指标到定性视觉效果的对比，且通过消融实验清晰地展示了双层优化相对于传统训练模式的优越性，逻辑严密且客观。

### 6. 主要结论与发现
*   **性能突破**：BiEvLight 在 SDE 数据集上比 SOTA 方法（EvLight）在 PSNR 上提升了 **1.30dB**，在 SSIM 上提升了 **0.047**。
*   **去噪质量**：该方法能有效识别并保留被重噪声淹没的真实事件（如暗光下的文字信息），显著提升了边缘重建的清晰度。
*   **协同效应**：双层优化机制成功解决了去噪与增强之间的任务冲突，证明了“任务感知”的去噪比“盲目”的预处理去噪更利于下游任务。

### 7. 优点
*   **创新性强**：首次将双层优化引入事件引导的低光增强任务，打破了去噪与增强相互独立的固有思维。
*   **空间自适应**：梯度引导策略考虑了图像内容的局部异质性，避免了全局阈值导致的细节丢失。
*   **推理高效**：虽然训练过程更复杂，但最终推理阶段不增加额外计算负担（FPS 与常规融合模型相当）。

### 8. 不足与局限
*   **依赖预训练模型**：框架中使用了预训练且冻结的分解网络（U-Retinex），如果该分解网络在极端退化场景下失效，可能会影响后续的梯度引导效果。
*   **训练成本**：双层优化的计算开销（显存和时间）高于传统的联合训练或交替训练。
*   **应用限制**：目前主要针对静态或低速动态场景的真实数据集，在极端高速运动下的表现（事件堆叠导致的伪影）尚需进一步验证。

（完）
