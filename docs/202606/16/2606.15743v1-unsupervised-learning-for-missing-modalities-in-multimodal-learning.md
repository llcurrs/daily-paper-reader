---
title: Unsupervised Learning for Missing Modalities in Multimodal Learning
title_zh: 多模态学习中缺失模态的无监督学习
authors: "Hassan Ismkhan, Hamid Bouchahcia"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15743v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 提出了用于多模态学习中缺失模态的无监督学习框架，直接满足需求
tldr: "多模态学习中模态缺失是常见难题。本文提出UL4M4无监督框架，通过模态特定归一化和部分模态距离度量对不完整观测进行公平聚类，再基于聚类中心迭代贪心填补缺失嵌入。在超过50%模态缺失的挑战性场景下，首次一致达到F1-Micro>0.7，显著优于基线。该框架任务无关、轻量且易集成，为缺失模态学习提供了通用解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15743v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1650, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15743v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1898, \"height\": 1387, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15743v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1707, \"height\": 709, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15743v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 541, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15743v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1491, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15743v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1104, \"height\": 610, \"label\": \"Table\"}]"
motivation: 现有方法依赖完整模态或特定任务，缺乏处理任意缺失模式的通用性。
method: 利用无监督聚类捕捉跨模态结构，通过模态特定归一化与部分模态距离度量实现公平聚类，并迭代贪心填补缺失嵌入。
result: "高度缺失场景下F1-Micro>0.7，性能稳定且超越所有基线方法。"
conclusion: 提出灵活、任务无关的缺失模态填补框架，有效提升多模态学习鲁棒性。
---

## 摘要
本文通过引入多模态学习中缺失模态的无监督学习（UL4M4），解决了多模态学习中的缺失模态挑战。UL4M4是一种灵活的框架，在监督预测之前以任务无关的方式填补缺失的特征嵌入。我们提出了模态特定的归一化和一种新颖的部分模态距离度量，以实现对不完整观测的公平聚类，捕捉跨模态结构，同时在不同维度和模态数量下保持尺度不变性。来自无监督阶段的聚类中心指导了训练或推理过程中任何缺失模态的迭代贪婪填补过程，支持任意数量的模态和每个样本的任意缺失模式。填补模块轻量级，使用冻结的编码器，并与下游任务解耦，从而可以轻松集成到任何融合/预测架构中。在多样且高度不完整的情境下进行的大量实验证明了UL4M4的鲁棒性，据我们所知，即使超过50%的模态槽缺失，在具有挑战性的缺失配置下，它首次实现了持续高于0.7的F1-Micro分数。结果在不同聚类大小下也表现稳定，并显著优于现有的最先进基线。代码可在此处获取：https://github.com/h-ismkhan/Multimodal-Learning-with-Missing-Modalities-via-Unsupervised-Learning。

## Abstract
This paper addresses the missing-modality challenge in multi-modal learning by introducing Unsupervised Learning for Missing Modalities in Multi-Modal Learning (UL4M4), a flexible framework that imputes missing feature embeddings in a task-independent manner before supervised prediction. We propose modality-specific   normalization and a novel partial-modality distance metric to enable fair clustering of incomplete observations, capturing cross-modal structures while preserving scale-invariance across varying dimensionalities and modality counts. Cluster centers from this unsupervised stage guide an iterative greedy imputation process for any   missing modalities during training or inference, supporting arbitrary numbers of modalities and arbitrary missing patterns per sample. The imputation module is lightweight, uses frozen encoders, and decouples from the downstream task, allowing easy integration with any fusion/prediction architecture. Extensive experiments under diverse and highly incomplete regimes demonstrate UL4M4's robustness, achieving, to   the best of our knowledge, the first consistent F1-Micro scores above 0.7 on challenging missing configurations even when more than 50\% of modality slots are missing. Results are also stable across cluster sizes and significantly outperform state-of-the-art baselines. Code is available here: https://github.com/h-ismkhan/Multimodal-Learning-with-Missing-Modalities-via-Unsupervised-Learning.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究问题**: 多模态学习（如文本、音频、视频融合）在实际应用中常面临模态缺失问题（传感器故障、隐私限制等），导致模型性能严重下降。现有方法存在三大局限：仅支持固定模态数目、仅处理特定缺失模式、填补模块与下游任务紧密耦合，难以泛化到高度不完整、任意缺失模式的真实场景。
- **研究动机**: 开发一种 **任务无关**、**轻量级**、**支持任意模态数目和任意缺失模式** 的无监督填补框架，在保持下游任务性能的同时，能处理超过50%缺失率的极端情况。
- **整体含义**: 论文提出的UL4M4框架首次在高度缺失场景下（>50%模态槽缺失）取得F1-Micro一致高于0.7，为多模态学习的鲁棒性提供了通用解决方案。

## 2. 方法论：核心思想、关键技术细节、公式/算法流程

### 核心思想
- **两阶段无监督填补**：第一阶段对不完整样本进行多模态聚类，得到聚类中心（可能本身也缺失部分模态）；第二阶段利用聚类中心，通过**迭代贪婪选择**为每个缺失模态样本填充缺失的嵌入。
- **关键设计**：模态特定归一化 + 部分模态距离度量，确保不同维度和不同模态数量的样本之间距离可比。

### 关键技术细节

1. **模态特定归一化**  
   对每个模态 \( m \)，利用可用样本计算均值和标准差：
   \[
   \mu_m = \frac{1}{|I_m|}\sum_{i\in I_m} \mathbf{z}^{(m)}_i,\quad \sigma_m = \sqrt{\frac{1}{|I_m|}\sum_{i\in I_m}\|\mathbf{z}^{(m)}_i - \mu_m\|^2}
   \]
   归一化：\(\tilde{\mathbf{z}}^{(m)}_i = (\mathbf{z}^{(m)}_i - \mu_m)/\sigma_m\)（逐元素）。

2. **部分模态距离度量**  
   样本 \( i \) 和聚类中心 \( j \) 之间，只使用两者都拥有的模态 \( M_{i,j} \)：
   \[
   d(i,j) = \sqrt{\frac{1}{|M_{i,j}|} \sum_{m\in M_{i,j}} \frac{\|\tilde{\mathbf{z}}^{(m)}_i - \tilde{\mathbf{c}}^{(m)}_j\|^2_2}{d_m}}
   \]
   - 外除以 \(|M_{i,j}|\)：避免共享模态数量不同导致的距离偏差。
   - 内除以 \(d_m\)（模态维度）：避免高维模态主导距离。

3. **聚类过程（Stage 1）**  
   - 初始化：随机选 \(k\) 个样本作为初始中心（继承其模态）。
   - 迭代：分配步骤（最小化部分模态距离）→ 更新步骤（每个模态独立计算簇内均值，若某模态在簇内无样本则中心缺失该模态）。

4. **迭代贪婪填补（Stage 2）**  
   对每个缺失样本 \( i \)：
   - 构造候选集 \( \mathcal{C}_i \)：用每个聚类中心填补缺失模态（若中心无该模态则仍缺失）。
   - 循环：计算每个候选与所有聚类中心的距离（使用部分模态距离），选出全局最小距离的候选，用该候选中的模态填补原样本；移除该候选，重复直到所有模态完整。
   - 收敛保证：所有中心联合覆盖所有模态，因此算法必然终止。

5. **下游任务**  
   - 填补后的完整嵌入送入融合模块（多头部自注意力）和预测头。
   - 所有编码器冻结，只训练融合和头部参数。

## 3. 实验设计

### 数据集与场景
- **MM-IMDb**：图像（海报）+ 文本（摘要），多标签分类（27种类型）。缺失配置：独立百分比（如100%图像-20%文本）和复杂混合（如γ%两者均存在，α%仅图像，β%仅文本）。
- **CMU-MOSI**：文本+音频+视频，情感回归（-3~+3）。类似配置：独立百分比和复杂混合（7种组合）。
- **Sleep-EDF**：5种信号模态（EEG等），5类睡眠阶段。使用两个参数 \( p_s \)（样本缺失比例）和 \( p_m \)（模态缺失比例）模拟高缺失。

### 对比方法
- 通用基线：ShaSpec、M3Care、ICL-CA、SimMLM。
- 任务特定SOTA：MissModal（专为CMU-MOSI设计）。
- 所有实验进行10次独立运行，报告均值。

## 4. 资源与算力

- **论文未明确说明GPU型号、数量、训练时长**。仅在实现细节中提到使用了预训练编码器（BERT、WavLM、CLIP）及轻量级融合模块，未给出整体计算开销。需读者自行推断，通常此类框架在单个GPU上可完成。

## 5. 实验数量与充分性

### 实验数量
- 在3个数据集上，每个数据集分别测试了多种缺失配置（MM-IMDb 6种，CMU-MOSI 8种，Sleep-EDF 8种）。
- 在CMU-MOSI上还与任务特定方法MissModal进行了三指标对比（MSE、F1-Micro、F1-Macro）。
- 对聚类数 \( k \) 进行了灵敏度分析（10~100共10个值），并报告了稳定性和方差。
- 提供t-SNE可视化定性评估填补质量。

### 充分性与客观性
- **充分性**：覆盖了不同模态数（2、3、5）、不同缺失率（轻度到>50%）和不同任务类型（分类、回归），且每种配置10次运行，结果统计显著（t检验、Wilcoxon秩和检验）。
- **客观公平**：与通用基线和任务特定SOTA均对比，且所有方法均在同一缺失配置下评估。MissModal对比中，明确了统计显著的优势情况。
- **微小不足**：没有进行完整的消融实验（如去掉归一化、距离度量、冻结编码器等），但论文通过灵敏度分析和可视化间接验证了关键设计的有效性。

## 6. 主要结论与发现

- UL4M4在所有缺失配置下显著优于所有基线（p<0.05），尤其是在高度缺失场景下（>50%缺失）仍能保持F1-Micro >0.7，而基线通常低于0.5。
- 与任务特定SOTA MissModal相比，UL4M4在大多数指标上持平或更优，仅在一个极端音频缺失场景下略有劣势。
- 聚类数 \( k \) 在10~100范围内性能稳定（≤0.02波动），无需精细调参。
- t-SNE显示填补嵌入与真实嵌入高度重合，分布真实。

## 7. 优点

1. **任务无关性**：填补模块完全独立于下游任务，可即插即用至任何融合/预测架构。
2. **支持任意模态数目与任意缺失模式**：无需重新设计架构，可处理极端复杂缺失。
3. **轻量高效**：仅使用冻结编码器和聚类，无需生成模型或对缺失分布建模。
4. **鲁棒性强**：在高缺失率下性能稳定，且对聚类数不敏感。
5. **开箱可用**：代码开源，便于复现和应用。

## 8. 不足与局限

1. **实验覆盖有限**：仅在三个标准数据集上验证，未涉及更多模态（如传感器、医疗影像等）更大规模场景。
2. **未充分评估计算开销**：未报告训练时长、GPU内存消耗，也未与其他方法对比效率。
3. **缺乏严格消融实验**：如移除归一化、距离度量、迭代策略的消融，虽提供了部分间接证据，但不够系统。
4. **编码器依赖**：假设有预训练的冻结编码器，对于无预训练模型的新模态（如EEG）需先训练自编码器，增加了额外步骤。
5. **潜在偏差**：聚类中心可能引入信息偏差，尤其在少量样本缺失时；贪婪填充可能不是全局最优。
6. **未考虑时序或在线场景**：聚类中心固定，无法处理流式数据。

（完）
