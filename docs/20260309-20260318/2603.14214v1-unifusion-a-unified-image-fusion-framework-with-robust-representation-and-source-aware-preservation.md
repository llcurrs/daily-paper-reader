---
title: "UniFusion: A Unified Image Fusion Framework with Robust Representation and Source-Aware Preservation"
title_zh: UniFusion：一种具有鲁棒表示和源感知保留的统一图像融合框架
authors: "Xingyuan Li, Songcheng Du, Yang Zou, HaoYuan Xu, Zhiying Jiang, Jinyuan Liu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14214v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 用于多模态和跨任务泛化的统一图像融合框架
tldr: UniFusion是一个统一的图像融合框架，旨在解决现有方法在多模态、多曝光和多聚焦任务中泛化性差及源信息丢失的问题。该框架利用DINOv3提取模态一致性特征，并引入重构对齐损失以保留源图像信息。通过双层优化策略平衡重构与融合目标，UniFusion在多种任务中展现了卓越的视觉质量和跨任务泛化能力。
selection_source: fresh_fetch
motivation: 现有图像融合方法多针对特定任务设计，且在深层传播中容易丢失源图像的关键信息。
method: 采用DINOv3提取一致性特征，结合重构对齐损失保留源信息，并利用双层优化策略解耦并联合优化重构与融合目标。
result: 在多项融合任务上的实验证明，UniFusion在视觉质量、泛化能力和实际场景适应性方面均优于现有方法。
conclusion: UniFusion通过统一的架构和鲁棒的特征表示，为跨任务图像融合提供了一种高效且信息保真度高的解决方案。
---

## 摘要
图像融合旨在整合来自多个源图像的互补信息，以生成更具信息量且视觉一致的表示，从而造福于人类感知和下游视觉任务。尽管最近取得了进展，但大多数现有的融合方法是针对特定任务（即多模态、多曝光或多聚焦融合）设计的，并且在融合过程中难以有效保留源信息。这种局限性主要源于特定任务的架构以及深层传播导致的源信息退化。为了克服这些问题，我们提出了 UniFusion，这是一个旨在实现跨任务泛化的统一图像融合框架。首先，利用 DINOv3 进行模态一致的特征提取，UniFusion 为多样化的输入建立了共享语义空间。其次，为了保留对每个源图像的理解，我们引入了重构对齐损失，以维持融合输出与输入之间的一致性。最后，我们采用双层优化策略来解耦并联合优化重构和融合目标，有效平衡了它们的耦合关系并确保平滑收敛。在多个融合任务上的广泛实验证明了 UniFusion 卓越的视觉质量、泛化能力以及对现实场景的适应性。代码可在 https://github.com/dusongcheng/UniFusion 获取。

## Abstract
Image fusion aims to integrate complementary information from multiple source images to produce a more informative and visually consistent representation, benefiting both human perception and downstream vision tasks. Despite recent progress, most existing fusion methods are designed for specific tasks (i.e., multi-modal, multi-exposure, or multi-focus fusion) and struggle to effectively preserve source information during the fusion process. This limitation primarily arises from task-specific architectures and the degradation of source information caused by deep-layer propagation. To overcome these issues, we propose UniFusion, a unified image fusion framework designed to achieve cross-task generalization. First, leveraging DINOv3 for modality-consistent feature extraction, UniFusion establishes a shared semantic space for diverse inputs. Second, to preserve the understanding of each source image, we introduce a reconstruction-alignment loss to maintain consistency between fused outputs and inputs. Finally, we employ a bilevel optimization strategy to decouple and jointly optimize reconstruction and fusion objectives, effectively balancing their coupling relationship and ensuring smooth convergence. Extensive experiments across multiple fusion tasks demonstrate UniFusion's superior visual quality, generalization ability, and adaptability to real-world scenarios. Code is available at https://github.com/dusongcheng/UniFusion.

---

## 论文详细总结（自动生成）

以下是对论文《UniFusion: A Unified Image Fusion Framework with Robust Representation and Source-Aware Preservation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的图像融合方法大多针对特定任务（如红外-可见光、多曝光、多聚焦）设计，缺乏跨任务的泛化能力。此外，由于深度神经网络的层级传播，源图像的关键信息（如纹理、语义细节）容易在融合过程中丢失或退化。
*   **研究背景**：图像融合旨在将多源图像的互补信息整合到单一图像中。目前的挑战在于如何构建一个通用的表示空间，既能处理异构信号，又能确保融合结果对源信息的“忠实度”。

### 2. 方法论
UniFusion 采用了一种结合大模型先验与双层优化的统一框架，核心组件包括：
*   **模态一致性特征提取**：利用预训练的自监督大模型 **DINOv3** 作为冻结的骨干网络（Backbone），提取具有目标中心先验和长程上下文依赖的语义特征。
*   **分层适配器（Adapter）**：引入轻量级适配器对 DINOv3 的多层特征进行调制，以对齐不同模态的分布差异，将通用语义转化为模态相关的嵌入表示。
*   **重构对齐机制（Reconstruction Alignment）**：为每个分支设计重构解码器，强制要求中间特征能够还原回原始输入图像。这一约束确保了特征空间保留了充足的源图像细节，防止语义漂移。
*   **双层优化策略（Bilevel Optimization）**：
    *   **内层（Lower-level）**：优化重构分支，学习模态一致且信息丰富的表示。
    *   **外层（Upper-level）**：在内层特征的基础上优化融合分支，实现互补信息的动态集成。
    *   通过交替迭代更新，解决了重构保真度与融合质量之间的耦合平衡问题。

### 3. 实验设计
*   **实验任务与数据集**：
    *   **红外-可见光融合 (IVIF)**：M3FD、TNO、RoadScene 数据集。
    *   **医学图像融合 (MIF)**：Medical Harvard 数据集。
    *   **多曝光融合 (MEF)**：MEFB 标杆数据集。
    *   **多聚焦融合 (MFF)**：MFIF（包含 Lytro, MFFW, MFI-WHU 子集）。
*   **对比方法（Benchmarks）**：对比了 10 种 SOTA 方法，包括 CDDFuse、SwinFusion、U2Fusion、TC-MoA（最新的多任务适配器方法）等。
*   **评价指标**：互信息 (MI)、视觉信息保真度 (VIF)、$Q_{abf}$、CC、PSNR 等。

### 4. 资源与算力
*   **硬件环境**：使用了一块 **NVIDIA RTX 5090 GPU** 进行实验。
*   **训练细节**：每个融合任务训练 **10,000 次迭代**，Batch Size 为 16。
*   **优化器**：Adam 优化器，初始学习率 $2 \times 10^{-4}$，采用指数衰减。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 类主流融合任务、超过 7 个公开数据集上进行了测试，涵盖了从自然场景到医学影像的广泛领域。
*   **消融实验**：设计了 4 组消融实验（无适配器、无 DINOv3、无重构分支、无双层优化），系统验证了每个模块的必要性。
*   **充分性评价**：实验设计非常充分且客观。不仅提供了量化指标，还展示了大量的定性视觉对比图，并进一步在下游任务（目标检测、语义分割）上验证了融合效果，证明了其在实际应用中的价值。

### 6. 主要结论与发现
*   **跨任务通用性**：UniFusion 在所有测试任务中均达到了 SOTA 水平，证明了单一框架处理多种融合需求的可行性。
*   **信息保真度**：重构对齐机制显著提升了融合图像的纹理清晰度和语义一致性，解决了深层网络信息丢失的顽疾。
*   **大模型优势**：DINOv3 提供的语义先验比传统的 CNN 或从零训练的 Transformer 具有更强的鲁棒性和泛化能力。

### 7. 优点与亮点
*   **架构统一**：无需针对特定任务修改网络结构，实现了真正的“全能型”融合。
*   **理论严谨**：引入双层优化（Bilevel Optimization）来处理重构与融合的竞争关系，比简单的多损失函数加权更科学。
*   **视觉质量高**：实验结果显示，该方法在保持对比度的同时，对边缘和细微纹理的保留效果极佳。

### 8. 不足与局限
*   **计算开销**：虽然推理时适配器很轻量，但依赖 DINOv3 这种大型 ViT 模型作为骨干网络，可能会增加部署时的显存占用和计算延迟。
*   **实时性未详述**：论文未明确给出在 RTX 5090 以外的低功耗设备上的推理速度数据。
*   **数据依赖**：虽然是统一框架，但仍需针对不同任务进行一定程度的训练/微调，尚未完全实现零样本（Zero-shot）的跨任务直接迁移。

（完）
