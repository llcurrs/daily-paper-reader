---
title: "AURORA-KITTI: Any-Weather Depth Completion and Denoising in the Wild"
title_zh: AURORA-KITTI：野外全天候深度补全与去噪
authors: "Yiting Wang, Tim Brödermann, Hamed Haghighi, Haonan Zhao, Christos Sakaridis, Kurt Debattista, Valentina Donzella"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14701v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 恶劣天气和稀疏输入损坏下的鲁棒RGB-LiDAR融合
tldr: 针对现有RGB-LiDAR融合方法在恶劣天气下性能下降的问题，本文提出了AURORA-KITTI，这是首个大规模多模态、多天气的深度补全与去噪（DCD）基准。研究将深度补全与去噪统一，并引入了基于蒸馏的高效基线模型DDCD，利用深度基础模型注入结构先验。实验证明，物理一致的天气数据对提升鲁棒性至关重要，DDCD在多个数据集上达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的深度补全方法在恶劣天气下因图像和LiDAR数据受损而表现不佳，且缺乏大规模的多天气基准。
method: 提出了统一的深度补全与去噪（DCD）任务，并开发了利用深度基础模型进行知识蒸馏的DDCD基线模型。
result: DDCD在AURORA-KITTI和真实世界DENSE数据集上均取得了最先进的性能，且保持了较高的运行效率。
conclusion: 物理一致的天气感知数据比单纯的架构改进更能有效提升模型在复杂环境下的鲁棒性。
---

## 摘要
鲁棒的深度补全对于现实世界的3D场景理解至关重要，然而现有的RGB-LiDAR融合方法在恶劣天气下性能显著下降，因为摄像机图像和LiDAR测量值都会受到天气引起的损坏。在本文中，我们介绍了AURORA-KITTI，这是首个用于野外鲁棒深度补全的大规模多模态、多天气基准。我们进一步将深度补全与去噪（DCD）公式化为一个统一的任务，旨在从受损的稀疏输入中联合重建稠密深度图，同时抑制天气引起的噪声。AURORA-KITTI包含超过8.2万对具有天气一致性的RGBL对，并附带度量深度真值，涵盖了多种天气类型、三个严重程度等级、昼夜场景、成对的清晰参考、镜头遮挡情况以及文本描述。此外，我们引入了DDCD，这是一种基于蒸馏的高效基准模型，它利用深度基础模型将清晰的结构先验注入到野外DCD训练中。DDCD在AURORA-KITTI和真实世界的DENSE数据集上均达到了最先进的性能，同时保持了高效性。值得注意的是，我们的结果进一步表明，感知天气的、物理一致的数据比单纯的架构修改对鲁棒性的贡献更大。数据和代码将在发表后发布。

## Abstract
Robust depth completion is fundamental to real-world 3D scene understanding, yet existing RGB-LiDAR fusion methods degrade significantly under adverse weather, where both camera images and LiDAR measurements suffer from weather-induced corruption. In this paper, we introduce AURORA-KITTI, the first large-scale multi-modal, multi-weather benchmark for robust depth completion in the wild. We further formulate Depth Completion and Denoising (DCD) as a unified task that jointly reconstructs a dense depth map from corrupted sparse inputs while suppressing weather-induced noise. AURORA-KITTI contains over \textit{82K} weather-consistent RGBL pairs with metric depth ground truth, spanning diverse weather types, three severity levels, day and night scenes, paired clean references, lens occlusion conditions, and textual descriptions. Moreover, we introduce DDCD, an efficient distillation-based baseline that leverages depth foundation models to inject clean structural priors into in-the-wild DCD training. DDCD achieves state-of-the-art performance on AURORA-KITTI and the real-world DENSE dataset while maintaining efficiency. Notably, our results further show that weather-aware, physically consistent data contributes more to robustness than architectural modifications alone. Data and code will be released upon publication.

---

## 论文详细总结（自动生成）

### 论文总结：AURORA-KITTI —— 野外全天候深度补全与去噪

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶和机器人导航中，精确的度量深度感知至关重要。现有的 RGB-LiDAR 融合深度补全（Depth Completion, DC）方法大多基于晴朗天气的假设。然而，在现实世界的恶劣天气（如雨、雪、雾、夜间）下，摄像头图像会因散射和衰减失去对比度，LiDAR 则会产生伪回波或点云缺失。
*   **核心挑战**：缺乏大规模、物理一致且具有高质量深度真值的多天气 RGB-LiDAR 数据集。
*   **研究动机**：现有的深度补全任务忽略了传感器噪声的抑制。本文旨在将“深度补全”与“去噪”统一为 **DCD（Depth Completion and Denoising）** 任务，并提供一个大规模基准平台。

#### 2. 论文提出的方法论
论文提出了两个核心贡献：**AURORA-KITTI 数据集**和 **DDCD 基线模型**。

*   **AURORA-KITTI 生成流程**：
    *   **物理一致性退化**：基于物理模型（如 Koschmieder 定律、粒子渲染引擎）对 RGB 图像和 3D LiDAR 点云同步施加退化。
    *   **多层级模拟**：涵盖 8 种复合条件（昼/夜 × 晴/雾/雨/雪）、3 种严重程度，并加入了镜头遮挡（水滴、雪花）和文本描述。
*   **DDCD (Distillation-based DCD) 模型**：
    *   **核心思想**：利用预训练的“深度基础模型”（Teacher）在清晰图像上生成的稠密几何先验，指导受损输入下的模型（Student）学习。
    *   **关键技术**：
        1.  **教师先验归一化**：通过最小二乘法拟合，将教师模型的相对深度转换为与真值对齐的度量深度。
        2.  **尺度与偏移不变（SSI）蒸馏损失**：允许学生模型在保持全局几何结构的同时，不受全局尺度偏差的影响。
        3.  **残差梯度正则化**：抑制由天气噪声引起的局部高频伪影，保持边缘锐度。

#### 3. 实验设计
*   **数据集**：
    *   **AURORA-KITTI**：82,177 对样本，用于训练和主要评估。
    *   **DENSE (Pixel-Accurate Depth)**：真实世界恶劣天气数据集，用于验证模型的跨域泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 7 种代表性方法：包括经典算法（IP-Basic）、监督学习（Sparse2Dense, BP-Net, DMD3C）、单目基础模型（Depth Anything V2/V3, UniK3D）以及扩散模型（Marigold-DC）。
*   **评估指标**：RMSE、MAE、iRMSE、iMAE。

#### 4. 资源与算力
*   **硬件环境**：所有重训练实验均在 **NVIDIA RTX 4090 GPU** 上进行。
*   **细节说明**：文中未明确提及具体的 GPU 数量、总训练时长或显存占用情况，但强调了 DDCD 模型在推理时具有实时性（约 48.6 ms/帧）。

#### 5. 实验数量与充分性
*   **实验规模**：使用了超过 8 万组样本进行训练，并在 1600 组测试集上进行了详尽评估。
*   **消融实验**：针对不同的基础模型（DA-V2 vs DA-V3）、有无蒸馏、有无合成数据重训练等变量进行了对比。
*   **充分性评价**：实验设计较为充分。通过在合成数据集（AURORA-KITTI）上训练并在真实数据集（DENSE）上测试，验证了实验的客观性和实用价值。对比方法涵盖了从传统到最前沿的多种范式，确保了公平性。

#### 6. 主要结论与发现
*   **数据驱动的鲁棒性**：实验发现，**物理一致的天气感知训练数据**对提升模型鲁棒性的贡献远大于单纯的架构修改。
*   **基础模型的局限**：虽然深度基础模型（如 DA-V3）在晴天表现优异，但在极端天气下仍会出现尺度不稳定和结构扭曲。
*   **DDCD 的优越性**：DDCD 在精度上显著超过了单目基础模型和传统的融合模型，且在效率上远优于基于扩散模型的方法（如 Marigold-DC 需 18 秒，而 DDCD 仅需 48 毫秒）。

#### 7. 优点
*   **任务定义新颖**：首次明确提出了 DCD 统一任务，强调了去噪在深度补全中的重要性。
*   **数据集质量高**：AURORA-KITTI 填补了大规模、多模态、物理一致天气数据集的空白，且包含镜头遮挡等长尾场景。
*   **蒸馏策略有效**：通过 Teacher-Student 架构成功将基础模型的知识迁移到恶劣天气场景，解决了真值获取难的问题。

#### 8. 不足与局限
*   **合成与真实的鸿沟**：尽管进行了真实性验证，但合成的天气效果（如 CycleGAN 生成的夜间效果）与极端复杂的现实物理环境仍可能存在偏差。
*   **教师模型依赖**：DDCD 的性能上限在一定程度上受限于教师模型在清晰图像上的表现，若教师模型在某些特定场景（如极远距离）存在系统性偏差，可能会传递给学生模型。
*   **算力细节缺失**：缺乏具体的训练资源消耗统计，不利于其他研究者评估复现成本。

（完）
