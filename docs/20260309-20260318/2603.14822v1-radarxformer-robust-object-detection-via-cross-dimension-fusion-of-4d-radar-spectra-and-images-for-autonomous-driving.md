---
title: "RadarXFormer: Robust Object Detection via Cross-Dimension Fusion of 4D Radar Spectra and Images for Autonomous Driving"
title_zh: RadarXFormer：基于 4D 雷达谱与图像跨维度融合的自动驾驶鲁棒目标检测
authors: "Yue Sun, Yeqiang Qian, Zhe Wang, Tianhui Li, Chunxiang Wang, Ming Yang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14822v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 4D雷达与图像的跨维度融合以实现鲁棒感知
tldr: 针对自动驾驶在恶劣天气下感知受限的问题，本文提出RadarXFormer框架。该方法通过融合4D毫米波雷达原始频谱与RGB图像，克服了传统雷达点云稀疏及4D雷达数据量大的挑战。其核心是跨维度（3D-2D）融合机制，将3D球形雷达特征立方体与2D图像特征结合，在K-Radar数据集上实现了高精度、强鲁棒性且实时的3D目标检测。
selection_source: fresh_fetch
motivation: 传统摄像头和激光雷达在恶劣天气下表现不佳，而现有雷达融合方案受限于点云稀疏性或4D雷达数据处理效率。
method: 提出一种跨维度融合机制，直接利用原始4D雷达频谱构建高效3D表示，并将其与多尺度2D图像特征图进行融合。
result: 在K-Radar数据集上的实验表明，该方法在复杂环境下显著提升了检测精度和鲁棒性，同时保持了实时推理能力。
conclusion: RadarXFormer证明了通过深度融合原始雷达频谱与视觉信息，可以有效提升自动驾驶系统在全天候条件下的感知可靠性。
---

## 摘要
可靠的感知对于自动驾驶系统在各种现实交通条件下安全运行至关重要。然而，基于摄像头和激光雷达（LiDAR）的感知系统在恶劣天气和光照条件下会出现性能下降，限制了其在智能交通系统中的鲁棒性和大规模部署。雷达与视觉融合通过结合毫米波（mmWave）雷达的环境鲁棒性、成本效益以及摄像头捕获的丰富语义信息，提供了一种极具前景的替代方案。然而，传统的 3D 雷达测量缺乏高度分辨率且高度稀疏，而新兴的 4D 毫米波雷达虽然引入了高度信息，但也带来了信号噪声和海量数据等挑战。为了解决这些问题，本文提出了 RadarXFormer，这是一个能够实现 4D 雷达谱与 RGB 图像之间高效跨模态融合的 3D 目标检测框架。RadarXFormer 不依赖于稀疏的雷达点云，而是直接利用原始雷达谱并构建了一种高效的 3D 表示，在减少数据量的同时保留了完整的 3D 空间信息。“X” 突出了所提出的跨维度（3D-2D）融合机制，其中多尺度 3D 球面雷达特征立方体与互补的 2D 图像特征图进行融合。在 K-Radar 数据集上的实验表明，该方法在挑战性条件下提高了检测精度和鲁棒性，同时保持了实时推理能力。

## Abstract
Reliable perception is essential for autonomous driving systems to operate safely under diverse real-world traffic conditions. However, camera- and LiDAR-based perception systems suffer from performance degradation under adverse weather and lighting conditions, limiting their robustness and large-scale deployment in intelligent transportation systems. Radar-vision fusion provides a promising alternative by combining the environmental robustness and cost efficiency of millimeter-wave (mmWave) radar with the rich semantic information captured by cameras. Nevertheless, conventional 3D radar measurements lack height resolution and remain highly sparse, while emerging 4D mmWave radar introduces elevation information but also brings challenges such as signal noise and large data volume. To address these issues, this paper proposes RadarXFormer, a 3D object detection framework that enables efficient cross-modal fusion between 4D radar spectra and RGB images. Instead of relying on sparse radar point clouds, RadarXFormer directly leverages raw radar spectra and constructs an efficient 3D representation that reduces data volume while preserving complete 3D spatial information. The "X" highlights the proposed cross-dimension (3D-2D) fusion mechanism, in which multi-scale 3D spherical radar feature cubes are fused with complementary 2D image feature maps. Experiments on the K-Radar dataset demonstrate improved detection accuracy and robustness under challenging conditions while maintaining real-time inference capability.

---

## 论文详细总结（自动生成）

以下是对论文《RadarXFormer: Robust Object Detection via Cross-Dimension Fusion of 4D Radar Spectra and Images for Autonomous Driving》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：自动驾驶系统在恶劣天气（雾、雪、雨）和极端光照（夜晚、强光）下，传统的摄像头和激光雷达（LiDAR）感知能力会大幅下降。
*   **研究动机**：
    *   **传统雷达局限**：3D毫米波雷达缺乏高度分辨率且数据稀疏。
    *   **4D雷达挑战**：虽然引入了高度信息，但原始数据量巨大（每帧约300MB），且传统的点云提取过程（如CFAR过滤）会导致严重的信息损失。
    *   **融合难题**：3D雷达特征与2D图像特征之间存在维度不匹配，现有的融合方法往往通过压缩雷达维度来适配，损失了空间细节。

### 2. 方法论
RadarXFormer 提出了一种直接利用 **4D雷达原始频谱（Spectra）** 与图像进行 **跨维度（3D-2D）融合** 的框架。
*   **高效3D雷达表示**：
    *   为了降低计算量同时保留空间信息，将4D雷达张量（多普勒、距离、俯仰、方位）沿多普勒维度进行统计压缩。
    *   提取多普勒维度的均值、标准差以及最大功率对应的多普勒值，合成一个3D特征立方体。
    *   **关键细节**：保留球坐标系表示，避免了转换为笛卡尔坐标系时产生的长程稀疏和插值误差。
*   **跨维度融合机制（X-Dimension）**：
    *   **特征提取**：使用 ResNet3D+FPN3D 提取多尺度雷达特征，使用 ResNet+FPN 提取多尺度图像特征。
    *   **Transformer 融合**：设计了一组 3D 对象查询（Queries）。通过**多尺度可变形注意力机制（MSDA）**，将查询点分别投影到 3D 雷达特征立方体和 2D 图像特征图上进行采样。
    *   **迭代细化**：通过 $n$ 次迭代不断更新查询特征和预测的目标中心，实现精确的 3D 边界框估计。

### 3. 实验设计
*   **数据集**：使用 **K-Radar** 数据集，这是目前唯一提供原始 4D 雷达频谱且包含多种恶劣天气（雨、雪、雾、冰雹）的公开数据集。
*   **Benchmark**：遵循 KITTI 评估协议，主要指标为 3D mAP 和 BEV mAP。
*   **对比方法**：
    *   **雷达单模态**：RTNH。
    *   **激光雷达/融合**：Voxel-RCNN、CasA、TED、VPFNet、MixedFusion 等。
    *   **雷达-视觉融合（SOTA）**：EchoFusion、DPFT。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：采用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，训练周期为 200 epochs。
*   **推理性能**：论文提到该模型保持了实时推理能力（Real-time inference capability），但未给出具体的 FPS 数值。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 种不同天气条件下进行了全面的性能测试。
    *   进行了详细的消融实验，包括：噪声过滤的影响、不同模态组合（单雷达、单视觉、融合）的效果、以及迭代次数对精度的影响。
*   **充分性评价**：实验设计较为充分，特别是在极端天气下的对比展示了该方法的鲁棒性。对比了多种传感器组合（L、C+L、R+L、R+C），确保了评估的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：RadarXFormer 在 K-Radar 测试集上达到了 **57.6% 的总 mAP**（IoU=0.3），超过了现有的所有雷达-视觉融合方法（如 DPFT 提升了 1.5%）。
*   **恶劣天气优势**：在浓雾和重雪条件下，RadarXFormer 的表现显著优于基于 LiDAR 的方法，证明了 4D 雷达频谱在极端环境下的可靠性。
*   **频谱优于点云**：直接利用雷达频谱而非稀疏点云，能提供更精确的物体边界和位置估计，尤其是在 Z 轴（高度）方向的误差显著降低。

### 7. 优点
*   **信息保留**：通过直接处理原始频谱，规避了 CFAR 过滤带来的弱信号丢失问题。
*   **坐标系优化**：坚持在球坐标系下处理雷达数据，有效解决了远距离数据稀疏和坐标转换失真的问题。
*   **跨维度设计**：Transformer 架构巧妙地解决了 3D 雷达与 2D 图像的对齐问题，无需显式的中间特征空间转换。

### 8. 不足与局限
*   **数据分布偏差**：在处理与自车行驶方向垂直的目标（横向车辆）时存在漏检，这主要是由于训练数据中此类样本较少导致的类别不平衡。
*   **传感器标定依赖**：尽管 Transformer 具有一定的容错性，但 K-Radar 数据集本身存在的传感器内外参微小失配仍会限制融合性能。
*   **去噪策略**：虽然提出了初步的去噪方法，但在抑制环境杂波和保留微弱目标信号之间仍存在平衡难题。

（完）
