---
title: "R4Det: 4D Radar-Camera Fusion for High-Performance 3D Object Detection"
title_zh: R4Det：用于高性能3D目标检测的4D雷达-相机融合
authors: "Zhongyu Xia, Yousen Tang, Yongtao Wang, Zhifeng Wang, Weijun Qin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11566v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 当雷达失效时检测依赖于视觉单模态先验
tldr: 针对自动驾驶中4D毫米波雷达与摄像头融合的3D目标检测挑战，本文提出R4Det框架。该框架通过全景深度融合模块增强深度估计，利用变形门控时间融合模块解决对车身位姿的依赖问题，并引入实例引导动态细化模块处理雷达点云稀疏导致的检测难题。实验证明，R4Det在TJ4DRadSet和VoD数据集上达到了SOTA性能，显著提升了检测精度和鲁棒性。
selection_source: fresh_fetch
motivation: 现有融合方法在深度估计精度、时间融合对位姿的依赖性以及处理雷达点云稀疏的小目标方面存在明显不足。
method: 提出R4Det框架，包含全景深度融合模块、无需位姿信息的变形门控时间融合模块以及基于2D实例引导的动态细化模块。
result: 实验结果显示，R4Det在TJ4DRadSet和VoD数据集上均实现了最先进的3D目标检测性能。
conclusion: R4Det通过改进深度估计和时间融合机制，有效提升了4D雷达与相机融合感知在复杂场景下的准确性与鲁棒性。
---

## 摘要
4D雷达-相机感知配置在自动驾驶中变得越来越重要。然而，现有的融合4D雷达和相机数据的3D目标检测方法面临着若干挑战。首先，它们的绝对深度估计模块不够鲁棒和准确，导致3D定位不精确。其次，当自车位姿缺失或不准确时，其时序融合模块的性能会大幅下降甚至失效。第三，对于一些小目标，稀疏的雷达点云可能完全无法从其表面反射。在这种情况下，检测必须完全依赖视觉单模态先验。为了解决这些局限性，我们提出了R4Det，它通过全景深度融合模块提高深度估计质量，实现绝对深度和相对深度之间的相互增强。对于时序融合，我们设计了一个不依赖自车位姿的可变形门控时序融合模块。此外，我们构建了一个实例引导动态细化模块，从2D实例引导中提取语义原型。实验表明，R4Det在TJ4DRadSet和VoD数据集上实现了最先进的3D目标检测结果。

## Abstract
4D radar-camera sensing configuration has gained increasing importance in autonomous driving. However, existing 3D object detection methods that fuse 4D Radar and camera data confront several challenges. First, their absolute depth estimation module is not robust and accurate enough, leading to inaccurate 3D localization. Second, the performance of their temporal fusion module will degrade dramatically or even fail when the ego vehicle's pose is missing or inaccurate. Third, for some small objects, the sparse radar point clouds may completely fail to reflect from their surfaces. In such cases, detection must rely solely on visual unimodal priors. To address these limitations, we propose R4Det, which enhances depth estimation quality via the Panoramic Depth Fusion module, enabling mutual reinforcement between absolute and relative depth. For temporal fusion, we design a Deformable Gated Temporal Fusion module that does not rely on the ego vehicle's pose. In addition, we built an Instance-Guided Dynamic Refinement module that extracts semantic prototypes from 2D instance guidance. Experiments show that R4Det achieves state-of-the-art 3D object detection results on the TJ4DRadSet and VoD datasets.

---

## 论文详细总结（自动生成）

### R4Det: 用于高性能3D目标检测的4D雷达-相机融合论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对自动驾驶中**4D毫米波雷达与相机融合**的3D目标检测任务，指出了当前主流方法面临的三个核心挑战：
*   **深度估计不准**：现有的融合框架对绝对深度的估计不够鲁棒，导致3D定位精度受限，且深度监督往往过于稀疏。
*   **时序融合依赖位姿**：大多数时序融合方法高度依赖精确的自车位姿（Ego-vehicle pose），在GPS信号丢失或位姿数据缺失的场景下（如TJ4DRadSet数据集）性能会剧烈下降。
*   **小目标检测困难**：雷达点云极其稀疏，对于远距离小目标（如骑行者）往往没有反射点，必须依赖视觉先验，但现有方法难以有效整合这种先验。

#### 2. 方法论：核心思想与关键技术
R4Det 提出了一个渐进式净化的 BEV 架构，包含三个关键模块：
*   **全景深度融合模块 (PDF)**：
    *   **核心思想**：结合绝对深度和相对深度监督，增强几何感知。
    *   **技术细节**：引入三重监督：1) **概率监督**（利用KL散度对齐深度分布）；2) **基础模型引导**（结合雷达深度与Metric3D生成的稠密伪标签）；3) **结构化排名监督**（采用带动态容差的成对序数排名损失，并利用边缘采样策略强化物体轮廓）。
*   **变形门控时序融合模块 (DGTF)**：
    *   **核心思想**：在不依赖自车位姿的情况下实现时序对齐。
    *   **技术细节**：将任务解耦为**空间对齐**（利用可变形卷积 DCNv2 预测偏移量以补偿运动）和**时序更新**（利用类 GRU 的门控机制自适应融合历史特征与当前观测）。
*   **实例引导动态细化模块 (IGDR)**：
    *   **核心思想**：利用 2D 实例语义原型校准 BEV 特征。
    *   **技术细节**：从 2D 检测分支提取实例原型向量，通过仿射变换参数（缩放 $\gamma$ 和偏移 $\beta$）动态校准 BEV 特征流，并使用前背景门控（Foreground Gate）防止背景噪声干扰。

#### 3. 实验设计
*   **数据集**：
    *   **TJ4DRadSet**：包含复杂路口、高架桥及夜间场景的 4D 雷达数据集。
    *   **View-of-Delft (VoD)**：复杂的城市交通场景数据集。
*   **Benchmark 与对比方法**：
    *   对比了纯雷达方法（PointPillars, CenterPoint, SMURF 等）和雷达-相机融合方法（BEVFusion, LXL, SGDet3D, CVFusion, RCBEVDet 等）。
    *   评价指标包括 $AP_{3D}$、$mAP_{3D}$、$AP_{BEV}$ 以及 VoD 特有的 $AP_{EAA}$ 和 $AP_{DC}$。

#### 4. 资源与算力
*   **硬件**：推理速度测试在单张 **NVIDIA RTX 3090 GPU** 上完成。
*   **性能**：R4Det 的推理速度约为 **8.3 到 9.2 FPS**（取决于模型配置）。
*   **训练策略**：采用两阶段训练法。第一阶段（15 epoch）预训练空间感知模块；第二阶段（15 epoch）进行端到端的时序与细化模块微调。使用 AdamW 优化器，初始学习率为 $4 \times 10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的实验，包括两个主流数据集的性能对比、针对三个核心模块的逐项消融实验（Ablation Study）。
*   **充分性与客观性**：
    *   **消融实验**：对 PDF 的损失权重、DGTF 的组件（Concat vs DCN vs GRU）、IGDR 的生成器类型（MLP vs Attention vs Conv）都做了对比。
    *   **即插即用验证**：将提出的模块集成到 BEVFusion 和 RCBEVDet 中，均获得了 5% 以上的提升，证明了方法的通用性。
    *   **可视化**：提供了深度估计对比图和 DGTF 学习到的运动偏移向量图，增强了结果的可解释性。

#### 6. 主要结论与发现
*   **性能领先**：R4Det 在 TJ4DRadSet 上将 $mAP_{3D}$ 提升至 47.29%，在 VoD 上达到 66.69% 的 $mAP_{EAA}$，均刷新了 SOTA 记录。
*   **小目标突破**：在 Cyclist（骑行者）类别上提升尤为显著（TJ4DRadSet 上提升了 7.91%），证明了 IGDR 模块对视觉先验利用的有效性。
*   **位姿无关性**：证明了即使没有自车位姿信息，通过 DCN 和门控机制也能实现高质量的时序特征对齐。

#### 7. 优点（亮点）
*   **模块化设计**：三个模块均为即插即用（Plug-and-play），易于集成到其他 BEV 框架中。
*   **几何与语义双重增强**：PDF 解决了“几何污染”问题，而 IGDR 解决了“语义模糊”问题，双管齐下。
*   **鲁棒性强**：不依赖自车位姿的设计使其在传感器失效或低端硬件平台上更具实用价值。

#### 8. 不足与局限
*   **计算开销**：虽然达到了实时性要求（约 9 FPS），但相比于一些轻量级纯雷达模型，其推理延迟仍较高，在超高性能要求的嵌入式平台上可能受限。
*   **伪标签依赖**：PDF 模块依赖于 Metric3D 生成的深度伪标签进行训练，这意味着模型的上限可能受限于预训练深度估计模型的质量。
*   **长时序限制**：实验显示融合过长的历史帧（如 $t-3$）会导致噪声积累，说明模型在处理超长时序依赖方面仍有改进空间。

（完）
