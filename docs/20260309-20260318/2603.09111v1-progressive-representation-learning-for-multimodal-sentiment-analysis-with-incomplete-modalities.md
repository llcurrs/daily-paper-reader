---
title: Progressive Representation Learning for Multimodal Sentiment Analysis with Incomplete Modalities
title_zh: 针对模态不完整的多模态情感分析的渐进式表示学习
authors: "Jindi Bao, Jianjun Qian, Mengkai Yan, Jian Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09111v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 针对不完整模态的多模态情感分析及渐进式表示学习
tldr: 针对多模态情感分析中常见的模态缺失问题，本文提出了一种渐进式表示学习框架（PRLF）。该框架通过自适应模态可靠性评估器（AMRE）动态确定主导模态，并利用渐进式交互模块（ProgInteract）将其他模态与主导模态对齐，从而抑制噪声并增强一致性。实验证明，PRLF在处理模态缺失和噪声干扰方面具有显著优势，在多个主流数据集上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 旨在解决现实应用中因硬件故障或隐私限制导致的模态缺失，以及由此引发的特征对齐失真和表示学习困难。
method: 提出PRLF框架，通过AMRE动态量化模态可靠性以确定主导模态，并利用ProgInteract模块进行迭代式的跨模态对齐。
result: 在CMU-MOSI、CMU-MOSEI和SIMS数据集上的广泛实验表明，该方法在模态间和模态内缺失场景下均优于现有最先进方法。
conclusion: PRLF通过动态可靠性评估和渐进式对齐机制，有效提升了多模态情感分析在不确定缺失条件下的鲁棒性和泛化能力。
---

## 摘要
多模态情感分析（MSA）旨在通过整合文本、音频和视觉线索来推断人类情感。然而，现有方法通常依赖于所有模态的完整性，而现实应用中经常会遇到噪声、硬件故障或隐私限制，从而导致模态缺失。不完整模态与完整模态之间存在显著的特征对齐偏差，直接融合它们甚至可能扭曲完整模态已学习良好的表示。为此，我们提出了 PRLF，一种专为不确定模态缺失条件下的 MSA 设计的渐进式表示学习框架。PRLF 引入了自适应模态可靠性估计器（AMRE），该估计器利用识别置信度和费舍尔信息（Fisher information）动态量化每个模态的可靠性，以确定主导模态。此外，渐进式交互（ProgInteract）模块迭代地将其他模态与主导模态对齐，从而在抑制噪声的同时增强跨模态一致性。在 CMU-MOSI、CMU-MOSEI 和 SIMS 上的广泛实验证明，PRLF 在模态间和模态内缺失场景下均优于现有先进方法，展示了其鲁棒性和泛化能力。

## Abstract
Multimodal Sentiment Analysis (MSA) seeks to infer human emotions by integrating textual, acoustic, and visual cues. However, existing approaches often rely on all modalities are completeness, whereas real-world applications frequently encounter noise, hardware failures, or privacy restrictions that result in missing modalities. There exists a significant feature misalignment between incomplete and complete modalities, and directly fusing them may even distort the well-learned representations of the intact modalities. To this end, we propose PRLF, a Progressive Representation Learning Framework designed for MSA under uncertain missing-modality conditions. PRLF introduces an Adaptive Modality Reliability Estimator (AMRE), which dynamically quantifies the reliability of each modality using recognition confidence and Fisher information to determine the dominant modality. In addition, the Progressive Interaction (ProgInteract) module iteratively aligns the other modalities with the dominant one, thereby enhancing cross-modal consistency while suppressing noise. Extensive experiments on CMU-MOSI, CMU-MOSEI, and SIMS verify that PRLF outperforms state-of-the-art methods across both inter- and intra-modality missing scenarios, demonstrating its robustness and generalization capability.

---

## 论文详细总结（自动生成）

以下是对论文《Progressive Representation Learning for Multimodal Sentiment Analysis with Incomplete Modalities》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态情感分析（MSA）在现实应用中常面临**模态不完整**（如硬件故障、噪声干扰或隐私限制导致的模态缺失）的问题。
*   **研究动机**：
    *   现有方法（生成式或蒸馏式）往往忽略了缺失模态与完整模态之间的**特征对齐偏差（Phase Shift）**。
    *   直接融合不完整模态可能会扭曲已学习良好的完整模态表示。
    *   不同模态在不同缺失情况下的贡献度（可靠性）是动态变化的，需要一种能够自适应识别“主导模态”并引导其他模态对齐的机制。

### 2. 方法论：PRLF 框架
论文提出了**渐进式表示学习框架（PRLF）**，包含两个核心组件：
*   **自适应模态可靠性估计器（AMRE）**：
    *   **置信度评估（CMI）**：通过各单模态分类头的预测准确率和置信度来初步衡量模态重要性。
    *   **费舍尔信息评估（FIMI）**：利用费舍尔信息矩阵（FIM）的迹来量化模型参数对输入数据的敏感度。当关键信息缺失时，FIM 的迹会显著下降，从而更客观地反映模态的有效信息量。
    *   **动态融合**：在训练初期依赖置信度，随着训练推进，动态增加 FIM 的权重，最终确定每条样本的“主导模态”。
*   **渐进式交互模块（ProgInteract）**：
    *   **迭代对齐**：不进行直接融合，而是通过多次迭代逐步优化。初期侧重于提取模态内特征，后期加强跨模态交互。
    *   **分解器（Decomposer）**：将辅助模态特征分解为与主导模态对齐的“投影项”和包含补充信息的“残差项”。
    *   **去噪机制**：对残差项进行去噪处理，确保辅助模态在学习主导模态一致性的同时，保留有益的互补信息并抑制噪声。
*   **损失函数**：结合了任务分类损失（$L_{task}$）、单模态辅助损失（$L_{uni}$）和相位一致性损失（$L_{phase}$）。

### 3. 实验设计
*   **数据集**：使用了三个主流基准数据集：**CMU-MOSI**、**CMU-MOSEI**（英文）和 **SIMS**（中文）。
*   **实验场景**：
    *   **模态间缺失（Inter-modality）**：随机缺失整个文本、音频或视觉模态。
    *   **模态内缺失（Intra-modality）**：以 0.1 到 0.9 的比例随机丢弃序列中的帧特征。
*   **对比方法（Benchmark）**：对比了包括 MISA、Self-MM、MMIM、TETFN 等经典方法，以及专门针对缺失模态的 SOTA 方法如 UMDF、HRLF、CorrKD 和 EMOE。

### 4. 资源与算力
*   **特征提取**：文本使用 GloVe 或 BERT，视觉使用 Facet，音频使用 COVAREP。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了超参数设置（如迭代步数为 4，平衡系数 $\gamma=0.8$ 等），且实验结果是基于 5 个随机种子的平均值。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上进行了全方位的性能对比。
    *   针对模态内缺失，测试了从 0% 到 90% 共 10 组不同的缺失率。
    *   进行了详尽的**消融实验**，验证了 AMRE、ProgInteract 模块以及各项损失函数的必要性。
    *   提供了迭代步数（1-5 步）的影响分析。
*   **充分性评价**：实验设计非常充分，涵盖了从宏观性能到微观模块验证的各个方面。通过 T-SNE 可视化和动态权重变化分析，客观地展示了模型在处理噪声和缺失数据时的有效性。

### 6. 主要结论与发现
*   **性能领先**：PRLF 在所有数据集和缺失场景下均达到或超过了现有最先进水平。
*   **鲁棒性强**：在极端缺失（如 90% 缺失率）情况下，PRLF 的性能下降最慢，显示出极强的残余信息利用能力。
*   **主导模态的重要性**：实验证明，动态识别并利用最可靠的模态来引导其他模态，比盲目融合所有模态更能提升模型的稳定性。
*   **迭代优化的价值**：4 次迭代被证明是性能与复杂度的最佳平衡点。

### 7. 优点
*   **理论支撑扎实**：引入费舍尔信息（FIM）来量化模态可靠性，相比单纯依靠分类置信度更具数学解释力。
*   **渐进式设计**：ProgInteract 模块通过“先内后外”的策略，有效缓解了缺失模态带来的特征扭曲问题。
*   **通用性**：在不同语言（中/英）和不同缺失模式下均表现稳健。

### 8. 不足与局限
*   **计算开销**：由于采用了迭代交互机制（Step=4），其推理延迟和计算复杂度可能高于单次前向传播的融合模型。
*   **超参数敏感性**：模型性能依赖于迭代步数和损失函数权重的精细调优，在不同数据集上可能需要重新搜索最优参数。
*   **硬件细节缺失**：未提供算力消耗数据，难以评估其在移动端或实时系统中的部署可行性。

（完）
