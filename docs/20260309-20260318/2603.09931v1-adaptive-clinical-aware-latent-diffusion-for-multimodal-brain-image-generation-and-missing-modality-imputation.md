---
title: Adaptive Clinical-Aware Latent Diffusion for Multimodal Brain Image Generation and Missing Modality Imputation
title_zh: 用于多模态脑图像生成和缺失模态填补的自适应临床感知潜扩散模型
authors: "Rong Zhou, Houliang Zhou, Yao Su, Brian Y. Chen, Yu Zhang, Lifang He, Alzheimer's Disease Neuroimaging Initiative"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09931v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 用于多模态脑图像生成和缺失模态插补的自适应融合
tldr: 针对阿尔茨海默病诊断中多模态神经影像数据缺失的问题，本文提出ACADiff框架。该框架利用自适应临床感知潜扩散模型，结合GPT-4o编码的临床元数据，在sMRI、FDG-PET和AV45-PET之间进行双向合成。实验证明，ACADiff在极高缺失率下仍能保持优异的生成质量和诊断性能，显著优于现有基准。
selection_source: fresh_fetch
motivation: 临床数据集中多模态神经影像的缺失严重阻碍了阿尔茨海默病的准确诊断。
method: 提出一种基于潜扩散的ACADiff框架，通过自适应融合机制和GPT-4o生成的临床语义引导来合成缺失模态。
result: "在ADNI数据集上，该方法在高达80%的数据缺失情况下依然实现了卓越的图像生成质量和稳健的诊断效果。"
conclusion: ACADiff为多模态脑影像缺失补全提供了高效方案，显著提升了临床诊断的可靠性。
---

## 摘要
多模态神经影像为阿尔茨海默病的诊断提供了互补的见解，但临床数据集经常面临模态缺失的问题。我们提出了 ACADiff，这是一个通过自适应临床感知扩散合成缺失脑影像模态的框架。ACADiff 通过在关注可用影像数据和临床元数据的同时，逐步对潜表示进行去噪，从而学习不完整多模态观测值与目标模态之间的映射。该框架采用自适应融合机制，可根据输入的可用性进行动态重构，并结合了通过 GPT-4o 编码提示提供的语义临床指导。三个专门的生成器实现了 sMRI、FDG-PET 和 AV45-PET 之间的双向合成。在 ADNI 受试者上的评估结果表明，ACADiff 实现了卓越的生成质量，即使在 80% 缺失的极端情况下也能保持稳健的诊断性能，优于所有现有基准模型。为了提高可复现性，代码已发布在 https://github.com/rongzhou7/ACADiff。

## Abstract
Multimodal neuroimaging provides complementary insights for Alzheimer's disease diagnosis, yet clinical datasets frequently suffer from missing modalities. We propose ACADiff, a framework that synthesizes missing brain imaging modalities through adaptive clinical-aware diffusion. ACADiff learns mappings between incomplete multimodal observations and target modalities by progressively denoising latent representations while attending to available imaging data and clinical metadata. The framework employs adaptive fusion that dynamically reconfigures based on input availability, coupled with semantic clinical guidance via GPT-4o-encoded prompts. Three specialized generators enable bidirectional synthesis among sMRI, FDG-PET, and AV45-PET. Evaluated on ADNI subjects, ACADiff achieves superior generation quality and maintains robust diagnostic performance even under extreme 80\% missing scenarios, outperforming all existing baselines. To promote reproducibility, code is available at https://github.com/rongzhou7/ACADiff

---

## 论文详细总结（自动生成）

### 论文总结：ACADiff —— 用于多模态脑图像生成和缺失模态填补的自适应临床感知潜扩散模型

#### 1. 核心问题与研究动机
在阿尔茨海默病（AD）的临床研究中，多模态神经影像（如 sMRI、FDG-PET、AV45-PET）能提供互补的病理信息。然而，受限于高昂成本、采集协议差异或患者流失，现实中的临床数据集往往存在严重的**模态缺失**问题。现有的生成模型（如 GAN 或标准扩散模型）在处理多变输入组合、整合非影像临床元数据以及理解医疗语义方面存在局限。本研究旨在开发一种能够自适应处理不同输入组合，并利用临床语义信息引导的高质量影像合成框架。

#### 2. 方法论
ACADiff 框架的核心思想是利用**潜扩散模型（Latent Diffusion Model）**在压缩的潜空间内进行跨模态合成。
*   **潜空间构建**：使用预训练的 3D VAE 将高维脑影像压缩为紧凑的潜表示，降低计算开销。
*   **分层自适应条件（Hierarchical Adaptive Conditioning）**：
    *   **自适应影像调节**：模型根据输入模态的数量动态切换机制。若有两个输入（2→1），使用**交叉注意力（Cross-Attention）**进行模态间信息交换；若只有一个输入（1→1），则使用**可学习投影（Projection）**。
    *   **语义临床引导**：利用 **GPT-4o** 将疾病诊断标签和连续认知评分（如 MMSE、ADAS13、CDR-SOB）编码为结构化文本提示（Prompt），通过交叉注意力机制引导去噪过程，确保生成的影像符合患者的临床特征。
    *   **时间步调制**：通过 FiLM 机制根据去噪时间步 $t$ 动态调整去噪强度。
*   **训练目标**：结合了噪声预测损失（$L_{diff}$）和一致性正则化损失（$L_{cons}$），以保证生成影像的分布对齐。

#### 3. 实验设计
*   **数据集**：使用 **ADNI** 队列中的 1,028 名受试者数据（包括 AD、MCI 和正常对照组 HC）。
*   **任务场景**：
    *   **影像生成评价**：评估 sMRI、FDG-PET 和 AV45-PET 之间的双向合成质量。
    *   **下游诊断评价**：在 20%、40%、60% 和 80% 的极端缺失率下，评估填补后的影像对 AD vs. HC 分类性能的影响。
*   **对比基准（Benchmarks）**：
    *   **GAN 类**：Pix2Pix、DS-GAN（疾病感知 GAN）。
    *   **扩散模型类**：LDM（标准潜扩散）、PASTA（病理感知扩散）、FICD（功能对齐约束扩散）。
    *   **消融版本**：ACADiff-emb（使用普通嵌入向量而非 GPT-4o 编码）。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **软件/模型**：采用了预训练的 3D Autoencoder-KL 模型（训练时冻结解码器），使用 AdamW 优化器。
*   **训练细节**：学习率为 $1 \times 10^{-4}$，扩散步数 $T=1000$。文中未明确给出具体的训练总时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 1,028 名受试者，分为生成器训练集（600 人）和分类实验集（428 人）。
*   **充分性**：
    *   进行了多维度的定量评估（PSNR, SSIM, NMI, MAE 用于生成；ACC, AUC, SEN, SPE 用于分类）。
    *   设置了从低到高（20% 到 80%）的四种缺失率场景，验证了模型的鲁棒性。
    *   通过 10 折蒙特卡洛采样进行推理，并进行了消融实验（对比 GPT-4o 编码与普通 Embedding），实验设计较为客观、严谨。

#### 6. 主要结论与发现
*   **生成质量领先**：ACADiff 在所有生成指标上均优于现有基准（如 PSNR 达到 27.9，显著高于 LDM）。
*   **极端场景下的稳健性**：在 **80% 模态缺失**的极端情况下，ACADiff 仍能保持 77.5% 的诊断准确率，而简单填补方法基本失效。
*   **语义引导的有效性**：ACADiff 优于 ACADiff-emb，证明了利用大语言模型（GPT-4o）编码临床元数据能提供更丰富的语义指导，帮助生成更具诊断价值的病理特征。

#### 7. 优点
*   **自适应性强**：单一模型即可处理 1→1 和 2→1 的多种生成路径，无需为不同输入组合单独建模。
*   **临床感知度高**：创新性地引入 LLM 编码临床认知评分，使生成的影像不仅在视觉上真实，在病理逻辑上也与临床表现一致。
*   **实用性高**：针对临床常见的模态缺失问题提供了端到端的解决方案，且代码开源。

#### 8. 不足与局限
*   **计算资源需求**：虽然使用了潜空间，但 3D 扩散模型和 A100 GPU 的需求对于小型医疗机构仍有较高门槛。
*   **数据局限性**：实验仅基于 ADNI 单一数据集，跨中心、跨设备的泛化能力尚待验证。
*   **黑盒风险**：依赖 GPT-4o 作为编码器，虽然提升了性能，但也引入了外部模型的不可解释性和潜在的偏见风险。
*   **篇幅限制**：作为 4 页的短论文，部分实现细节（如 VAE 的具体架构、不同模态生成的定性视觉对比）展示有限。

（完）
