---
title: "Missing No More: Dictionary-Guided Cross-Modal Image Fusion under Missing Infrared"
title_zh: 不再缺失：红外缺失情况下的字典引导跨模态图像融合
authors: "Yafei Zhang, Meng Ma, Huafeng Li, Yu Liu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08018v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 在红外模态缺失情况下的字典引导跨模态图像融合
tldr: 针对红外-可见光图像融合中红外模态缺失的问题，本文提出一种基于字典引导的系数域融合框架。通过联合共享字典表示学习（JSRL）构建可解释的原子空间，利用可见光引导的红外推理（VGII）在系数域生成伪红外特征，并结合大语言模型先验进行优化，最后通过自适应融合（AFRI）重建图像。该方法避免了不可控的像素级生成，显著提升了缺失红外场景下的感知质量和检测性能。
selection_source: fresh_fetch
motivation: 现有的红外-可见光融合方法在红外模态缺失时，像素空间生成难以控制且缺乏可解释性。
method: 提出一种包含共享字典学习、系数域红外推理及自适应融合的流水线，利用大语言模型作为语义先验进行闭环优化。
result: 在红外缺失的实验设置下，该方法在感知质量和下游目标检测任务中均表现出持续的性能提升。
conclusion: 这是首个通过联合学习共享字典并在系数域进行推理融合来解决红外缺失问题的框架，兼具可解释性与实用性。
---

## 摘要
红外-可见光（IR-VIS）图像融合对于感知和安全至关重要，但大多数方法在训练和推理过程中都依赖于两种模态的可用性。当红外模态缺失时，像素空间的生成式替代方案变得难以控制，且本质上缺乏可解释性。我们通过提出一种基于共享卷积字典的字典引导系数域框架，来解决红外缺失下的融合问题。该流水线包含三个关键组件：(1) 联合共享字典表示学习（JSRL），学习红外和可见光模态共享的统一且可解释的原子空间；(2) 可见光引导的红外推理（VGII），在系数域中将可见光系数转换为伪红外系数，并在冻结的大语言模型作为弱语义先验的引导下进行一步闭环细化；(3) 通过表示推理的自适应融合（AFRI），通过窗口注意力和卷积混合在原子层级合并可见光结构和推理出的红外线索，随后使用共享字典进行重建。这种“编码-传输-融合-重建”流水线避免了不受控制的像素空间生成，同时确保了在可解释的字典-系数表示中保留先验。在红外缺失设置下的实验表明，感知质量和下游检测性能得到了一致的提升。据我们所知，这是第一个通过联合学习共享字典并执行系数域推理融合来解决红外缺失融合问题的框架。源代码可在 https://github.com/harukiv/DCMIF 公开获取。

## Abstract
Infrared-visible (IR-VIS) image fusion is vital for perception and security, yet most methods rely on the availability of both modalities during training and inference. When the infrared modality is absent, pixel-space generative substitutes become hard to control and inherently lack interpretability. We address missing-IR fusion by proposing a dictionary-guided, coefficient-domain framework built upon a shared convolutional dictionary. The pipeline comprises three key components: (1) Joint Shared-dictionary Representation Learning (JSRL) learns a unified and interpretable atom space shared by both IR and VIS modalities; (2) VIS-Guided IR Inference (VGII) transfers VIS coefficients to pseudo-IR coefficients in the coefficient domain and performs a one-step closed-loop refinement guided by a frozen large language model as a weak semantic prior; and (3) Adaptive Fusion via Representation Inference (AFRI) merges VIS structures and inferred IR cues at the atom level through window attention and convolutional mixing, followed by reconstruction with the shared dictionary. This encode-transfer-fuse-reconstruct pipeline avoids uncontrolled pixel-space generation while ensuring prior preservation within interpretable dictionary-coefficient representation. Experiments under missing-IR settings demonstrate consistent improvements in perceptual quality and downstream detection performance. To our knowledge, this represents the first framework that jointly learns a shared dictionary and performs coefficient-domain inference-fusion to tackle missing-IR fusion. The source code is publicly available at https://github.com/harukiv/DCMIF.

---

## 论文详细总结（自动生成）

以下是对论文《Missing No More: Dictionary-Guided Cross-Modal Image Fusion under Missing Infrared》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的红外-可见光（IR-VIS）图像融合方法高度依赖双模态数据的同时存在。但在实际应用（如传感器故障、成本限制）中，红外模态往往缺失。
*   **研究动机**：
    *   **现有方法局限**：现有的缺失模态融合多采用像素空间生成（如GAN或扩散模型），这类“黑盒”生成过程难以控制，物理一致性差，且缺乏可解释性，容易产生伪影或丢失结构细节。
    *   **本文目标**：提出一种在**系数域（Coefficient Domain）**而非像素空间进行推理和融合的框架，利用共享字典保证跨模态的一致性。

### 2. 方法论：DCMIF 框架
论文提出了一个“编码 $\rightarrow$ 传输 $\rightarrow$ 融合 $\rightarrow$ 重建”的闭环流水线，核心组件包括：
*   **联合共享字典表示学习 (JSRL)**：
    *   通过模型驱动的展开网络（Unfolding Network），学习一个红外和可见光模态共享的卷积字典。
    *   将图像分解为字典原子和对应的系数，建立起两种模态在原子层级的对应关系，为后续推理提供可解释的基石。
*   **可见光引导的红外推理 (VGII)**：
    *   在系数域内将可见光系数映射为伪红外系数。
    *   **关键创新**：引入冻结的**大语言模型（LLM，如Qwen-7B）**作为弱语义先验。通过文本描述引导，利用特征线性调制（FiLM）对推理出的红外系数进行细化，增强热红外语义的准确性。
*   **表示推理自适应融合 (AFRI)**：
    *   利用窗口注意力和卷积混合机制，在原子层级自适应地合并可见光结构和推理出的红外线索。
    *   最后通过共享字典将融合后的系数重建为最终图像。

### 3. 实验设计
*   **数据集**：
    *   **融合评估**：FLIR、KAIST、MSRS 数据集。
    *   **下游任务评估**：M3FD（目标检测）、FMB（语义分割）。
*   **对比方法**：对比了 10 种最先进（SOTA）的融合方法（如 U2Fusion, CDDFuse, LRRNet, SwinFusion 等）。
*   **基准设置**：值得注意的是，对比方法均使用了**双模态输入**，而本文方法在推理阶段**仅使用可见光输入**。此外，还对比了“先生成红外图像再融合”的传统多阶段方案（如结合 EGGAN 或 PID 生成模型）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA GeForce RTX 4090 GPU**。
*   **软件框架**：PyTorch 3.10.18。
*   **训练细节**：
    *   JSRL 模块在 MSRS 数据集上训练 1000 个 epoch（学习率 0.0001）。
    *   VGII 和 AFRI 模块训练 10 个 epoch（学习率 0.001）。
    *   字典核大小设为 $5 \times 5$。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流数据集上进行了广泛测试，涵盖了从主观视觉对比、客观指标评价到下游视觉任务的全方位验证。
*   **消融实验**：针对“共享字典”和“LLM 调制”两个核心组件进行了消融研究，验证了各自对热区域增强和细节保留的贡献。
*   **客观性**：通过与“双模态输入”方法的直接对比，证明了该方法在信息缺失情况下的鲁棒性。同时，补充材料中对比了基于生成模型的方案，进一步体现了系数域推理的效率和质量优势。实验设计逻辑严密，对比维度全面。

### 6. 主要结论与发现
*   **性能卓越**：即使在完全缺失红外输入的情况下，DCMIF 生成的融合图像在感知质量上仍能与双模态输入的方法相媲美。
*   **下游任务增益**：在目标检测和语义分割任务中，该方法显著优于仅使用可见光图像的结果，且表现接近甚至在某些指标上超过了全模态融合方法。
*   **可解释性强**：通过字典原子和系数域的操作，避免了像素级生成的随机性，确保了结构的物理一致性。

### 7. 优点与亮点
*   **范式创新**：首次尝试在红外缺失的情况下，通过联合学习共享字典并在系数域进行推理融合。
*   **轻量化语义先验**：巧妙地利用冻结的 LLM 作为语义指导，而非使用沉重的生成头，既保证了热语义的补全，又避免了引入生成噪声。
*   **闭环设计**：整个流程在统一的原子空间内完成，保证了表示与约束在同一域内，增强了系统的稳定性。

### 8. 不足与局限
*   **环境极端限制**：该方法依赖于可见光图像提供的结构信息。在极端恶劣环境（如浓烟、重雾）下，若可见光传感器完全无法成像，该方法将难以推理出有效的红外线索。
*   **推理延迟**：虽然比扩散模型快，但由于引入了 LLM 模块和复杂的字典展开计算，其推理时间（约 5.79s/张）相比于纯 CNN 方法仍有提升空间，可能限制其在极高实时性要求的场景应用。
*   **LLM 依赖**：性能受限于预训练 LLM 的语义理解能力，且文本 Prompt 的设计对最终效果有一定影响。

（完）
