---
title: "Solution to the 10th ABAW Expression Recognition Challenge: A Robust Multimodal Framework with Safe Cross-Attention and Modality Dropout"
title_zh: 第十届 ABAW 表情识别挑战赛解决方案：一种具有安全交叉注意力和模态丢弃的鲁棒多模态框架
authors: "Jun Yu, Naixiang Zheng, Guoyuan Wang, Yunxiang Zhang, Lingsi Zhu, Jiaen Liang, Wei Huang, Shengping Liu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08034v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 针对模态缺失的鲁棒多模态框架，采用模态丢弃策略
tldr: 本研究针对ABAW表情识别挑战赛中遮挡、模态缺失及类别不平衡等难题，提出了一种鲁棒的多模态框架。该框架采用双支路Transformer架构，结合安全交叉注意力机制与模态丢弃策略，在视觉信息缺失时能动态依赖音频预测。通过引入Focal Loss和滑动窗口软投票策略，有效解决了长尾分布及帧级分类抖动问题，在Aff-Wild2数据集上表现优异。
selection_source: fresh_fetch
motivation: 旨在解决真实场景下表情识别面临的遮挡、模态缺失以及严重的类别不平衡问题。
method: 提出一种基于双支路Transformer的多模态框架，集成安全交叉注意力、模态丢弃、Focal Loss及滑动窗口软投票技术。
result: "在Aff-Wild2验证集上取得了60.79%的准确率和0.5029的F1分数，证明了框架在处理复杂时空依赖方面的有效性。"
conclusion: 该多模态框架通过动态融合视听信息和优化训练策略，显著提升了在极端环境下的表情识别鲁棒性。
---

## 摘要
现实环境中的情感识别受到局部遮挡、模态缺失和严重类别不平衡的阻碍。为了解决这些问题，特别是针对野外情感行为分析 (ABAW) 表情挑战赛，我们提出了一种动态融合视觉和音频表示的多模态框架。我们的方法采用双支路 Transformer 架构，具有安全交叉注意力机制和模态丢弃策略。这种设计允许网络在视觉线索缺失时依赖基于音频的预测。为了缓解 Aff-Wild2 数据集的长尾分布，我们应用了 Focal Loss 优化，并结合滑动窗口软投票策略来捕捉动态情感转换并减少帧级分类抖动。实验表明，我们的框架有效地处理了模态缺失和复杂的时空依赖关系，在 Aff-Wild2 验证集上达到了 60.79% 的准确率和 0.5029 的 F1 分数。

## Abstract
Emotion recognition in real-world environments is hindered by partial occlusions, missing modalities, and severe class imbalance. To address these issues, particularly for the Affective Behavior Analysis in-the-wild (ABAW) Expression challenge, we propose a multimodal framework that dynamically fuses visual and audio representations. Our approach uses a dual-branch Transformer architecture featuring a safe cross-attention mechanism and a modality dropout strategy. This design allows the network to rely on audio-based predictions when visual cues are absent. To mitigate the long-tail distribution of the Aff-Wild2 dataset, we apply focal loss optimization, combined with a sliding-window soft voting strategy to capture dynamic emotional transitions and reduce frame-level classification jitter. Experiments demonstrate that our framework effectively handles missing modalities and complex spatiotemporal dependencies, achieving an accuracy of 60.79% and an F1-score of 0.5029 on the Aff-Wild2 validation set.

---

## 论文详细总结（自动生成）

以下是对论文《Solution to the 10th ABAW Expression Recognition Challenge: A Robust Multimodal Framework with Safe Cross-Attention and Modality Dropout》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在现实世界（In-the-wild）环境下的情感识别面临三大挑战：**局部遮挡**、**模态缺失**（如人脸离开画面）以及**严重的类别不平衡**（长尾分布）。
*   **核心问题**：传统的表情识别模型在受控实验室环境下表现良好，但在复杂的野外场景中，由于视觉信号的不连续性和数据分布的极端不均，模型往往会出现性能剧降或预测抖动。
*   **研究目的**：为第10届ABAW表情识别挑战赛提供一种鲁棒的多模态解决方案，旨在提升模型在极端不确定环境下的容错能力和分类精度。

### 2. 论文提出的方法论
该框架由五个关键组件构成：
*   **两阶段特征提取**：
    *   **视觉支路**：采用 **BEiT-large** 架构。先在 Raf-DB、FERPlus 和 AffectNet 等静态数据集上预训练，再在 Aff-Wild2 视频帧上进行领域自适应微调。
    *   **音频支路**：使用预训练的 **WavLM-large** 提取语音特征，并通过线性插值在时间轴上与视频帧对齐。
*   **双支路 Transformer 网络**：
    *   利用自注意力（Self-Attention）提取模态内上下文，利用交叉注意力（Cross-Attention）实现视听交互。
    *   引入**可学习门控融合机制**，动态平衡单模态特征与跨模态融合特征的贡献。
*   **安全交叉注意力与模态丢弃**：
    *   **模态丢弃（Modality Dropout）**：训练时以概率 $p$ 随机掩蔽视觉输入，强制模型学习音频线索。
    *   **安全注意力机制**：当视觉特征完全缺失时，通过数值保护逻辑防止 Softmax 产生无效值，使网络能优雅地降级为纯音频决策。
*   **优化目标**：使用 **Focal Loss** 结合基于样本有效数量的类别权重，降低高频样本权重，迫使模型关注长尾分布中的少数类表情。
*   **推理策略**：采用**重叠滑动窗口**（窗口 64，步长 8）进行局部建模，通过**软投票（Logit 平均）**和**中值滤波**（核大小 11）减少帧级分类的瞬时抖动。

### 3. 实验设计
*   **数据集**：主要使用 **Aff-Wild2** 数据集（548 个视频，约 270 万帧），涵盖 8 类表情（6 种基本表情 + 中性 + 其他）。
*   **Benchmark 与对比方法**：
    *   **视觉主干对比**：对比了 ResNet50、EfficientNetV2-M、MAE-ViT-Base 和 BEiT-large。
    *   **模态权重分析**：对比了纯音频、纯视觉及不同融合比例（$\lambda$）下的性能。
    *   **消融实验**：针对模态丢弃概率 $p$、Transformer 隐藏层维度 $d$、层数 $l$ 进行了多组实验。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。但考虑到使用了 BEiT-large 和 WavLM-large 等大型预训练模型，可以推断该实验需要较高的显存（如 A100 或 H100 级别）和计算资源。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了约 12 组核心对比实验，包括 4 种视觉主干的横向对比、4 种融合权重的测试以及 8 组关于超参数（$p, d, l$）的消融实验。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅验证了多模态的必要性，还通过消融实验揭示了模型复杂度与泛化性能之间的权衡（即模型过大在噪声数据上易过拟合）。

### 6. 论文的主要结论与发现
*   **BEiT 优于 CNN**：在野外表情识别中，基于 Transformer 的 BEiT-large 捕捉全局上下文的能力显著强于传统的卷积网络（ResNet/EfficientNet）。
*   **视觉主导，音频辅助**：实验证明视觉是表情识别的主导模态，但音频在视觉受限时能提供关键补充。
*   **模态丢弃的有效性**：设置 $p=0.1$ 的模态丢弃能显著提升 F1 分数（从 0.4764 提升至 0.5029），证明了模拟信号丢失能增强模型的鲁棒性。
*   **模型规模陷阱**：在 Aff-Wild2 这种噪声较大的数据集上，增加 Transformer 的深度或宽度反而可能导致性能下降，中等规模（$d=256, l=3$）的配置表现最佳。

### 7. 优点
*   **鲁棒性设计**：提出的“安全交叉注意力”解决了多模态模型在实际应用中常见的“输入缺失导致崩溃”的问题，具有很强的工程实用价值。
*   **端到端优化**：从特征提取、模态融合、损失函数到后期处理，形成了一套完整的针对长尾数据和动态视频的闭环方案。
*   **推理平滑**：软投票和中值滤波的结合有效解决了视频表情识别中常见的预测标签频繁跳变的问题。

### 8. 不足与局限
*   **算力细节缺失**：缺乏训练环境和效率的描述，不利于其他研究者评估复现成本。
*   **验证集依赖**：目前结果主要基于验证集，尚未展示在测试集上的最终排名表现。
*   **自监督潜力未尽**：作者在结论中提到未来将探索大规模自监督学习，说明当前模型在利用无标签数据方面仍有提升空间。
*   **应用限制**：滑动窗口机制虽然提升了准确性，但会引入推理延迟，对于极高实时性要求的场景可能需要进一步优化。

（完）
