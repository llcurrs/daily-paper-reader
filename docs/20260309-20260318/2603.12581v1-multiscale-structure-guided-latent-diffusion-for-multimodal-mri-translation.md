---
title: Multiscale Structure-Guided Latent Diffusion for Multimodal MRI Translation
title_zh: 多尺度结构引导的潜在扩散模型用于多模态 MRI 翻译
authors: "Jianqiang Lin, Zhiqiang Shen, Peng Cao, Jinzhu Yang, Osmar R. Zaiane, Xiaoli Liu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12581v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 处理MRI翻译中任意模态缺失的情况
tldr: 针对多模态MRI图像翻译中解剖结构不一致和纹理细节丢失的问题，本文提出了一种多尺度结构引导的潜在扩散模型（MSG-LDM）。该方法通过在潜在空间中解耦模态特定风格与共享结构特征，并结合多尺度高低频信息，实现了对缺失模态的高质量合成。实验证明，该模型在保持解剖结构完整性和细节恢复方面显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有扩散模型在处理缺失模态的MRI翻译时，难以保证合成图像与原图在解剖结构上的一致性及纹理细节的完整性。
method: 提出一种基于潜在扩散的框架，利用风格-结构解耦机制和多尺度特征空间来联合建模低频解剖布局与高频边界细节。
result: 在BraTS2020和WMH数据集上的广泛实验证明，该方法在重建完整解剖结构方面表现优异，超越了现有的MRI合成方法。
conclusion: 通过显式引入多尺度结构引导和解耦学习，MSG-LDM有效解决了多模态MRI翻译中的结构失真问题，提升了合成图像的质量。
---

## 摘要
尽管扩散模型在多模态磁共振成像（MRI）翻译任务中取得了显著进展，但现有方法在处理任意模态缺失的情况时，仍往往面临解剖结构不一致或纹理细节退化的问题。为了解决这些问题，我们提出了一种基于潜在扩散的多模态 MRI 翻译框架，称为 MSG-LDM。通过利用可用模态，该方法推断出完整的结构信息，从而保留了可靠的边界细节。具体而言，我们在潜在空间中引入了一种风格-结构解耦机制，将特定模态的风格特征与共享的结构表示显式分离，并在多尺度特征空间中联合建模低频解剖布局和高频边界细节。在结构解耦阶段，显式融入高频结构信息以增强特征表示，引导模型在学习模态不变的低频解剖表示时，关注细粒度的结构线索。此外，为了减少特定模态风格的干扰并提高结构表示的稳定性，我们设计了风格一致性损失和结构感知损失。在 BraTS2020 和 WMH 数据集上的广泛实验表明，所提方法优于现有的 MRI 合成方法，特别是在重建完整结构方面。源代码可在 https://github.com/ziyi-start/MSG-LDM 公开获取。

## Abstract
Although diffusion models have achieved remarkable progress in multi-modal magnetic resonance imaging (MRI) translation tasks, existing methods still tend to suffer from anatomical inconsistencies or degraded texture details when handling arbitrary missing-modality scenarios. To address these issues, we propose a latent diffusion-based multi-modal MRI translation framework, termed MSG-LDM. By leveraging the available modalities, the proposed method infers complete structural information, which preserves reliable boundary details. Specifically, we introduce a style--structure disentanglement mechanism in the latent space, which explicitly separates modality-specific style features from shared structural representations, and jointly models low-frequency anatomical layouts and high-frequency boundary details in a multi-scale feature space. During the structure disentanglement stage, high-frequency structural information is explicitly incorporated to enhance feature representations, guiding the model to focus on fine-grained structural cues while learning modality-invariant low-frequency anatomical representations. Furthermore, to reduce interference from modality-specific styles and improve the stability of structure representations, we design a style consistency loss and a structure-aware loss. Extensive experiments on the BraTS2020 and WMH datasets demonstrate that the proposed method outperforms existing MRI synthesis approaches, particularly in reconstructing complete structures. The source code is publicly available at https://github.com/ziyi-start/MSG-LDM.

---

## 论文详细总结（自动生成）

### 论文总结：多尺度结构引导的潜在扩散模型用于多模态 MRI 翻译 (MSG-LDM)

#### 1. 核心问题与整体含义（研究动机和背景）
在临床实践中，由于采集时间长、成本高或患者耐受性等原因，往往无法获得完整的四种 MRI 序列（T1, T2, T1CE, FLAIR）。多模态 MRI 翻译旨在从现有模态合成缺失模态。
*   **现有挑战**：目前的扩散模型（Diffusion Models）在处理任意模态缺失时，容易出现解剖结构扭曲、高频纹理细节丢失，以及模态特有的“风格”与共享的“结构”信息相互缠绕的问题。
*   **核心动机**：通过显式引入多尺度结构引导，解决扩散模型对解剖结构不敏感的问题，提高合成图像的结构保真度和生成效率。

#### 2. 方法论：核心思想与关键技术
MSG-LDM 框架基于潜在扩散模型（LDM），核心思想是在 VAE 的潜在空间中实现**风格与结构的深度解耦**。
*   **风格-结构解耦机制**：
    *   **结构编码器 (Estr)**：提取模态不变的解剖特征。
    *   **风格编码器 (Esty)**：提取模态特有的纹理和对比度特征。
*   **关键技术组件**：
    *   **高频注入模块 (HFIB)**：利用可学习的动态高斯滤波器分解特征，提取高频残差（边缘、细微纹理）并重新注入，增强模型对细粒度结构的感知。
    *   **多模态结构特征融合 (MMSF)**：采用注意力门控机制，自适应地融合来自不同可用模态的结构特征，抑制无关的模态噪声。
    *   **多尺度结构特征增强 (MSSE)**：通过跨尺度注意力机制，将低层的高频细节对齐并注入高层语义特征，形成统一的结构表示 $F_s$。
*   **损失函数设计**：
    *   **风格一致性损失 ($L_{sc}$)**：借鉴对比学习，拉近同模态风格，推开异模态风格，确保解耦彻底。
    *   **结构感知损失 ($L_{sa}$)**：结合空间域 $L_1$ 损失和频域 SSIM 损失（基于离散余弦变换 DCT），约束解剖布局和频率分布。
    *   **总损失**：包含分割辅助损失、风格一致性损失、结构感知损失和扩散模型去噪损失。

#### 3. 实验设计
*   **数据集**：
    *   **BraTS2020**：脑肿瘤数据集，包含 T1, T1CE, T2, FLAIR 四种模态。
    *   **WMH**：白质高信号数据集，包含 T1 和 FLAIR 模态，用于验证跨数据集的泛化性。
*   **Benchmark 与对比方法**：
    *   对比了代表性的 SOTA 方法：**MM-GAN**（基于 GAN）、**SynDiff**（基于扩散模型）、**MISA-LDM**（最新的基于解耦的扩散模型）。
*   **评估指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、Dice 分数（用于评估合成图像对下游分割任务的贡献）。

#### 4. 资源与算力
*   **硬件**：使用了 **3 台 NVIDIA RTX 4090 GPU**。
*   **软件与参数**：基于 PyTorch 2.1.0；学习率 $1 \times 10^{-4}$；Batch size 为 9。
*   **训练时长**：训练了 100 个 epoch。文中未明确给出具体的总训练小时数，但 4090 集群通常意味着较高的训练效率。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 BraTS2020 上进行了针对 1、2、3 个可用模态组合的详尽定量对比。
    *   在 WMH 数据集上进行了跨模态翻译实验。
    *   **消融实验**：系统地验证了解耦模块、MMSF、HFIB、MSSE 以及两种核心损失函数对性能的提升。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标、定性视觉对比、热图分析以及下游分割任务的验证，能够客观反映模型在结构保持上的优势。

#### 6. 主要结论与发现
*   **性能领先**：MSG-LDM 在所有模态合成任务中均优于现有方法，尤其在 SSIM 和 Dice 指标上提升显著，证明了结构引导的有效性。
*   **结构保真度**：通过引入结构先验，模型能够生成更清晰的肿瘤边界和脑部组织轮廓，减少了伪影。
*   **加速收敛**：实验观察到，显式的结构引导能显著加快扩散模型的去噪过程，使其在更少的步数内达到理想的结构稳定性。

#### 7. 优点（亮点）
*   **多尺度高频建模**：HFIB 和 MSSE 的设计精准捕捉了医学图像中最关键的边缘信息，解决了传统扩散模型生成图像偏“平滑”的问题。
*   **解耦策略有效**：通过风格一致性损失，成功分离了模态信息，使得结构表示在不同输入组合下保持鲁棒。
*   **即插即用潜力**：这种结构引导的思想可以推广到其他医学图像翻译任务（如 CT 转 MRI）。

#### 8. 不足与局限
*   **计算复杂度**：虽然在潜在空间操作，但引入了多个额外的编码器和注意力模块，推理时的计算开销可能高于基础 LDM。
*   **2D 局限性**：目前实验基于 2D 切片，未充分利用 MRI 的 3D 空间上下文信息，可能导致层间不连续。
*   **极端缺失情况**：当所有模态都缺失或质量极差时，模型对结构的“推断”能力仍受限于训练集的分布。

（完）
