---
title: Bidirectional Cross-Attention Fusion of High-Res RGB and Low-Res HSI for Multimodal Automated Waste Sorting
title_zh: 高分辨率 RGB 与低分辨率 HSI 的双向交叉注意力融合，用于多模态自动化垃圾分类
authors: "Jonas V. Funk, Lukas Roming, Andreas Michel, Paul Bäcker, Georg Maier, Thomas Längle, Markus Klute"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13941v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: RGB与高光谱图像的双向交叉注意力融合
tldr: 针对自动化垃圾分类中RGB图像空间分辨率高但材质识别能力弱、高光谱图像（HSI）光谱信息丰富但分辨率低的问题，本文提出双向交叉注意力融合（BCAF）模型。该模型通过局部双向交叉注意力机制，在原始网格上直接对齐高分辨率RGB与低分辨率HSI，避免了预上采样带来的信息损失。在SpectralWaste和K3I-Cycling数据集上，BCAF均达到了最先进的分割性能，为工业级多模态垃圾分类提供了高效解决方案。
selection_source: fresh_fetch
motivation: 为了克服单一RGB模态在区分外观相似材质时的局限性，通过融合低分辨率高光谱数据来提升垃圾分类的准确性。
method: 采用双向交叉注意力机制在原生网格上对齐不同分辨率的模态，并结合针对高光谱数据优化的Swin Transformer骨干网络进行特征提取。
result: "在SpectralWaste基准测试中取得76.4% mIoU的SOTA成绩，并在新型工业数据集K3I-Cycling上展现了卓越的材质与塑料类型分割能力。"
conclusion: BCAF模型成功实现了高分辨率空间细节与丰富光谱信息的有效融合，在保证推理速度的同时显著提升了复杂工业环境下的垃圾分类精度。
---

## 摘要
随着垃圾流的增长和向循环经济的转型，需要高效的自动化垃圾分类。在工业环境中，物料在快速传送带上移动，可靠的识别和剔除需要像素级的精确分割。RGB 成像提供高分辨率的空间细节，这对于精确分割至关重要，但它会混淆在可见光谱中看起来相似的材料。高光谱成像 (HSI) 提供的光谱特征可以区分这些材料，但其较低的空间分辨率限制了细节。因此，有效的垃圾分类需要融合这两种模态的方法，以利用它们的互补优势。我们提出了双向交叉注意力融合 (BCAF)，它通过局部的双向交叉注意力，在原始网格上对齐高分辨率 RGB 和低分辨率 HSI，从而避免了预上采样或早期的光谱崩溃。BCAF 使用两个独立的骨干网络：用于 RGB 的标准 Swin Transformer 和通过 3D 标记化及光谱自注意力保留光谱结构的 HSI 适配 Swin 骨干网络。我们还分析了 RGB 输入分辨率与 HSI 光谱切片数量之间的权衡。尽管我们的评估针对 RGB-HSI 融合，但 BCAF 是模态无关的，适用于已配准的 RGB 与低分辨率、多通道辅助传感器的融合。在基准 SpectralWaste 数据集上，BCAF 在 31 帧/秒时达到了 76.4% mIoU，在 55 帧/秒时达到了 75.4% mIoU 的最先进性能。我们进一步评估了一个新的工业数据集：K3I-Cycling（第一个 RGB 子集已在 Fordatis 上发布）。在该数据集上，BCAF 在材料分割（纸张、金属、塑料等）方面达到了 62.3% mIoU，在塑料类型分割（PET、PP、HDPE、LDPE、PS 等）方面达到了 66.2% mIoU。

## Abstract
Growing waste streams and the transition to a circular economy require efficient automated waste sorting. In industrial settings, materials move on fast conveyor belts, where reliable identification and ejection demand pixel-accurate segmentation. RGB imaging delivers high-resolution spatial detail, which is essential for accurate segmentation, but it confuses materials that look similar in the visible spectrum. Hyperspectral imaging (HSI) provides spectral signatures that separate such materials, yet its lower spatial resolution limits detail. Effective waste sorting therefore needs methods that fuse both modalities to exploit their complementary strengths. We present Bidirectional Cross-Attention Fusion (BCAF), which aligns high-resolution RGB with low-resolution HSI at their native grids via localized, bidirectional cross-attention, avoiding pre-upsampling or early spectral collapse. BCAF uses two independent backbones: a standard Swin Transformer for RGB and an HSI-adapted Swin backbone that preserves spectral structure through 3D tokenization with spectral self-attention. We also analyze trade-offs between RGB input resolution and the number of HSI spectral slices. Although our evaluation targets RGB-HSI fusion, BCAF is modality-agnostic and applies to co-registered RGB with lower-resolution, high-channel auxiliary sensors. On the benchmark SpectralWaste dataset, BCAF achieves state-of-the-art performance of 76.4% mIoU at 31 images/s and 75.4% mIoU at 55 images/s. We further evaluate a novel industrial dataset: K3I-Cycling (first RGB subset already released on Fordatis). On this dataset, BCAF reaches 62.3% mIoU for material segmentation (paper, metal, plastic, etc.) and 66.2% mIoU for plastic-type segmentation (PET, PP, HDPE, LDPE, PS, etc.).

---

## 论文详细总结（自动生成）

这是一份关于论文《Bidirectional Cross-Attention Fusion of High-Res RGB and Low-Res HSI for Multimodal Automated Waste Sorting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在工业自动化垃圾分类（如传送带场景）中，如何有效结合**高分辨率 RGB 图像**（提供精细空间细节但难以区分外观相似的材质）与**低分辨率高光谱图像（HSI）**（提供丰富的材质光谱特征但空间分辨率低）来提升像素级语义分割的准确性。
*   **研究背景**：循环经济要求对废弃物进行精准分类（如区分不同类型的聚合物 PET, PP, HDPE 等）。现有的融合方法通常采用预上采样或早期光谱降维（如 PCA），这会导致空间细节模糊或关键光谱信息丢失。

### 2. 论文提出的方法论：BCAF 架构
*   **核心思想**：提出**双向交叉注意力融合（BCAF）**机制，在两种模态各自的**原生分辨率网格**上进行局部对齐和特征交换，避免了信息损耗。
*   **关键技术细节**：
    *   **HSI 适配骨干网络**：基于 Swin Transformer 改造，采用 **3D 分组标记化（Grouped Tokenization）** 和 **因子化空间-光谱注意力**。它将 HSI 波段分为 $K$ 个切片，在保留光谱结构的同时进行特征提取。
    *   **双向局部交叉注意力**：在 RGB 的细网格和 HSI 的粗网格之间建立联系。通过 `Pixel-Unshuffle` 操作，让每个 HSI 粗像素与其对应的 $r \times r$ 个 RGB 细像素进行注意力交互，实现跨模态对齐。
    *   **光谱 SE 池化（Spectral SE Pooling）**：在融合前，利用挤压-激励机制自适应地压缩 HSI 的光谱切片维度，将其转化为 2D 特征图。
    *   **门控模态融合（Gated Fusion）**：使用可学习的门控参数动态调节 RGB 与 HSI 特征在不同阶段的贡献比例。
*   **算法流程**：双骨干并行提取特征 $\rightarrow$ 四个阶段进行双向交叉注意力交换 $\rightarrow$ 门控融合 $\rightarrow$ 共享的 U-Net 式解码器输出分割结果。

### 3. 实验设计
*   **数据集与场景**：
    1.  **SpectralWaste**：公开基准数据集，包含 852 对 RGB-HSI 图像，涵盖 6 类垃圾。
    2.  **K3I-Cycling**：新型工业数据集，包含 354 对高分辨率（4096px）图像，分为材质分类（4类）和塑料类型分类（8类）两个任务。
*   **对比方法（Baselines）**：
    *   **单模态**：基于 MiT (SegFormer) 和 Swin-T 的 RGB 模型；适配不同光谱切片数 $K$ 的 HSI 模型。
    *   **融合方法**：Logit Fusion（晚融合/决策级融合）。
    *   **SOTA 对比**：与 SpectralWaste 上的现有最优模型进行 mIoU 和推理速度对比。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 4090** GPU 进行推理速度测试。
*   **算力指标**：
    *   在 256px 分辨率下，BCAF 达到 **55 images/s**；在 1024px 分辨率下为 **31 images/s**。
    *   详细记录了各模型的参数量（Params）和理论计算量（GFLOPs）。
*   **训练时长**：文中未明确给出总训练小时数，但提到 SpectralWaste 训练 1000 epochs，K3I 训练 300 epochs，采用两阶段训练策略（先单模态预训练，后融合微调）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个数据集上进行了多轮测试，SpectralWaste 采用 5 个随机种子取平均，K3I 采用 3 个种子，确保了结果的统计客观性。
    *   **消融实验**：非常详尽，涵盖了 HSI 嵌入方式（PCA vs. 分组卷积）、光谱建模方式（1D 卷积 vs. 注意力）、融合阶段（单阶段 vs. 全阶段）以及对**模态错位（Misalignment）**的鲁棒性测试。
*   **公平性**：所有对比模型使用相同的解码器架构和训练超参数，确保了骨干网络和融合机制比较的公正性。

### 6. 主要结论与发现
*   **性能领先**：BCAF 在 SpectralWaste 上达到 **76.4% mIoU**，创下新纪录。
*   **分辨率即信息**：提高 RGB 输入分辨率能显著提升分割精度，且保留原生高分辨率细节比单纯上采样更有效。
*   **任务相关性**：简单的材质分类（如纸 vs. 金属）对光谱切片数 $K$ 要求不高；而精细的塑料分类（如区分 PP 和 PE）则显著受益于较大的 $K$ 值（如 $K=7$ 或 $10$）。
*   **鲁棒性**：BCAF 的局部交叉注意力机制对轻微的传感器配准误差具有一定的容忍度，优于简单的晚融合。

### 7. 优点
*   **架构优雅**：解决了高低分辨率传感器融合中常见的“空间模糊”和“光谱崩溃”问题。
*   **工业实用性**：在 RTX 4090 上实现了实时处理（>30 FPS），满足工业传送带的速度要求。
*   **模块化设计**：HSI 适配的 Swin 骨干网络和双向注意力模块具有通用性，可推广至其他 RGB+X（如深度、热成像）任务。

### 8. 不足与局限
*   **配准依赖**：虽然对小误差有鲁棒性，但仍高度依赖于传感器间的精确空间配准，严重错位会导致性能大幅下降。
*   **计算开销**：双骨干网络相比单模态模型参数量翻倍（约 100M），在嵌入式或低功耗设备上的部署可能受限。
*   **超参数敏感**：光谱切片数 $K$ 需要根据具体任务手动调优，缺乏自动化的波段选择或切片分配机制。

（完）
