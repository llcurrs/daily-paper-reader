---
title: "CLoE: Expert Consistency Learning for Missing Modality Segmentation"
title_zh: CLoE：面向缺失模态分割的专家一致性学习
authors: "Xinyu Tong, Meihua Zhou, Bowu Fan, Haitao Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09316v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 缺失模态分割的一致性驱动框架
tldr: 针对多模态医学图像分割中推理阶段模态缺失导致专家预测不一致和融合不稳定的问题，本文提出CLoE框架。该框架通过双分支专家一致性学习（全局与区域一致性）来减少输入缺失时的预测漂移，并利用轻量化门控网络实现可靠性感知特征重校准。实验证明其在BraTS 2020等数据集上达到SOTA，显著提升了对临床关键结构的分割鲁棒性。
selection_source: fresh_fetch
motivation: 解决多模态医学图像分割在推理时因模态缺失导致的专家预测分歧及融合不稳定问题。
method: 提出包含全局模态一致性和局部区域一致性的双分支学习目标，并结合门控网络进行可靠性感知特征重校准。
result: 在BraTS 2020和MSD Prostate数据集上超越了现有最先进方法，并展现出强大的跨数据集泛化能力。
conclusion: CLoE通过强化专家间的一致性控制，有效提升了缺失模态场景下医学图像分割的鲁棒性和准确性。
---

## 摘要
多模态医学图像分割在推理阶段经常面临模态缺失的问题，这会导致模态专家之间产生分歧并使融合过程变得不稳定，尤其是在细小的前景结构上。我们提出了专家一致性学习（CLoE），这是一种一致性驱动的缺失模态分割框架，在所有模态均可用时仍能保持强大的性能。CLoE 将鲁棒性建模为决策层面的专家一致性控制，并引入了双分支专家一致性学习目标。模态专家一致性强制专家预测之间达成全局一致，以减少部分输入下的个案漂移；而区域专家一致性则强调在临床关键前景区域的一致性，以避免受背景主导的正则化影响。我们进一步利用轻量级门控网络将一致性分数映射为模态可靠性权重，从而在融合前实现可靠性感知的特征重校准。在 BraTS 2020 和 MSD Prostate 数据集上的广泛实验表明，CLoE 在不完整多模态分割方面优于现有先进方法，同时展现出强大的跨数据集泛化能力，并提高了在临床关键结构上的鲁棒性。

## Abstract
Multimodal medical image segmentation often faces missing modalities at inference, which induces disagreement among modality experts and makes fusion unstable, particularly on small foreground structures. We propose Consistency Learning of Experts (CLoE), a consistency-driven framework for missing-modality segmentation that preserves strong performance when all modalities are available. CLoE formulates robustness as decision-level expert consistency control and introduces a dual-branch Expert Consistency Learning objective. Modality Expert Consistency enforces global agreement among expert predictions to reduce case-wise drift under partial inputs, while Region Expert Consistency emphasizes agreement on clinically critical foreground regions to avoid background-dominated regularization. We further map consistency scores to modality reliability weights using a lightweight gating network, enabling reliability-aware feature recalibration before fusion. Extensive experiments on BraTS 2020 and MSD Prostate demonstrate that CLoE outperforms state-of-the-art methods in incomplete multimodal segmentation, while exhibiting strong cross-dataset generalization and improving robustness on clinically critical structures.

---

## 论文详细总结（自动生成）

以下是对论文《CLoE: Expert Consistency Learning for Missing Modality Segmentation》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在临床多模态 MRI 诊断中，由于扫描协议差异、图像质量问题或患者耐受性，推理阶段经常会出现**模态缺失**（如缺少 T1 或 FLAIR 序列）。
*   **核心挑战**：现有的多模态分割模型在模态缺失时，不同模态的“专家”（即模态特定编码器）之间会产生预测分歧（Expert Drift），且传统的融合机制（如固定权重或简单注意力）容易被背景区域主导，导致对细小但关键的前景结构（如肿瘤核心）分割不稳定。
*   **研究目标**：提出一种一致性驱动的框架，在减少模态缺失导致的决策不确定性的同时，保持全模态情况下的高性能。

### 2. 论文提出的方法论
论文提出了 **CLoE (Consistency Learning of Experts)** 框架，核心思想是将鲁棒性建模为“决策层面的专家一致性控制”。
*   **双分支专家一致性学习 (ECL)**：
    *   **模态专家一致性 (MEC)**：计算所有可用模态专家预测图之间的全局余弦相似度，强制各专家在全局分布上达成共识，抑制个案漂移。
    *   **区域专家一致性 (REC)**：通过一个轻量化投影头生成概率区域图（Region Map），重点约束前景（病灶）区域的一致性，防止模型因背景像素过多而忽略关键结构。
*   **一致性驱动的动态门控 (Dynamic Gating)**：
    *   将 MEC 和 REC 计算的一致性分数输入轻量级门控网络，映射为各模态的**可靠性权重**。
    *   在特征融合前，利用该权重对模态特征进行重校准，自动下调不一致（不可靠）专家的贡献。
*   **学习目标**：总损失函数结合了融合分割损失 ($L_{seg}$)、鲁棒专家一致性损失 ($L_{ECL}$) 和对比表征损失 ($L_{contrast}$)，后者用于增强特征解耦。

### 3. 实验设计
*   **数据集**：
    1.  **BraTS 2020**：脑肿瘤分割，包含 4 种模态（T1, T1c, T2, FLAIR），涉及 15 种缺失组合。
    2.  **MSD Prostate (Task 05)**：前列腺分割，包含 2 种模态（T2, ADC），样本量较小（48 例），用于测试细粒度边界和跨数据集鲁棒性。
*   **Benchmark 与对比方法**：
    *   对比了 HeMIS、RobustSeg、RFNet、M³AE（大模型预训练）和 DC-Seg（SOTA 解耦学习）。
    *   还对比了通用医学大模型 MedSAM 在全模态下的表现。

### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，初始学习率为 0.0002，权重衰减 0.0001。
*   **训练规模**：训练 500 个 epoch，batch size 为 1，输入 patch 大小为 $112 \times 112 \times 112$。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量，也未提及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验覆盖面**：在 BraTS 2020 上测试了全部 15 种模态缺失组合，涵盖了从单模态到全模态的所有场景，实验设计非常全面。
*   **消融实验**：针对 MEC、REC、门控网络和权重融合四个核心组件进行了消融，验证了各模块的贡献。
*   **客观性**：通过在两个任务（脑部和前列腺）上进行验证，证明了方法在不同解剖结构和模态数量下的泛化能力。对比实验均引用了前序工作的公开结果，确保了公平性。

### 6. 论文的主要结论与发现
*   **性能领先**：CLoE 在 BraTS 2020 的全指标（WT, TC, ET）上均优于 DC-Seg 和 M³AE，尤其在缺失模态较多时表现更稳健。
*   **区域一致性的重要性**：消融实验显示，移除 REC 会导致性能显著下降（Dice 降低约 1.98%），证明了针对前景区域的一致性约束是提升分割精度的关键。
*   **可靠性感知**：门控网络能有效将一致性信号转化为可靠性权重，从而在融合阶段抑制“拖后腿”的模态。

### 7. 优点
*   **机制创新**：将缺失模态问题转化为“专家一致性”问题，并区分了全局和局部一致性，切中医学图像背景噪声大的痛点。
*   **推理高效**：门控网络非常轻量，在提升鲁棒性的同时几乎不增加推理阶段的计算负担。
*   **鲁棒性强**：在极小数据集（Prostate 48例）上依然表现优异，显示出比大模型更好的专用任务适应性。

### 8. 不足与局限
*   **超参数敏感性**：框架引入了多个超参数（如 $\eta, \lambda_{rec}, \alpha, \beta$），论文未详细讨论这些参数在不同任务间的敏感度或调节策略。
*   **计算资源信息缺失**：未提供算力消耗数据，难以评估其训练成本。
*   **极端情况限制**：虽然在缺失模态下表现良好，但如果所有可用模态的质量都极差，一致性信号本身可能失效，导致门控权重分配出现偏差。

（完）
