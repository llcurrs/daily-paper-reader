---
title: "Blind Hyperspectral and Multispectral Images Fusion: A Unified Tensor Fusion Framework from Coupled Inverse Problem Perspective"
title_zh: 盲高光谱与多光谱图像融合：基于耦合反问题视角的统一张量融合框架
authors: "Ying Gao, Michael K. Ng, Chunfeng cui"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11530v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 高光谱与多光谱图像融合框架
tldr: 针对高光谱与多光谱图像融合中空间模糊和光谱响应函数通常未知的局限性，本文提出了一种基于耦合反问题视角的统一张量融合框架。该方法将空间盲解卷积与光谱盲解算相结合，无需预训练即可实现实时融合。通过偏线性化ADMM算法联合估计高分辨率图像及退化算子，在多个数据集上达到了领先性能。
selection_source: fresh_fetch
motivation: 传统的图像融合方法通常假设空间模糊和光谱响应函数已知，这在实际应用中难以满足。
method: 将盲融合建模为耦合反问题，并提出一种结合张量分解、Moreau包络平滑及偏线性化ADMM算法的统一优化框架。
result: 在合成和真实数据集上的实验表明，该方法在盲融合任务中优于现有的最先进算法。
conclusion: 该研究为盲图像融合提供了一个灵活、高效且具有收敛性保证的张量处理方案。
---

## 摘要
高光谱与多光谱图像融合旨在整合低分辨率高光谱图像（LR-HSI）和高分辨率多光谱图像（HR-MSI），以构建高分辨率高光谱图像（HR-HSI）。通常假设空间模糊算子和光谱响应算子是先验已知的。然而，这种假设在实践中具有极大的局限性。为了克服这一限制，本文将盲融合建模为一个耦合反问题，将空间域的盲解卷积与光谱域的盲解混相结合。从这一新颖视角出发，我们提出了一个统一的张量融合框架，该框架能够进行灵活的自我调整，且无需预训练即可实现实时融合。我们进一步引入了一个优化模型，用于联合估计目标 HR-HSI、空间点扩散函数和光谱响应函数。为了求解该模型，我们设计了一种带有 Moreau 包络平滑的部分线性化交替方向乘子法（ADMM）算法，并附带了严格的收敛性分析。针对融合问题的具体特征，提出了一种初始化估计器。在合成数据集和真实数据集上与现有先进方法的数值对比表明，所提方法具有极具竞争力的性能。

## Abstract
Hyperspectral and multispectral images fusion aims at integrating a low-resolution hyperspectral image (LR-HSI) and a high-resolution multispectral image (HR-MSI) to construct a high-resolution hyperspectral image (HR-HSI). It is generally assumed that spatial blurring operator and spectral response operator are prior-known. However, such an assumption is extremely restrictive in practice. To overcome this limitation, this paper formulates blind fusion as a coupled inverse problem, integrating blind deconvolution in the spatial domain with blind unmixing in the spectral domain. From this novel perspective, we propose a unified tensor fusion framework capable of flexible self-adjustment and real-time fusion without pre-training. We further introduce an optimization model for the joint estimation of the target HR-HSI, the spatial point spread function, and the spectral response function. To solve this model, we devise a partially linearized alternating direction method of multipliers (ADMM) algorithm with Moreau envelope smoothing, accompanied by the rigorous convergence analysis. An initialization estimator tailored to the specific characteristics of the fusion problem is proposed. Numerical comparisons with state-of-the-art methods on both synthetic and real-world datasets demonstrate the compelling performance of the proposed method.

---

## 论文详细总结（自动生成）

### 论文总结：盲高光谱与多光谱图像融合的统一张量框架

#### 1. 核心问题与整体含义
*   **研究背景**：在遥感领域，高光谱图像（HSI）具有极高的光谱分辨率但空间分辨率较低，而多光谱图像（MSI）空间分辨率高但光谱波段有限。融合两者以获取高时空分辨率图像（HR-HSI）是核心任务。
*   **核心问题**：现有的融合方法大多假设空间退化（点扩散函数 PSF）和光谱退化（光谱响应函数 SRF）是先验已知的（非盲融合）。但在实际应用中，这些参数往往未知，导致“盲融合”问题极具挑战性。
*   **研究动机**：开发一种无需预训练、具备物理可解释性、且能联合估计图像与退化算子的统一数学框架，以解决盲融合中的误差累积和参数估计难题。

#### 2. 方法论
*   **核心思想**：将盲融合建模为一个**耦合反问题**，即空间维度的“盲解卷积”与光谱维度的“盲解混”相结合。
*   **关键技术细节**：
    *   **统一张量框架**：利用张量积建模退化过程，将 HR-HSI 视为待恢复的核张量。
    *   **结构化约束**：
        *   **TTNN 正则化**：引入变换管状核范数（Transformed Tubal Nuclear Norm）来捕捉图像的低秩结构。
        *   **物理约束**：对 PSF 和 SRF 施加单位单纯形约束（非负且和为1），确保物理意义。
    *   **算法流程（Tenfuse）**：
        1.  **初始化**：采用基于回归的嵌套超锐化方法（Nested Hypersharpening）获取 HR-HSI 的初始估计。
        2.  **优化求解**：设计了一种**部分线性化 ADMM 算法**。
        3.  **Moreau 包络平滑**：针对模型中的非光滑项（如指示函数），引入 Moreau 包络进行平滑处理，使算法能够处理复杂的非凸非光滑目标函数。
        4.  **交替更新**：通过闭式解或投影梯度法交替更新图像张量、辅助变量、拉格朗日乘子以及 PSF/SRF 参数。

#### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：Washington DC Mall（HYDICE 传感器）、Chikusei（日本筑西市数据）。
    *   **真实数据集**：Hyperion/Sentinel-2（德国弗莱堡地区）、EnMAP/Sentinel-2（德国纽伦堡地区）。
*   **Benchmark 与对比方法**：
    *   对比了 9 种最先进（SOTA）方法，分为三类：
        *   **超锐化类**：AWLP, SM-SaBC, Nested-GSA。
        *   **学习类（深度学习）**：UDALN, SURE, ZSL。
        *   **模型类**：CNMF, Hysure, IR-TenSR。
*   **评价指标**：采用 PSNR、SAM（光谱角映射）、UIQI、ERGAS（全参考）以及 QNR、$D_\lambda$、$D_s$（无参考）等指标。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、内存大小等）。
*   **效率表现**：文中记录了运行时间（Time）。在处理大规模真实数据时，Tenfuse 的耗时（约 30-240 秒）远低于深度学习方法（通常需数千至数万秒），体现了较好的实时性潜力。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成到真实的 4 个主流数据集，实验设置包括了不同的下采样倍数和噪声环境。
*   **消融与鲁棒性实验**：
    *   分析了正则化参数 $\lambda_1$ 对光谱与空间质量平衡的影响。
    *   探讨了光谱混合窗口大小 $k_3$ 的敏感性。
    *   验证了 Moreau 包络参数 $\mu$ 的稳定性。
    *   进行了**带状噪声（Band-dependent noise）**的鲁棒性测试。
*   **评价**：实验设计全面，对比了不同技术路线（传统 vs 深度学习），且包含了收敛性的理论证明与数值验证，实验结果具有较高的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：Tenfuse 在合成数据集上的 PSNR 和 SAM 指标均优于对比方法，在真实数据上也展现了最高的 QNR 分数。
*   **无需预训练**：作为一种模型驱动的方法，它在处理跨传感器数据时比无监督学习方法更稳定且高效。
*   **收敛性保证**：理论证明了该算法生成的序列收敛至 Moreau 平滑问题的平稳点，迭代复杂度为 $O(\epsilon^{-4})$。

#### 7. 优点
*   **理论完备**：将盲融合提升到耦合反问题的理论高度，并给出了严谨的收敛性分析。
*   **物理可解释性**：模型参数（PSF, SRF）具有明确的物理含义，且约束条件符合传感器成像原理。
*   **灵活性**：支持多种变换矩阵（如 DFT, DWT），可根据数据特性自适应调整。

#### 8. 不足与局限
*   **假设简化**：假设 PSF 是波段无关的（Band-independent），在某些存在严重色差的传感器系统中可能不完全成立。
*   **边界效应**：采用循环卷积处理空间退化，在图像边缘可能存在伪影风险。
*   **超参数依赖**：虽然提供了参数建议，但在极端不同的场景下，$\lambda_1, \lambda_2$ 等超参数仍可能需要手动微调以达到最优效果。
*   **硬件信息缺失**：缺乏具体的算力环境描述，不利于其他研究者评估硬件成本。

（完）
