---
title: Co-Learning for Missing Arbitrary Modalities in Multi-modal Classification
title_zh: 多模态分类中缺失任意模态的协同学习
authors: "Francisco Mena, Dino Ienco, Roberto Interdonato, Cassio F. Dantas, Simon Besnard"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24683v1"
tags: ["query:multi-modal"]
score: 8.0
evidence: 多模态分类中任意缺失模态的共学习框架
tldr: 现实场景中传感器故障或隐私限制导致多模态训练与推理时模态不一致。现有方法多针对双模态缺失。本文提出多模态共学习框架，通过模态间协作传递信息，而非融合，处理任意模态缺失。两种方法分别在特征和决策层次利用互补信息。在分类基准上，第一种在单模态缺失时鲁棒性最佳，第二种在极端缺失（仅剩一模态）时更优，显著提升鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24683v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1177, \"height\": 683, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24683v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1145, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24683v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1255, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24683v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 952, \"height\": 736, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24683v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 951, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24683v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1275, \"height\": 821, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24683v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 416, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24683v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1272, \"height\": 569, \"label\": \"Table\"}]"
motivation: 现有方法多针对双模态缺失，依赖鲁棒融合，缺乏处理任意模态缺失的通用框架。
method: 提出基于共学习的框架，通过模态间信息传递，在特征和决策层面进行协作，无需预设缺失模式。
result: 在两基准上，第一种方法在单模态缺失时鲁棒性最佳，第二种在极端缺失时表现更优，显著优于基线。
conclusion: 本文共学习框架有效应对任意模态缺失，为多模态分类提供鲁棒新范式。
---

## 摘要
多模态分类利用不同数据源之间的互补信息来增强预测性能。然而，现实世界场景受到操作约束（如传感器故障或隐私限制）的影响，导致训练和推理时模态可用性不一致。为解决缺失模态问题，先前研究主要涵盖双模态数据设置，并专注于设计鲁棒的融合过程。相反，我们采用多模态协同学习框架，优先考虑模态间协作而非多模态融合。具体而言，我们考虑任何模态子集都可能缺失，而不假设预定义的缺失模态模式，我们将这种推理场景称为缺失任意模态。为应对这一挑战，我们引入了两种替代方法，分别利用特征层和决策层的信息。在两个多模态分类基准上的实验表明，在各种缺失模态条件下，我们的方法在鲁棒性上有显著提升。第一种方法在最小缺失条件（单个模态缺失）下表现更鲁棒，而第二种方法在极端缺失条件（仅剩一个模态）下表现更好。我们的代码可在 https://github.com/fmenat/Co4Miss 获取。

## Abstract
Multi-modal classification leverages complementary information across diverse data sources to enhance predictive performance. However, real-world scenarios subject to operational constraints, such as sensor failures or privacy restrictions, lead to inconsistent modality availability between training and inference times. To handle missing modalities, prior studies have mainly covered bimodal data setups and focused on designing robust fusion processes. Instead, we adopt a multi-modal co-learning framework that prioritizes inter-modal collaboration rather than multi-modal fusion. Specifically, we consider that any subset of modalities may be absent, without assuming predefined missing-modality patterns, an inference scenario we refer to as missing arbitrary modalities. To address this challenge, we introduce two alternative approaches that leverage information at both feature- and decision-level. Experiments on two multi-modal classification benchmarks demonstrate significant robustness gains in various missing modality conditions. The first method shows more robust behavior under minimal missing conditions, where a single modality is absent, whereas the second performs better under extreme missing conditions, where all-but-one modalities are missing. Our code is available at https://github.com/fmenat/Co4Miss.

---

## 论文详细总结（自动生成）

好的，以下是根据论文内容生成的结构化、深入、客观的中文总结。

---

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态分类模型在实际部署时，常常面临**任意模态子集在推理时缺失**的问题（如传感器故障、隐私限制、数据采集不连续等）。现有的方法大多针对**双模态**场景（如缺失一个模态）或假设预定义的缺失模式，对于**任意多模态缺失**（从单个缺失到仅剩一个模态）缺乏通用解决方案。
- **研究意义**：该问题在地球观测（EO）和人机交互等真实应用中普遍存在。解决它能够显著提高多模态模型在资源受限或传感器不可靠环境下的鲁棒性，从而支持更可靠的决策。
- **出发角度**：不同于以往专注于设计鲁棒融合过程（如ModDrop、跨模态重建）的思路，本文转向**多模态协同学习（Co-learning）** 范式，强调**模态间的知识协作与传递**，而非简单地融合。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于多模态协同学习和知识蒸馏，构建一个**既包含特征级协作又包含决策级蒸馏**的框架。框架不依赖鲁棒融合，而是通过**模态间的信息共享与预测对齐**来提升任意缺失条件下的分类性能。
- **关键技术细节**：
  - **模型框架**：每个模态使用专用编码器，提取**共享特征（shared）** 和**特定特征（specific）**，并拼接后通过专用线性头输出该模态的预测概率 $\hat{y}_m$。将所有可用模态的预测取平均得到融合预测（称为共识 $\hat{y}_{full}$）。
  - **损失函数设计**（共四个部分）：
    1. **主分类损失 $L_{main}$**：基于完整模态共识 $\hat{y}_{full}$ 与真实标签 $y$ 的交叉熵损失。
    2. **模态判别损失 $L_{mod}$**：使用一个辅助线性分类器 $P_{spe}$ 对每个模态的特定特征进行模态识别（交叉熵），迫使模型学习到模态特有的判别信息。
    3. **体积对比损失 $L_{cont}$**：采用 GRAM 多模态对比损失（基于 Gramian 体积），将不同模态的共享特征拉近，促进模态共享表征的学习。
    4. **决策级蒸馏损失**：本文提出了两种变体，构成两种方法：
       - **Co-Miss (Co-learning for Missing)**：采用**缺失蒸馏（Missing Distillation）**。训练时模拟所有 $2^M - 1$ 种缺失情况（ModDrop），通过损失 $L_{miss}$ 使缺失模态的融合预测 $\hat{y}_{miss}$ 同时拟合真实标签 $y$ 和完整模态共识 $\hat{y}_{full}$（知识蒸馏）。
       - **FullCo (Full Co-learning)**：采用**相互蒸馏（Mutual Distillation）**。对每个模态的单独预测 $\hat{y}_m$ 施加损失 $L_{mut}$，使其同时拟合真实标签 $y$ 和完整模态共识 $\hat{y}_{full}$，从而训练模态专用头在单模态下也能作出较好预测。
  - **优化**：Co-Miss 优化 $L_{total}=L_{main}+L_{mod}+L_{cont}+L_{miss}$；FullCo 优化 $L_{total}=L_{main}+L_{mod}+L_{cont}+L_{mut}$。

## 3. 实验设计

- **数据集**：
  - **Multi-CropHarvest**（作物类型识别）：包含4种模态（光学Sentinel-2、雷达Sentinel-1、气象、地形），约29,642个样本，10个类别。采用10折交叉验证。
  - **HL-Opportunity**（人体活动识别）：19种传感器模态（身体各部位佩戴的传感器），5类高层面活动（放松、咖啡时间等）。训练/验证/测试集划分：264,279/6,528/12,688个样本。
- **基准评估**：
  - **缺失条件**：分为**最小缺失**（仅缺1个模态）、**中度缺失**（缺多个但非全部）、**极端缺失**（仅剩1个模态）。评估每个缺失情况下的加权F1分数，并计算宏观平均值。
  - **对比方法**：
    - **基线**：单模态模型、特征融合（Feat）和决策融合（Dec）基线（缺失模态填零）。
    - **当前先进方法**（共7种）：EmbrNet、MissModal、ShaSpec、FCoM、SimMLM、DML、DSensD+。这些方法均适用于多模态设置，分别采用ModDrop、跨模态重建、相互蒸馏等策略。

## 4. 资源与算力

- 论文中**未明确说明**所使用的 GPU 型号、数量或训练时长。仅提及使用 AdamW 优化器，学习率 $10^{-3}$，batch-size 128，早停法（基于验证集F1）。因此，无法提供具体的算力资源信息。

## 5. 实验数量与充分性

- **实验数量**：共进行了**两大类实验**：
  - **主实验**：在两个数据集上，对每个缺失条件的所有可能缺失组合（Multi-CropHarvest 有15种缺失情况，HL-Opportunity 有 $2^{19}-1$ 种但报告中只给出了平均）进行了评估。
  - **消融实验**：在 Multi-CropHarvest 上，分别去掉 $L_{mod}$、$L_{cont}$、同时去掉两者、替换对比损失为 pairwise 版本、去掉决策级蒸馏损失等共约 **10种变体**。此外还比较了**缺失数量增加时的性能曲线**。
- **充分性与公平性**：
  - 实验设计覆盖了从最小到极端的所有缺失范围，对比了7种现代方法，统计显著性检验（Welch's t-test）确保了结果可比性。
  - 消融实验系统性地验证了各损失项的必要性和互补性，结论清晰。
  - 评估指标（加权F1）在类别不平衡数据集上合理。
  - 但在 HL-Opportunity 上仅报告了平均结果（由于缺失组合爆炸），未能像 Multi-CropHarvest 那样展示每种具体缺失情形的细节，略有限制。

## 6. 论文的主要结论与发现

- **主要结论**：Co-Miss 和 FullCo 两种方法能显著提升多模态分类在任意模态缺失下的鲁棒性，优于所有对比方法（在 Multi-CropHarvest 上统计学显著，在 HL-Opportunity 上与最佳方法无显著差异）。
- **互补优势**：
  - **Co-Miss**：在**完整模态和最小缺失**条件下表现最佳（如准确率仅下降12.1点 vs 基线的17.1点）。
  - **FullCo**：在**中等和极端缺失**条件下表现最佳（如极端缺失下F1下降23.9点，比Co-Miss的25.2点稍好）。
- **消融揭示的关键贡献**：
  - 特征级协作（特别是对比损失 $L_{cont}$）在**轻度缺失**时最关键。
  - 决策级蒸馏（$L_{miss}$ 或 $L_{mut}$）在**重度缺失**时最关键。
  - 体积对比损失优于标准pairwise对比损失。

## 7. 优点

- **问题定义清晰且实用**：首次将“缺失任意模态”作为统一场景处理，覆盖了从最小到极端的缺失范围，更贴合真实应用。
- **方法设计简洁而有效**：将协同学习与知识蒸馏巧妙结合，无需复杂的鲁棒融合机制，计算量合理。
- **特征与决策双层面协作**：特征级共享/专有特征学习 + 决策级蒸馏，互补性强。
- **实验全面且统计严谨**：使用了两个不同规模的传感器数据集，对比多种先进方法，并进行了消融和敏感性分析，结果可信。
- **代码已开源**：提升了可复现性。

## 8. 不足与局限

- **扩展性限制**：Co-Miss 方法在模态数量 $M$ 较大（如>10）时训练复杂度为 $O(2^M)$，组合爆炸，不适合高模态场景。论文建议 $M<10$ 时使用 Co-Miss，否则使用 FullCo；但 FullCo 在极端缺失下仍有一定性能损失。
- **领域泛化未知**：方法仅在传感器数据（EO、人体活动）上验证，未在图像-文本-音频等通用多模态基准（如CMU-MOSI）上测试，通用性有待验证。
- **模态间相关性假设**：方法假设模态间存在可学习的共享和专有特征，但未对**信息完全冗余或完全独立的极端情况**进行讨论或适应性建模。
- **缺失组合的评估完整性**：在 HL-Opportunity 数据集上，由于缺失组合过多，未报告具体每个缺失情形的结果，仅给出平均，难以分析具体缺失模式的影响。
- **未提供的资源细节**：缺乏训练时间、GPU模型等资源信息，影响复现和效率评估。

（完）
