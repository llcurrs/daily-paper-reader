---
title: "GraphPL: Leveraging GNN for Efficient and Robust Modalities Imputation in Patchwork Learning"
title_zh: GraphPL：利用图神经网络实现拼布学习中高效且稳健的模态插补
authors: "Xingjian Hu, Zuoyu Yan, Jianhua Zhu, Liangcai Gao, Fei Wang, Tengfei Ma"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25352v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 填补每个客户端的缺失模态
tldr: 在分布式多模态学习中，客户端常面临模态缺失问题，现有填补方法未能充分利用所有观测模态。本文提出GraphPL，将图神经网络与拼布学习结合，灵活整合全部可用模态并抵御噪声。在基准数据集和电子健康记录数据上，GraphPL均取得最优填补性能，学习到的特征有效支撑疾病预测等下游任务，为鲁棒、高效的模态填补提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25352v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2604.25352v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1762, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2604.25352v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 878, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2604.25352v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 579, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2604.25352v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1212, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2604.25352v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1158, \"height\": 781, \"label\": \"Table\"}]"
motivation: 现有拼布学习方法依赖部分观测模态，未能充分整合多模态信息，导致填补准确度受限，亟需更全面的融合策略。
method: 提出GraphPL框架，利用图神经网络灵活建模模态间关联，整合所有观测模态进行无监督填补，并引入机制增强对噪声输入的鲁棒性。
result: 在基准数据集上达到最先进性能；在真实电子健康记录数据中，GraphPL填补的模态学习出强下游特征，显著提升疾病预测效果。
conclusion: GraphPL通过图神经网络有效融合多模态信息，实现了高效、鲁棒的模态填补，为分布式场景下的多模态学习奠定了坚实基础。
---

## 摘要
当前关于分布式多模态学习的研究通常假设客户端可以访问所有模态的完整信息，但这在实际中可能并不成立。本文探讨了拼布学习，其中不同客户端可用的模态各异，目标是以无监督方式为每个客户端插补缺失的模态。现有方法未能充分利用模态信息，因为它们往往仅依赖于观察到的模态子集。为了解决这一问题，我们提出了GraphPL，它将图神经网络与拼布学习相结合，灵活集成所有观察到的模态，并在噪声输入下保持稳健。实验结果表明，GraphPL在基准数据集上达到了当时最优性能。我们在真实世界分布式电子健康记录数据集上的结果证明，GraphPL能学习强大的下游特征，并通过优越的模态插补实现疾病预测等任务。

## Abstract
Current research on distributed multi-modal learning typically assumes that clients can access complete information across all modalities, which may not hold in practice. In this paper, we explore patchwork learning, in which the modalities available to different clients vary, and the objective is to impute the missing modalities for each client in an unsupervised manner. Existing methods are shown not to fully utilize the modality information as they tend to rely on only a subset of the observed modalities. To address this issue, we propose GraphPL, which combines graph neural networks with patchwork learning to flexibly integrate all observed modalities and remains robust with noisy inputs. Experimental results show that GraphPL achieves SOTA performance on benchmark datasets. Our results on real-world distributed electronic health record dataset show GraphPL learns strong downstream features and enables tasks like disease prediction via superior modality imputation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在分布式多模态学习（拼布学习）场景中，不同客户端拥有的模态往往是不完整且异质的。如何在没有数据共享的条件下，**准确地为每个客户端插补缺失的模态**，是提升下游任务性能的关键。现有方法（如基于产品专家（POE）的融合）存在**模态坍缩问题**——过度依赖部分模态信息，导致插补质量差、对噪声敏感。
- **整体含义**：该工作属于**健康信息学与多模态学习交叉领域**，旨在为真实世界中隐私受限、模态缺失的分布式学习（如跨医院电子健康记录学习）提供一种有效、鲁棒的缺失模态生成方案，使得下游任务（如疾病预测、治疗推荐）能够从更完整的表征中获益。

### 2. 论文提出的方法论

- **核心思想**：利用**图神经网络（GNN）自适应地融合所有可观测模态**，为每个缺失模态生成高质量表征，从而缓解模态坍缩。
- **关键技术细节**：
  - **框架**：GraphPL 为每个模态配备一个独立的 VAE（变分自编码器）。训练时，在客户端本地轮流将一个可观测模态视为目标（需插补），其余作为条件模态。
  - **GNN 融合模块**：
    - 将条件模态视为图中节点，构建完全连接的“模态-模态图”；目标模态以虚拟节点形式加入，并与所有条件节点相连。
    - 融合模块由多个堆叠的模块组成，每个模块包含**分组图卷积（GCNConv）** + 两层 FFN，并采用**通道混洗**促进跨组信息流动，**LayerNorm** 稳定训练。
    - 节点嵌入：条件模态来自各自 VAE 编码器，目标模态从标准正态分布采样初始化。通过消息传递，为目标模态生成融合后的潜在特征。
  - **损失函数**：直接建模条件对数似然 \(\log p(x_{\text{target}} | x_{\text{cond}})\)，采用近似损失：
    \[
    L_{\text{impute}} = -\mathbb{E}_{q(z|x_{\text{cond}})} \log p(x_{\text{target}} | z)
    \]
    并结合单模态重构正则项 \(L_{\text{single}}\)（类似 β‑VAE），总损失 \(L_{\text{local}} = L_{\text{impute}} + \lambda L_{\text{single}}\)。
  - **联邦聚合**：客户端定期上传编码器、解码器及 GNN 参数，服务器通过 FedAvg 更新全局模型。
  - **推理**：利用 GNN 输出特征经对应解码器恢复缺失模态，或与观测模态特征拼接后用于下游任务。

### 3. 实验设计

- **数据集**：
  - 非分布式基准：**PolyMNIST**、**MST**、**Quad‑CelebA**（在原 Bimodal CelebA 基础上新增 Canny 边缘草图和人脸分割图，形成四模态）。
  - 真实世界分布式 EHR 数据集：**eICU**，以医院为客户端，包含人口统计、诊断（ICD‑9）、治疗、用药（HICL）、实验室值和生命体征等模态。
- **拼布划分**：
  - 基准数据集：通过类别不平衡采样或不同性别比例构造 5 个客户端。
  - eICU：随机从诊断、治疗、用药中每客户端丢弃一个模态，形成三个核心插补任务（疾病诊断、药物推荐、治疗推荐）。
- **对比方法**：MVAE、MMVAE、MoPoE、CLAP（后三者均代表当时先进的多模态 VAE 或拼布学习方法）。
- **评价指标**：
  - 生成质量（GQ）：预训练分类器判断插补模态的类标签是否与真值一致。
  - 表征质量（RQ）：在客户端上训练逻辑回归分类器，评估拼接表征的分类准确率（基准）或 AUPRC（eICU 的下游死亡率预测任务）。
- **实验设置**：5 次重复（相同拼布、不同数据划分），报告均值与标准差。

### 4. 资源与算力

- 论文**未明确提及**所用 GPU 型号、数量、显存以及单次训练的具体时长。
- 仅说明训练采用 Adam 优化器、batch size 256，但未给出硬件配置或训练时间。因此无法从现有信息评估其算力开销。

### 5. 实验数量与充分性

- **定量实验**：
  - 3 个基准数据集 × 5 种方法 × 2 个指标（GQ、RQ），共提供三张结果表（其中基准数据集汇总于表 1，eICU 不同医院数（10/20/50）的结果在表 2）。
  - eICU 上区分三种插补任务和一种下游任务，按医院数分组报告 5 个方法的 GQ 和 RQ。
- **鲁棒性分析**：
  - 在 PolyMNIST 上对不同模态施加递增噪声，展示各方法生成质量随噪声强度的变化（图 4），用以验证 GraphPL 的抗噪能力。
- **模态坍缩验证**：
  - 结合图 2 的示意及上述噪声实验，间接证明 GNN 融合减轻了模态坍缩。
- **充分性与公平性**：
  - 对比对象均为当时主流的多模态生成或拼布学习方法，评价指标客观。
  - 实验覆盖合成基准和真实分布式医疗数据，规模尚可。但**缺少消融实验**（如 GNN 组件替换、不同图结构的影响、有无通道混洗的对比等），且没有报告统计显著性检验。此外，仅在 eICU 的一种拼布模式下测试（随机丢弃诊断/治疗/用药），而其他模态的缺失情况未探讨。

### 6. 论文的主要结论与发现

- GraphPL 在所有基准数据集上均**取得最优的生成质量和表征质量**（比次优方法平均提升 9.2%）。
- 在真实 eICU 分布式环境中，GraphPL 在三个核心插补任务（疾病诊断、药物推荐、治疗推荐）的 AUPRC 上平均**超越最强基线 8.7%**，且下游死亡率预测亦有提升。
- GNN‑based 融合能**动态整合所有可观测信息**，有效克服 POE 类的模态坍缩问题，显著提高高噪声场景下的鲁棒性。

### 7. 优点：方法或实验设计上的亮点

- **新颖的融合视角**：首次在拼布学习中引入 GNN 进行模态间自适应融合，用图结构自然建模多模态关系，避免固定聚合策略。
- **理论上的合理性**：直接优化条件似然而非标准 ELBO，更贴合缺失插补的目标。
- **全面的实证验证**：同时涵盖合成图像数据集和真实 EHR 数据，并在分布式联邦设置下评估，展示了方法的通用性与实用性。
- **揭示并缓解模态坍缩**：通过可视化与噪声实验清晰说明 POE 方法的脆弱性，并给出针对性解决方案。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺少消融实验与组件分析**：未单独验证 GNN 模块的结构（层数、分组数、通道混洗等）对性能的贡献，也未分析 λ 等超参数的影响。
- **隐私与安全风险未深入讨论**：尽管在联邦学习框架下，但服务器聚合编码器/解码器参数仍可能泄露客户端数据分布，未提供差分隐私等保护机制的评估。
- **模态插补的假设较强**：训练时要求客户端至少拥有目标模态作为训练标签（局部回合反复轮换），若某些客户端完全缺失某一模态，则其解码器无法本地训练，文中未说明如何处理这种极端情况。
- **泛化到更多模态与更大规模系统的验证不足**：实验限定在 5 或少量模态，eICU 最多 50 个客户端，对更大规模、模态数量更多的场景扩展性未知。
- **通信成本未提及**：GNN 参数及编解码器共享会带来通信开销，文中未进行成本分析或压缩方法讨论。
- **偶然性风险**：5 次重复的均值和标准差不足以完全消除随机种子影响，且未进行统计显著性检验。

（完）
