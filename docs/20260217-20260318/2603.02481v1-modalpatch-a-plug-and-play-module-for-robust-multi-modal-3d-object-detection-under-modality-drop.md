---
title: "ModalPatch: A Plug-and-Play Module for Robust Multi-Modal 3D Object Detection under Modality Drop"
title_zh: ModalPatch：一种用于模态缺失下鲁棒多模态3D目标检测的即插即用模块
authors: "Shuangzhi Li, Lei Ma, Xingyu Li"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02481v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 任意模态丢失场景下的鲁棒检测
tldr: 针对自动驾驶中多模态3D目标检测在传感器失效（模态丢失）时的可靠性问题，本文提出了ModalPatch。这是一个即插即用的模块，无需修改架构或重新训练即可集成到现有框架中。它利用历史传感器数据的时序性来预测并补偿缺失特征，并结合不确定性引导的融合策略来动态评估特征可靠性，显著提升了检测器在各种模态丢失场景下的鲁棒性和准确性。
selection_source: fresh_fetch
motivation: 解决自动驾驶中因硬件故障或恶劣天气导致的传感器数据瞬时中断（模态丢失）所带来的安全风险。
method: 提出一种基于历史信息的特征预测模块和不确定性引导的跨模态融合策略，实现对缺失模态特征的动态补偿与可靠性评估。
result: 实验表明，ModalPatch 在多种模态丢失场景下均能显著增强现有最先进 3D 目标检测器的鲁棒性和检测精度。
conclusion: ModalPatch 为多模态 3D 检测提供了一种无需重训、灵活且高效的解决方案，有效应对了现实环境中的传感器失效挑战。
---

## 摘要
多模态3D目标检测对于自动驾驶至关重要，它集成了激光雷达（LiDAR）和摄像头等互补传感器。然而，其在现实世界中的可靠性面临着瞬时数据中断和缺失的挑战，其中模态可能因硬件故障、恶劣天气或遮挡而暂时丢失。这构成了严重的风险，特别是在多模态同时丢失的情况下，车辆会瞬间处于“失明”状态。为了解决这一问题，我们提出了 ModalPatch，这是首个旨在实现任意模态丢失场景下鲁棒检测的即插即用模块。无需更改架构或重新训练，ModalPatch 即可无缝集成到各种检测框架中。在技术上，ModalPatch 利用传感器数据的时序特性来保证感知连续性，通过一个基于历史信息的模块来预测并补偿暂时不可用的特征。为了提高预测特征的保真度，我们进一步引入了一种不确定性引导的跨模态融合策略，该策略能够动态评估补偿特征的可靠性，在增强信息信号的同时抑制偏差信号。大量实验表明，ModalPatch 在多种模态丢失条件下，均能持续提升最先进3D目标检测器的鲁棒性和准确性。

## Abstract
Multi-modal 3D object detection is pivotal for autonomous driving, integrating complementary sensors like LiDAR and cameras. However, its real-world reliability is challenged by transient data interruptions and missing, where modalities can momentarily drop due to hardware glitches, adverse weather, or occlusions. This poses a critical risk, especially during a simultaneous modality drop, where the vehicle is momentarily blind. To address this problem, we introduce ModalPatch, the first plug-and-play module designed to enable robust detection under arbitrary modality-drop scenarios. Without requiring architectural changes or retraining, ModalPatch can be seamlessly integrated into diverse detection frameworks. Technically, ModalPatch leverages the temporal nature of sensor data for perceptual continuity, using a history-based module to predict and compensate for transiently unavailable features. To improve the fidelity of the predicted features, we further introduce an uncertainty-guided cross-modality fusion strategy that dynamically estimates the reliability of compensated features, suppressing biased signals while reinforcing informative ones. Extensive experiments show that ModalPatch consistently enhances both robustness and accuracy of state-of-the-art 3D object detectors under diverse modality-drop conditions.

---

## 论文详细总结（自动生成）

以下是对论文《ModalPatch: A Plug-and-Play Module for Robust Multi-Modal 3D Object Detection under Modality Drop》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：在自动驾驶等实际应用中，多模态3D目标检测系统常面临“模态丢失”（Modality Drop）的挑战，即由于硬件故障、恶劣天气或遮挡导致传感器（如LiDAR或摄像头）数据瞬时中断。
*   **研究动机**：
    *   **现有局限性**：现有方法大多假设至少有一个模态可用（依赖性丢失），难以处理所有传感器同时失效的极端情况（独立性丢失）；且通常需要修改模型架构或进行昂贵的重新训练，缺乏灵活性。
    *   **目标**：开发一种**即插即用**、无需重训原模型、且能应对任意模态丢失场景（包括双模态同时丢失）的鲁棒性增强模块。

### 2. 方法论
ModalPatch 的核心思想是利用传感器数据的**时序连续性**进行特征补偿，并通过**不确定性估计**优化融合效果。
*   **基于历史的特征预测 (HFP)**：
    *   维护一个历史特征存储库（Memory Bank），记录过去 $\tau$ 帧的特征。
    *   使用**时序 Transformer**（基于可变形注意力机制）建模特征演化，根据历史信息预测当前缺失模态的特征。
    *   **损失函数**：使用 $L_2$ 损失（$L_{TemPred}$）监督预测特征与真实特征的一致性。
*   **不确定性引导的跨模态融合 (UCF)**：
    *   **不确定性估计**：利用轻量级 MLP 回归预测特征的空域方差（$\sigma^2$），作为可靠性度量。使用负对数似然损失（NLL）进行训练。
    *   **融合策略**：在跨模态注意力计算中引入不确定性图，通过 Softmax 归一化抑制高不确定性（不可靠）的区域，增强可信信号。
*   **训练与推理策略**：
    *   **两阶段训练**：先训练 HFP 捕获时序动态，再冻结 HFP 训练 UCF，防止噪声传播。
    *   **推理**：若模态存在则提取特征并更新存储库；若缺失则使用预测特征替代，确保时序流不中断。

### 3. 实验设计
*   **数据集**：使用大规模自动驾驶数据集 **nuScenes**（1000个场景）。
*   **评估指标**：平均精度（mAP）和 nuScenes 检测分数（NDS）。
*   **实验场景**：模拟随机独立的模态丢失，设置丢失率（Drop Rate）为 10%、30% 和 50%。
*   **对比基准 (Benchmark)**：
    *   **基础检测器**：BEVFusion（基于 BEV）、CMT（基于 Transformer）。
    *   **鲁棒性扩展版本**：UniBEV、MEFormer。
*   **对比方式**：对比各检测器在原始状态下与集成 ModalPatch 后的性能表现。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **训练细节**：
    *   Batch size 为 1。
    *   两个训练阶段各进行 12 个 Epoch。
    *   学习率设为 0.0002，使用 AdamW 优化器。
*   **推理速度**：集成后平均 FPS 从 5.33 降至 4.90，计算开销较小。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流检测器在 4 种丢失率下的表现，并进行了定性可视化分析。
*   **消融实验**：
    *   验证了 HFP 和 UCF 模块的各自贡献。
    *   对比了有无不确定性建模的融合效果。
    *   测试了单模态（仅 LiDAR 或仅 Camera）存在时的补偿能力。
*   **评价**：实验设计全面，覆盖了从轻微丢失到极端“全盲”的场景，对比实验对象包含最新的 SOTA 模型，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **显著提升鲁棒性**：在 50% 的高丢失率下，ModalPatch 为 CMT 带来了 +17.00% mAP 的巨大提升，为 UniBEV 提升了 +10.83% mAP。
*   **应对全盲场景**：在双模态同时丢失的极端时刻，ModalPatch 凭借时序预测能力仍能捕获目标实例，而基准模型几乎完全失效。
*   **保持原始性能**：在无模态丢失的正常情况下，ModalPatch 不会干扰原始特征，保持了检测器的原有精度。

### 7. 优点
*   **即插即用**：无需修改现有检测器的权重或架构，极易部署到不同框架中。
*   **时序补偿**：巧妙利用自动驾驶数据的时序特性，解决了瞬时传感器失效的问题。
*   **不确定性感知**：通过显式建模预测误差，有效防止了时序预测偏差在融合过程中的传播。

### 8. 不足与局限
*   **性能上限受限**：ModalPatch 的效果高度依赖于基础检测器在单模态下的固有能力。如果检测器本身在某一模态（如仅摄像头）表现极差，补偿效果也会受限。
*   **长时失效挑战**：虽然能处理瞬时丢失，但如果某一传感器长时间失效，时序预测的累积误差可能会增大。
*   **推理延迟**：虽然 FPS 下降不多，但在对实时性要求极高的毫秒级场景中，额外的 Transformer 计算仍需考虑。

（完）
