---
title: "UP-Fuse: Uncertainty-guided LiDAR-Camera Fusion for 3D Panoptic Segmentation"
title_zh: UP-Fuse：用于 3D 全景分割的不确定性引导激光雷达-相机融合
authors: "Rohit Mohan, Florian Drews, Yakov Miron, Daniele Cattaneo, Abhinav Valada"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19349v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 相机传感器退化和失效下的鲁棒融合
tldr: 针对激光雷达与摄像头融合在恶劣天气或传感器故障下鲁棒性不足的问题，本文提出 UP-Fuse 框架。该方法在 2D 距离视图中进行融合，通过不确定性引导模块动态调节跨模态交互，并利用混合 2D-3D Transformer 解决投影歧义。实验证明，该方法在传感器退化和校准偏移的情况下仍能保持高性能，显著提升了 3D 全景分割的可靠性。
selection_source: fresh_fetch
motivation: 现有的激光雷达-相机融合方法在相机传感器退化、故障或校准漂移时容易失效，严重影响感知系统的安全性。
method: 提出一种不确定性引导的融合模块，通过量化视觉退化下的表征差异来动态调节融合权重，并结合混合 2D-3D Transformer 进行解码。
result: 在多个主流基准测试中，UP-Fuse 在面对严重视觉损坏或传感器错位时仍能保持强劲性能，优于现有融合方法。
conclusion: UP-Fuse 通过引入不确定性感知机制，有效提升了多模态融合在极端环境下的鲁棒性，适用于安全关键的机器人感知场景。
---

## 摘要
激光雷达-相机融合通过利用相机图像补充稀疏的激光雷达扫描，增强了 3D 全景分割性能，但同时也引入了一种关键的失效模式。在恶劣条件下，相机传感器的退化或故障会显著损害感知系统的可靠性。为解决这一问题，我们提出了 UP-Fuse，这是一种在 2D 距离视图（range-view）中运行的新型不确定性感知融合框架，在相机传感器退化、校准漂移和传感器故障的情况下仍能保持鲁棒性。原始激光雷达数据首先被投影到距离视图中并由激光雷达编码器进行编码，同时提取相机特征并将其投影到相同的共享空间中。UP-Fuse 的核心采用了一个不确定性引导的融合模块，该模块利用预测的不确定性图动态调节跨模态交互。这些图通过量化各种视觉退化下的表示差异来学习，从而确保只有可靠的视觉线索会影响融合后的表示。融合后的距离视图特征由一种新型的混合 2D-3D Transformer 进行解码，该 Transformer 减轻了 2D 投影固有的空间歧义，并直接预测 3D 全景分割掩码。在 Panoptic nuScenes、SemanticKITTI 以及我们引入的 Panoptic Waymo 基准测试上进行的大量实验证明了 UP-Fuse 的有效性和鲁棒性。即使在严重的视觉损坏或失准情况下，它也能保持强大的性能，使其非常适用于安全至关重要的机器人感知场景。

## Abstract
LiDAR-camera fusion enhances 3D panoptic segmentation by leveraging camera images to complement sparse LiDAR scans, but it also introduces a critical failure mode. Under adverse conditions, degradation or failure of the camera sensor can significantly compromise the reliability of the perception system. To address this problem, we introduce UP-Fuse, a novel uncertainty-aware fusion framework in the 2D range-view that remains robust under camera sensor degradation, calibration drift, and sensor failure. Raw LiDAR data is first projected into the range-view and encoded by a LiDAR encoder, while camera features are simultaneously extracted and projected into the same shared space. At its core, UP-Fuse employs an uncertainty-guided fusion module that dynamically modulates cross-modal interaction using predicted uncertainty maps. These maps are learned by quantifying representational divergence under diverse visual degradations, ensuring that only reliable visual cues influence the fused representation. The fused range-view features are decoded by a novel hybrid 2D-3D transformer that mitigates spatial ambiguities inherent to the 2D projection and directly predicts 3D panoptic segmentation masks. Extensive experiments on Panoptic nuScenes, SemanticKITTI, and our introduced Panoptic Waymo benchmark demonstrate the efficacy and robustness of UP-Fuse, which maintains strong performance even under severe visual corruption or misalignment, making it well suited for robotic perception in safety-critical settings.

---

## 论文详细总结（自动生成）

### UP-Fuse: 用于 3D 全景分割的不确定性引导激光雷达-相机融合论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶等安全关键型任务中，3D 全景分割（结合语义和实例理解）至关重要。虽然激光雷达（LiDAR）与相机融合能显著提升性能，但现有的融合方法存在一个**关键失效模式**：当相机传感器在恶劣天气（如强光、黑夜）、传感器故障或校准漂移（机械错位）下退化时，融合系统的性能会剧烈下降，甚至低于纯 LiDAR 基准。
**研究动机**在于开发一种能够识别“何时该信任哪种模态”的融合范式，使系统在视觉信息不可靠时能自动减弱其影响，从而保证感知的鲁棒性和安全性。

#### 2. 方法论：核心思想与关键技术
UP-Fuse 提出了一种在 **2D 距离视图（Range-View, RV）** 空间中进行的不确定性感知融合框架。
*   **统一表征空间**：将 LiDAR 点云和多视图相机图像均投影并对齐到 2D 距离视图中，利用 Swin Transformer 提取多尺度特征。
*   **不确定性感知融合模块（Uncertainty-Aware Fusion Module）**：
    *   **量化不确定性**：通过轻量级 MLP 预测特征的不稳定性。训练时，对比原始图像特征与经过各种非空间增强（如亮度偏移、噪声、域匹配）后的特征，计算其 $L_2$ 范数作为地面真值，使用 Huber Loss 训练模型预测该差异。
    *   **引导交互**：利用预测的不确定性图动态调节可变形注意力（Deformable Attention）的权重。公式上，相机特征会乘以 $(1 - U_{C,s})$，其中 $U_{C,s}$ 是不确定性得分，从而抑制不可靠的视觉线索。
*   **混合 2D-3D 全景解码器（Hybrid 2D-3D Panoptic Decoder）**：
    *   针对 RV 投影带来的遮挡歧义和 360° 环绕断裂问题，设计了 **3D 感知掩码头（3D-Aware Mask Head）**。
    *   它在解码器末端通过 K 近邻（KNN）搜索 3D 空间中一致的邻居，聚合特征后再生成点级预测，有效缓解了“标签流血”和实例碎片化现象。

#### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **Panoptic nuScenes**：标准 360° 场景。
    *   **SemanticKITTI**：前视相机场景。
    *   **Panoptic Waymo**：论文作者新引入的基准，通过合并 Waymo Open Dataset 的语义和边界框标注生成，提供了更高分辨率的 LiDAR 数据。
*   **对比方法**：包括纯 LiDAR 方法（如 EfficientLPS, P3Former, IAL-L）和多模态融合方法（如 LCPS, IAL, Panoptic-FusionNet）。
*   **鲁棒性评估场景**：专门设计了传感器丢失（Dropout）、校准漂移（0°-5° 旋转偏差）和视觉域偏移（白天训练，黑夜测试）三种极端实验。

#### 4. 资源与算力
*   **硬件**：推理速度（FPS）是在单张 **NVIDIA A40 GPU** 上测量的。
*   **训练参数**：
    *   Panoptic nuScenes：Batch size 16，训练 80 epochs。
    *   Panoptic Waymo：Batch size 8，训练 48 epochs。
    *   SemanticKITTI：Batch size 4，训练 24 epochs。
*   **算力说明**：文中未明确给出总训练时长（如总 GPU 小时数），但提供了详细的优化器设置（AdamW）和学习率衰减策略。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了全面测试，并提供了测试集（Test Set）的提交结果。
*   **消融实验**：非常充分。涵盖了解码器设计（2D vs 3D 混合）、视图转换方法（稀疏 vs 稠密深度）、融合机制（连接 vs 注意力）、不确定性模块的作用、以及超参数 $K$（邻居数）和 Head 位置的影响。
*   **客观性**：实验设计考虑了公平性，例如在鲁棒性对比中，为所有基准模型都应用了相同的增强策略。结果显示 UP-Fuse 在标准性能上与 SOTA 持平，但在鲁棒性和效率（FPS 约快 6 倍）上具有显著优势。

#### 6. 主要结论与发现
*   **性能与效率平衡**：UP-Fuse 在 nuScenes 上达到 80.7% PQ，推理速度为 5.7 FPS，在保持高精度的同时显著优于其他融合方法的运行效率。
*   **鲁棒性卓越**：在相机完全失效时，UP-Fuse 的性能下降仅为 1.2% PQ，而其他融合方法下降超过 4%；在 5° 校准偏差下，其性能损失仅为 4.4%，远低于其他方法的 8% 以上。
*   **不确定性的价值**：实验证明，仅靠数据增强不足以获得鲁棒性，必须配合能够主动调节权重的架构设计。

#### 7. 优点
*   **鲁棒性设计**：通过显式建模 aleatoric 不确定性，解决了多模态感知系统在极端环境下的“信任”难题。
*   **解码器创新**：混合 2D-3D 解码器巧妙地结合了 RV 投影的计算效率和 3D 空间的几何一致性。
*   **贡献社区**：发布了 Panoptic Waymo 这一更高质量的多模态全景分割基准。
*   **高效率**：相比于基于 Voxel 或 Point 的复杂融合，RV 空间的处理速度更快，更符合实时机器人应用需求。

#### 8. 不足与局限
*   **校准依赖性**：虽然不确定性模块能抑制错位特征，但它属于“消极防御”，即通过忽略错误信息来维持性能，而不能主动纠正（Refine）外参偏差。
*   **性能上限**：在极端失效情况下，其性能上限被限制在纯 LiDAR 基准水平，无法在传感器失准时通过算法手段找回融合增益。
*   **数据集偏差**：虽然在三个数据集上验证，但主要针对自动驾驶场景，在室内或非结构化环境下的表现尚待验证。

（完）
