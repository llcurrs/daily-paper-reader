---
title: "MIRAGE: Knowledge Graph-Guided Cross-Cohort MRI Synthesis for Alzheimer's Disease Prediction"
title_zh: MIRAGE：知识图谱引导的跨队列 MRI 合成，用于阿尔茨海默病预测
authors: "Guanchen Wu, Zhe Huang, Yuzhang Xie, Runze Yan, Akul Chopra, Deqiang Qiu, Xiao Hu, Fei Wang, Carl Yang"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02434v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 将缺失 MRI 问题重构为跨模态潜空间蒸馏
tldr: 针对阿尔茨海默病诊断中MRI缺失的问题，本文提出MIRAGE框架。该框架利用生物医学知识图谱和图注意力网络将异构EHR变量映射到统一嵌入空间，并通过预训练的3D U-Net作为辅助正则化引擎，将解剖结构信息蒸馏至1D潜在表示中。MIRAGE在推理时无需重建3D图像，仅凭蒸馏后的诊断代理表示即可显著提升无MRI队列的AD预测准确率。
selection_source: fresh_fetch
motivation: 旨在解决临床诊断中MRI扫描成本高、缺失率高，以及从高维表格数据合成3D图像存在的技术挑战和临床风险。
method: 采用知识图谱引导的跨模态潜在蒸馏技术，利用冻结的3D U-Net作为正则化引擎，强制1D潜在表示编码宏观病理语义。
result: "实验表明，该框架在无真实MRI的队列中将阿尔茨海默病分类准确率比单模态基准提升了13%。"
conclusion: MIRAGE证明了通过解剖引导的特征蒸馏可以有效绕过昂贵的3D重建，实现跨队列的缺失模态补偿。
---

## 摘要
可靠的阿尔茨海默病（AD）诊断日益依赖于结合结构磁共振成像（MRI）和电子健康记录（EHR）的多模态评估。然而，由于 MRI 扫描成本高昂且在许多患者队列中经常缺失，模态缺失成为了部署这些模型的瓶颈。此外，从稀疏、高维的表格记录中合成全新的 3D 解剖扫描在技术上具有挑战性，并会带来严重的临床风险。为解决这一问题，我们提出了 MIRAGE，这是一个将 MRI 缺失问题重新定义为解剖引导的跨模态潜空间蒸馏任务的新型框架。首先，MIRAGE 利用生物医学知识图谱（KG）和图注意力网络将异构 EHR 变量映射到一个统一的嵌入空间，该空间可以从具有真实 MRI 的队列传播到没有 MRI 的队列。为了弥合语义鸿沟并增强物理空间感知，我们采用了一个冻结的预训练 3D U-Net 解码器，严格将其作为辅助正则化引擎。在一种新型的队列聚合跳跃特征补偿策略的支持下，该解码器充当了严格的结构惩罚，迫使一维潜表征编码具有生物学合理性的宏观病理语义。通过在推理过程中仅利用这种蒸馏出的“诊断替代”表征，MIRAGE 完全绕过了计算成本高昂的 3D 体素重建。实验表明，我们的框架成功弥合了模态缺失的差距，在没有真实 MRI 的队列中，与单模态基准相比，AD 分类率提高了 13%。

## Abstract
Reliable Alzheimer's disease (AD) diagnosis increasingly relies on multimodal assessments combining structural Magnetic Resonance Imaging (MRI) and Electronic Health Records (EHR). However, deploying these models is bottlenecked by modality missingness, as MRI scans are expensive and frequently unavailable in many patient cohorts. Furthermore, synthesizing de novo 3D anatomical scans from sparse, high-dimensional tabular records is technically challenging and poses severe clinical risks. To address this, we introduce MIRAGE, a novel framework that reframes the missing-MRI problem as an anatomy-guided cross-modal latent distillation task. First, MIRAGE leverages a Biomedical Knowledge Graph (KG) and Graph Attention Networks to map heterogeneous EHR variables into a unified embedding space that can be propagated from cohorts with real MRIs to cohorts without them. To bridge the semantic gap and enforce physical spatial awareness, we employ a frozen pre-trained 3D U-Net decoder strictly as an auxiliary regularization engine. Supported by a novel cohort-aggregated skip feature compensation strategy, this decoder acts as a rigorous structural penalty, forcing 1D latent representations to encode biologically plausible, macro-level pathological semantics. By exclusively utilizing this distilled "diagnostic-surrogate" representation during inference, MIRAGE completely bypasses computationally expensive 3D voxel reconstruction. Experiments demonstrate that our framework successfully bridges the missing-modality gap, improving the AD classification rate by 13% compared to unimodal baselines in cohorts without real MRIs.

---

## 论文详细总结（自动生成）

以下是对论文《MIRAGE: Knowledge Graph-Guided Cross-Cohort MRI Synthesis for Alzheimer's Disease Prediction》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在阿尔茨海默病（AD）诊断中，多模态数据（MRI + EHR）能显著提升预测准确率，但 MRI 扫描成本高、获取难，导致许多临床队列中存在严重的**模态缺失**问题。
*   **研究动机**：现有的缺失模态补全方法（如直接从表格数据合成 3D 图像）面临 EHR 数据稀疏异构、跨模态语义鸿沟大以及缺乏解剖结构约束等挑战。
*   **整体含义**：本文提出 MIRAGE 框架，将缺失 MRI 问题重构为**解剖引导的跨模态潜空间蒸馏**任务。其核心逻辑是不在推理时生成完整的 3D 图像，而是通过知识图谱和预训练解码器的约束，将 MRI 的结构先验“蒸馏”到 EHR 的特征表示中。

### 2. 方法论：核心思想与关键技术
MIRAGE 框架分为三个主要阶段：
*   **MRI 表征学习**：预训练一个 3D U-Net 自动编码器（AE），将真实 MRI 压缩到紧凑的潜空间（Latent Space）。训练完成后，冻结解码器作为后续阶段的“结构正则化引擎”。
*   **知识图谱（KG）引导的嵌入传播**：
    *   利用生物医学知识图谱（如 iBKH）作为语义锚点，通过 SapBERT 将患者的 EHR 变量与图谱中的医学概念链接。
    *   使用图注意力网络（GATv2）在图谱上进行信息传播，将有 MRI 队列的知识传递给仅有 EHR 的队列，从而估计缺失的 MRI 潜嵌入。
*   **适配器与解码器对齐（核心创新）**：
    *   **WC 变换**：使用白化-着色（Whitening-Coloring）变换对齐图传播嵌入与 AE 潜空间的分布。
    *   **队列聚合跳跃特征补偿**：由于 3D 解码器需要高频解剖细节（Skip Connections），MIRAGE 从训练集中检索 Top-K 个临床相似患者的跳跃特征进行加权聚合，为解码器提供“解剖画布”。
    *   **联合优化**：通过重建损失（L1、SSIM、TV 损失）和分类损失共同约束适配器，迫使 1D 潜向量编码宏观病理语义（如海马体萎缩）。

### 3. 实验设计
*   **数据集**：使用 ADNI 数据集，包含 1,175 名患者的 88 维 EHR 特征和 3D MRI 扫描。
*   **场景设置**：将数据集按 7:3 划分，测试集患者的 MRI 被屏蔽，模拟“仅有 EHR”的临床场景。
*   **Benchmark 与对比方法**：
    *   **单模态基准**：逻辑回归（LR）、随机森林（RF）、多层感知机（MLP）。
    *   **生成式基准**：直接 EHR-to-MRI 生成、MVAE（多模态变分自编码器）、条件扩散模型（Conditional Diffusion）。
    *   **上界参考**：使用真实 MRI 和 EHR+MRI 融合的性能作为理论上限。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到了使用 FSL-BET 进行颅骨剥离，图像配准至 MNI152 空间（1mm 等向性），最终体素大小为 182×218×182。代码已在匿名仓库开源。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：对比了 7 种以上的方法，涵盖了传统机器学习和前沿深度生成模型。
    *   **消融实验**：设计了四组实验（w/o KG, w/o GAT, w/o AE, w/o Adapter），验证了每个模块的必要性。
    *   **人类专家评估**：邀请两名生物医学专家对 50 组扫描（真实 vs 合成）进行盲测，评估解剖真实感、临床可用性和诊断解释性。
*   **充分性评价**：实验设计较为充分，不仅关注模型指标（BAcc, AUC），还通过专家评估验证了合成图像的临床生物标志物（如脑室扩大）的真实性，体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：MIRAGE 在无真实 MRI 的队列中，将 AD 分类准确率比最强的单模态 EHR 基准提升了 **13%**。
*   **跨模态有效性**：简单的生成模型（如扩散模型）在从低维表格数据合成高维 3D 图像时容易发生模式崩溃，而 MIRAGE 通过潜空间蒸馏成功绕过了这一难题。
*   **生物学一致性**：消融实验证明，诊断准确率的提升源于潜向量捕捉到了病理特征，而非简单复制邻近患者的解剖结构。

### 7. 优点（亮点）
*   **重构问题定义**：不追求视觉完美的图像重建，而是追求“诊断替代”表征的蒸馏，降低了计算开销并提高了推理效率。
*   **KG 解决异构性**：引入知识图谱作为语义桥梁，有效解决了不同医疗机构间 EHR 模式（Schema）不一致的问题。
*   **结构化约束**：巧妙地利用冻结的预训练解码器作为物理约束，确保了生成的特征具有生物学合理性。

### 8. 不足与局限
*   **外部验证缺失**：实验仅在 ADNI 单一数据集上完成，缺乏在其他独立临床队列（如不同种族、不同扫描协议）上的泛化性验证。
*   **任务局限性**：目前仅针对 AD 的二分类（正常 vs 患病），未探讨在轻度认知障碍（MCI）转化预测等更复杂临床任务中的表现。
*   **对 KG 的依赖**：模型性能高度依赖于知识图谱的质量和患者-概念链接的准确性，对于罕见病征或图谱未覆盖的特征可能失效。

（完）
