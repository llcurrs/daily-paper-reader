---
title: Small Target Detection Based on Mask-Enhanced Attention Fusion of Visible and Infrared Remote Sensing Images
title_zh: 基于可见光与红外遥感图像掩码增强注意力融合的小目标检测
authors: "Qianqian Zhang, Xiaolong Jia, Ahmed M. Abdelmoniem, Li Zhou, Junshe An"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06925v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 可见光与红外图像融合用于小目标检测
tldr: 针对遥感图像中目标小、背景复杂及跨模态对齐难的问题，本文提出轻量化可见光-红外融合网络ESM-YOLO+。通过掩码增强注意力融合（MEAF）模块实现像素级特征对齐与增强，并引入训练时结构表征（SR）增强辅助监督以保留细粒度空间结构。实验表明，该模型在保持高性能的同时大幅降低了参数量和计算量，非常适合实时部署。
selection_source: fresh_fetch
motivation: 遥感图像中小目标特征弱且易受背景干扰，现有算法在处理可见光与红外跨模态融合时面临对齐和尺度异构的挑战。
method: 提出MEAF模块利用可学习掩码和空间注意力进行像素级融合，并结合训练时结构表征增强技术提升特征判别力。
result: "在VEDAI和DroneVehicle数据集上分别达到84.71%和74.0%的mAP，且参数量较基准减少93.6%，计算量降低68.0%。"
conclusion: ESM-YOLO+在实现高精度小目标检测的同时显著提升了计算效率，为复杂遥感场景下的实时检测提供了高效实用的解决方案。
---

## 摘要
遥感图像中的目标通常具有尺寸小、纹理弱且易受复杂背景干扰的特点，这对通用算法的高精度检测提出了挑战。在前期 ESM-YOLO 的基础上，本研究提出了 ESM-YOLO+，这是一种轻量级的可见光-红外融合网络。为了增强检测性能，ESM-YOLO+ 包含两项关键创新：(1) 掩码增强注意力融合（MEAF）模块通过可学习的空间掩码和空间注意力在像素级融合特征，有效对齐了 RGB 和红外特征，增强了小目标的表征能力，并缓解了跨模态错位和尺度异质性问题；(2) 训练时结构表征（SR）增强提供了辅助监督，以在训练过程中保留细粒度的空间结构，在不增加额外推理成本的情况下提升了特征的可区分性。在 VEDAI 和 DroneVehicle 数据集上的大量实验验证了 ESM-YOLO+ 的优越性。该模型在 VEDAI 上达到了 84.71% 的 mAP，在 DroneVehicle 上达到了 74.0% 的 mAP，同时大幅降低了模型复杂度，其参数量比基准模型减少了 93.6%，GFLOPs 降低了 68.0%。这些结果证实了 ESM-YOLO+ 兼具强大的性能与实时部署的实用性，为复杂遥感场景下的高性能小目标检测提供了有效的解决方案。

## Abstract
Targets in remote sensing images are usually small, weakly textured, and easily disturbed by complex backgrounds, challenging high-precision detection with general algorithms. Building on our earlier ESM-YOLO, this work presents ESM-YOLO+ as a lightweight visible infrared fusion network. To enhance detection, ESM-YOLO+ includes two key innovations. (1) A Mask-Enhanced Attention Fusion (MEAF) module fuses features at the pixel level via learnable spatial masks and spatial attention, effectively aligning RGB and infrared features, enhancing small-target representation, and alleviating cross-modal misalignment and scale heterogeneity. (2) Training-time Structural Representation (SR) enhancement provides auxiliary supervision to preserve fine-grained spatial structures during training, boosting feature discriminability without extra inference cost. Extensive experiments on the VEDAI and DroneVehicle datasets validate ESM-YOLO+'s superiority. The model achieves 84.71\% mAP on VEDAI and 74.0\% mAP on DroneVehicle, while greatly reducing model complexity, with 93.6\% fewer parameters and 68.0\% lower GFLOPs than the baseline. These results confirm that ESM-YOLO+ integrates strong performance with practicality for real-time deployment, providing an effective solution for high-performance small-target detection in complex remote sensing scenes.

---

## 论文详细总结（自动生成）

### 论文总结：基于掩码增强注意力融合的可见光与红外遥感小目标检测 (ESM-YOLO+)

#### 1. 核心问题与研究动机
在无人机和卫星遥感影像中，目标通常具有**像素占比极小、纹理特征微弱、分布密集**且易受复杂背景干扰的特点。
*   **可见光（RGB）**：虽有丰富的纹理和形状，但在光照不足或恶劣天气下表现差。
*   **红外（IR）**：对热源敏感，抗干扰强，但缺乏细粒度细节。
*   **现有挑战**：跨模态融合存在特征异构、空间对齐不准、尺度不一等问题，且现有融合模型往往过于沉重，难以部署在资源受限的嵌入式平台上。
*   **研究目标**：开发一种既能精准对齐融合特征，又能保持极低计算开销的轻量化实时检测网络。

#### 2. 核心方法论
论文提出了 **ESM-YOLO+**，其核心思想是通过“掩码引导融合”和“训练时结构增强”来提升性能。
*   **掩码增强注意力融合模块 (MEAF)**：
    *   **可学习空间掩码**：通过卷积生成模态特有的空间掩码，对不可靠的交互区域进行抑制，实现软对齐。
    *   **空间注意力机制**：在掩码处理后，利用通道平均池化和最大池化提取空间权重，重新加权特征图，保留小目标的细粒度空间支撑。
    *   **像素级融合**：将处理后的RGB和IR特征拼接，并通过通道激励向量进行加权融合。
*   **训练时结构表征 (SR) 增强**：
    *   在训练阶段，在骨干网络中间层挂载一个基于 **EDSR** 的超分辨率辅助分支。
    *   **核心逻辑**：通过重建损失（L1 Loss）强制骨干网络学习并保留在下采样过程中易丢失的细粒度空间拓扑结构。
    *   **推理零成本**：该分支仅在训练时存在，推理时移除，不增加任何计算延迟。

#### 3. 实验设计
*   **数据集**：
    *   **VEDAI**：包含1246对RGB/IR图像，涵盖草地、公路、城市等复杂背景，目标极小。
    *   **DroneVehicle**：目前最大的跨模态车辆检测基准，包含28,439对图像，涵盖5类车辆。
*   **对比方法 (Benchmarks)**：
    *   通用检测器：YOLOv3, YOLOv4, YOLOv5 (s/m/l/x)。
    *   专用遥感/融合检测器：SuperYOLO, ESM-YOLO (基准), CFT, ICAFusion, MIR-YOLO, ACDF-YOLO 等。

#### 4. 资源与算力
*   **硬件环境**：AMD 7945 CPU 和 **NVIDIA RTX 4060 GPU**。
*   **训练配置**：使用 SGD 优化器，学习率 0.01，动量 0.937，权重衰减 0.0005。
*   **训练时长**：训练 300 个 epoch，由于显存限制，Batch Size 设置为 2。
*   **分辨率**：训练时使用 1024×1024，测试时使用 512×512（VEDAI）或原始分辨率（DroneVehicle）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度对比，包括与 SOTA 方法的精度对比、单/多模态对比、消融实验。
*   **充分性**：
    *   在两个主流数据集上验证，证明了泛化性。
    *   **消融实验**针对 MEAF 模块和 SR 增强分别验证，逻辑清晰。
    *   **公平性**：在相同硬件和超参数条件下对比了多种 YOLO 变体，实验设计较为客观。

#### 6. 主要结论与发现
*   **精度提升**：在 VEDAI 上达到 **84.71% mAP**（比基准提升 2.29%），在 DroneVehicle 上达到 **74.0% mAP**。
*   **极致轻量化**：参数量仅为 **5.1M**，相比基准模型减少了 **93.6%**；计算量（GFLOPs）仅为 **20.8G**，降低了 **68.0%**。
*   **融合优势**：MEAF 模块能有效消除阴影和复杂背景干扰，生成比传统 BEF 模块更高质量的融合特征。
*   **SR 价值**：证明了通过辅助监督可以在不改变推理架构的前提下，显著增强模型对小目标的判别力。

#### 7. 优点与亮点
*   **平衡性极佳**：在大幅削减参数量的同时，精度反而超越了许多重型模型（如基于 Transformer 的 CFT）。
*   **即插即用**：SR 增强策略为工业界提供了一个“免费”提升性能的思路（训练增强，推理不增重）。
*   **针对性强**：MEAF 模块直接回应了跨模态对齐和背景噪声抑制这两个核心痛点。

#### 8. 不足与局限
*   **极端环境验证不足**：实验主要基于公开数据集，未深入探讨在强雾、暴雨或极端低照度等极端气象条件下的鲁棒性。
*   **Batch Size 限制**：受限于显卡显存（RTX 4060），训练时的 Batch Size 极小（仅为 2），这可能影响 Batch Normalization 层的稳定性及最终收敛的最优性。
*   **实时性定义**：虽然 GFLOPs 低，但未给出在特定嵌入式平台（如 Jetson Orin）上的具体 FPS 数值。

（完）
