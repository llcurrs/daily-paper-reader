---
title: "SiMO: Single-Modality-Operable Multimodal Collaborative Perception"
title_zh: SiMO：单模态可运行的多模态协同感知
authors: "Jiageng Wen, Shengjie Zhao, Bing Li, Jiafeng Huang, Kenan Ye, Hao Deng"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08240v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 在模态失效期间自适应处理剩余模态特征
tldr: 本研究针对多模态协同感知中传感器失效（如LiDAR缺失）导致的语义不匹配问题，提出了SiMO框架。该框架引入长度自适应多模态融合（LAMMA）机制，在模态缺失时能自适应处理剩余特征并维持语义空间一致性。同时，通过创新的“预训练-对齐-融合-RD”训练策略解决了模态竞争问题，确保了各模态分支的独立性。SiMO在实现多模态高效对齐的同时，保留了单模态性能，显著提升了协同感知系统的鲁棒性。
selection_source: fresh_fetch
motivation: 解决多模态协同感知中因关键传感器缺失导致的特征语义不匹配以及现有方法在单模态下性能大幅下降的问题。
method: 提出SiMO框架，利用长度自适应多模态融合（LAMMA）和“预训练-对齐-融合-RD”策略来维持语义一致性并克服模态竞争。
result: 实验表明SiMO在有效对齐多模态特征的同时保留了模态特有信息，使其在各种单模态失效场景下均能保持最优性能。
conclusion: SiMO实现了首个在单模态下仍可高效运行的多模态协同感知系统，为提升自动驾驶感知系统的可靠性提供了新方案。
---

## 摘要
协同感知整合了多智能体视角，以扩大感知范围并克服遮挡问题。虽然现有的多模态方法利用互补传感器来提高性能，但它们极易发生故障——特别是当激光雷达（LiDAR）等关键传感器不可用时。其根本原因在于特征融合导致了单模态特征与下游模块之间的语义不匹配。本文在协同感知领域首次解决了这一挑战，引入了单模态可运行的多模态协同感知（SiMO）。通过采用所提出的长度自适应多模态融合（LAMMA），SiMO 可以在模态失效期间自适应地处理剩余的模态特征，同时保持语义空间的一致性。此外，利用创新的“预训练-对齐-融合-RD”（Pretrain-Align-Fuse-RD）训练策略，SiMO 解决了现有方法通常忽略的模态竞争问题，确保了每个独立模态分支的独立性。实验表明，SiMO 在有效对齐多模态特征的同时，保留了模态特有特征，使其能够在所有单个模态上保持最佳性能。实现细节可在 https://github.com/dempsey-wen/SiMO 找到。

## Abstract
Collaborative perception integrates multi-agent perspectives to enhance the sensing range and overcome occlusion issues. While existing multimodal approaches leverage complementary sensors to improve performance, they are highly prone to failure--especially when a key sensor like LiDAR is unavailable. The root cause is that feature fusion leads to semantic mismatches between single-modality features and the downstream modules. This paper addresses this challenge for the first time in the field of collaborative perception, introducing Single-Modality-Operable Multimodal Collaborative Perception (SiMO). By adopting the proposed Length-Adaptive Multi-Modal Fusion (LAMMA), SiMO can adaptively handle remaining modal features during modal failures while maintaining consistency of the semantic space. Additionally, leveraging the innovative "Pretrain-Align-Fuse-RD" training strategy, SiMO addresses the issue of modality competition--generally overlooked by existing methods--ensuring the independence of each individual modality branch. Experiments demonstrate that SiMO effectively aligns multimodal features while simultaneously preserving modality-specific features, enabling it to maintain optimal performance across all individual modalities. The implementation details can be found in https://github.com/dempsey-wen/SiMO.

---

## 论文详细总结（自动生成）

### 论文总结：SiMO - 单模态可运行的多模态协同感知

#### 1. 核心问题与研究动机
*   **研究背景**：多智能体协同感知（MACP）通过共享信息克服单车感知的遮挡和范围限制。现有的多模态方法（如结合 LiDAR 和相机）虽能提升精度，但存在“串联电路”效应：一旦某个关键传感器（如 LiDAR）失效，整个系统往往会崩溃。
*   **核心问题**：
    1.  **语义不匹配**：特征融合过程改变了特征空间，导致单模态特征无法适配为融合特征设计的下游任务头。
    2.  **模态竞争**：在联合训练中，易收敛的模态（如 LiDAR）会抑制难训练模态（如相机）的学习，导致单模态分支无法独立工作。
    3.  **异构失效**：在协同场景中，不同智能体可能面临不同的传感器故障，维持跨智能体的语义一致性极具挑战。

#### 2. 方法论
SiMO 旨在构建一个像“并联电路”一样的系统，核心思想是**在统一且无偏的特征空间中对齐并融合特征**。
*   **LAMMA（长度自适应多模态融合）**：
    *   采用基于注意力机制的结构，通过并行拼接 Query 并共享权重，使模块能自适应处理不同数量的输入。
    *   当某一模态缺失时，LAMMA 会自动退化为自注意力机制，无需显式切换模型。
    *   使用**加法融合**而非拼接（Concat），确保融合前后的特征在数学空间上保持一致，从而适配相同的下游检测头。
*   **PAFR 训练策略（预训练-对齐-融合-随机丢弃）**：
    1.  **Pretrain**：独立预训练各模态分支，确保每个分支都能提取完整的任务相关特征。
    2.  **Align**：冻结提取器，训练对齐模块（Aligner），将不同模态的 BEV 特征映射到 LAMMA 兼容的空间。
    3.  **Fuse**：冻结对齐器，训练融合模块和检测头。
    4.  **RD (Random Drop)**：通过随机丢弃模态进行微调，增强模型对传感器失效的鲁棒性。

#### 3. 实验设计
*   **数据集**：主要在 **OPV2V-H**（异构协同感知数据集）上验证，并在 **V2XSet**、**DAIR-V2X**（真实世界数据集）上进行了扩展验证。
*   **Benchmark 与对比方法**：
    *   **多模态基准**：BM2CP、BEVFusion（适配版）、UniBEV（适配版）。
    *   **单模态基准**：AttFusion（LiDAR/Camera）、HEAL (Pyramid Fusion)。
*   **评价指标**：不同 IoU 阈值下的平均精度（AP@30, 50, 70）。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   采用 Adam 优化器，Batch Size 为 1。
    *   训练过程分为多个阶段：预训练（20+20 epoch）、对齐（15+5 epoch）、融合（5 epoch）、RD 微调（5 epoch）。
    *   由于采用了冻结编码器的分步训练，显存需求相对较低，可在单张 3090 上完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量实验，涵盖了全模态、单模态（仅 LiDAR 或仅相机）、异构失效（部分车 LiDAR 坏，部分车相机坏）等多种组合。
*   **消融实验**：验证了 LAMMA 模块、PAFR 策略、RD 微调以及对齐器训练顺序对结果的影响。
*   **可视化分析**：通过 **t-SNE** 和 **Procrustes 分析**（普氏分析）量化了特征空间的对齐程度，证明了 LAMMA 能在保留模态特性的同时实现语义对齐。
*   **评价**：实验设计全面，对比了 SOTA 方法，并考虑了现实中复杂的异构失效场景，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **鲁棒性**：SiMO 是首个能在 LiDAR 完全失效（仅剩相机）时仍保持高效协同感知的框架。
*   **性能保持**：在全模态正常时，SiMO 达到了 SOTA 水平；在模态缺失时，其性能远超现有方法（现有方法在 LiDAR 缺失时 AP 几乎降为 0）。
*   **特征对齐**：加法融合配合 PAFR 策略能有效解决模态竞争，使相机分支在没有 LiDAR 辅助时也能发挥出应有的检测能力。

#### 7. 优点
*   **架构通用性**：LAMMA 是即插即用的，可以轻松集成到现有的协同感知框架（如 AttFusion 或 Pyramid Fusion）中。
*   **解决模态竞争**：通过分步训练而非复杂的梯度调节，提供了一种更确定、更简单的方法来平衡多模态学习。
*   **语义一致性**：成功解决了多模态融合中长期存在的“预融合”与“后融合”特征空间不一致的问题。

#### 8. 不足与局限
*   **训练流程复杂**：PAFR 策略涉及多个阶段，相比端到端训练，总训练时长较长。
*   **物理信息瓶颈**：在单视角相机场景（如 DAIR-V2X）下，受限于缺乏深度信息，单相机分支的性能上限仍受物理条件制约。
*   **噪声敏感度**：由于采用加法融合且追求特征独立性，系统对高强度传感器噪声的抑制能力可能弱于具有隐式平滑作用的卷积融合（需额外增加去噪模块）。

（完）
