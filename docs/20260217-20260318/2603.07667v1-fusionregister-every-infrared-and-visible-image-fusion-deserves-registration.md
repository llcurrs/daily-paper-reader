---
title: "FusionRegister: Every Infrared and Visible Image Fusion Deserves Registration"
title_zh: FusionRegister：每一对红外与可见光图像融合都值得配准
authors: "Congcong Bian, Haolong Ma, Hui Li, Zhongwei Shen, Xiaoqing Luo, Xiaoning Song, Xiao-Jun Wu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07667v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 红外与可见光图像融合的跨模态配准
tldr: 针对红外与可见光图像融合中的空间配准难题，本文提出FusionRegister。该方法通过学习跨模态失配表示而非强制对齐所有差异，实现了极高的鲁棒性。它直接作用于融合结果，能无缝集成多种融合算法，并利用融合骨干网络作为视觉先验以提高处理效率。实验证明，该方法在保持高水平融合质量的同时，显著提升了细节对齐精度，适用于多种实际感知场景。
selection_source: fresh_fetch
motivation: 现有的配准融合方法通常依赖复杂的预配准操作，导致处理效率低下且难以适配不同的融合框架。
method: 提出一种由视觉先验引导的通用配准方法，通过学习失配表示并利用融合网络提供的先验信息来精准对齐图像区域。
result: 在三个主流数据集上的实验结果显示，FusionRegister 在细节对齐和鲁棒性方面均优于现有技术，且具有良好的通用性。
conclusion: FusionRegister 为红外与可见光图像融合提供了一种高效、通用且鲁棒的配准方案，有效解决了跨模态对齐的瓶颈问题。
---

## 摘要
不同视觉模态间的空间配准是现实世界感知中多模态图像融合的关键但极具挑战性的步骤。尽管已经提出了几种方法来解决这一问题，但现有的基于配准的融合方法通常需要大量的预配准操作，限制了其效率。为了克服这些局限性，本文针对红外与可见光图像融合任务，提出了一种由视觉先验引导的通用跨模态配准方法，称为 FusionRegister。首先，FusionRegister 通过学习跨模态误配准表示，而非强制对齐所有差异来实现鲁棒性，从而确保即使在具有挑战性的输入条件下也能获得稳定的输出。此外，FusionRegister 通过直接作用于融合结果展现了强大的通用性，其中误配准被显式表示并得到有效处理，从而能够与多种融合方法无缝集成，同时保留其固有特性。此外，通过将骨干融合方法作为天然的视觉先验提供者，其效率得到了进一步提升，这引导配准过程仅关注失配区域，从而避免了冗余操作。在三个数据集上的广泛实验表明，FusionRegister 不仅继承了现有先进方法的融合质量，还提供了卓越的细节对齐和鲁棒性，使其非常适用于红外与可见光图像融合。代码将在 https://github.com/bociic/FusionRegister 上发布。

## Abstract
Spatial registration across different visual modalities is a critical but formidable step in multi-modality image fusion for real-world perception. Although several methods are proposed to address this issue, the existing registration-based fusion methods typically require extensive pre-registration operations, limiting their efficiency. To overcome these limitations, a general cross-modality registration method guided by visual priors is proposed for infrared and visible image fusion task, termed FusionRegister. Firstly, FusionRegister achieves robustness by learning cross-modality misregistration representations rather than forcing alignment of all differences, ensuring stable outputs even under challenging input conditions. Moreover, FusionRegister demonstrates strong generality by operating directly on fused results, where misregistration is explicitly represented and effectively handled, enabling seamless integration with diverse fusion methods while preserving their intrinsic properties. In addition, its efficiency is further enhanced by serving the backbone fusion method as a natural visual prior provider, which guides the registration process to focus only on mismatch regions, thereby avoiding redundant operations. Extensive experiments on three datasets demonstrate that FusionRegister not only inherits the fusion quality of state-of-the-art methods, but also delivers superior detail alignment and robustness, making it highly suitable for infrared and visible image fusion method. The code will be available at https://github.com/bociic/FusionRegister.

---

## 论文详细总结（自动生成）

以下是对论文《FusionRegister: Every Infrared and Visible Image Fusion Deserves Registration》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在红外与可见光图像融合（IVIF）中，传感器间的空间失配（Misalignment）会导致融合结果出现重影和伪影。
*   **现有瓶颈**：
    1.  **效率低下**：现有的配准融合方法通常需要复杂的预处理或全局配准，计算开销大。
    2.  **鲁棒性差**：许多方法依赖人工合成的变形数据进行训练，在面对真实世界的复杂失配时容易失效。
    3.  **通用性不足**：难以直接适配现有的各种高性能融合算法。
*   **核心意义**：本文提出了一种**后配准（Post-registration）**范式，主张“先融合，再局部纠偏”，利用融合结果作为视觉先验来引导配准，从而实现高效、通用且鲁棒的对齐。

### 2. 方法论
FusionRegister 包含三个协同阶段，核心思想是利用视觉先验定位并修复失配区域：
*   **失配定位（ML, Misregistration Localization）**：采用分层架构（类似 MIMO-UNet），通过比较源图像与融合图像的特征，估计概率图（表示失配位置）和变形场（表示失配程度）。
*   **位置配准（LR, Location Registration）**：采用**双向翘曲（Bi-directional Warping）**策略。不同于单向翘曲容易导致的边缘撕裂，该方法对称地补偿失配，确保对齐的精确性和结构完整性。
*   **模态保留模块（MRB, Modality Retainment Block）**：
    *   **核心技术**：利用轻量级的 **gMLP（Gated MLP）** 和相关层（Correlation Layer）来捕捉局部几何关系。
    *   **功能**：补偿空间变换过程中损失的纹理和对比度，通过双重注意力机制（可见光语义一致性和红外高频细节）恢复精细特征。
*   **损失函数**：结合了边缘损失（DoG 算子）、全局空间损失、频率损失（FFT）和细节损失（Sobel 算子），从空间和频率双域约束对齐质量。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 MSRS 和 M3FD 数据集中手动裁剪了 1,333 对完全对齐的图像块。
    *   **测试集**：MSRS (27对)、M3FD (21对) 和 LLVIP (20对)，均包含自然发生的真实失配。
*   **Benchmark 与对比方法**：
    *   **配准融合对比**：对比了 SemLA, MURF, IVFWSR, IMF, MulFS-CAP, C2RF 等 6 种最先进的配准融合方法。
    *   **通用性测试**：将 FusionRegister 集成到 5 种不同架构的融合模型中（CNN-based, GAN-based, Transformer-based, Diffusion-based, Mamba-based）。
*   **评价指标**：
    *   **融合质量**：EN, SF, AG, SD。
    *   **配准精度**：创新性地引入 **SAM（Segment Anything Model）** 提取物体掩码，计算 IoU 和 PR（准确率-召回率调和平均值）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练细节**：训练 5000 个 epoch，Batch size 为 20，图像块大小为 256×256。
*   **效率表现**：在 MSRS 数据集上，单对图像处理时间约为 **0.019秒**，参数量仅为 **2.94M**，在对比方法中处于领先水平（仅次于 IVFWSR，但效果远优于后者）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集，进行了 5 种融合算法的通用性验证，以及 6 种 SOTA 方法的横向对比。
*   **消融实验**：针对 MRB 模块、双向翘曲机制、网络深度以及不同算子（gMLP vs. Transformer vs. Deformable Conv）进行了详细测试。
*   **充分性评价**：实验设计非常充分，不仅验证了配准精度，还通过小提琴图展示了性能的稳定性（低方差），客观地证明了该方法在真实场景下的鲁棒性。

### 6. 主要结论与发现
*   **通用性极强**：FusionRegister 可以作为“插件”提升任何现有融合算法的性能，平均提升 IoU 约 5%。
*   **局部纠偏更高效**：验证了失配主要存在于模态共享区域，仅对这些区域进行后处理比全局预配准更有效。
*   **真实场景适应性**：相比于依赖合成变形的方法，FusionRegister 在处理未见过的真实失配数据时表现出极强的泛化能力。

### 7. 优点
*   **范式创新**：打破了传统的“先配准再融合”思路，提出的“后配准”显著降低了冗余计算。
*   **视觉先验引导**：巧妙地利用融合骨干网络作为先验提供者，使配准过程更具针对性。
*   **评估客观**：引入 SAM 进行配准精度评估，解决了 IVIF 领域缺乏配准真值（Ground Truth）的评价难题。
*   **轻量高效**：参数量小，推理速度快，适合实时应用。

### 8. 不足与局限
*   **初始对齐依赖**：该方法假设输入图像已经过粗略配准（Coarse Registration）。如果两路图像存在巨大的视点差异（如极大的平移或旋转），该方法可能无法达到理想效果。
*   **骨干网络依赖**：作为后处理框架，其最终效果在一定程度上受限于初始融合算法生成的视觉先验质量。

（完）
