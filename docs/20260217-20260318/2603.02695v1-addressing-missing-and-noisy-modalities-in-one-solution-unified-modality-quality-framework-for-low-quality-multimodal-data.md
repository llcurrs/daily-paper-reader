---
title: "Addressing Missing and Noisy Modalities in One Solution: Unified Modality-Quality Framework for Low-quality Multimodal Data"
title_zh: 一种方案解决缺失与噪声模态：面向低质量多模态数据的统一模态质量框架
authors: "Sijie Mai, Shiqin Han, Haifeng Hu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02695v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 同时处理缺失和噪声模态以增强鲁棒性
tldr: 针对现实场景中多模态数据存在的噪声和缺失问题，本文提出了统一模态质量（UMQ）框架。该框架将噪声和缺失视为统一的低质量问题，通过秩引导的质量评估器、跨模态质量增强器以及质量感知的混合专家网络（MoE），有效提升了低质量模态的表示能力。实验证明，UMQ在完整、缺失和噪声等多种数据设置下均优于现有先进方法。
selection_source: fresh_fetch
motivation: 现有的多模态研究通常将模态缺失和模态噪声分开处理，缺乏一种能够同时应对多种低质量数据场景的统一框架。
method: 提出UMQ框架，包含基于排序约束的质量评估器、利用跨模态信息的质量增强器，以及针对不同质量问题进行特定路由的质量感知MoE模块。
result: 在多个数据集上的实验结果显示，UMQ在模态完整、缺失及含噪声的各种复杂环境下，性能均一致优于当前最先进的基准模型。
conclusion: 该研究通过统一视角处理低质量模态，显著增强了多模态情感计算模型在现实不确定环境下的鲁棒性和泛化能力。
---

## 摘要
现实场景中遇到的多模态数据通常质量较低，其中噪声模态和缺失模态是严重阻碍模型性能和鲁棒性的典型形式。然而，以往的工作通常分别处理噪声模态和缺失模态。相比之下，我们共同处理缺失和噪声模态，以增强模型在低质量数据场景下的鲁棒性。我们将噪声和缺失模态视为一个统一的低质量模态问题，并提出了一个统一模态质量（UMQ）框架，以增强多模态情感计算中的低质量表示。首先，我们通过一种排序引导的训练策略，利用显式监督信号训练质量估计器，该策略通过添加排序约束来比较不同表示的相对质量，从而避免了由不准确的绝对质量标签引起的训练噪声。然后，为每个模态构建一个质量增强器，利用其他模态提供的样本特定信息以及定义的模态基准表示提供的模态特定信息来增强单模态表示的质量。最后，我们提出了一个具有特定路由机制的质量感知混合专家（mixture-of-experts）模块，以便更具针对性地解决多种模态质量问题。在完整、缺失和噪声模态的设置下，UMQ 在多个数据集上始终优于最先进的基准模型。

## Abstract
Multimodal data encountered in real-world scenarios are typically of low quality, with noisy modalities and missing modalities being typical forms that severely hinder model performance and robustness. However, prior works often handle noisy and missing modalities separately. In contrast, we jointly address missing and noisy modalities to enhance model robustness in low-quality data scenarios. We regard both noisy and missing modalities as a unified low-quality modality problem, and propose a unified modality-quality (UMQ) framework to enhance low-quality representations for multimodal affective computing. Firstly, we train a quality estimator with explicit supervised signals via a rank-guided training strategy that compares the relative quality of different representations by adding a ranking constraint, avoiding training noise caused by inaccurate absolute quality labels. Then, a quality enhancer for each modality is constructed, which uses the sample-specific information provided by other modalities and the modality-specific information provided by the defined modality baseline representation to enhance the quality of unimodal representations. Finally, we propose a quality-aware mixture-of-experts module with particular routing mechanism to enable multiple modality-quality problems to be addressed more specifically. UMQ consistently outperforms state-of-the-art baselines on multiple datasets under the settings of complete, missing, and noisy modalities.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **UMQ（Unified Modality-Quality Framework）** 的统一框架，旨在同时解决多模态情感计算（MAC）中常见的**模态缺失**和**模态噪声**问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实场景中，多模态数据（如视觉、听觉、文本）往往质量低下，表现为“模态缺失”（传感器故障、数据丢失）或“模态噪声”（环境干扰、传输失真）。
*   **研究动机**：
    *   **现有方法局限性**：以往研究通常将缺失和噪声视为两个独立问题分别处理，缺乏统一应对复杂低质量场景的能力。
    *   **质量评估难点**：难以定义绝对的模态质量标签，直接预测绝对质量得分容易引入训练噪声。
    *   **特征恢复缺陷**：现有的重构方法往往只利用现有模态生成缺失模态，导致生成的特征缺乏该模态特有的固有信息。

### 2. 方法论：UMQ 框架
UMQ 将缺失和噪声统一视为“低质量模态”问题，通过以下三个核心组件协同工作：

*   **秩引导的质量评估器 (Quality Estimator)**：
    *   **核心思想**：不预测绝对质量分，而是通过“排序”来学习。
    *   **技术细节**：利用显式监督信号（如高斯噪声设为0，低预测损失样本设为高分），并引入**秩引导训练策略 (Rank-guided strategy)**。通过比较不同样本间的相对预测损失来约束质量得分的顺序，避免了不准确绝对标签带来的干扰。
*   **质量增强器 (Quality Enhancer)**：
    *   **核心思想**：结合“样本特定”和“模态特定”信息进行特征修复。
    *   **技术细节**：
        *   **模态解耦**：将特征分解为样本特定（Sample-specific）和模态特定（Modality-specific）部分。
        *   **模态基准表示 (Modality Baseline)**：通过移动平均和可学习嵌入构建该模态的全局分布特征。
        *   **特征恢复**：利用其他模态的样本信息和本模态的基准信息，通过注意力机制增强低质量表示。
*   **质量感知混合专家网络 (MQ-MoE)**：
    *   **核心思想**：针对不同的质量组合（$2^{|M|}$ 种可能）采用专门的专家处理。
    *   **技术细节**：设计了特定的路由机制和约束损失（平衡损失、样本方差损失、同质质量约束损失），确保具有相同质量配置（如：文本好、音视频差）的样本被路由到相似的专家模块。

### 3. 实验设计
*   **数据集**：
    *   **情感分析 (MSA)**：CMU-MOSI, CMU-MOSEI, CH-SIMS。
    *   **幽默检测 (MHD)**：UR-FUNNY。
    *   **讽刺检测 (MSD)**：MUStARD。
*   **Benchmark 与对比方法**：
    *   对比了 15+ 种 SOTA 方法，包括 MFM, Self-MM, C-MIB, GCNet, IMDer, MoMKE 等。
    *   甚至对比了最新的多模态大模型 (MLLMs)，如 Qwen2.5-Omni, HumanOmni, MiniCPM-o 等。
*   **实验场景**：
    *   **完整模态**：验证基础性能。
    *   **缺失模态**：设置缺失率 (MR) 从 0.1 到 0.7。
    *   **噪声模态**：设置噪声率 (NR) 从 0.1 到 0.7，并测试了拉普拉斯噪声和随机擦除等分布外 (OOD) 噪声。

### 4. 资源与算力
*   **硬件环境**：论文明确指出使用了一台 **NVIDIA RTX 2080Ti GPU**。
*   **软件环境**：CUDA 11.6, PyTorch 1.13.1。
*   **训练细节**：使用了 AdamW 优化器，学习率在 5e-6 到 1e-5 之间，针对每个数据集进行了 50 次随机网格搜索以确定超参数。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流数据集上进行了测试，涵盖了分类、回归等多种任务。
*   **充分性**：
    *   **多场景覆盖**：不仅做了标准测试，还详细讨论了不同缺失率和噪声率下的表现。
    *   **消融实验**：针对质量评估、秩引导训练、质量增强、解耦操作、MQ-MoE 等每个组件都做了详尽的消融分析。
    *   **可视化与案例研究**：通过 t-SNE 可视化增强后的特征，并展示了具体样本的质量得分与专家路由情况。
*   **客观性**：实验采用了多次运行取平均值的方法，并对比了多种类型的基准模型，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：UMQ 在所有数据集和所有质量设置（完整、缺失、噪声）下均达到了 SOTA 性能。
*   **统一方案的有效性**：将缺失视为噪声的一种特殊形式是可行的，统一框架能显著提升模型在复杂现实环境下的鲁棒性。
*   **质量评估的重要性**：秩引导的训练策略比直接回归绝对质量标签更有效。
*   **大模型对比**：虽然 MLLMs 具有很强的通用能力，但在特定任务（如情感分析）上，经过专门设计的轻量化模型（如 UMQ）在微调后仍具有明显优势。

### 7. 优点与亮点
*   **创新视角**：首次提出将模态缺失和噪声统一在一个框架下处理，具有很强的实用价值。
*   **训练策略巧妙**：引入排序损失解决质量标签难以定义的问题，非常符合多模态数据的特性。
*   **特征恢复更全面**：通过引入“模态基准表示”，解决了生成特征缺乏模态固有信息的问题。
*   **架构自适应**：MQ-MoE 能够根据输入质量动态调整处理逻辑，提高了模型的灵活性。

### 8. 不足与局限
*   **计算开销**：虽然使用了 MoE 结构，但引入多个专家模块和复杂的解耦/增强操作会增加推理时的计算负担。
*   **噪声模拟单一**：训练时主要依赖高斯噪声来模拟低质量，虽然测试了 OOD 噪声，但真实世界中的噪声类型可能更加复杂多变。
*   **超参数敏感性**：框架包含多个辅助损失函数（如 $\beta_{de}, \beta_{est}, \beta_{EH}, \beta_{moe}$），超参数的调优过程可能较为繁琐。

（完）
