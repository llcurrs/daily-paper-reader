---
title: Adaptive Clinical-Aware Latent Diffusion for Multimodal Brain Image Generation and Missing Modality Imputation
title_zh: 面向多模态脑图像生成与缺失模态插补的自适应临床感知潜在扩散模型
authors: "Rong Zhou, Houliang Zhou, Yao Su, Brian Y. Chen, Yu Zhang, Lifang He, Alzheimer's Disease Neuroimaging Initiative"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09931v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 通过自适应临床感知扩散合成缺失的脑成像模态
tldr: 针对阿尔茨海默病诊断中多模态神经影像数据缺失的问题，本文提出ACADiff框架。该框架利用自适应临床感知潜扩散模型，结合GPT-4o编码的临床元数据，实现了sMRI、FDG-PET和AV45-PET之间的双向合成。实验表明，ACADiff在极高缺失率下仍能保持优异的生成质量和诊断性能，显著优于现有基准。
selection_source: fresh_fetch
motivation: 临床数据集中多模态神经影像的频繁缺失严重阻碍了阿尔茨海默病的准确诊断和研究。
method: 提出ACADiff框架，通过自适应融合机制和GPT-4o编码的临床语义引导，在潜空间内进行多模态图像的去噪与合成。
result: "在ADNI数据集上，该方法在高达80%的数据缺失情况下依然实现了领先的生成质量和稳健的诊断性能。"
conclusion: ACADiff为处理不完整多模态医学影像数据提供了一种高效且具备临床感知能力的生成式解决方案。
---

## 摘要
多模态神经影像为阿尔茨海默病的诊断提供了互补的见解，但临床数据集经常面临模态缺失的问题。我们提出了 ACADiff，这是一个通过自适应临床感知扩散合成缺失脑成像模态的框架。ACADiff 通过在关注可用影像数据和临床元数据的同时逐步对潜在表示进行去噪，学习不完整多模态观测值与目标模态之间的映射。该框架采用了根据输入可用性动态重构的自适应融合，并结合了通过 GPT-4o 编码提示提供的语义临床指导。三个专门的生成器实现了 sMRI、FDG-PET 和 AV45-PET 之间的双向合成。在 ADNI 受试者上的评估结果表明，ACADiff 实现了卓越的生成质量，即使在 80% 缺失的极端情况下也能保持稳健的诊断性能，优于所有现有基准模型。为了提高可复现性，代码可在 https://github.com/rongzhou7/ACADiff 获取。

## Abstract
Multimodal neuroimaging provides complementary insights for Alzheimer's disease diagnosis, yet clinical datasets frequently suffer from missing modalities. We propose ACADiff, a framework that synthesizes missing brain imaging modalities through adaptive clinical-aware diffusion. ACADiff learns mappings between incomplete multimodal observations and target modalities by progressively denoising latent representations while attending to available imaging data and clinical metadata. The framework employs adaptive fusion that dynamically reconfigures based on input availability, coupled with semantic clinical guidance via GPT-4o-encoded prompts. Three specialized generators enable bidirectional synthesis among sMRI, FDG-PET, and AV45-PET. Evaluated on ADNI subjects, ACADiff achieves superior generation quality and maintains robust diagnostic performance even under extreme 80\% missing scenarios, outperforming all existing baselines. To promote reproducibility, code is available at https://github.com/rongzhou7/ACADiff

---

## 论文详细总结（自动生成）

### 论文总结：ACADiff —— 面向多模态脑图像生成与缺失模态插补的自适应临床感知潜在扩散模型

#### 1. 核心问题与研究动机
*   **核心问题**：在阿尔茨海默病（AD）的研究中，多模态神经影像（如 sMRI、FDG-PET、AV45-PET）能提供互补的病理信息，但现实临床数据常因成本、采集协议或患者流失导致**模态缺失**，严重影响诊断准确性。
*   **研究动机**：现有的生成模型（如 GAN 或标准扩散模型）在处理缺失模态时存在三个局限：
    1.  缺乏针对不同输入组合（如 1 补 2 或 2 补 1）的自适应融合机制；
    2.  对临床元数据（如认知评分）的利用不足；
    3.  缺乏对医疗元数据的语义理解。

#### 2. 方法论
ACADiff 框架基于潜在扩散模型（Latent Diffusion），核心思想是在压缩的潜在空间内，结合可用影像和临床语义引导进行图像合成。
*   **潜在空间构建**：使用预训练的 3D VAE 将高维脑图像压缩为紧凑的潜在表示，降低计算开销。
*   **分层自适应条件（核心技术）**：
    *   **自适应影像调节**：根据输入模态的数量动态切换。若有两个输入，使用**交叉注意力（Cross-Attention）**进行模态间信息交换；若只有一个输入，则使用**可学习投影（Projection）**。
    *   **语义临床指导**：利用 **GPT-4o** 将疾病诊断和认知评分（MMSE, ADAS13, CDR-SOB）编码为结构化文本提示（Prompt），通过交叉注意力机制引导去噪过程，确保生成的图像符合病理特征。
    *   **时间步调制**：通过 FiLM 机制根据去噪时间步 $t$ 动态调整网络层的激活。
*   **训练目标**：结合了噪声预测损失（$L_{diff}$）和一致性正则化损失（$L_{cons}$），以确保生成图像与真实分布对齐。

#### 3. 实验设计
*   **数据集**：使用 ADNI 队列的 1,028 名受试者（包括 AD、MCI 和健康对照 HC），涵盖 sMRI、FDG-PET 和 AV45-PET 三种模态。
*   **实验场景**：
    *   **生成任务**：评估 sMRI、FDG-PET 和 AV45-PET 之间的双向合成质量。
    *   **分类任务**：在 20%、40%、60%、80% 四种不同的缺失率下，评估插补后的图像对 AD 诊断的贡献。
*   **对比方法（Benchmark）**：
    *   GAN 类：Pix2Pix, DS-GAN。
    *   扩散模型类：LDM, PASTA, FICD。
    *   消融对比：ACADiff-emb（使用普通嵌入而非 GPT-4o 编码）。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练细节**：使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，扩散步数 $T=1000$。
*   **时长**：文中未明确提及具体的训练总时长，但指出使用了预训练且冻结的 3D Autoencoder 以减少计算量。

#### 5. 实验数量与充分性
*   **实验规模**：基于 1,028 例样本，划分为生成器训练集（600 例）和分类实验集（428 例）。
*   **充分性**：
    *   涵盖了从轻微（20%）到极端（80%）的缺失场景，实验设置较为全面。
    *   采用了 10 折蒙特卡洛采样进行推理，增加了结果的统计稳健性。
    *   对比了多种最先进的（SOTA）生成模型，并进行了临床语义编码的消融实验，证明了 GPT-4o 引入的语义信息的有效性。

#### 6. 主要结论与发现
*   **生成质量**：ACADiff 在 PSNR、SSIM、NMI 等指标上全面超越基准模型，PSNR 达到 27.9。
*   **诊断性能**：在 20% 缺失时，诊断准确率达到 89.4%（接近全数据预期的 92%）；在 **80% 极端缺失**下，仍能保持 77.5% 的准确率，而传统方法在此时基本失效。
*   **语义价值**：使用 GPT-4o 编码比普通 Embedding 提升了约 1.8 PSNR，证明了大型语言模型在理解临床上下文方面的优势。

#### 7. 优点
*   **灵活性强**：单一模型即可处理多种输入组合（1→1 或 2→1），无需为每种缺失情况单独训练。
*   **临床感知**：首次将 LLM 编码的复杂临床语义（非简单标签）引入扩散模型，增强了生成图像的病理一致性。
*   **鲁棒性高**：在极高数据缺失率下表现出极强的性能韧性。

#### 8. 不足与局限
*   **计算成本**：尽管使用了潜在空间，但 3D 扩散模型的推理速度通常仍慢于 GAN。
*   **数据集局限**：实验仅在 ADNI 单一数据集上完成，跨中心、跨设备的泛化能力尚待验证。
*   **LLM 依赖**：性能部分依赖于 GPT-4o 的编码能力，对于无法访问此类 API 的环境可能存在限制。
*   **篇幅限制**：作为 4 页短论文，部分实现细节（如 VAE 的具体架构、不同模态生成的定性对比图）展示有限。

（完）
