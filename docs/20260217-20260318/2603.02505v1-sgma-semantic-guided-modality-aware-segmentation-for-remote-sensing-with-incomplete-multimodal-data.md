---
title: "SGMA: Semantic-Guided Modality-Aware Segmentation for Remote Sensing with Incomplete Multimodal Data"
title_zh: SGMA：面向不完全多模态遥感数据的语义引导模态感知分割
authors: "Lekang Wen, Liang Liao, Jing Xiao, Mi Wang"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02505v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 遥感中的不完整多模态数据与模态缺失
tldr: 针对遥感中模态缺失导致的分割难题，本文提出语义引导模态感知（SGMA）框架。该框架通过SGF模块提取类别原型以减少类内差异并协调跨模态冲突，并利用MAS模块动态调整训练权重以解决模态不平衡。实验证明，SGMA在多个数据集上均优于现有方法，显著提升了脆弱模态的分割性能。
selection_source: fresh_fetch
motivation: 现有的不完整多模态语义分割方法难以处理模态不平衡、类内差异及跨模态异质性，且容易丢失模态特有信息。
method: 提出SGMA框架，包含利用类别原型进行自适应融合的SGF模块，以及根据模态鲁棒性动态重采样训练样本的MAS模块。
result: 在多个遥感数据集和骨干网络上的实验表明，SGMA在处理缺失模态时表现优异，尤其在脆弱模态上提升显著。
conclusion: SGMA通过语义引导和模态感知机制，有效解决了IMSS中的不平衡与异质性挑战，为鲁棒的遥感多模态分割提供了新方案。
---

## 摘要
多模态语义分割整合了来自不同传感器的互补信息，用于遥感地球观测。然而，实际系统常因传感器故障或覆盖不全而面临模态缺失问题，这被称为不完全多模态语义分割（IMSS）。IMSS 面临三个关键挑战：(1) 多模态不平衡，即主导模态抑制了脆弱模态；(2) 跨模态在尺度、形状和方向上的类内差异；(3) 跨模态异质性，即冲突的线索产生不一致的语义响应。现有方法依赖对比学习或联合优化，这存在过度对齐（丢弃模态特定线索）或训练不平衡（偏向鲁棒模态）的风险，同时在很大程度上忽视了类内差异和跨模态异质性。为了解决这些局限性，我们提出了语义引导模态感知（SGMA）框架，该框架通过语义引导确保平衡的多模态学习，同时减少类内差异并调和跨模态不一致性。SGMA 引入了两个互补的即插即用模块：(1) 语义引导融合（SGF）模块，提取多尺度、类别的语义原型，以捕获跨模态的一致类别表示，基于原型-特征对齐评估各模态的鲁棒性，并根据鲁棒性得分进行自适应融合，以减轻类内差异和跨模态异质性；(2) 模态感知采样（MAS）模块，利用 SGF 的鲁棒性评估动态重新加权训练样本，优先处理来自脆弱模态的挑战性样本，以解决模态不平衡问题。在多个数据集和骨干网络上的广泛实验表明，SGMA 始终优于现有最先进的方法，在脆弱模态上的提升尤为显著。

## Abstract
Multimodal semantic segmentation integrates complementary information from diverse sensors for remote sensing Earth observation. However, practical systems often encounter missing modalities due to sensor failures or incomplete coverage, termed Incomplete Multimodal Semantic Segmentation (IMSS). IMSS faces three key challenges: (1) multimodal imbalance, where dominant modalities suppress fragile ones; (2) intra-class variation in scale, shape, and orientation across modalities; and (3) cross-modal heterogeneity with conflicting cues producing inconsistent semantic responses. Existing methods rely on contrastive learning or joint optimization, which risk over-alignment, discarding modality-specific cues or imbalanced training, favoring robust modalities, while largely overlooking intra-class variation and cross-modal heterogeneity. To address these limitations, we propose the Semantic-Guided Modality-Aware (SGMA) framework, which ensures balanced multimodal learning while reducing intra-class variation and reconciling cross-modal inconsistencies through semantic guidance. SGMA introduces two complementary plug-and-play modules: (1) Semantic-Guided Fusion (SGF) module extracts multi-scale, class-wise semantic prototypes that capture consistent categorical representations across modalities, estimates per-modality robustness based on prototype-feature alignment, and performs adaptive fusion weighted by robustness scores to mitigate intra-class variation and cross-modal heterogeneity; (2) Modality-Aware Sampling (MAS) module leverages robustness estimations from SGF to dynamically reweight training samples, prioritizing challenging samples from fragile modalities to address modality imbalance. Extensive experiments across multiple datasets and backbones demonstrate that SGMA consistently outperforms state-of-the-art methods, with particularly significant improvements in fragile modalities.

---

## 论文详细总结（自动生成）

### SGMA：面向不完全多模态遥感数据的语义引导模态感知分割论文总结

#### 1. 论文的核心问题与研究动机
*   **核心问题**：针对遥感领域中的**不完全多模态语义分割（IMSS）**问题。在实际应用中，由于传感器故障或覆盖不全，多模态数据（如 RGB、DSM、NIR、SAR）经常出现模态缺失。
*   **研究动机**：现有的多模态分割方法在模态完整时表现良好，但在模态缺失时性能剧增。IMSS 面临三大挑战：
    1.  **模态不平衡**：主导模态（如 RGB）会抑制脆弱模态（如 DSM、SAR）的学习。
    2.  **类内差异**：同类物体在不同模态下的尺度、形状和方向存在巨大差异。
    3.  **跨模态异质性**：不同传感器对同一语义目标的响应可能存在冲突。
*   **现有局限**：对比学习或联合优化方法容易导致过度对齐（丢失模态特性）或训练不平衡，且忽视了类内差异。

#### 2. 论文提出的方法论
论文提出了 **SGMA（Semantic-Guided Modality-Aware）** 框架，包含两个核心即插即用模块：
*   **语义引导融合模块（SGF）**：
    *   **核心思想**：利用全局语义原型作为“锚点”来统一异质特征。
    *   **技术细节**：首先通过模态特定投影器（MP）将特征转换到统一空间；接着利用类别感知语义过滤器（CSF）提取各模态的**全局语义原型**；随后通过**空间感知器（SP）**以原型为查询（Query）聚合像素特征，减少类内方差；最后通过**鲁棒性感知器（RP）**评估各模态的可靠性得分，实现自适应加权融合。
*   **模态感知采样模块（MAS）**：
    *   **核心思想**：根据 SGF 输出的鲁棒性得分，动态调整训练样本的采样概率。
    *   **技术细节**：将鲁棒性得分进行倒数转换（Inverted Robustness），使得得分较低（即较脆弱）的模态在训练中获得更高的采样频率，从而强制模型加强对弱势模态的学习，缓解模态不平衡。

#### 3. 实验设计
*   **数据集**：
    1.  **ISPRS Potsdam**：包含 RGB、DSM、NIR 三种模态。
    2.  **DFC2023**：包含 RGB、DSM、SAR 三种模态。
    3.  **DELIVER**：自动驾驶数据集（用于验证泛化性），包含 RGB、Depth、Event、LiDAR 四种模态。
*   **Benchmark 与对比方法**：
    *   对比了 **MuSS**（通用多模态分割）、**M3L**（随机模态丢弃）、**IMLT**（对比学习+掩码预训练）、**MAGIC**（鲁棒/脆弱模态分组优化）等 SOTA 方法。
*   **评估指标**：平均交并比（mIoU）和 F1 分数，重点关注 Average（平均性能）、Top-1（最佳组合）和 Last-1（最差/脆弱模态性能）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练细节**：初始学习率为 $6 \times 10^{-5}$，采用 AdamW 优化器，训练 200 个 epoch，包含 10 个 epoch 的预热期（Warm-up）。
*   **超参数**：损失函数权重 $\lambda_{SGF}=2, \lambda_{MAS}=1$。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同领域的数据集上进行了测试，涵盖了从 3 模态到 4 模态的多种组合。
*   **消融实验**：详细分析了 SGF 和 MAS 模块的有效性、不同尺度下的类内方差变化、鲁棒性得分的演变过程以及超参数敏感性。
*   **可视化分析**：提供了 Grad-CAM 激活图、t-SNE 聚类可视化和雷达图，直观展示了方法在减少类内差异和增强特征判别力方面的作用。
*   **客观性**：通过在 PVT-v2 和 ResNet-50 两种不同架构的骨干网络上进行实验，证明了方法的通用性和公平性。

#### 6. 主要结论与发现
*   **性能提升**：SGMA 在所有数据集上均显著优于现有方法。在 ISPRS 数据集上，平均 mIoU 提升了 **9.20%**；在最脆弱的单模态（Last-1）场景下，提升高达 **18.26%**。
*   **模态互补性**：SGMA 能够有效利用“脆弱+脆弱”模态组合（如 DSM+SAR），其表现甚至能超过某些单鲁棒模态。
*   **鲁棒性验证**：实验证明，MAS 模块确实提高了脆弱模态的特征判别力（Silhouette Score 显著提升），而 SGF 有效降低了复杂目标的类内方差。

#### 7. 优点
*   **即插即用**：不改变骨干网络结构，计算开销极小（仅增加约 1.1% FLOPs 和 1.7% 参数量）。
*   **语义锚点机制**：通过全局原型引导融合，从语义层面解决了跨模态异质性问题，比单纯的特征对齐更具解释性。
*   **动态平衡**：MAS 模块通过闭环反馈（利用 SGF 的输出指导采样）实现了真正的训练均衡。

#### 8. 不足与局限
*   **解释性深度**：虽然提出了鲁棒性得分，但对于模型内部如何具体量化不同模态间的“语义冲突”仍有待进一步深入研究。
*   **时序扩展**：目前主要针对静态图像，尚未探讨在多时相遥感数据或视频流中的应用。
*   **极端情况**：在所有模态均极度退化（如极端噪声）的情况下的表现未被充分讨论。

（完）
