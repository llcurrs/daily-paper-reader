---
title: "CAWM-Mamba: A unified model for infrared-visible image fusion and compound adverse weather restoration"
title_zh: CAWM-Mamba：一种用于红外-可见光图像融合与复合恶劣天气修复的统一模型
authors: "Huichun Liu, Xiaosong Li, Zhuangfan Huang, Tao Ye, Yang Liu, Haishu Tan"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02560v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 红外-可见光图像融合与修复的统一模型
tldr: 针对红外与可见光图像融合在复合恶劣天气（如雨雪交加）下表现不佳的问题，本文提出了CAWM-Mamba框架。这是首个利用统一共享权重同时实现图像融合与复合天气修复的端到端模型。通过天气感知预处理、跨模态特征交互及小波状态空间块，该模型能有效解耦并修复多频段退化，在多个基准测试及下游感知任务中均达到领先水平。
selection_source: fresh_fetch
motivation: 现有的多模态图像融合方法通常只能处理单一类型的恶劣天气退化，难以应对多种天气退化并存的复杂现实场景。
method: 提出CAWM-Mamba框架，通过天气感知预处理、跨模态交互及基于小波的状态空间块（WSSB）实现特征增强与多频段退化解耦。
result: 在AWMM-100K及标准融合数据集上的实验表明，该方法在复合及单一天气场景下的性能均优于现有最先进技术。
conclusion: CAWM-Mamba不仅提升了恶劣天气下的图像融合质量，还显著增强了语义分割和目标检测等下游任务的感知能力。
---

## 摘要
多模态图像融合（MMIF）整合了来自不同模态的互补信息，以生成更清晰、信息更丰富的融合图像。恶劣天气下的 MMIF 在自动驾驶和无人机（UAV）监测应用中尤为关键。然而，现有的恶劣天气融合方法通常仅处理单一类型的退化（如雾、雨或雪），当多种退化共存时（例如雾+雨、雨+雪）则会失效。为了应对这一挑战，我们提出了复合恶劣天气 Mamba（CAWM-Mamba），这是首个利用统一共享权重共同执行图像融合和复合天气修复的端到端框架。我们的网络包含三个关键组件：(1) 天气感知预处理模块（WAPM），用于增强退化的可见光特征并提取全局天气嵌入；(2) 跨模态特征交互模块（CFIM），旨在促进异构模态的对齐以及跨模态互补特征的交换；(3) 小波空间状态块（WSSB），利用小波域分解来解耦多频退化。WSSB 包含 Freq-SSM（一个无冗余地建模各向异性高频退化的模块）以及一种统一的退化表示机制，以进一步提高在复杂复合天气条件下的泛化能力。在 AWMM-100K 基准测试和三个标准融合数据集上的广泛实验表明，CAWM-Mamba 在复合天气和单一天气场景下均始终优于现有最先进的方法。此外，我们的融合结果在涵盖语义分割和目标检测的下游任务中表现出色，证实了其在现实世界恶劣天气感知中的实用价值。源代码将在 https://github.com/Feecuin/CAWM-Mamba 公布。

## Abstract
Multimodal Image Fusion (MMIF) integrates complementary information from various modalities to produce clearer and more informative fused images. MMIF under adverse weather is particularly crucial in autonomous driving and UAV monitoring applications. However, existing adverse weather fusion methods generally only tackle single types of degradation such as haze, rain, or snow, and fail when multiple degradations coexist (e.g., haze+rain, rain+snow). To address this challenge, we propose Compound Adverse Weather Mamba (CAWM-Mamba), the first end-to-end framework that jointly performs image fusion and compound weather restoration with unified shared weights. Our network contains three key components: (1) a Weather-Aware Preprocess Module (WAPM) to enhance degraded visible features and extracts global weather embeddings; (2) a Cross-modal Feature Interaction Module (CFIM) to facilitate the alignment of heterogeneous modalities and exchange of complementary features across modalities; and (3) a Wavelet Space State Block (WSSB) that leverages wavelet-domain decomposition to decouple multi-frequency degradations. WSSB includes Freq-SSM, a module that models anisotropic high-frequency degradation without redundancy, and a unified degradation representation mechanism to further improve generalization across complex compound weather conditions. Extensive experiments on the AWMM-100K benchmark and three standard fusion datasets demonstrate that CAWM-Mamba consistently outperforms state-of-the-art methods in both compound and single-weather scenarios. In addition, our fusion results excel in downstream tasks covering semantic segmentation and object detection, confirming the practical value in real-world adverse weather perception. The source code will be available at https://github.com/Feecuin/CAWM-Mamba.

---

## 论文详细总结（自动生成）

以下是对论文《CAWM-Mamba: A unified model for infrared-visible image fusion and compound adverse weather restoration》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的红外与可见光图像融合（IVIF）方法在理想条件下表现良好，但在恶劣天气下性能大幅下降。虽然已有研究关注单一退化（如仅去雨或去雾），但**无法处理多种退化因素并存的“复合恶劣天气”**（如雨雪交加、雾雨同在）。
*   **研究背景**：在自动驾驶和无人机监控中，可见光图像易受天气干扰导致细节丢失，红外图像虽能穿透烟雾但分辨率低且缺乏纹理。现有的“先修复后融合”两阶段方法效率低且容易产生伪影，因此需要一个统一的端到端框架来同时解决融合与复合修复问题。

### 2. 方法论：核心思想与技术细节
CAWM-Mamba 采用端到端架构，核心思想是利用 **Mamba（状态空间模型）** 的线性复杂度和 **小波变换** 的频域解耦能力。
*   **天气感知预处理模块 (WAPM)**：对退化的可见光图像进行初步增强，并提取全局“天气嵌入（Weather Embedding）”作为先验信息，指导后续修复。
*   **跨模态特征交互模块 (CFIM)**：通过多尺度池化（最大池化捕获红外显著目标，平均池化保留可见光纹理）和自适应特征融合，实现红外与可见光特征的精确对齐与互补。
*   **小波空间状态块 (WSSB)**：
    *   **频域解耦**：利用离散小波变换（DWT）将图像分解为低频（对应雾等全局退化）和高频（对应雨滴、雪花等局部各向异性退化）。
    *   **Freq-SSM**：针对高频子带的各向异性特征，设计了特定方向的扫描机制，避免冗余并精准建模定向退化。
    *   **通用退化空间机制 (CDSM)**：学习统一的退化表示，增强模型对不同复合天气组合的泛化能力。
*   **损失函数**：结合了小波损失（Wavelet Loss）、亮度损失、颜色一致性损失、感知损失、梯度损失和 SSIM 损失，并引入可学习的权重进行动态平衡。

### 3. 实验设计
*   **数据集**：
    *   **AWMM-100K**：包含10万对图像，涵盖复合退化（雨+雾、雨+雪、雾+雪）和单一退化场景。
    *   **标准数据集**：LLVIP、MSRS、M3FD（用于验证在理想/干净场景下的泛化性）。
*   **对比方法**：对比了 10 种最先进（SOTA）方法，包括 LRRNet、TarDal、SHIP、FusionBooster、MaeFuse、GIFNet、SAGE、FreeFusion 和 AWFusion。
*   **评价指标**：使用了 QMI、SSIM、Qabf、QG 等 9 种图像融合质量指标，以及下游任务的 mAP（检测）和 mIoU（分割）。

### 4. 资源与算力
*   **硬件环境**：使用 **2 张 NVIDIA GeForce RTX 3090 GPU** 进行训练。
*   **训练细节**：采用 Adam 优化器，初始学习率 0.0001，训练过程中图像裁剪尺寸从 64 逐渐扩大到 144（渐进式学习）。
*   **效率优势**：计算复杂度仅为 371.01G FLOPs，约为 Transformer 架构基准模型 AWFusion 的 1/11。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 **6 种恶劣天气场景**（3种复合、3种单一）的定量与定性对比。
    *   在 **3 个标准干净数据集**上进行了泛化性测试。
    *   进行了 **真实场景（Real-world）** 的零样本（Zero-shot）泛化测试。
    *   完成了详尽的**消融实验**，验证了 WAPM、CFIM、Freq-SSM 和 CDSM 各组件的贡献。
*   **充分性评价**：实验设计非常充分且客观。通过引入“平均排名（Avg. Rank）”和统计显著性检验（p-value），证明了其在各种复杂环境下的稳健性。

### 6. 主要结论与发现
*   **性能领先**：CAWM-Mamba 在所有复合和单一天气场景下的 9 项指标中几乎全部排名第一或第二。
*   **下游任务增益**：融合后的图像显著提升了 YOLOv7 的目标检测精度和 BANet 的语义分割效果，证明了其在实际感知任务中的价值。
*   **高效性**：得益于 Mamba 的线性复杂度，该模型在保持高性能的同时，计算开销远低于基于 Transformer 的方法，更适合部署于无人机等资源受限设备。

### 7. 优点与亮点
*   **首创性**：首个专门针对“复合恶劣天气”下的红外-可见光融合提出的端到端统一框架。
*   **架构创新**：将小波变换的频域定位能力与 Mamba 的长程建模能力结合，通过 Freq-SSM 解决了高频定向退化（如雨丝）的建模难题。
*   **泛化能力强**：不仅能处理极端天气，在干净图像上也表现出极佳的兼容性，且具备良好的 Sim-to-Real 跨域泛化能力。

### 8. 不足与局限
*   **极端并发退化**：当三种或更多退化（如雨+雪+雾）同时发生时，小波高频子带可能出现“频率掩蔽（Frequency Masking）”效应，导致解耦难度增加，可能残留微小噪声。
*   **视频一致性**：目前主要针对单帧图像处理，尚未利用视频流中的时间连续性来进一步增强动态场景下的融合稳定性。
*   **真实数据标注**：由于缺乏真实复合天气下的高质量标注数据，定量评估仍部分依赖于合成数据。

（完）
