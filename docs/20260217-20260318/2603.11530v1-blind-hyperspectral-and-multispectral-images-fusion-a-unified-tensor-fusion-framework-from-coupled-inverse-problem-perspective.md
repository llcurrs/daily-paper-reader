---
title: "Blind Hyperspectral and Multispectral Images Fusion: A Unified Tensor Fusion Framework from Coupled Inverse Problem Perspective"
title_zh: 盲高光谱与多光谱图像融合：基于耦合逆问题视角的统一张量融合框架
authors: "Ying Gao, Michael K. Ng, Chunfeng cui"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11530v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 高光谱与多光谱图像的统一张量融合框架
tldr: 针对高光谱与多光谱图像融合中空间模糊和光谱响应函数通常未知的局限性，本文提出了一种基于耦合反问题视角的统一张量融合框架。该方法将空间盲解卷积与光谱盲解算相结合，无需预训练即可实现实时融合。通过偏线性化ADMM算法联合估计高分辨率图像及退化算子，在多个数据集上达到了领先性能。
selection_source: fresh_fetch
motivation: 传统的图像融合方法通常假设空间模糊和光谱响应函数已知，这在实际应用中难以满足。
method: 将盲融合建模为耦合反问题，并提出一种结合张量分解、Moreau包络平滑及偏线性化ADMM算法的统一优化框架。
result: 在合成和真实数据集上的实验表明，该方法在盲融合任务中优于现有的最先进算法。
conclusion: 该研究为盲图像融合提供了一个灵活、高效且具有收敛性保证的张量处理方案。
---

## 摘要
高光谱与多光谱图像融合旨在整合低分辨率高光谱图像（LR-HSI）和高分辨率多光谱图像（HR-MSI），以构建高分辨率高光谱图像（HR-HSI）。通常假设空间模糊算子和光谱响应算子是先验已知的。然而，这种假设在实践中具有极大的局限性。为了克服这一限制，本文将盲融合建模为一个耦合逆问题，将空间域的盲解卷积与光谱域的盲解混相结合。从这一新颖视角出发，我们提出了一个统一的张量融合框架，该框架能够进行灵活的自我调整，且无需预训练即可实现实时融合。我们进一步引入了一个优化模型，用于联合估计目标 HR-HSI、空间点扩散函数和光谱响应函数。为了求解该模型，我们设计了一种带有 Moreau 包络平滑的部分线性化交替方向乘子法（ADMM）算法，并附带了严格的收敛性分析。针对融合问题的具体特征，提出了一种定制的初始化估计器。在合成数据集和真实数据集上与最先进方法的数值对比表明，所提方法具有卓越的性能。

## Abstract
Hyperspectral and multispectral images fusion aims at integrating a low-resolution hyperspectral image (LR-HSI) and a high-resolution multispectral image (HR-MSI) to construct a high-resolution hyperspectral image (HR-HSI). It is generally assumed that spatial blurring operator and spectral response operator are prior-known. However, such an assumption is extremely restrictive in practice. To overcome this limitation, this paper formulates blind fusion as a coupled inverse problem, integrating blind deconvolution in the spatial domain with blind unmixing in the spectral domain. From this novel perspective, we propose a unified tensor fusion framework capable of flexible self-adjustment and real-time fusion without pre-training. We further introduce an optimization model for the joint estimation of the target HR-HSI, the spatial point spread function, and the spectral response function. To solve this model, we devise a partially linearized alternating direction method of multipliers (ADMM) algorithm with Moreau envelope smoothing, accompanied by the rigorous convergence analysis. An initialization estimator tailored to the specific characteristics of the fusion problem is proposed. Numerical comparisons with state-of-the-art methods on both synthetic and real-world datasets demonstrate the compelling performance of the proposed method.

---

## 论文详细总结（自动生成）

这是一份关于论文《Blind Hyperspectral and Multispectral Images Fusion: A Unified Tensor Fusion Framework from Coupled Inverse Problem Perspective》的结构化技术总结：

### 1. 核心问题与研究背景
*   **研究背景**：在遥感成像中，受限于传感器能量平衡，高光谱图像（HSI）通常具有高光谱分辨率但低空间分辨率，而多光谱图像（MSI）则相反。融合两者以获取高时空分辨率的图像（HR-HSI）是核心任务。
*   **核心问题**：**盲融合（Blind Fusion）**。传统方法大多假设空间模糊算子（PSF）和光谱响应函数（SRF）是预先已知的（非盲），但在实际应用中，这些参数往往难以精确获取。
*   **研究动机**：现有的盲融合方法中，基于提升（Hypersharpening）的方法缺乏理论基础，两阶段模型法容易产生误差累积，而深度学习方法则面临计算量大、跨传感器泛化性差且缺乏收敛性保证的问题。

### 2. 方法论
*   **核心思想**：将盲融合建模为一个**耦合逆问题**，即同时解决空间域的“盲解卷积”和光谱域的“盲解混”。
*   **关键技术细节**：
    *   **统一张量框架**：利用张量积建模退化过程，将目标 HR-HSI、PSF 和 SRF 纳入统一的优化目标。
    *   **正则化约束**：
        1.  **TTNN（变换管状核范数）**：利用张量的低秩特性捕捉空间-光谱相关性。
        2.  **非负约束**：确保物理意义上的像素值合理性。
        3.  **单位单纯形约束**：对 PSF 的模糊核和 SRF 的混合向量施加约束，以解决盲问题的非唯一性。
    *   **算法流程**：
        1.  **初始化**：采用嵌套高频提升（Nested Hypersharpening）生成初始 HR-HSI。
        2.  **部分线性化 ADMM**：针对模型中的非凸、非光滑且不可分结构，设计了改进的 ADMM 算法。
        3.  **Moreau 包络平滑**：引入平滑技术处理非光滑指示函数，并提供了严格的收敛性证明（收敛至平滑问题的驻点）。
        4.  **迭代更新**：交替更新 HR-HSI、辅助变量、拉格朗日乘子以及 PSF/SRF 算子。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：Washington DC Mall（HYDICE 传感器）、Chikusei（日本筑西市数据）。
    *   **真实数据集**：Hyperion/Sentinel-2（德国弗赖堡地区）、EnMAP/Sentinel-2（德国纽伦堡地区）。
*   **Benchmark（对比方法）**：共对比了 9 种最先进（SOTA）方法，涵盖三类：
    *   **提升类**：AWLP, SM-SaBC, Nested-GSA。
    *   **学习类**：UDALN, SURE, ZSL（零样本学习）。
    *   **模型类**：CNMF, Hysure, IR-TenSR。
*   **评价指标**：PSNR, SAM（光谱角映射）, UIQI, ERGAS（合成数据）；D_lambda, D_s, QNR（真实数据，无参考指标）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或 CPU 硬件配置。
*   **效率体现**：文中提供了运行时间（Time）对比。结果显示，该方法（Tenfuse）在合成数据集上耗时约 67-184 秒，远快于深度学习方法（如 UDALN/SURE 动辄数千至数万秒），在模型类方法中处于中等水平，兼顾了精度与效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流数据集上进行了完整对比。
    *   **消融实验/鲁棒性分析**：详细讨论了权衡参数 $\lambda_1$、光谱混合窗口大小 $k_3$、Moreau 平滑参数 $\mu$ 的敏感性。
    *   **噪声鲁棒性**：专门测试了在带变（Band-dependent）非均匀噪声下的表现。
*   **充分性评价**：实验设计较为充分且客观。通过合成数据验证了理论上限，通过真实卫星数据验证了实际应用潜力，对比方法涵盖了近年的主流技术路线。

### 6. 主要结论与发现
*   **性能领先**：Tenfuse 在大多数指标上优于现有的盲融合方法，尤其在光谱保真度（SAM 指标最低）和空间细节恢复之间达到了极佳平衡。
*   **理论完备**：证明了算法生成的序列具有全局收敛性，并给出了迭代复杂度分析（$O(\epsilon^{-4})$ 达到 $\epsilon$-驻点）。
*   **无需预训练**：作为一种无监督的模型驱动方法，它不需要外部训练数据，能够直接处理跨传感器数据。

### 7. 优点（亮点）
*   **物理可解释性**：将传感器信息（下采样率、波段范围）直接嵌入张量约束，比纯黑盒深度学习更具物理依据。
*   **数学严谨性**：在处理非凸非光滑问题时，引入 Moreau 平滑并给出收敛证明，这在同类图像融合论文中较为少见。
*   **灵活性**：TTNN 框架允许根据数据特性选择不同的变换矩阵（如 DFT、DWT 或数据驱动变换）。

### 8. 不足与局限
*   **边界效应风险**：由于在频率域处理卷积，若不进行适当的边缘填充，可能会产生边缘伪影。
*   **PSF 简化假设**：假设 PSF 是波段无关的（Band-independent），在某些存在严重色差的成像系统中可能不够精确。
*   **SRF 局部性**：模型假设每个 MSI 波段仅由相邻 HSI 波段混合而成，对于光谱响应范围极宽的特殊传感器，可能存在截断误差。
*   **噪声敏感度**：虽然做了鲁棒性分析，但主要针对高斯噪声，对于复杂的泊松噪声或条带噪声的处理能力尚待进一步验证。

（完）
