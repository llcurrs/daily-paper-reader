---
title: "RTFDNet: Fusion-Decoupling for Robust RGB-T Segmentation"
title_zh: RTFDNet：用于鲁棒 RGB-T 分割的融合-解耦网络
authors: "Kunyu Tan, Mingjian Liang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09149v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 传感器信号部分缺失时的鲁棒RGB-T分割
tldr: RTFDNet 针对 RGB-T 语义分割中传感器信号缺失导致的鲁棒性不足问题，提出了一种融合-解耦统一框架。该方法通过协同特征融合（SFF）提取互补信息，并利用跨模态解耦正则化（CMDR）和区域解耦正则化（RDR）强化单模态路径，在不增加推理负担的前提下，显著提升了模型在各种光照和传感器故障条件下的分割性能。
selection_source: fresh_fetch
motivation: 传统 RGB-T 分割方法在传感器信号部分缺失时鲁棒性差，且现有改进方案往往需要复杂的多阶段训练或教师-学生框架。
method: 提出包含协同特征融合（SFF）和两种解耦正则化机制（CMDR、RDR）的三分支编解码网络，实现融合与解耦的统一。
result: 实验证明 RTFDNet 在多种模态条件下均表现出一致的高性能，且支持高效的独立推理。
conclusion: 该研究通过创新的融合-解耦机制，为构建在极端环境下具有高鲁棒性的 RGB-T 语义分割系统提供了有效方案。
---

## 摘要
RGB-红外（RGB-T）语义分割对于在低光照或黑暗环境中运行的机器人系统至关重要。然而，传统方法往往过度强调模态平衡，导致在传感器信号部分缺失时鲁棒性有限且性能严重下降。近期的进展，如跨模态知识蒸馏和模态自适应微调，试图增强跨模态交互，但它们通常将模态融合与模态自适应解耦，需要使用冻结模型或教师-学生框架进行多阶段训练。我们提出了 RTFDNet，这是一种三分支编码器-解码器，它统一了融合与解耦，以实现鲁棒的 RGB-T 分割。协同特征融合（SFF）执行通道级门控交换和轻量级空间注意力，以注入互补线索。跨模态解耦正则化（CMDR）从融合表示中分离出特定模态的组件，并通过停止梯度目标监督单模态解码器。区域解耦正则化（RDR）在置信区域内强制执行类别选择性预测一致性，同时阻断流向融合分支的梯度。这种反馈循环在不降低融合流性能的情况下强化了单模态路径，从而在测试时实现高效的独立推理。大量实验证明了 RTFDNet 的有效性，展示了其在不同模态条件下的稳定性能。我们的实现将公开发布以促进进一步研究。源代码可在 https://github.com/curapima/RTFDNet 获取。

## Abstract
RGB-Thermal (RGB-T) semantic segmentation is essential for robotic systems operating in low-light or dark environments. However, traditional approaches often overemphasize modality balance, resulting in limited robustness and severe performance degradation when sensor signals are partially missing. Recent advances such as cross-modal knowledge distillation and modality-adaptive fine-tuning attempt to enhance cross-modal interaction, but they typically decouple modality fusion and modality adaptation, requiring multi-stage training with frozen models or teacher-student frameworks. We present RTFDNet, a three-branch encoder-decoder that unifies fusion and decoupling for robust RGB-T segmentation. Synergistic Feature Fusion (SFF) performs channel-wise gated exchange and lightweight spatial attention to inject complementary cues. Cross-Modal Decouple Regularization (CMDR) isolates modality-specific components from the fused representation and supervises unimodal decoders via stop-gradient targets. Region Decouple Regularization (RDR) enforces class-selective prediction consistency in confident regions while blocking gradients to the fusion branch. This feedback loop strengthens unimodal paths without degrading the fused stream, enabling efficient standalone inference at test time. Extensive experiments demonstrate the effectiveness of RTFDNet, showing consistent performance across varying modality conditions. Our implementation will be released to facilitate further research. Our source code are publicly available at https://github.com/curapima/RTFDNet.

---

## 论文详细总结（自动生成）

以下是对论文《RTFDNet: Fusion-Decoupling for Robust RGB-T Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 RGB-T（可见光-红外）语义分割模型高度依赖双模态信号的完整性。在实际应用（如自动驾驶、机器人）中，传感器常因硬件故障、环境干扰或信号丢失导致模态缺失。
*   **研究背景**：传统方法在模态缺失时性能会剧烈下降，甚至不如单模态模型。现有的改进方案（如知识蒸馏或自适应微调）通常需要复杂的多阶段训练，或者需要冻结主干网络使用适配器，导致训练效率低且模态融合与解耦过程脱节。
*   **整体含义**：本文旨在提出一个统一的框架，在增强双模态融合性能的同时，通过“解耦”机制强化单模态分支，使模型在任何一种传感器失效时都能保持高鲁棒性，并支持高效的独立推理。

### 2. 方法论
RTFDNet 采用三分支（RGB、红外、融合）编解码结构，核心包含以下三个关键技术：
*   **协同特征融合 (SFF)**：
    *   **通道门控交换**：通过计算两模态通道注意力的符号一致性（$R \odot T < 0$），识别互补信息并进行动态门控交换，使各分支吸收对方的显著特征。
    *   **空间注意力**：在融合阶段引入轻量级线性空间注意力，进一步精炼融合特征。
*   **跨模态解耦正则化 (CMDR)**：
    *   **特征提取**：利用符号一致性门控从“融合特征”中反向提取出特定模态的组件。
    *   **梯度阻断监督**：将提取的组件作为目标，通过 $L_2$ 损失监督单模态解码器。使用 `stop-gradient`（停止梯度）操作，确保监督信息仅从融合分支流向单模态分支，避免干扰融合路径的学习。
*   **区域解耦正则化 (RDR)**：
    *   **类别一致性**：在预测层面，利用融合分支生成的置信类别掩码（One-hot Masks），强制单模态分支在这些区域的预测与融合分支保持一致。
    *   **局部精炼**：通过 $L_1$ 损失优化物体边界和模糊区域的预测。

### 3. 实验设计
*   **数据集与场景**：
    *   **MFNet**：城市街道场景，包含白天和夜晚的 1569 对图像。
    *   **FMB**：高像素标注率（>98%）的自动驾驶数据集，包含 1500 对图像。
    *   **PST900**：地下环境（洞穴、隧道）数据集，包含 1444 对高分辨率图像。
*   **测试设置 (Benchmark)**：分别在“全模态 (RGB-T)”、“仅 RGB”和“仅红外”三种设置下评估 mIoU 和 mAcc。
*   **对比方法**：对比了包括 RTFNet、EAEFNet、CMXNet、StitchFusion、CRM、CMNeXt 等在内的多种 SOTA（州际领先）模型。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA A100 (40G)** GPU 进行计算。
*   **训练细节**：
    *   在三个数据集上均训练了 **300 个 epoch**。
    *   使用了 AdamW 优化器，设置了特定的学习率（编码器 $1\times10^{-4}$，解码器 $6\times10^{-4}$）。
    *   输入分辨率根据数据集不同设定为 $480\times640$、$512\times512$ 或 $720\times1280$。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流 benchmark 数据集上进行了详尽测试，并针对每个数据集对比了 10 种以上的现有方法。
*   **消融实验**：专门针对 SFF、CMDR、RDR 三个组件进行了组合验证，证明了每个模块对提升单模态鲁棒性的贡献。
*   **效率分析**：提供了 FLOPs、参数量和 FPS 的对比，验证了模型在推理时的实时性。
*   **评价**：实验设计全面，涵盖了不同光照（昼/夜）、不同场景（城市/地下）以及不同失效模式，结果客观且具有说服力。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在模态缺失的情况下，RTFDNet 的性能下降幅度远小于其他模型。例如在 MFNet 上，缺失红外信号时 mIoU 仅下降 4.02%，远优于 CMNeXt 等模型。
*   **单模态强化**：通过解耦正则化，单模态分支在独立推理时能够“继承”融合分支的上下文感知能力，甚至在极端黑暗环境下也能准确分割目标。
*   **推理高效性**：在传感器故障时，模型可无缝切换到单模态分支，计算量（FLOPs）减半，推理速度（FPS）几乎翻倍。

### 7. 优点
*   **统一框架**：将融合与解耦集成在一次训练中，无需多阶段蒸馏，简化了训练流程。
*   **参数可分离推理**：推理时只需加载对应模态的编码器和解码器，极大地节省了部署资源。
*   **创新的正则化机制**：CMDR 和 RDR 通过停止梯度技术实现了有效的知识迁移，解决了“模态竞争”导致的单模态性能弱化问题。

### 8. 不足与局限
*   **对齐依赖**：该方法仍基于 RGB 和红外图像严格对齐的假设，对于未对齐或动态错位的鲁棒性尚未讨论。
*   **超参数敏感性**：损失函数中包含多个权重系数（$\lambda, \lambda', \lambda''$），在不同数据集上可能需要精细调优以达到最佳平衡。
*   **模态扩展性**：目前仅验证了 RGB-T 双模态，对于 LiDAR 或事件相机等多模态融合的扩展效果有待进一步验证。

（完）
