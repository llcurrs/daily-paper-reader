---
title: "Solution to the 10th ABAW Expression Recognition Challenge: A Robust Multimodal Framework with Safe Cross-Attention and Modality Dropout"
title_zh: 第十届 ABAW 表情识别挑战赛解决方案：一种具有安全交叉注意力和模态丢弃策略的鲁棒多模态框架
authors: "Jun Yu, Naixiang Zheng, Guoyuan Wang, Yunxiang Zhang, Lingsi Zhu, Jiaen Liang, Wei Huang, Shengping Liu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08034v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 针对模态缺失和部分遮挡的多模态框架
tldr: 针对真实场景中表情识别面临的遮挡、模态缺失及类别不平衡挑战，本文提出一种鲁棒的多模态框架。该框架采用双分支Transformer架构，结合安全交叉注意力机制与模态丢弃策略，在视觉信息缺失时能动态依赖音频预测。通过引入Focal Loss优化长尾分布，并利用滑动窗口软投票捕捉动态情感转换，显著提升了在Aff-Wild2数据集上的识别性能。
selection_source: fresh_fetch
motivation: 旨在解决野外环境下表情识别中存在的模态缺失、遮挡以及严重的类别不平衡问题。
method: 提出一种包含安全交叉注意力和模态丢弃的双分支Transformer框架，并结合Focal Loss与滑动窗口软投票策略。
result: "在Aff-Wild2验证集上实现了60.79%的准确率和0.5029的F1分数，有效应对了模态缺失和时空依赖。"
conclusion: 该多模态框架通过动态融合与鲁棒性设计，为复杂环境下的情感行为分析提供了有效的解决方案。
---

## 摘要
现实环境中的情绪识别受到局部遮挡、模态缺失以及严重的类别不平衡的阻碍。为了解决这些问题，特别是针对野外情感行为分析（ABAW）表情挑战赛，我们提出了一种动态融合视觉和音频表征的多模态框架。我们的方法采用双分支 Transformer 架构，其特点是具有安全交叉注意力机制和模态丢弃策略。这种设计使得网络在视觉线索缺失时能够依赖基于音频的预测。为了缓解 Aff-Wild2 数据集的长尾分布问题，我们应用了 Focal Loss 优化，并结合滑动窗口软投票策略来捕捉动态情感转换并减少帧级分类抖动。实验表明，我们的框架能够有效处理模态缺失和复杂的时空依赖关系，在 Aff-Wild2 验证集上达到了 60.79% 的准确率和 0.5029 的 F1 分数。

## Abstract
Emotion recognition in real-world environments is hindered by partial occlusions, missing modalities, and severe class imbalance. To address these issues, particularly for the Affective Behavior Analysis in-the-wild (ABAW) Expression challenge, we propose a multimodal framework that dynamically fuses visual and audio representations. Our approach uses a dual-branch Transformer architecture featuring a safe cross-attention mechanism and a modality dropout strategy. This design allows the network to rely on audio-based predictions when visual cues are absent. To mitigate the long-tail distribution of the Aff-Wild2 dataset, we apply focal loss optimization, combined with a sliding-window soft voting strategy to capture dynamic emotional transitions and reduce frame-level classification jitter. Experiments demonstrate that our framework effectively handles missing modalities and complex spatiotemporal dependencies, achieving an accuracy of 60.79% and an F1-score of 0.5029 on the Aff-Wild2 validation set.

---

## 论文详细总结（自动生成）

这篇论文针对第10届野外情感行为分析（ABAW）挑战赛中的表情识别（EXPR）任务，提出了一种鲁棒的多模态框架。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在现实（In-the-wild）环境下，表情识别面临三大核心挑战：**局部遮挡**、**模态缺失**（如人脸移出画面）以及**严重的类别不平衡**（长尾分布）。
*   **核心问题**：传统的识别模型在视觉信号中断时性能会剧烈下降，且容易受到数据噪声和少数类样本识别率低的困扰。
*   **研究目的**：开发一种能够动态融合视听信息、在模态缺失时具备容错能力，并能有效处理长尾数据的鲁棒识别框架。

### 2. 论文提出的方法论
该框架包含五个关键组件，核心思想是“动态融合”与“安全降级”：
*   **特征提取流水线**：
    *   **视觉**：采用 **BEiT-large** 架构，先在 Raf-DB、FERPlus 等大规模静态数据集上预训练，再在 Aff-Wild2 视频帧上进行领域自适应微调。
    *   **音频**：使用预训练的 **WavLM-large** 提取语音特征，并通过线性插值实现与视频帧的时间对齐。
*   **双分支 Transformer 架构**：
    *   使用两个独立的 Transformer 编码器分别处理视、听特征。
    *   引入**可学习的门控融合机制（Gating Mechanism）**，通过 Sigmoid 激活函数动态调节单模态上下文与跨模态融合特征的权重。
*   **安全交叉注意力（Safe Cross-Attention）与模态丢弃**：
    *   **模态丢弃**：训练时以概率 $p$ 随机掩蔽视觉输入，强迫模型学习音频线索。
    *   **安全机制**：当检测到视觉特征完全缺失时，通过数值保护和动态掩码清理逻辑，防止 Softmax 产生无效值，使网络能通过残差连接平滑降级，完全依赖音频分支决策。
*   **优化目标**：使用 **Focal Loss** 替代标准交叉熵，通过调节因子 $\gamma$ 降低高频简单样本的权重，迫使模型关注难分类的长尾情感类别。
*   **推理策略**：采用**重叠滑动窗口**（窗口 64 帧，步长 8 帧）进行局部建模，结合**软投票（Soft Voting）**和**中值滤波**平滑预测结果，减少帧级分类抖动。

### 3. 实验设计
*   **数据集**：使用 **Aff-Wild2** 数据集，包含 548 个视频，约 270 万帧，涵盖 8 种表情类别。
*   **Benchmark**：以 Aff-Wild2 官方验证集作为评估基准。
*   **对比实验**：
    *   **视觉主干对比**：对比了 ResNet50、EfficientNetV2-M、MAE-ViT-Base 和 BEiT-large。
    *   **模态权重分析**：通过改变融合系数 $\lambda$（0.0 到 1.0），分析视觉与音频的贡献度。
    *   **消融实验**：针对模态丢弃概率 $p$、Transformer 隐藏层维度 $d$ 和层数 $l$ 进行了多组组合测试。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量以及总训练时长。但从其使用的模型规模（BEiT-large, WavLM-large）来看，该实验需要较高的显存和计算能力。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了约 16 组主要的对比和消融实验数据。
*   **充分性与客观性**：
    *   实验涵盖了从主干网络选择到超参数调优的完整流程。
    *   通过对比单模态与多模态的表现，客观证明了音频信息的补充作用。
    *   消融实验展示了模型深度和宽度对性能的影响，发现过大的模型在噪声数据上容易过拟合，这一观察具有较高的参考价值。

### 6. 论文的主要结论与发现
*   **BEiT-large 表现最优**：相比卷积网络，基于 Transformer 的模型能更好地捕捉全局面部上下文。
*   **多模态优于单模态**：视觉是主导模态，但加入音频特征（尤其在视觉受限时）能显著提升 F1 分数。
*   **模态丢弃的有效性**：设置 $p=0.10$ 的模态丢弃能将 F1 分数从 0.4764 提升至 0.5029，证明了模拟信号丢失能增强模型的鲁棒性。
*   **模型容量陷阱**：在 Aff-Wild2 这种带噪声的小规模数据集上，增加网络深度或宽度反而可能导致性能下降（过拟合）。

### 7. 优点
*   **鲁棒性设计**：提出的“安全交叉注意力”解决了多模态模型在缺失输入时的数值稳定性问题，具有很强的工程实用性。
*   **端到端优化**：从预训练策略到后处理（滑动窗口软投票）都有针对性设计，有效缓解了视频分类中的抖动问题。
*   **长尾处理**：结合 Focal Loss 和类别权重缩放，较好地处理了极度不平衡的数据分布。

### 8. 不足与局限
*   **计算开销**：BEiT-large 和 WavLM-large 均为大型模型，推理延迟可能较高，论文未讨论实时性表现。
*   **超参数敏感性**：模态丢弃概率 $p$ 对结果影响较大（从 0.1 增至 0.15 性能即下降），在不同场景下的泛化性需进一步验证。
*   **端到端训练限制**：采用两阶段特征提取（先提取特征再训练融合层），可能限制了视听模态之间更深层次的底层交互。

（完）
