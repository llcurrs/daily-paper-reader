---
title: "UniFusion: A Unified Image Fusion Framework with Robust Representation and Source-Aware Preservation"
title_zh: UniFusion：一种具有鲁棒表示和源感知保持的统一图像融合框架
authors: "Xingyuan Li, Songcheng Du, Yang Zou, HaoYuan Xu, Zhiying Jiang, Jinyuan Liu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14214v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 用于多模态任务的统一图像融合框架
tldr: 针对现有图像融合方法在特定任务局限性及深层特征传播中源信息丢失的问题，本文提出UniFusion统一框架。该框架利用DINOv3构建共享语义空间以提取一致性特征，通过重构对齐损失确保融合结果对源图像信息的有效保留，并采用双层优化策略协调重构与融合目标。实验表明，UniFusion在多模态、多曝光及多聚焦等多种任务中均表现出优异的视觉效果和跨任务泛化能力。
selection_source: fresh_fetch
motivation: 现有图像融合方法多针对特定任务且在深层传播中易丢失源图像信息，缺乏跨任务泛化能力。
method: 提出UniFusion框架，利用DINOv3提取模态一致性特征，引入重构对齐损失保持源信息，并采用双层优化策略平衡重构与融合目标。
result: 在多模态、多曝光和多聚焦融合任务上的实验证明，该方法具有卓越的视觉质量、泛化能力和现实场景适应性。
conclusion: UniFusion通过统一的架构和优化策略，有效解决了图像融合中的信息丢失与任务局限性问题。
---

## 摘要
图像融合旨在整合来自多个源图像的互补信息，以生成更具信息量且视觉一致的表示，从而造福于人类感知和下游视觉任务。尽管近期取得了进展，但大多数现有的融合方法是针对特定任务（即多模态、多曝光或多聚焦融合）设计的，且在融合过程中难以有效保留源信息。这一局限性主要源于特定任务的架构以及深层传播导致的源信息退化。为了克服这些问题，我们提出了 UniFusion，这是一个旨在实现跨任务泛化的统一图像融合框架。首先，利用 DINOv3 进行模态一致的特征提取，UniFusion 为多样化的输入建立了共享语义空间。其次，为了保留对每个源图像的理解，我们引入了重构对齐损失，以维持融合输出与输入之间的一致性。最后，我们采用双层优化策略来解耦并联合优化重构和融合目标，有效平衡了它们的耦合关系并确保平滑收敛。在多个融合任务上的广泛实验证明了 UniFusion 卓越的视觉质量、泛化能力以及对现实场景的适应性。代码可在 https://github.com/dusongcheng/UniFusion 获取。

## Abstract
Image fusion aims to integrate complementary information from multiple source images to produce a more informative and visually consistent representation, benefiting both human perception and downstream vision tasks. Despite recent progress, most existing fusion methods are designed for specific tasks (i.e., multi-modal, multi-exposure, or multi-focus fusion) and struggle to effectively preserve source information during the fusion process. This limitation primarily arises from task-specific architectures and the degradation of source information caused by deep-layer propagation. To overcome these issues, we propose UniFusion, a unified image fusion framework designed to achieve cross-task generalization. First, leveraging DINOv3 for modality-consistent feature extraction, UniFusion establishes a shared semantic space for diverse inputs. Second, to preserve the understanding of each source image, we introduce a reconstruction-alignment loss to maintain consistency between fused outputs and inputs. Finally, we employ a bilevel optimization strategy to decouple and jointly optimize reconstruction and fusion objectives, effectively balancing their coupling relationship and ensuring smooth convergence. Extensive experiments across multiple fusion tasks demonstrate UniFusion's superior visual quality, generalization ability, and adaptability to real-world scenarios. Code is available at https://github.com/dusongcheng/UniFusion.

---

## 论文详细总结（自动生成）

以下是对论文《UniFusion: A Unified Image Fusion Framework with Robust Representation and Source-Aware Preservation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的图像融合方法大多针对特定任务（如红外-可见光、多曝光、多聚焦）设计，缺乏跨任务的泛化能力。此外，由于深度神经网络的层级传播，源图像的关键信息（如纹理、对比度）在特征提取过程中容易发生退化和丢失，导致融合结果出现语义漂移或细节模糊。
*   **研究背景**：图像融合旨在整合多源图像的互补信息。虽然已有通用框架尝试使用共享骨干网络，但仍面临两个挑战：一是缺乏能稳健编码异构信号的模态一致性特征提取机制；二是难以在深度传播中平衡信息保留与融合质量。

### 2. 方法论
UniFusion 提出了一个基于双层优化（Bilevel Optimization）的统一框架，核心组件包括：
*   **基于 DINOv3 的语义先验适配**：利用预训练的自监督大模型 DINOv3 作为通用的语义骨干网络（特征提取器保持冻结），提取具有物体中心先验和长程上下文依赖的特征。
*   **分层适配器（Hierarchical Adapter）**：引入轻量级适配器，将 DINOv3 的多层特征（第2, 5, 8, 11层）进行逐步校准和上采样，以弥补预训练模型与特定模态数据之间的分布差异。
*   **重构对齐机制（Reconstruction Alignment）**：为每个模态设计一个重构分支，强制要求适配后的特征能够还原回原始输入图像。这种自监督约束确保了特征空间保留了充足的模态特定信息，防止语义丢失。
*   **双层优化策略**：将学习过程建模为双层优化问题。内层循环（Inner Loop）快速更新重构相关的表示，以捕获模态语义；外层循环（Outer Loop）根据演进的特征空间调整融合策略。通过交替优化，实现了信息保留与融合质量之间的动态平衡。

### 3. 实验设计
*   **实验任务与数据集**：
    *   **红外-可见光融合 (IVIF)**：使用 M3FD、TNO 和 RoadScene 数据集。
    *   **医学图像融合 (MIF)**：使用 Medical Harvard 数据集（PET-MRI）。
    *   **多曝光融合 (MEF)**：使用 MEFB 基准测试。
    *   **多聚焦融合 (MFF)**：使用 MFIF（包含 Lytro, MFFW, MFI-WHU 子集）。
*   **对比方法**：与 10 种最先进（SOTA）方法进行对比，包括 CDDFuse、SwinFusion、U2Fusion、TC-MoA 等。
*   **评价指标**：采用互信息 (MI)、视觉信息保真度 (VIF)、Qabf、PSNR、CC 等多种定量指标，并结合定性视觉对比。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 5090 GPU** 上完成。
*   **训练细节**：Batch size 设置为 16，每个任务训练 10,000 次迭代。
*   **优化器**：使用 Adam 优化器，初始学习率为 $2 \times 10^{-4}$，采用指数衰减。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 4 大类融合任务，涉及 7 个以上的主流数据集。
*   **消融实验**：设计了四组消融实验（无适配器、无 DINOv3、无重构分支、无双层优化），验证了每个模块的必要性。
*   **充分性评价**：实验设计非常充分且客观。不仅在通用指标上进行了对比，还通过特征图可视化展示了重构模块对特征保留的作用，并提及了在下游任务（检测、分割）中的潜力，证明了其在不同场景下的稳健性。

### 6. 主要结论与发现
*   **性能领先**：UniFusion 在所有测试任务中均达到了 SOTA 水平，尤其在 MI 和 VIF 指标上提升显著，表明其生成的图像更符合人类视觉感知且信息量更丰富。
*   **泛化能力强**：即使不针对特定任务进行微调，该模型在多聚焦融合等任务上依然表现出色，证明了 DINOv3 语义先验的强大泛化力。
*   **细节保持优异**：通过重构对齐，模型能有效保留红外图像的热目标轮廓和可见光图像的精细纹理（如树枝末端、建筑窗户）。

### 7. 优点
*   **架构统一性**：用一个框架解决了多种异构图像融合问题，降低了任务切换的成本。
*   **引入大模型先验**：巧妙利用 DINOv3 的自监督表示能力，提升了特征提取的鲁棒性。
*   **数学严谨性**：采用双层优化策略解决了重构与融合两个目标之间的耦合冲突，使训练过程更稳定。
*   **源感知能力**：重构分支的设计有效缓解了深度学习模型中常见的“细节丢失”痛点。

### 8. 不足与局限
*   **计算开销**：虽然推理时适配器很轻量，但双层优化在训练阶段的计算复杂度较高，且依赖高性能 GPU（如 RTX 5090）。
*   **骨干网络依赖**：由于 DINOv3 骨干网络被冻结，模型性能在很大程度上受限于预训练模型的表示能力，对于某些极端非自然图像领域（如特殊的工业探伤图像）可能存在适配偏差。
*   **实时性说明不足**：论文未详细讨论在移动端或嵌入式设备上的推理延迟，这对于自动驾驶等实时任务至关重要。

（完）
