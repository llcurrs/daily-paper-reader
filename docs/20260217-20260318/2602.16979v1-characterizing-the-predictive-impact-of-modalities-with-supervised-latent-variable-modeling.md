---
title: Characterizing the Predictive Impact of Modalities with Supervised Latent-Variable Modeling
title_zh: 利用有监督潜变量建模刻画模态的预测影响
authors: "Divyam Madaan, Sumit Chopra, Kyunghyun Cho"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.16979v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 量化多模态学习中任何缺失模态的预测影响
tldr: 针对多模态学习中数据缺失的普遍问题，本文提出了PRIMO模型。该模型利用有监督潜变量建模来填补缺失模态，并量化其对预测结果的影响。PRIMO支持利用不完整数据进行训练，在推理阶段通过对潜变量采样实现预测并分析模态贡献。实验证明其在多种数据集上表现优异，能有效评估不同模态在实例级别的预测价值。
selection_source: fresh_fetch
motivation: 现有多模态模型通常假设所有模态在训练和推理时均完备，但在实际应用中数据缺失和异步采集现象非常普遍。
method: 提出PRIMO模型，将缺失模态建模为潜变量，通过有监督学习捕捉其与观测模态及预测目标的关系，并利用采样技术进行推理。
result: 在合成数据集、Audio-Vision MNIST和MIMIC-III上的实验显示，PRIMO在模态缺失或完备的情况下均能达到与基准模型相当的性能。
conclusion: PRIMO不仅能有效处理不完整的多模态数据，还能通过基于方差的指标量化特定模态对预测结果的个体化影响。
---

## 摘要
尽管多模态大语言模型（MLLMs）近期取得了成功，但现有方法主要假设在训练和推理过程中多种模态均可用。在实践中，多模态数据通常是不完整的，因为模态可能缺失、异步采集，或者仅对部分样本可用。在这项工作中，我们提出了 PRIMO，这是一种有监督潜变量插补模型，用于量化多模态学习设置中任何缺失模态的预测影响。PRIMO 能够利用所有可用的训练样本，无论模态是完整的还是部分的。具体而言，它通过一个潜变量对缺失模态进行建模，该潜变量捕捉了在预测背景下缺失模态与观测模态之间的关系。在推理过程中，我们从学习到的缺失模态分布中抽取多个样本，既为了获得边缘预测分布（用于预测目的），也为了分析缺失模态对每个实例预测的影响。我们在合成 XOR 数据集、Audio-Vision MNIST 以及用于死亡率和 ICD-9 预测的 MIMIC-III 上评估了 PRIMO。在所有数据集上，当某一模态完全缺失时，PRIMO 的性能与单模态基准相当；当所有模态均可用时，其性能与多模态基准相当。PRIMO 使用基于方差的指标（该指标由跨潜变量补全的预测计算得出）在实例层面量化模态的预测影响。我们直观地展示了缺失模态的不同补全方式如何产生一组合理的标签。

## Abstract
Despite the recent success of Multimodal Large Language Models (MLLMs), existing approaches predominantly assume the availability of multiple modalities during training and inference. In practice, multimodal data is often incomplete because modalities may be missing, collected asynchronously, or available only for a subset of examples. In this work, we propose PRIMO, a supervised latent-variable imputation model that quantifies the predictive impact of any missing modality within the multimodal learning setting. PRIMO enables the use of all available training examples, whether modalities are complete or partial. Specifically, it models the missing modality through a latent variable that captures its relationship with the observed modality in the context of prediction. During inference, we draw many samples from the learned distribution over the missing modality to both obtain the marginal predictive distribution (for the purpose of prediction) and analyze the impact of the missing modalities on the prediction for each instance. We evaluate PRIMO on a synthetic XOR dataset, Audio-Vision MNIST, and MIMIC-III for mortality and ICD-9 prediction. Across all datasets, PRIMO obtains performance comparable to unimodal baselines when a modality is fully missing and to multimodal baselines when all modalities are available. PRIMO quantifies the predictive impact of a modality at the instance level using a variance-based metric computed from predictions across latent completions. We visually demonstrate how varying completions of the missing modality result in a set of plausible labels.

---

## 论文详细总结（自动生成）

以下是对论文《Characterizing the Predictive Impact of Modalities with Supervised Latent-Variable Modeling》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在实际的多模态学习场景（如医疗诊断）中，数据往往是不完整的。现有的多模态大模型（MLLM）大多假设训练和推理时所有模态均完备，但在现实中，某些检查可能未做、采集异步或仅对部分样本可用。
*   **核心问题**：当某个模态缺失时，它对最终预测的具体影响是什么？
*   **研究动机**：现有的插补方法（Imputation）多关注于“重建”缺失的输入，而非优化“预测”性能。此外，缺乏一种在实例级别（Instance-level）量化缺失模态对预测结果贡献的方法。

### 2. 方法论：PRIMO 模型
论文提出了 **PRIMO**（Supervised Latent-Variable Imputation Model），其核心思想是将缺失模态建模为潜变量 $z$，并直接优化预测目标。
*   **核心思想**：不追求重建原始的缺失模态 $x_m$，而是学习一个捕捉 $x_m$ 中与标签 $y$ 相关信息的潜空间 $z$。
*   **关键技术细节**：
    *   **生成过程**：假设标签 $y$ 在给定观测模态 $x_o$ 和潜变量 $z$ 的情况下与缺失模态 $x_m$ 条件独立。
    *   **联合训练**：模型同时最大化完整数据（$x_o, x_m$ 可见）和缺失数据（仅 $x_o$ 可见）的变分下界（ELBO）。
    *   **正则化**：为了打破潜空间的平移对称性，将缺失模态的先验 $p(z|x_o)$ 锚定在标准正态分布，并使完整模态先验 $p(z|x_o, x_m)$ 向其靠拢。
    *   **推理与量化**：推理时通过蒙特卡洛采样从先验分布中抽取多个 $z$，计算预测分布的方差 $V$（基于全变分距离 TVD）。$V$ 越大，说明缺失模态对该样本的预测影响越大。
    *   **聚类分析**：利用 DPGMM 对采样产生的 Logits 进行聚类，可视化不同补全方式下可能产生的“候选标签”。

### 3. 实验设计
*   **数据集**：
    1.  **合成 XOR 数据集**：用于验证模型在逻辑依赖场景下捕捉模态重要性的能力。
    2.  **Audio-Vision MNIST (AV-MNIST)**：数字分类，通过添加噪声和 PCA 降维控制模态难度。
    3.  **MIMIC-III**：真实医疗数据，包含静态信息（人口统计）和时序信息（生理指标），预测死亡率和 ICD-9 疾病编码。
*   **对比方法（Benchmarks）**：
    *   **单模态基准**：仅使用 $x_o$。
    *   **多模态基准**：使用完整的 $(x_o, x_m)$。
    *   **生成式基准**：MVAE、MMVAE（侧重重建）。
    *   **判别式基准**：CMMD、LVAE（侧重分类）。

### 4. 资源与算力
*   论文提到了使用了 **NYU IT 高性能计算资源（HPC）** 以及 NYU Langone 的计算核心。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但从模型架构（MLP, LeNet, GRU）和数据集规模来看，该实验在常规科研级 GPU（如 A100 或 V100）上应能高效完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据到真实复杂医疗数据的三个维度。
*   **消融与对比**：对比了 5 种以上的基准方法，并在 MIMIC-III 上针对不同任务（死亡率、肿瘤预测、呼吸系统疾病预测）进行了深入分析。
*   **客观性与公平性**：
    *   进行了**偏差分析（Bias Analysis）**，通过与“Oracle（先知）”模型对比，证明 PRIMO 在边缘化潜变量后能逼近最优预测器。
    *   所有实验均报告了多次运行的均值和标准差，确保了结果的可重复性。

### 6. 主要结论与发现
*   **性能表现**：PRIMO 在模态缺失时达到单模态最优水平，在模态完备时达到多模态最优水平，解决了传统 VAE 模型在分类任务上表现不佳的问题。
*   **模态影响的异质性**：
    *   在 MIMIC-III 中，静态信息对肿瘤预测（ICD-9 140-239）已足够，时序模态影响极小。
    *   对于呼吸系统疾病（ICD-9 460-519），时序模态至关重要，缺失会导致预测方差剧增。
*   **实例级洞察**：即使在同一任务中，不同患者对缺失模态的敏感度也不同（如高龄患者的死亡率预测更依赖时序数据）。

### 7. 优点
*   **端到端支持**：支持在训练和推理阶段同时处理完整和不完整的数据，无需丢弃样本。
*   **可解释性强**：通过方差指标 $V$ 和聚类可视化，为“黑盒”多模态模型提供了实例级的诊断工具，能识别模型是否依赖“捷径”。
*   **无需重建**：避免了高维原始数据（如图像、音频）重建的巨大计算开销，专注于预测相关的特征。

### 8. 不足与局限
*   **验证难题**：在实际应用中，由于缺失模态的真实值不可知，很难绝对验证“实例级重要性评估”的准确性（缺乏 Ground Truth）。
*   **模态数量限制**：目前的实验主要集中在双模态场景，虽然理论上可扩展，但在处理 3 个以上具有复杂缺失模式的模态时，计算复杂度和潜空间对齐难度会增加。
*   **基准局限**：主要在较小规模的模型和数据集上验证，尚未在超大规模多模态预训练模型（如 GPT-4V 级别）上进行压力测试。

（完）
