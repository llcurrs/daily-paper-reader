---
title: "Missing-by-Design: Certifiable Modality Deletion for Revocable Multimodal Sentiment Analysis"
title_zh: Missing-by-Design：面向可撤销多模态情感分析的可验证模态删除
authors: "Rong Fu, Wenxin Zhang, Ziming Wang, Chunlei Meng, Jiaxuan Lu, Jiekai Wu, Kangan Qian, Hao Zhang, Simon Fong"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16144v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 在保留任务相关信号的同时恢复缺失通道
tldr: 本研究针对多模态情感分析中的隐私合规需求，提出了Missing-by-Design (MBD) 框架，旨在实现特定数据模态的可验证撤销。该框架结合了结构化表示学习与可证明的参数修改流程，通过属性感知嵌入和生成式重构处理缺失模态，并利用显著性驱动的更新机制生成删除证书。MBD不仅在模态缺失时保持了高预测精度，还提供了一种比全量重训更高效的“手术式”遗忘方案，有效平衡了隐私保护与模型效用。
selection_source: fresh_fetch
motivation: 随着多模态系统处理敏感数据，用户和监管机构对撤销特定模态数据的需求日益增长，以满足隐私合规性和用户自主权。
method: 提出MBD框架，通过属性感知嵌入和生成式重构处理缺失模态，并利用显著性驱动的选择与高斯更新实现可验证的模态删除。
result: 实验表明，MBD在输入不完整的情况下仍能保持强劲的预测性能，并实现了实用的隐私与效用权衡。
conclusion: 该研究证明了精确的“手术式”遗忘是全量重新训练的高效替代方案，为可撤销的多模态情感分析提供了可验证的保障。
---

## 摘要
随着多模态系统越来越多地处理敏感个人数据，选择性撤销特定数据模态的能力已成为隐私合规和用户自主权的关键要求。我们提出了 Missing-by-Design (MBD)，这是一个用于可撤销多模态情感分析的统一框架，它将结构化表示学习与可验证的参数修改流水线相结合。在隐私敏感的应用中，可撤销性至关重要，因为用户或监管机构可能会要求删除特定模态的信息。MBD 学习属性感知嵌入，并采用基于生成器的重构来恢复缺失通道，同时保留与任务相关的信号。针对删除请求，该框架应用显著性驱动的候选选择和校准的高斯更新，以生成机器可验证的模态删除证书。在基准数据集上的实验表明，MBD 在输入不完整的情况下实现了强大的预测性能，并提供了实际的隐私-效用权衡，使手术式遗忘（surgical unlearning）成为全量重新训练的高效替代方案。

## Abstract
As multimodal systems increasingly process sensitive personal data, the ability to selectively revoke specific data modalities has become a critical requirement for privacy compliance and user autonomy. We present Missing-by-Design (MBD), a unified framework for revocable multimodal sentiment analysis that combines structured representation learning with a certifiable parameter-modification pipeline. Revocability is critical in privacy-sensitive applications where users or regulators may request removal of modality-specific information. MBD learns property-aware embeddings and employs generator-based reconstruction to recover missing channels while preserving task-relevant signals. For deletion requests, the framework applies saliency-driven candidate selection and a calibrated Gaussian update to produce a machine-verifiable Modality Deletion Certificate. Experiments on benchmark datasets show that MBD achieves strong predictive performance under incomplete inputs and delivers a practical privacy-utility trade-off, positioning surgical unlearning as an efficient alternative to full retraining.

---

## 论文详细总结（自动生成）

这篇论文提出了一个名为 **Missing-by-Design (MBD)** 的框架，旨在解决多模态情感分析（MSA）中的隐私合规与数据撤销问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **研究动机**：随着多模态系统广泛处理敏感个人数据（文本、音频、视频），用户或监管机构（如遵循 GDPR）可能要求撤销特定模态的数据。
*   **核心挑战**：
    1.  **鲁棒性**：如何在某些模态缺失或被删除时，依然保持高精度的情感预测。
    2.  **可验证的删除**：如何在不进行耗时的“全量重训”情况下，从已训练模型中彻底且可验证地移除特定模态的信息（即“机器遗忘”）。
*   **整体含义**：MBD 建立了一个统一的框架，既能通过生成式重构处理缺失模态，又能通过“参数手术”实现可证明的模态级信息删除。

### 2. 方法论：核心思想与关键技术
MBD 框架分为两个主要阶段：**属性感知的联合训练**和**可验证的权重手术**。

*   **属性感知表示学习**：
    *   **属性嵌入（Property Embedding, $P_m$）**：学习模态级别的先验知识，将样本无关的模态特征与样本特定的信号分离。
    *   **生成式重构**：利用生成器 $G_m$ 根据现有模态和属性嵌入恢复缺失通道。
    *   **对比反向翻译**：通过噪声对比估计（NCE）确保融合后的嵌入保留了必要的任务相关信号。
*   **可验证的权重手术（Weight Surgery）**：
    *   **重要性代理（SwiftPrune 启发）**：利用二阶导数近似（Hessian-free）评估参数对损失的影响，确保删除操作不严重损害模型效用。
    *   **模态显著性（Saliency）**：通过计算特定模态重构损失的梯度，识别出对该模态贡献最大的参数子集。
    *   **校准高斯更新**：基于差分隐私（DP）理论，对选定参数进行零化或注入校准噪声，满足 $( \epsilon_{mod}, \delta_{mod} )$ 不可区分性保证。
    *   **模态删除证书（MDC）**：输出包含删除元数据、哈希摘要和隐私预算的机器可读证书，供第三方验证。

### 3. 实验设计
*   **数据集**：使用了三个标准基准数据集：**CMU-MOSI**、**CMU-MOSEI**（情感分析）和 **IEMOCAP**（情绪识别）。
*   **Benchmark 与对比方法**：
    *   **全模态对比**：对比了 UniMSE、ConFEDE、MGCL、MSAmba 等 10 余种最先进（SOTA）模型。
    *   **缺失模态对比**：在不同缺失率（0.1 到 0.7）和固定缺失模式下，对比了 GCNet、IMDer、LNLN 等鲁棒性模型。
    *   **删除验证**：通过白盒/黑盒攻击成功率（ASR）来验证删除效果。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX-3090 (24GB)** GPU。
*   **效率对比**：
    *   **删除耗时**：在 CMU-MOSI 上执行一次音频模态删除仅需 **39 秒**（包括显著性计算、校准和手术）。
    *   **重训耗时**：从头训练一个缺少该模态的模型需要 **2.9 小时**。
    *   **结论**：MBD 实现了约 **270 倍** 的加速，且无需额外的显存开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了全模态性能测试、7 种不同缺失率下的鲁棒性测试、固定模态组合测试。
    *   进行了详尽的**消融实验**，验证了属性嵌入、重构模块、融合模块和手术模块的各自贡献。
    *   包含**超参数敏感性分析**（如手术预算 $r$、校准批次大小 $|B|$ 等）。
*   **客观性与公平性**：所有实验均基于 3 个独立随机种子取平均值，并进行了显著性检验（p < 0.01）。对比基线涵盖了从 2016 年到 2025 年的经典与最新方法，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能领先**：MBD 在全模态和缺失模态场景下均优于现有 SOTA 方法，Acc2 和 F1 分数提升约 1-2 个百分点。
*   **隐私-效用权衡**：在 $\epsilon_{mod} \leq 1$ 的保守预算下，模型能有效抵御成员推理攻击（攻击成功率降至接近随机猜测），同时情感预测精度仅下降约 1-1.5%。
*   **重构质量**：属性嵌入显著提升了重构信号的质量，t-SNE 可视化显示其生成的特征空间更具判别性。

### 7. 优点
*   **统一性**：将“处理缺失数据”和“主动删除数据”这两个看似独立的问题在一个框架下统一解决。
*   **理论完备**：引入了差分隐私的不可区分性证明，为模态删除提供了数学保障，而非简单的启发式剪枝。
*   **实用性强**：极高的处理效率（秒级完成）使其非常适合需要频繁响应用户删除请求的生产环境。

### 8. 不足与局限
*   **人口统计学偏差**：论文指出所用基准数据集缺乏种族、方言等人口统计学标注，因此删除操作对不同群体的公平性影响尚不明确。
*   **残留风险**：虽然满足数学上的不可区分性，但在极端分布偏移或更强攻击下，可能仍存在微量信息残留。
*   **证书完整性**：证书（MDC）本身需要硬件认证或区块链等机制来防止篡改或重放攻击，这在实际部署中增加了系统复杂度。

（完）
