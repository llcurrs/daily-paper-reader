---
title: "PAKAN: Pixel Adaptive Kolmogorov-Arnold Network Modules for Pansharpening"
title_zh: PAKAN：用于全色锐化的像素自适应 Kolmogorov-Arnold 网络模块
authors: "Haoyu Zhang, Haojing Chen, Zhen Zhong, Liangjian Deng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15109v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 用于全色锐化空间光谱融合的像素自适应网络
tldr: 针对全色锐化任务中静态激活函数难以动态建模复杂非线性映射的问题，本文提出了一种像素自适应柯尔莫哥洛夫-阿诺德网络（PAKAN）。该框架通过设计2D和1D自适应KAN模块，分别在空间和光谱维度生成动态样条求和权重，实现了特征的高效融合与精炼。实验证明，PAKAN显著提升了全色锐化的性能，展现了自适应激活函数的优越性。
selection_source: fresh_fetch
motivation: 传统深度学习模型和KAN在全色锐化中缺乏推理时的动态适应性，限制了对复杂空间-光谱映射的建模能力。
method: 提出了包含2D空间自适应和1D光谱自适应权重的PAKAN模块，并构建了用于特征融合的PAKAN 2to1和用于特征精炼的PAKAN 1to1。
result: 大量实验表明，所提模块在全色锐化任务中显著增强了网络性能，优于现有方法。
conclusion: 像素自适应激活机制能有效提升全色锐化中的非线性建模能力，是改进该任务性能的有效途径。
---

## 摘要
全色锐化（Pansharpening）旨在将全色图像的高分辨率空间细节与多光谱图像的丰富光谱信息进行融合。现有的用于该任务的深度神经网络通常依赖于静态激活函数，这限制了它们动态建模实现最优空间-光谱融合所需的复杂非线性映射的能力。虽然最近引入的 Kolmogorov-Arnold 网络（KAN）利用了可学习的激活函数，但传统的 KAN 在推理过程中缺乏动态自适应性。为了解决这一局限性，我们提出了一个像素自适应 Kolmogorov-Arnold 网络框架。从 KAN 出发，我们设计了两种自适应变体：一种是在空间维度上生成样条求和权重的二维自适应 KAN，另一种是在光谱通道上生成权重的一维自适应 KAN。随后，这两个组件被组装成用于特征融合的 PAKAN 2to1 和用于特征细化的 PAKAN 1to1。大量的实验表明，我们提出的模块显著增强了网络性能，证明了像素自适应激活在全色锐化任务中的有效性和优越性。

## Abstract
Pansharpening aims to fuse high-resolution spatial details from panchromatic images with the rich spectral information of multispectral images. Existing deep neural networks for this task typically rely on static activation functions, which limit their ability to dynamically model the complex, non-linear mappings required for optimal spatial-spectral fusion. While the recently introduced Kolmogorov-Arnold Network (KAN) utilizes learnable activation functions, traditional KANs lack dynamic adaptability during inference. To address this limitation, we propose a Pixel Adaptive Kolmogorov-Arnold Network framework. Starting from KAN, we design two adaptive variants: a 2D Adaptive KAN that generates spline summation weights across spatial dimensions and a 1D Adaptive KAN that generates them across spectral channels. These two components are then assembled into PAKAN 2to1 for feature fusion and PAKAN 1to1 for feature refinement. Extensive experiments demonstrate that our proposed modules significantly enhance network performance, proving the effectiveness and superiority of pixel-adaptive activation in pansharpening tasks.

---

## 论文详细总结（自动生成）

### PAKAN：用于全色锐化的像素自适应 Kolmogorov-Arnold 网络模块总结

#### 1. 核心问题与研究动机
*   **核心问题**：全色锐化（Pansharpening）需要将全色图像（PAN）的高空间分辨率与多光谱图像（MS）的高光谱分辨率融合。现有的深度学习模型（如 CNN、MLP）多依赖**静态激活函数**，无法针对遥感图像中复杂的空间异质性和光谱多样性进行动态建模。
*   **研究动机**：虽然新提出的 Kolmogorov-Arnold 网络（KAN）具有可学习的激活函数，但其权重在训练后固定，缺乏推理时的**动态自适应性**。本文旨在开发一种能够根据输入内容实时调整激活函数权重的框架，以实现更精细的空间-光谱融合。

#### 2. 方法论
*   **核心思想**：将 KAN 的样条函数（Spline function）权重参数化为输入特征的函数，通过轻量级生成器动态产生权重，实现“像素级自适应”。
*   **关键技术细节**：
    *   **2D 自适应 KAN（空间维度）**：针对每个空间位置 $(h, w)$ 生成样条求和权重，捕捉局部纹理和边缘特征。
    *   **1D 自适应 KAN（光谱维度）**：针对每个光谱通道 $c$ 生成权重，利用全局上下文（通过全局平均池化）保持光谱一致性。
    *   **耦合机制**：采用乘法交互（Hadamard 积）结合 2D 和 1D 分支，只有当空间和光谱证据同时支持时才放大特征，减少伪影。
    *   **模块化设计**：
        *   **PAKAN 2to1**：用于特征融合，将 PAN 和 MS 特征拼接后进行通道压缩与自适应映射。
        *   **PAKAN 1to1**：用于特征精炼，作为即插即用的增强块提升单流特征质量。

#### 3. 实验设计
*   **数据集**：使用了三个经典卫星数据集：WorldView-3 (WV3)、QuickBird (QB) 和 GaoFen-2 (GF2)。
*   **实验场景**：涵盖了**降低分辨率（Reduced-resolution）**（有地面真值参考）和**全分辨率（Full-resolution）**（无参考）两种评估场景。
*   **Benchmark 与对比方法**：
    *   **传统方法**：MTF-GLP-FS, BDSD-PC, TV。
    *   **深度学习方法**：PNN, PanNet, DiCNN, FusionNet, LAGNet, LGPNet, U2Net（CNN 类）以及 PanMamba（状态空间模型类）。
*   **评价指标**：PSNR, SAM, ERGAS, Q4/Q8, $D_\lambda, D_s$, HQNR。

#### 4. 资源与算力
*   **硬件**：单张 NVIDIA GeForce RTX 4090 GPU。
*   **软件**：PyTorch 框架。
*   **训练细节**：训练 500 个 epoch，使用 Adam 优化器，初始学习率 $4 \times 10^{-4}$，Batch Size 为 32。推理时采用滑动窗口切片处理全分辨率图像。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同传感器的数据集上进行了测试，对比了 11 种主流方法。
*   **消融实验**：
    *   验证了“像素自适应（PA）”与“KAN 结构”各自的贡献。
    *   验证了 1D 光谱分支与 2D 空间分支的互补性。
*   **通用性测试**：将 PAKAN 模块分别嵌入 PanNet、FusionNet 和 U2Net，均取得了性能提升。
*   **评价**：实验设计全面，涵盖了定量指标、定性视觉对比（HQNR 图、残差图）及通用性验证，结果客观公平。

#### 6. 主要结论与发现
*   **性能领先**：PAKAN 在所有数据集和指标上均表现优异，尤其在光谱保真度（SAM）和空间重建（ERGAS）方面提升显著。
*   **动态优势**：动态生成的激活函数权重能有效应对遥感场景中的非平稳性（如建筑物与植被的差异）。
*   **物理一致性**：通过空间与光谱维度的分解建模，PAKAN 很好地契合了全色锐化的物理本质。

#### 7. 优点
*   **创新性**：首次将动态自适应机制引入 KAN 并应用于全色锐化任务。
*   **即插即用**：模块设计灵活，可以轻松集成到现有的各种深度学习架构中。
*   **效率平衡**：通过 1D 和 2D 的分解设计，在提供高容量自适应能力的同时，保持了较低的计算开销。

#### 8. 不足与局限
*   **样条函数开销**：尽管生成器轻量，但 B-样条（B-splines）的计算相比传统 ReLU 等激活函数仍更为复杂，在大规模实时处理中可能存在延迟压力。
*   **超参数敏感性**：样条函数的阶数、网格大小（Grid size）等超参数可能需要针对不同传感器进行微调以达到最优效果。
*   **推理速度**：文中未详细对比各方法在推理阶段的耗时（FPS），仅强调了参数量的紧凑性。

（完）
