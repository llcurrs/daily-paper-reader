---
title: "Hybrid Fusion: One-Minute Efficient Training for Zero-Shot Cross-Domain Image Fusion"
title_zh: 混合融合：面向零样本跨域图像融合的一分钟高效训练
authors: "Ran Zhang, Xuanhua He, Liu Liu"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20851v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 用于零样本跨域图像融合的混合框架
tldr: 本研究针对图像融合中传统方法性能不足及深度学习方法训练效率低、存在训练-推理差异的问题，提出了一种混合融合框架。该框架利用可学习的U-Net生成动态引导图，并结合固定的拉普拉斯金字塔内核进行融合，实现了策略学习与像素合成的解耦。该方法支持全分辨率训练，仅需约一分钟即可完成训练，且在红外-可见光及医学图像等多种任务中展现出卓越的零样本泛化能力和高保真度。
selection_source: fresh_fetch
motivation: 传统图像融合方法缺乏自适应性，而深度学习方法因基于补丁的训练导致效率低下且与全分辨率推理存在差异。
method: 提出一种将可学习U-Net生成的引导图与固定拉普拉斯金字塔融合内核相结合的混合框架，实现高效的全分辨率训练。
result: 在RTX 4090上仅需一分钟训练即可达到SOTA性能，并在多种跨领域任务中表现出强大的零样本泛化能力。
conclusion: 该方法通过解耦策略与合成，为高效、高保真且具备跨领域适应性的图像融合提供了新方案。
---

## 摘要
图像融合旨在将来自多个源的互补信息整合到单张更优质的图像中。虽然传统方法速度较快，但缺乏适应性且性能有限。相反，深度学习方法虽然取得了最先进（SOTA）的结果，但存在严重的效率问题：它们依赖于缓慢且耗资源的基于补丁（patch-based）的训练，这导致了训练与全分辨率推理之间存在显著差距。我们提出了一种新型混合框架来解决这一权衡问题。我们的方法利用可学习的 U-Net 生成动态引导图，用于指导经典的固定拉普拉斯金字塔（Laplacian pyramid）融合算子。这种将策略学习与像素合成相解耦的设计实现了极高效率的全分辨率训练，消除了训练与推理之间的差距。因此，我们的模型在不依赖任何外部模型的情况下，仅需在 RTX 4090 上训练约一分钟或在消费级笔记本 GPU 上训练约两分钟，即可从零开始达到与 SOTA 相当的性能，并在从红外-可见光到医学成像的各种任务中展现出强大的零样本泛化能力。在设计上，融合输出仅由源信息线性构建，确保了关键应用中的高保真度。代码可在 https://github.com/Zirconium233/HybridFusion 获取。

## Abstract
Image fusion seeks to integrate complementary information from multiple sources into a single, superior image. While traditional methods are fast, they lack adaptability and performance. Conversely, deep learning approaches achieve state-of-the-art (SOTA) results but suffer from critical inefficiencies: their reliance on slow, resource-intensive, patch-based training introduces a significant gap with full-resolution inference. We propose a novel hybrid framework that resolves this trade-off. Our method utilizes a learnable U-Net to generate a dynamic guidance map that directs a classic, fixed Laplacian pyramid fusion kernel. This decoupling of policy learning from pixel synthesis enables remarkably efficient full-resolution training, eliminating the train-inference gap. Consequently, our model achieves SOTA-comparable performance in about one minute on a RTX 4090 or two minutes on a consumer laptop GPU from scratch without any external model and demonstrates powerful zero-shot generalization across diverse tasks, from infrared-visible to medical imaging. By design, the fused output is linearly constructed solely from source information, ensuring high faithfulness for critical applications. The codes are available at https://github.com/Zirconium233/HybridFusion

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Hybrid Fusion** 的新型图像融合框架，旨在解决深度学习方法在图像融合领域面临的训练效率低下、训练与推理不一致以及生成伪影（幻觉）等核心问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的深度学习图像融合方法（如基于 Transformer 或扩散模型的方法）通常依赖于**基于补丁（patch-based）的训练**，这导致在处理全分辨率图像时存在“训练-推理差异”。此外，这些模型往往试图从头学习像素合成，不仅耗费数小时甚至数天的训练时间，还容易在医学等敏感领域产生不真实的“幻觉”信息。
*   **研究动机**：作者认为图像融合本质上是信息的分配而非生成。因此，需要一种既能保留传统方法解释性和高效性，又能发挥深度学习自适应能力的方案，实现“分钟级”的高效训练和强大的跨域泛化。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：**解耦策略学习与像素合成**。使用一个轻量级的可学习网络（U-Net）来预测“引导权重图（Guidance Map）”，而实际的像素融合过程由一个固定的、不可学习的经典算法——**拉普拉斯金字塔（Laplacian Pyramid）**执行。
*   **关键流程**：
    1.  **预处理**：将可见光图像转为 YCbCr 空间，仅对亮度通道（Y）和红外图像进行融合。
    2.  **引导图生成**：U-Net 输入级联的源图像，输出像素级的权重图 $\mu$。
    3.  **多尺度融合**：将源图像分解为拉普拉斯金字塔。在每个层级，利用下采样后的权重图 $\mu$ 对源图像特征进行线性加权合并：$L_{fused} = (1-\mu) \cdot L_{vi} + \mu \cdot L_{ir}$。
    4.  **无监督损失函数**：包含强度最大值损失（$L_{max}$）、梯度最大值损失（$L_{grad}$）、结构相似性损失（$L_{ssim}$）和强度一致性损失（$L_{consist}$）。通过对 240 组超参数进行网格搜索，确定了最优的损失权重。

### 3. 实验设计与对比基准
*   **数据集/场景**：
    *   **红外-可见光融合 (IVF)**：MSRS、M3FD、RoadScene。
    *   **医学图像融合**：PET-MRI、CT-MRI、SPECT-MRI。
    *   **视频融合**：M3SVD。
*   **对比方法**：对比了包括 CDDFuse、SwinFusion、DDFM（扩散模型）、Text-IF（大模型引导）、DTPF 等在内的 10 余种 SOTA 方法。
*   **下游任务**：使用 YOLOv8n 在融合图像上进行目标检测，以验证融合结果对语义信息的保留能力。

### 4. 资源与算力
论文详细列出了不同硬件下的训练表现：
*   **RTX 4090 (Ubuntu)**：训练 1 个 epoch 仅需 **30 秒**，达到 SOTA 性能仅需约 1 分钟。
*   **RTX 4060 Laptop (WSL2)**：训练 1 个 epoch 约 **2.3 分钟**。
*   **Google Colab T4 (免费版)**：训练 1 个 epoch 约 **5.1 分钟**。
*   **显存占用**：由于采用了全分辨率训练且模型轻量，在 640x480 分辨率下推理仅需约 12GB 显存（bs=16），远低于 Restormer 等架构。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 类主流融合任务、7 个公开数据集，并进行了视频融合的零样本测试。
*   **消融实验**：针对混合框架的有效性、损失函数超参数（240 组网格搜索）、模型缩放（从 80K 到 17M 参数）进行了详细分析。
*   **客观性**：作者在相同硬件（RTX 4090）上重新训练并对比了多个主流模型，确保了效率和性能对比的公平性。

### 6. 主要结论与发现
*   **极速收敛**：该方法在 1-2 个 epoch 内即可达到甚至超过训练了数百个 epoch 的复杂模型性能。
*   **零样本泛化**：仅在 MSRS（自然场景）上训练的模型，在医学图像任务上表现出极强的零样本迁移能力，甚至优于专门在医学数据上训练的模型。
*   **高保真度**：由于融合是基于源图像的线性构建，该方法有效避免了生成式模型常见的颜色偏移和纹理伪影。
*   **模型缩放效应**：增大 U-Net 参数量虽然增加了计算量，但能显著加快收敛速度，使训练更加高效。

### 7. 优点
*   **训练效率极高**：将训练时间从“小时级”缩短至“分钟级”，极大地降低了科研和应用门槛。
*   **消除训练-推理差异**：支持全分辨率端到端训练，避免了 patch 训练带来的边界效应或性能下降。
*   **物理回退机制**：即使网络训练不足，由于底层是拉普拉斯融合，输出仍能保持基本的图像结构，不会产生随机噪声。
*   **可解释性强**：生成的权重图直观地展示了模型对红外目标和可见光背景的选择策略。

### 8. 不足与局限
*   **视频一致性**：虽然在视频数据集上做了测试，但由于是逐帧处理，缺乏专门的时域约束，可能在极端动态场景下存在闪烁风险。
*   **架构依赖**：性能高度依赖于拉普拉斯金字塔的分解层数和 U-Net 的引导能力，对于某些非线性极强的融合需求，线性加权可能存在表达上限。
*   **色彩保持**：目前主要在 YCbCr 空间处理亮度，对于需要深度改变色彩分布的特殊融合任务（如伪彩色增强）可能需要额外设计。

（完）
