---
title: "Missing No More: Dictionary-Guided Cross-Modal Image Fusion under Missing Infrared"
title_zh: 不再缺失：红外缺失情况下的字典引导跨模态图像融合
authors: "Yafei Zhang, Meng Ma, Huafeng Li, Yu Liu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08018v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 红外模态缺失下的跨模态图像融合
tldr: 针对红外-可见光图像融合中红外模态缺失的问题，本文提出一种基于字典引导的系数域融合框架。通过联合共享字典表示学习（JSRL）构建可解释的原子空间，利用可见光引导的红外推理（VGII）在系数域生成伪红外特征，并结合大语言模型先验进行优化，最后通过自适应融合（AFRI）重建图像。该方法避免了不可控的像素级生成，显著提升了缺失红外场景下的感知质量和检测性能。
selection_source: fresh_fetch
motivation: 现有的红外-可见光融合方法在红外模态缺失时，像素空间生成难以控制且缺乏可解释性。
method: 提出一种包含共享字典学习、系数域红外推理及自适应融合的流水线，利用大语言模型作为语义先验进行闭环优化。
result: 在红外缺失的实验设置下，该方法在感知质量和下游目标检测任务中均表现出持续的性能提升。
conclusion: 这是首个通过联合学习共享字典并在系数域进行推理融合来解决红外缺失问题的框架，具有较强的可解释性和实用性。
---

## 摘要
红外-可见光（IR-VIS）图像融合对于感知和安全至关重要，但大多数方法在训练和推理阶段都依赖于两种模态的可用性。当红外模态缺失时，像素空间的生成式替代方案变得难以控制，且本质上缺乏可解释性。我们通过提出一种基于共享卷积字典的字典引导系数域框架，来解决红外缺失下的融合问题。该流程包含三个关键组件：（1）联合共享字典表示学习（JSRL），学习 IR 和 VIS 模态共享的统一且可解释的原子空间；（2）可见光引导的红外推理（VGII），在系数域中将 VIS 系数转换为伪 IR 系数，并在冻结的大语言模型作为弱语义先验的引导下进行一步闭环细化；（3）通过表示推理的自适应融合（AFRI），通过窗口注意力和卷积混合在原子层级合并 VIS 结构和推理出的 IR 线索，随后使用共享字典进行重建。这种“编码-转换-融合-重建”流程避免了不可控的像素空间生成，同时确保了在可解释的字典-系数表示中保留先验。在红外缺失设置下的实验表明，感知质量和下游检测性能得到了一致的提升。据我们所知，这是首个通过联合学习共享字典并执行系数域推理融合来解决红外缺失融合问题的框架。源代码已在 https://github.com/harukiv/DCMIF 公开。

## Abstract
Infrared-visible (IR-VIS) image fusion is vital for perception and security, yet most methods rely on the availability of both modalities during training and inference. When the infrared modality is absent, pixel-space generative substitutes become hard to control and inherently lack interpretability. We address missing-IR fusion by proposing a dictionary-guided, coefficient-domain framework built upon a shared convolutional dictionary. The pipeline comprises three key components: (1) Joint Shared-dictionary Representation Learning (JSRL) learns a unified and interpretable atom space shared by both IR and VIS modalities; (2) VIS-Guided IR Inference (VGII) transfers VIS coefficients to pseudo-IR coefficients in the coefficient domain and performs a one-step closed-loop refinement guided by a frozen large language model as a weak semantic prior; and (3) Adaptive Fusion via Representation Inference (AFRI) merges VIS structures and inferred IR cues at the atom level through window attention and convolutional mixing, followed by reconstruction with the shared dictionary. This encode-transfer-fuse-reconstruct pipeline avoids uncontrolled pixel-space generation while ensuring prior preservation within interpretable dictionary-coefficient representation. Experiments under missing-IR settings demonstrate consistent improvements in perceptual quality and downstream detection performance. To our knowledge, this represents the first framework that jointly learns a shared dictionary and performs coefficient-domain inference-fusion to tackle missing-IR fusion. The source code is publicly available at https://github.com/harukiv/DCMIF.

---

## 论文详细总结（自动生成）

以下是对论文《Missing No More: Dictionary-Guided Cross-Modal Image Fusion under Missing Infrared》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的红外-可见光（IR-VIS）融合方法高度依赖双模态数据的同时存在。但在实际应用（如传感器故障、成本限制）中，红外模态往往缺失。
*   **研究动机**：现有的缺失模态补全方法多在“像素空间”进行黑盒生成，存在生成结果不可控、物理一致性差、缺乏可解释性以及容易产生伪影等问题。本文旨在探索如何仅利用单路可见光图像，通过可解释的推理机制实现高质量的红外信息增强与融合。

### 2. 方法论
论文提出了一种名为 **DCMIF** 的框架，核心思想是将图像映射到统一的“字典-系数”空间进行推理与融合，主要包含三个模块：
*   **联合共享字典表示学习 (JSRL)**：基于模型驱动的展开网络（Unfolding Network），学习一个红外与可见光共享的卷积字典。通过交替优化算法（如 Sherman-Morrison 公式求解数据一致性项），将图像分解为原子空间中的系数表示，建立跨模态的结构对应关系。
*   **可见光引导的红外推理 (VGII)**：在系数域（而非像素域）执行模态转换。利用冻结的大语言模型（LLM，如 Qwen-7B-Chat）作为弱语义先验，通过特征线性调制（FiLM）对可见光系数进行语义校正，预测出潜在的红外系数。
*   **通过表示推理的自适应融合 (AFRI)**：采用卷积-注意力融合块（CAF），在原子层级通过门控机制自适应地合并可见光结构和推理出的红外线索，最后利用共享字典重建出融合图像。
*   **流程总结**：编码（映射至系数域） $\rightarrow$ 转换（预测红外系数） $\rightarrow$ 融合（原子级合并） $\rightarrow$ 重建（字典合成）。

### 3. 实验设计
*   **数据集**：
    *   **融合质量评估**：FLIR、KAIST、MSRS。
    *   **下游任务评估**：M3FD（目标检测）、FMB（语义分割）。
*   **Benchmark 与对比方法**：对比了 10 种最先进的（SOTA）融合方法，包括 U2Fusion、TarDAL、CDDFuse、LRRNet、IVFWSR、CoCoNet、EMMA、TIMFusion、DCEvo 和 SAGE。
*   **特别说明**：对比方法在推理时均使用了红外+可见光双输入，而本文方法在推理时**仅使用可见光输入**。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA GeForce RTX 4090 GPU**。
*   **软件框架**：PyTorch 3.10.18。
*   **训练细节**：
    *   JSRL 模块在 MSRS 数据集上训练 1000 个 epoch（Batch size 24）。
    *   VGII 和 AFRI 模块训练 10 个 epoch（Batch size 10）。
    *   优化器使用 Adam，初始学习率分别为 0.0001（JSRL）和 0.001（推理/融合）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流数据集上进行了测试，涵盖了从主观视觉对比、客观指标评价到下游任务验证的全流程。
*   **消融实验**：针对共享字典的有效性、LLM 调制的必要性、字典核大小（3x3, 5x5, 7x7）、迭代块数量（N=1, 2, 3）以及文本提示（Prompt）的完整性进行了详细的消融研究。
*   **客观性**：通过与“先生成红外再融合”的多阶段方案（结合 EGGAN 或 PID 生成模型）进行对比，证明了其单阶段系数域推理的优越性。实验设计全面，对比维度客观。

### 6. 主要结论与发现
*   **性能领先**：即使在缺失红外输入的情况下，该方法在多个指标（如 AG, EI, SF 等）上达到了与双模态输入方法相当甚至更优的水平。
*   **下游任务增益**：在 M3FD 检测任务和 FMB 分割任务中，该方法生成的图像显著提升了模型的识别精度，证明了推理出的红外特征具有实际的语义价值。
*   **稳定性**：相比像素级生成，系数域的推理能更好地保持结构一致性，避免了幻觉模式（Hallucinated patterns）的产生。

### 7. 优点与亮点
*   **可解释性强**：将深度学习与传统的稀疏表示/字典学习结合，使跨模态转换发生在具有物理意义的原子空间。
*   **闭环设计**：通过“编码-推理-融合-重建”的闭环，确保了数据一致性约束贯穿始终。
*   **轻量化语义先验**：巧妙地利用冻结的 LLM 提供宏观语义指导，无需训练沉重的生成头，降低了训练难度并提升了热线索补全的准确性。

### 8. 不足与局限
*   **极端环境限制**：该方法依赖可见光提供的结构信息来推理红外特征。在极端恶劣天气（如浓雾、完全黑暗）下，若可见光传感器无法捕捉到任何有效信息，模型将难以推理出准确的红外线索。
*   **算力开销**：虽然推理时不需要真实红外图，但引入 LLM 作为语义批评者以及复杂的卷积字典展开运算，可能会在嵌入式实时部署中面临计算压力（尽管文中提到推理时间约为 5.7s，但在实时性要求极高的场景下仍有优化空间）。

（完）
