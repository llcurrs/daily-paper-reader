---
title: Federated Modality-specific Encoders and Partially Personalized Fusion Decoder for Multimodal Brain Tumor Segmentation
title_zh: 用于多模态脑肿瘤分割的联邦模态特定编码器与部分个性化融合解码器
authors: "Hong Liu, Dong Wei, Qian Dai, Xian Wu, Yefeng Zheng, Liansheng Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04887v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 仅拥有完整成像模态子集的联邦学习
tldr: 本文针对多模态脑肿瘤分割中的联邦学习挑战，提出了FedMEPD框架。该框架旨在解决模态缺失导致的异构性以及客户端的个性化需求。通过引入联邦模态特定编码器和部分个性化的融合解码器，并利用服务器端的全局全模态锚点引导客户端校准缺失模态的特征表示，FedMEPD实现了高效的跨模态协作。实验证明，该方法在BraTS基准测试中表现优异，有效提升了在数据不完整情况下的分割性能。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中多模态数据缺失导致的模态间异构性，以及客户端对个性化模型的实际需求。
method: 提出FedMEPD框架，结合了联邦模态特定编码器、动态部分个性化解码器，并利用全局锚点与交叉注意力机制补偿缺失模态信息。
result: 在BraTS 2018和2020数据集上的实验结果显示，该方法在多模态和个性化联邦学习场景下均优于现有先进算法。
conclusion: FedMEPD通过创新的编码器-解码器设计和锚点校准机制，有效平衡了全局知识共享与局部个性化适配，提升了脑肿瘤分割的鲁棒性。
---

## 摘要
现有的医学图像分析联邦学习（FL）方法大多仅考虑了模态内异质性，限制了其在多模态成像应用中的适用性。在实践中，部分联邦学习参与者可能仅拥有完整成像模态的一个子集，这带来了模态间异质性挑战，难以在所有参与者的数据上有效训练全局模型。同时，每个参与者都希望在联邦学习中获得针对其本地数据特征定制的个性化模型。本研究提出了一种新的联邦学习框架，即具有联邦模态特定编码器和部分个性化多模态融合解码器的 FedMEPD，以同时解决这两个问题。具体而言，FedMEPD 为每种模态采用专属编码器以应对模态间异质性。虽然这些编码器是完全联邦化的，但解码器是部分个性化的，以满足个体需求——利用全局和本地参数更新之间的差异来动态确定哪些解码器滤波器是个性化的。在实现方面，拥有全模态数据的服务器采用融合解码器来融合来自所有模态特定编码器的表示，从而桥接各模态，通过反向传播优化编码器。此外，从融合的多模态表示中提取多个锚点（anchors），并与模型参数一起分发给客户端。相反，模态不完整的客户端通过缩放点积交叉注意力机制，将其缺失模态的表示向全局全模态锚点进行校准，从而弥补因模态缺失造成的信息损失。FedMEPD 在 BraTS 2018 和 2020 多模态脑肿瘤分割基准数据集上进行了验证。结果表明，该方法优于多种最新的多模态和个性化联邦学习方法，且其新颖设计是有效的。

## Abstract
Most existing federated learning (FL) methods for medical image analysis only considered intramodal heterogeneity, limiting their applicability to multimodal imaging applications. In practice, some FL participants may possess only a subset of the complete imaging modalities, posing intermodal heterogeneity as a challenge to effectively training a global model on all participants' data. Meanwhile, each participant expects a personalized model tailored to its local data characteristics in FL. This work proposes a new FL framework with federated modality-specific encoders and partially personalized multimodal fusion decoders (FedMEPD) to address the two concurrent issues. Specifically, FedMEPD employs an exclusive encoder for each modality to account for the intermodal heterogeneity. While these encoders are fully federated, the decoders are partially personalized to meet individual needs -- using the discrepancy between global and local parameter updates to dynamically determine which decoder filters are personalized. Implementation-wise, a server with full-modal data employs a fusion decoder to fuse representations from all modality-specific encoders, thus bridging the modalities to optimize the encoders via backpropagation. Moreover, multiple anchors are extracted from the fused multimodal representations and distributed to the clients in addition to the model parameters. Conversely, the clients with incomplete modalities calibrate their missing-modal representations toward the global full-modal anchors via scaled dot-product cross-attention, making up for the information loss due to absent modalities. FedMEPD is validated on the BraTS 2018 and 2020 multimodal brain tumor segmentation benchmarks. Results show that it outperforms various up-to-date methods for multimodal and personalized FL, and its novel designs are effective.

---

## 论文详细总结（自动生成）

### 论文总结：用于多模态脑肿瘤分割的联邦模态特定编码器与部分个性化融合解码器 (FedMEPD)

#### 1. 核心问题与整体含义
*   **研究背景**：在医学图像分析中，联邦学习（FL）通常只关注模态内的异质性（如不同中心的设备差异）。但在实际应用中，不同医疗机构可能由于扫描协议、成本或患者过敏等原因，仅拥有部分成像模态（如只有 MRI 的 T2 和 FLAIR，缺失 T1 和 T1c），这被称为**模态间异质性**。
*   **核心问题**：如何在保护隐私的前提下，利用模态不完整的客户端数据训练一个高性能的全局全模态模型，同时为每个客户端提供针对其特定模态组合定制的个性化模型。

#### 2. 论文提出的方法论 (FedMEPD)
该框架通过“联邦编码器+部分个性化解码器+全局锚点校准”的组合来解决问题：
*   **联邦模态特定编码器 (Federated Modality-specific Encoders)**：为 T1、T1c、T2、FLAIR 四种模态分别设置专属编码器。只有拥有对应模态的客户端才会参与该模态编码器的参数聚合，从而实现参数的专业化。
*   **部分个性化融合解码器 (Partially Personalized Fusion Decoder)**：
    *   **动态筛选**：利用服务器与客户端之间参数更新的方向一致性（余弦相似度）来决定哪些滤波器（Filter）需要联邦化，哪些需要个性化。
    *   **策略**：如果某个滤波器的更新方向在多个回合内持续冲突，则将其设为本地个性化，不再参与全局聚合，以适应本地数据分布。
*   **多锚点多模态表示与 LACCA 模块**：
    *   **全局锚点**：服务器利用全模态数据提取各类别（如水肿、增强肿瘤等）的多个特征原型（Anchors），并分发给客户端。
    *   **局部自适应校准 (LACCA)**：客户端通过**交叉注意力机制 (Cross-Attention)**，将本地缺失模态的特征向服务器的全模态锚点进行对齐，从而在特征层面补偿缺失的信息。

#### 3. 实验设计
*   **数据集**：使用了公共基准数据集 **BraTS 2018** 和 **BraTS 2020**，以及用于验证泛化性的 **HaN-Seg**（头颈部器官分割）数据集。
*   **实验场景**：将数据划分为 9 个子集（1 个服务器，8 个客户端）。客户端被分配不同的模态组合（单模态、双模态、三模态、全模态）。
*   **对比方法 (Baselines)**：
    *   基础方法：本地训练 (Local models)、RFNet (集中式训练)。
    *   经典/先进 FL 方法：FedAvg、perFL、FedNorm、IOP-FL、FedCostWAvg、FedPIDAvg。
    *   多模态/个性化 FL 专门方法：FedMSplit、CreamFL、FedIoT。
*   **评价指标**：平均 Dice 系数 (mDSC) 和 95% 豪斯多夫距离 (HD95)。

#### 4. 资源与算力
*   **硬件环境**：使用了 **5 台 NVIDIA RTX 2080Ti GPU**。
*   **训练配置**：服务器占用 1 台 GPU，其余客户端均匀分布在另外 4 台 GPU 上。
*   **训练时长**：共进行 **1000 个通信回合 (Rounds)**，每个回合内服务器和客户端各训练 1 个 Epoch。
*   **软件框架**：基于 PyTorch 1.13.0 实现。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流脑肿瘤数据集上进行了详尽测试，并额外增加了一个头颈部数据集验证通用性。
*   **消融实验**：非常充分。分别验证了模态特定编码器、部分个性化解码器、LACCA 模块、单锚点 vs 多锚点的有效性。
*   **敏感性分析**：探讨了超参数 $P$（个性化耐心值）、锚点数量 $Nk$、特征层级 $l$ 以及服务器数据量/质量对结果的影响。
*   **客观性**：采用了 Wilcoxon 符号秩检验进行统计显著性分析，确保结果对比的科学性。

#### 6. 主要结论与发现
*   **性能领先**：FedMEPD 在所有客户端（无论缺失何种模态）和服务器（全模态）上的表现均优于现有的 SOTA 方法。
*   **双赢局面**：该框架不仅提升了客户端的个性化分割精度，还通过有效整合异构数据，显著增强了服务器全局模型的鲁棒性。
*   **校准有效性**：通过交叉注意力机制引入全局全模态锚点，能有效弥补客户端因模态缺失造成的信息损失。

#### 7. 优点
*   **架构设计巧妙**：将“完全联邦”与“部分个性化”结合，通过参数更新一致性动态调整模型，比简单的层级拆分更灵活。
*   **隐私保护**：通过传输抽象的类别锚点（Prototypes）而非原始图像特征，降低了隐私泄露风险。
*   **实用性强**：直接针对医疗场景中常见的“模态缺失”痛点，且对服务器端的数据量依赖具有较好的鲁棒性。

#### 8. 不足与局限
*   **测试集规模**：由于按机构划分数据，单个客户端的测试样本量较小（约 20-35 例），可能导致某些统计对比的显著性不够突出。
*   **计算开销**：虽然推理速度较快，但引入交叉注意力机制和多模态编码器会增加客户端的显存占用和计算负担。
*   **类别依赖**：锚点的提取依赖于准确的类别标注，在极端类别不平衡或标注噪声巨大的情况下，校准效果可能会受到影响。

（完）
