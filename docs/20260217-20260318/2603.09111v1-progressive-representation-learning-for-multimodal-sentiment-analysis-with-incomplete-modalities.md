---
title: Progressive Representation Learning for Multimodal Sentiment Analysis with Incomplete Modalities
title_zh: 模态不完整情况下的多模态情感分析渐进式表示学习
authors: "Jindi Bao, Jianjun Qian, Mengkai Yan, Jian Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09111v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 模态不完整情况下的多模态情感分析
tldr: 针对多模态情感分析中常见的模态缺失问题，本文提出了一种渐进式表示学习框架（PRLF）。该框架通过自适应模态可靠性评估器（AMRE）动态确定主导模态，并利用渐进式交互模块（ProgInteract）迭代对齐各模态，有效解决了特征失配和噪声干扰问题，在多个基准数据集上显著提升了模型的鲁棒性。
selection_source: fresh_fetch
motivation: 现实场景中多模态数据常因硬件故障或隐私限制导致模态缺失，现有的融合方法难以处理不完整模态带来的特征失配和噪声干扰。
method: 提出PRLF框架，利用AMRE基于识别置信度和费舍尔信息确定主导模态，并通过ProgInteract模块进行迭代对齐以增强跨模态一致性。
result: 在CMU-MOSI、CMU-MOSEI和SIMS数据集上的实验证明，该方法在模态缺失和完整场景下均优于现有的最先进模型。
conclusion: PRLF通过动态评估模态可靠性并实施渐进式交互，为处理不完整多模态情感分析任务提供了一种高效且鲁棒的解决方案。
---

## 摘要
多模态情感分析 (MSA) 旨在通过整合文本、音频和视觉线索来推断人类情感。然而，现有方法通常依赖于所有模态的完整性，而现实世界的应用经常会遇到噪声、硬件故障或隐私限制，从而导致模态缺失。不完整模态与完整模态之间存在显著的特征失配，直接融合它们甚至可能扭曲完整模态已学习良好的表示。为此，我们提出了 PRLF，一种专为不确定模态缺失条件下的 MSA 设计的渐进式表示学习框架。PRLF 引入了自适应模态可靠性估计器 (AMRE)，该估计器利用识别置信度和 Fisher 信息动态量化每个模态的可靠性，以确定主导模态。此外，渐进式交互 (ProgInteract) 模块将其他模态与主导模态进行迭代对齐，从而在抑制噪声的同时增强跨模态一致性。在 CMU-MOSI、CMU-MOSEI 和 SIMS 上的广泛实验验证了 PRLF 在模态间和模态内缺失场景下均优于现有最先进方法，证明了其鲁棒性和泛化能力。

## Abstract
Multimodal Sentiment Analysis (MSA) seeks to infer human emotions by integrating textual, acoustic, and visual cues. However, existing approaches often rely on all modalities are completeness, whereas real-world applications frequently encounter noise, hardware failures, or privacy restrictions that result in missing modalities. There exists a significant feature misalignment between incomplete and complete modalities, and directly fusing them may even distort the well-learned representations of the intact modalities. To this end, we propose PRLF, a Progressive Representation Learning Framework designed for MSA under uncertain missing-modality conditions. PRLF introduces an Adaptive Modality Reliability Estimator (AMRE), which dynamically quantifies the reliability of each modality using recognition confidence and Fisher information to determine the dominant modality. In addition, the Progressive Interaction (ProgInteract) module iteratively aligns the other modalities with the dominant one, thereby enhancing cross-modal consistency while suppressing noise. Extensive experiments on CMU-MOSI, CMU-MOSEI, and SIMS verify that PRLF outperforms state-of-the-art methods across both inter- and intra-modality missing scenarios, demonstrating its robustness and generalization capability.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **PRLF**（Progressive Representation Learning Framework，渐进式表示学习框架）的方法，旨在解决多模态情感分析（MSA）中因硬件故障、噪声或隐私限制导致的**模态不完整（缺失）**问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态情感分析模型大多假设输入模态是完整的，但在现实场景中，模态缺失（如视频丢帧、音频中断）会导致特征分布在海量空间中产生“相位偏移”（Phase Shift），直接融合不完整模态会干扰甚至破坏完整模态的表示。
*   **研究动机**：作者通过 Fisher 信息矩阵（FIM）发现，关键数据的缺失会导致信息量显著下降。因此，需要一种能够动态评估各模态可靠性，并以“主导模态”引导“辅助模态”进行渐进式对齐的机制，以增强模型在不确定缺失条件下的鲁棒性。

### 2. 核心方法论
PRLF 框架主要由两个核心组件构成：
*   **自适应模态可靠性评估器 (AMRE)**：
    *   **基于置信度的重要性 (CMI)**：利用各单模态分类头的预测置信度来初步评估模态贡献。
    *   **基于 Fisher 信息的重要性 (FIMI)**：通过计算模型参数相对于输出分布的梯度二阶矩（FIM 的迹），量化模态包含的有效信息量。
    *   **动态融合机制**：在训练初期依赖置信度，随着训练进行，逐渐增加 Fisher 信息的权重，从而准确识别出当前样本的“主导模态”。
*   **渐进式交互模块 (ProgInteract)**：
    *   **迭代对齐**：不进行一次性融合，而是通过多个迭代步骤。初期侧重于提取模态内特征，后期加强跨模态交互。
    *   **分解器 (Decomposer)**：将辅助模态特征分解为与主导模态对齐的“投影项”和包含补充信息的“残差项”。
    *   **去噪与对齐**：通过正交性约束（Phase Loss）减少相位偏差，并利用去噪网络抑制残差项中的噪声，使辅助模态逐步向主导模态靠拢。

### 3. 实验设计
*   **数据集**：使用了三个主流基准数据集：**CMU-MOSI**、**CMU-MOSEI**（英文）和 **SIMS**（中文）。
*   **实验场景**：
    1.  **模态间缺失 (Inter-modality)**：随机缺失整个文本、音频或视觉模态。
    2.  **模态内缺失 (Intra-modality)**：以 0.1 到 0.9 的比例随机丢弃模态内的帧特征。
*   **对比方法**：对比了包括 MISA、Self-MM、MMIM 等经典模型，以及专门针对缺失模态的 SOTA 方法如 UMDF、HRLF、CorrKD 和 EMOE。

### 4. 资源与算力
*   **特征提取**：文本使用 GloVe 或 BERT，视觉使用 Facet，音频使用 COVAREP。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。但提到了超参数设置（如迭代步数为 4，平衡系数 $\gamma=0.8$ 等），并指出结果是基于 5 个随机种子的平均值。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的对比实验，涵盖了从 0% 到 90% 的不同缺失率。
*   **消融实验**：非常充分。分别验证了 CMI、FIMI、AMRE 整体、ProgInteract 模块以及不同损失函数（单模态损失、相位损失）的有效性。
*   **迭代步数分析**：专门测试了迭代步数（1-5 步）对性能的影响，证明了“渐进式”学习的必要性。
*   **可视化**：提供了 T-SNE 特征分布可视化和模态权重动态演变图，增强了实验的说服力和可解释性。

### 6. 主要结论与发现
*   **性能领先**：PRLF 在所有数据集和缺失场景下均达到或超过了现有最先进水平，尤其在**高缺失率（如 90%）**下表现出极强的鲁棒性。
*   **Fisher 信息的价值**：实验证明 Fisher 信息比单纯的分类置信度更能反映模态在缺失情况下的真实可靠性。
*   **渐进式优于直接融合**：通过迭代对齐和去噪，可以有效缓解因模态缺失导致的特征空间扭曲。

### 7. 优点与亮点
*   **理论支撑**：引入 Fisher 信息矩阵来量化模态可靠性，具有较强的数学直觉。
*   **动态适应**：AMRE 能够根据样本实时调整模态权重，而非使用全局固定的权重。
*   **抗噪性强**：ProgInteract 中的分解与去噪机制专门针对缺失数据产生的“相位偏移”设计，针对性强。

### 8. 不足与局限
*   **计算开销**：Fisher 信息的计算涉及梯度运算，且 ProgInteract 采用迭代机制（4 步），相比传统的一阶融合模型，推理和训练的计算复杂度会有所增加。
*   **极端缺失依赖**：虽然在 90% 缺失下表现良好，但如果主导模态（通常是文本）完全缺失且辅助模态质量极差时，模型性能仍会面临挑战。
*   **硬件细节缺失**：未提供算力消耗的具体数据，不便于评估其在移动端或实时系统中的部署成本。

（完）
