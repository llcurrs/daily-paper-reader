---
title: "DeLo: Dual Decomposed Low-Rank Experts Collaboration for Continual Missing Modality Learning"
title_zh: DeLo：用于持续缺失模态学习的双重分解低秩专家协作
authors: "Xiwei Liu, Yulong Li, Feilong Tang, Imran Razzak"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01632v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 处理多模态模型中频繁出现的模态缺失问题
tldr: 本研究针对持续缺失模态学习（CMML）中存在的跨任务和跨模态干扰问题，提出了DeLo框架。该框架首次采用双分解低秩专家架构，通过解耦的模态特定因子池动态构建LoRA更新矩阵，并结合跨模态引导路由和任务键存储机制。DeLo在防止灾难性遗忘的同时，有效处理了数据不完整性，在多个基准测试中显著优于现有最先进方法，为现实世界多模态挑战提供了高效的架构设计。
selection_source: fresh_fetch
motivation: 现有的提示微调和朴素LoRA方法在处理持续学习和缺失模态时，容易受到跨任务干扰和模态间梯度竞争的影响。
method: 提出DeLo框架，利用双分解低秩专家架构、跨模态引导路由策略和任务键存储机制来解耦模态信息并防止遗忘。
result: 在多个CMML基准测试上的实验结果表明，DeLo在性能上显著超越了现有的最先进方法。
conclusion: 该研究证明了针对特定架构设计的LoRA专家协作模式在解决复杂多模态持续学习任务中的优越性和必要性。
---

## 摘要
将大语言多模态模型（LMMs）应用于现实场景面临着双重挑战：既要从序列数据流中学习，又要处理频繁的模态不完整性，这一任务被称为持续缺失模态学习（CMML）。然而，现有的 CMML 研究主要依赖于提示微调（prompt tuning），由于其可学习提示在共享嵌入空间中存在跨任务干扰，该技术在处理此任务时面临困难。简单地应用带有模态共享模块的低秩自适应（LoRA）也会因竞争梯度而遭受模态干扰。为此，我们提出了 DeLo，这是首个利用新型双重分解低秩专家架构来解决 CMML 的框架。具体而言，该架构通过分解的 LoRA 专家解决了模态干扰，利用来自解耦的模态特定因子池中的秩一因子动态构建 LoRA 更新矩阵。该专家系统嵌入在一个从结构上防止灾难性遗忘的任务分区框架中，并由两个关键机制支持：用于处理不完整数据的跨模态引导路由策略，以及用于高效、任务无关推理的任务键存储器（Task-Key Memory）。在已有的 CMML 基准测试上进行的大量实验表明，我们的方法显著优于现有最先进的方法。这突显了针对现实世界多模态挑战，采用有原则且具备架构感知能力的 LoRA 设计的价值。

## Abstract
Adapting Large Multimodal Models (LMMs) to real-world scenarios poses the dual challenges of learning from sequential data streams while handling frequent modality incompleteness, a task known as Continual Missing Modality Learning (CMML). However, existing works on CMML have predominantly relied on prompt tuning, a technique that struggles with this task due to cross-task interference between its learnable prompts in their shared embedding space. A naive application of Low-Rank Adaptation (LoRA) with modality-shared module will also suffer modality interference from competing gradients. To this end, we propose DeLo, the first framework to leverage a novel dual-decomposed low-rank expert architecture for CMML. Specifically, this architecture resolves modality interference through decomposed LoRA expert, dynamically composing LoRA update matrix with rank-one factors from disentangled modality-specific factor pools. Embedded within a task-partitioned framework that structurally prevents catastrophic forgetting, this expert system is supported by two key mechanisms: a Cross-Modal Guided Routing strategy to handle incomplete data and a Task-Key Memory for efficient, task-agnostic inference. Extensive experiments on established CMML benchmarks demonstrate that our method significantly outperforms state-of-the-art approaches. This highlights the value of a principled, architecturally-aware LoRA design for real-world multimodal challenges.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeLo: Dual Decomposed Low-Rank Experts Collaboration for Continual Missing Modality Learning》的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**持续学习（Continual Learning）**的过程中，处理多模态数据中频繁出现的**模态缺失（Missing Modality）**问题。这一任务被称为“持续缺失模态学习”（CMML）。
*   **研究动机**：
    *   **现有技术的局限**：目前的 CMML 方法主要依赖提示微调（Prompt Tuning），但提示词在共享嵌入空间中容易产生跨任务干扰。
    *   **朴素 LoRA 的不足**：直接应用 LoRA 会因为不同模态（如纯图像 vs 纯文本）产生的梯度冲突，导致模态间的相互干扰。
    *   **现实需求**：在实际应用中，数据往往以流的形式到达，且由于传感器故障或隐私保护，模态缺失是常态。

### 2. 方法论
DeLo 框架提出了**双重分解低秩专家协作架构**，核心思想是将 LoRA 权重进一步分解并进行模态解耦：
*   **双重分解低秩专家（Decomposed LoRA Experts）**：
    *   不同于标准 LoRA 使用两个低秩矩阵 $A$ 和 $B$，DeLo 将更新矩阵 $\Delta W$ 分解为多个**秩一（Rank-one）因子对**的求和：$\Delta W = \sum_{i=1}^{r} b_i \otimes a_i$。
    *   这些因子从预先定义的“因子池”中动态选择。
*   **模态特定因子池（Modality-Specific Factor Pools）**：
    *   设置独立的视觉因子池 $P_v$ 和文本因子池 $P_t$，从物理架构上隔离不同模态的知识，避免梯度干扰。
*   **跨模态引导路由（Cross-Modal Guided Routing）**：
    *   当某一模态缺失时，利用可用模态的查询信号（Query）作为缺失模态的代理（Proxy），在共享语义空间中引导专家选择。
*   **任务键存储器（Task-Key Memory）**：
    *   利用指数移动平均（EMA）更新每个任务的特征质心（Task-Key），在推理时通过余弦相似度匹配任务 ID，实现**任务无关（Task-agnostic）**的推理。
*   **训练目标**：结合了分类损失 $L_c$、对齐损失 $L_{align}$（增强模态间查询信号的一致性）和一致性损失 $L_{con}$（确保代理信号与真实信号推理结果一致）。

### 3. 实验设计
*   **数据集**：
    *   **UPMC-Food101-CMML**：包含 101 类食物，分为 10 个序列任务。
    *   **MM-IMDb-CMML**：电影海报与剧情简介的多标签分类，分为 5 个序列任务。
*   **基准测试（Benchmarks）**：
    *   **缺失模态方法**：MAP, MSP。
    *   **持续学习方法**：L2P, DualPrompt。
    *   **CMML 专用方法**：RebQ（当前最先进的 SOTA）。
*   **评估指标**：平均性能（AP）和平均遗忘率（FG）。
*   **场景设置**：设置了多种缺失比例 $\eta$（从 10% 到 95%），涵盖了训练和测试阶段均存在模态缺失的复杂情况。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA A100 GPU**。
*   **模型骨干**：采用预训练的 **ViLT-B/32** 作为多模态 Transformer 骨干网络，且在训练过程中保持冻结。
*   **超参数**：Batch size 为 4，使用 AdamW 优化器，学习率 1e-4，采用余弦退火调度。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到使用了线性预热（Warmup）和单卡环境。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了 15 种不同缺失配置的对比实验。
    *   进行了详尽的**消融实验**，验证了模态特定池、引导路由、以及三种损失函数的有效性。
    *   探讨了因子池大小 $E$ 和秩 $r$ 对性能与参数量（从 7.1M 到 46.0M 训练参数）的影响。
*   **充分性与公平性**：实验覆盖了从轻微缺失到极端缺失（95%）的场景，对比对象涵盖了从通用 PEFT 到 CMML 专有模型，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能领先**：DeLo 在所有配置下均显著优于 RebQ 和其他基准方法，尤其在 AP 指标上提升明显。
*   **抗遗忘能力**：通过任务分区架构，DeLo 有效抑制了灾难性遗忘，FG 指标保持在较低水平。
*   **模态解耦的重要性**：消融实验证明，若不区分模态因子池，性能会因模态干扰而大幅下降。
*   **文本主导性**：实验观察到，文本模态的可用性对最终性能的影响通常大于图像模态。

### 7. 优点（亮点）
*   **架构创新**：首次将分解的 LoRA 专家引入 CMML 领域，解决了传统提示微调的容量瓶颈。
*   **动态协作**：通过秩一因子的动态组合，实现了比标准 LoRA 更灵活的参数自适应。
*   **任务无关推理**：引入 Task-Key 机制，解决了持续学习中推理阶段通常需要已知任务 ID 的局限性。

### 8. 不足与局限
*   **多标签挑战**：在 MM-IMDb 这种多标签任务中，遗忘率（FG）相对单标签任务仍然较高，说明复杂语义关联下的持续学习仍有提升空间。
*   **骨干网络依赖**：该方法高度依赖于预训练模型（如 ViLT）本身具备的跨模态对齐能力，若骨干网络未经过充分的多模态预训练，引导路由的效果可能会打折扣。
*   **参数增长**：虽然是参数高效的（PEFT），但随着任务数量增加，存储多个任务的因子池和 Task-Key 仍会带来一定的线性存储开销。

（完）
