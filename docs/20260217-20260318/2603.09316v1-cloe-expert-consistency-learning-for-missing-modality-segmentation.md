---
title: "CLoE: Expert Consistency Learning for Missing Modality Segmentation"
title_zh: CLoE：缺失模态分割的专家一致性学习
authors: "Xinyu Tong, Meihua Zhou, Bowu Fan, Haitao Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09316v1"
tags: ["query:mm-fus"]
score: 10.0
evidence: 缺失模态分割与融合稳定性
tldr: 针对多模态医学图像分割中推理阶段模态缺失导致专家预测不一致及融合不稳定的问题，本文提出CLoE框架。该框架通过双分支专家一致性学习目标，分别在全局和关键前景区域强制专家预测达成一致，并利用轻量化门控网络根据一致性得分重校准特征。实验证明，CLoE在BraTS 2020等数据集上优于现有方法，显著提升了缺失模态下的分割鲁棒性和跨数据集泛化能力。
selection_source: fresh_fetch
motivation: 解决推理时模态缺失导致的专家预测分歧及小目标结构融合不稳定的挑战。
method: 提出包含全局和区域一致性约束的双分支专家一致性学习目标，并结合门控网络进行可靠性感知特征重校准。
result: 在BraTS 2020和MSD Prostate数据集上，CLoE在不完整模态分割任务中显著优于现有SOTA方法。
conclusion: CLoE通过强化专家间的一致性，有效提升了多模态分割模型在模态缺失场景下的鲁棒性和泛化性。
---

## 摘要
多模态医学图像分割在推理阶段经常面临模态缺失的问题，这会导致模态专家之间产生分歧，并使融合过程变得不稳定，特别是在细小的前景结构上。我们提出了专家一致性学习（CLoE），这是一个针对缺失模态分割的一致性驱动框架，在所有模态均可用时仍能保持强大的性能。CLoE 将鲁棒性建模为决策层面的专家一致性控制，并引入了双分支专家一致性学习目标。模态专家一致性（Modality Expert Consistency）强制专家预测之间达成全局一致，以减少部分输入下的个案漂移；而区域专家一致性（Region Expert Consistency）则强调在临床关键前景区域的一致性，以避免受背景主导的正规化影响。我们进一步利用轻量级门控网络将一致性分数映射为模态可靠性权重，从而在融合前实现可靠性感知的特征重校准。在 BraTS 2020 和 MSD Prostate 数据集上的广泛实验表明，CLoE 在不完整多模态分割方面优于现有先进方法，同时展现出强大的跨数据集泛化能力，并提高了在临床关键结构上的鲁棒性。

## Abstract
Multimodal medical image segmentation often faces missing modalities at inference, which induces disagreement among modality experts and makes fusion unstable, particularly on small foreground structures. We propose Consistency Learning of Experts (CLoE), a consistency-driven framework for missing-modality segmentation that preserves strong performance when all modalities are available. CLoE formulates robustness as decision-level expert consistency control and introduces a dual-branch Expert Consistency Learning objective. Modality Expert Consistency enforces global agreement among expert predictions to reduce case-wise drift under partial inputs, while Region Expert Consistency emphasizes agreement on clinically critical foreground regions to avoid background-dominated regularization. We further map consistency scores to modality reliability weights using a lightweight gating network, enabling reliability-aware feature recalibration before fusion. Extensive experiments on BraTS 2020 and MSD Prostate demonstrate that CLoE outperforms state-of-the-art methods in incomplete multimodal segmentation, while exhibiting strong cross-dataset generalization and improving robustness on clinically critical structures.

---

## 论文详细总结（自动生成）

### 论文总结：CLoE —— 缺失模态分割的专家一致性学习

#### 1. 核心问题与研究动机
在临床多模态 MRI 诊断中，由于扫描协议差异、图像质量不佳或患者耐受性等原因，推理阶段经常出现模态缺失（Missing Modality）的情况。现有的多模态分割方法在面对不完整输入时，往往面临以下挑战：
*   **决策不一致性**：不同模态的专家网络（Expert）对同一区域的预测可能存在分歧，简单的融合会放大这种误差。
*   **背景主导问题**：在体积数据中，背景像素远多于病灶，全局一致性约束容易被背景淹没，导致模型忽略细小但关键的前景结构（如肿瘤核心）。
*   **融合不稳定性**：缺乏一种动态机制来评估当前可用模态的可靠性。

#### 2. 方法论
论文提出了 **CLoE (Consistency Learning of Experts)** 框架，核心思想是将鲁棒性建模为“决策层面的专家一致性控制”。
*   **双分支专家一致性学习 (ECL)**：
    *   **模态专家一致性 (MEC)**：计算不同模态专家预测图之间的全局余弦相似度，强制各专家在全图分布上达成共识，减少个案漂移。
    *   **区域专家一致性 (REC)**：利用浅层特征生成概率区域图（Region Map），对预测图进行加权，使模型重点关注前景区域的一致性，避免背景干扰。
*   **一致性驱动的动态门控 (Dynamic Gating)**：
    *   将 MEC 和 REC 计算得到的一致性得分输入一个轻量化门控网络，映射为各模态的**可靠性权重**。
    *   在特征融合前，利用该权重对各模态特征进行重校准，抑制偏离共识（不可靠）的模态。
*   **学习目标**：总损失函数结合了融合分割损失（$L_{seg}$）、鲁棒专家一致性损失（$L_{ECL}$）以及用于特征解耦的对比表征损失（$L_{contrast}$）。

#### 3. 实验设计
*   **数据集**：
    *   **BraTS 2020**：脑肿瘤分割，包含 T1、T1c、T2、FLAIR 四种模态，涉及 15 种缺失组合。
    *   **MSD Prostate (Task 05)**：前列腺分割，包含 T2 和 ADC 两种模态，用于验证小样本和跨任务的泛化性。
*   **对比方法 (Benchmarks)**：
    *   传统/经典方法：HeMIS, RobustSeg, RFNet。
    *   最新 SOTA：M³AE（大模型预训练）、DC-Seg（解耦对比学习）。
    *   通用大模型：MedSAM（带框提示）。

#### 4. 资源与算力
*   **优化器**：Adam，初始学习率 0.0002，权重衰减 0.0001。
*   **训练配置**：训练 500 个 epoch，batch size 为 1，输入 patch 大小为 $112 \times 112 \times 112$。
*   **硬件说明**：论文**未明确指出**具体的 GPU 型号和数量，也未提及具体的训练总时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 BraTS 2020 上详尽测试了全部 15 种模态组合，涵盖了从单模态到全模态的所有场景。
*   **消融实验**：针对 MEC、REC、门控网络和权重融合四个核心组件进行了消融分析，验证了各模块的贡献。
*   **客观性**：通过在两个完全不同的解剖部位（大脑和前列腺）进行实验，证明了方法的通用性。对比实验均引用了前序工作的公开结果，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能领先**：CLoE 在 BraTS 2020 的所有缺失组合中均表现优异，全模态平均 Dice 达到 88.09% (WT)，显著优于 DC-Seg 和 M³AE。
*   **小结构鲁棒性**：在肿瘤核心 (TC) 和增强肿瘤 (ET) 等细小结构上，CLoE 的提升尤为明显，证明了 REC 模块的有效性。
*   **可靠性感知**：一致性得分能有效转化为模态权重，使模型在推理时能自动“信任”更可靠的模态。
*   **超越通用大模型**：实验显示，即使有 Bounding Box 提示，MedSAM 在此类复杂多模态任务上的表现仍逊于专门设计的 CLoE。

#### 7. 优点
*   **创新的一致性视角**：将缺失模态问题转化为专家间的共识问题，提供了清晰的决策逻辑。
*   **区域感知能力**：通过 REC 解决了医学图像分割中常见的类别不平衡（背景主导）对一致性学习的负面影响。
*   **轻量化推理**：门控网络非常轻量，在显著提升鲁棒性的同时，几乎不增加推理阶段的计算负担。

#### 8. 不足与局限
*   **超参数敏感性**：模型引入了多个超参数（如 $\alpha, \beta, \eta, \lambda_{rec}$），在不同数据集上可能需要精细调优。
*   **计算开销**：虽然推理轻量，但训练阶段需要运行多个专家分支并计算成对的一致性，训练资源消耗可能高于标准 U-Net。
*   **极端缺失场景**：当所有可用模态质量均极差时，基于“共识”的机制可能会失效（即专家集体出错）。

（完）
