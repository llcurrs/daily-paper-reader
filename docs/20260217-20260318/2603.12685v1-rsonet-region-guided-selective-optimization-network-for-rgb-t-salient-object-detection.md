---
title: "RSONet: Region-guided Selective Optimization Network for RGB-T Salient Object Detection"
title_zh: RSONet：用于 RGB-T 显著目标检测的区域引导选择性优化网络
authors: "Bin Wan, Runmin Cong, Xiaofei Zhou, Hao Fang, Chengtao Lv, Sam Kwong"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12685v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 用于显著性目标检测的红外与可见光图像融合
tldr: 针对RGB与热红外图像中显著区域不一致的问题，本文提出了一种区域引导选择性优化网络（RSONet）。该网络分为区域引导和显著性生成两个阶段：首先通过并行分支生成引导图并计算模态相似度；随后利用选择性优化模块（SO）动态融合特征，并结合密集细节增强（DDE）和相互交互语义（MIS）模块优化细节与定位。该方法有效缓解了模态间分布差异的影响，显著提升了检测精度。
selection_source: fresh_fetch
motivation: 旨在解决RGB和热红外模态之间显著目标分布不一致导致的检测性能下降问题。
method: 提出一种双阶段架构，利用区域引导计算相似度，并通过选择性优化、密集细节增强及语义交互模块进行特征融合与优化。
result: 在RGB-T数据集上的实验结果显示，RSONet在性能上优于27种当前最先进的显著性目标检测方法。
conclusion: RSONet通过区域引导的选择性优化策略，成功实现了多模态特征的高效融合，为RGB-T显著性检测提供了高质量的解决方案。
---

## 摘要
本文关注 RGB 图像与热红外图像之间显著区域的不一致性。为了解决这一问题，我们提出了用于 RGB-T 显著目标检测的区域引导选择性优化网络（RSONet），该网络由区域引导阶段和显著性生成阶段组成。在区域引导阶段，设计了三个具有相同编码器-解码器结构的并行分支，并配备了上下文交互（CI）模块和空间感知融合（SF）模块，用于生成引导图并据此计算相似度得分。随后，在显著性生成阶段，选择性优化（SO）模块根据先前获得的相似度值融合 RGB 和热红外特征，以减轻两种模态之间显著目标分布不一致的影响。此后，为了生成高质量的检测结果，将采用多重密集连接和视觉状态空间块的密集细节增强（DDE）模块应用于低层特征，以优化细节信息。此外，在深层特征中加入了相互交互语义（MIS）模块，通过相互融合策略挖掘位置线索。我们在 RGB-T 数据集上进行了广泛的实验，结果表明所提出的 RSONet 与 27 种最先进的 SOD 方法相比具有竞争力的性能。

## Abstract
This paper focuses on the inconsistency in salient regions between RGB and thermal images. To address this issue, we propose the Region-guided Selective Optimization Network for RGB-T Salient Object Detection, which consists of the region guidance stage and saliency generation stage. In the region guidance stage, three parallel branches with same encoder-decoder structure equipped with the context interaction (CI) module and spatial-aware fusion (SF) module are designed to generate the guidance maps which are leveraged to calculate similarity scores. Then, in the saliency generation stage, the selective optimization (SO) module fuses RGB and thermal features based on the previously obtained similarity values to mitigate the impact of inconsistent distribution of salient targets between the two modalities. After that, to generate high-quality detection result, the dense detail enhancement (DDE) module which adopts the multiple dense connections and visual state space blocks is applied to low-level features for optimizing the detail information. In addition, the mutual interaction semantic (MIS) module is placed in the high-level features to dig the location cues by the mutual fusion strategy. We conduct extensive experiments on the RGB-T dataset, and the results demonstrate that the proposed RSONet achieves competitive performance against 27 state-of-the-art SOD methods.

---

## 论文详细总结（自动生成）

以下是对论文《RSONet: Region-guided Selective Optimization Network for RGB-T Salient Object Detection》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：RGB 图像与热红外（Thermal）图像在显著区域表现上存在**不一致性**。在复杂场景下（如光照不足、背景过热或物体与背景温差极小），单一模态可能失效，而传统的简单融合策略（如加法、拼接或注意力机制）往往假设两模态同等重要，这会导致引入大量无关噪声，损害检测精度。
*   **研究背景**：RGB-T 显著目标检测（SOD）旨在结合可见光和热红外信息。本文动机在于如何动态地识别并利用更可靠的模态信息，以应对模态间的分布差异。

### 2. 方法论
论文提出了 **RSONet**，分为两个主要阶段：
*   **区域引导阶段 (Region Guidance Stage)**：
    *   **三分支架构**：设计了三个并行的编码器-解码器分支，分别输入 RGB、Thermal 以及两者的加和（RT）。
    *   **上下文交互 (CI) 模块**：针对不同层级的特征，使用不同卷积核大小的分支来捕捉多尺度上下文信息。
    *   **空间感知融合 (SF) 模块**：利用空间注意力机制优化特征，生成三个引导图（$G_R, G_T, G_{RT}$）。
    *   **相似度计算**：将 $G_R$ 和 $G_T$ 分别与包含丰富信息的 $G_{RT}$ 进行相似度对比，确定哪个模态在当前场景中更具主导性。
*   **显著性生成阶段 (Saliency Generation Stage)**：
    *   **选择性优化 (SO) 模块**：根据第一阶段的相似度结果，让更可靠的模态特征去引导和优化另一模态特征，减少噪声干扰。
    *   **密集细节增强 (DDE) 模块**：应用于低层特征，结合了多重密集连接和**视觉状态空间 (VSS) 块**（借鉴了 Mamba 的思想），以提取复杂的空间结构和细节。
    *   **相互交互语义 (MIS) 模块**：应用于高层特征，通过不同扩张率的卷积和相互融合策略，精准定位目标位置。
*   **损失函数**：结合了二元交叉熵（BCE）、边界交并比（IOU）和 F-measure（FM）损失进行深度监督。

### 3. 实验设计
*   **数据集**：在三个主流 RGB-T 数据集上进行评估：**VT5000**（大规模、全天候场景）、**VT1000** 和 **VT821**。
*   **评价指标**：平均绝对误差 ($M$)、F-measure ($F_\beta$)、E-measure ($E_\xi$) 和 Structure-measure ($S_\alpha$)。
*   **对比方法**：与 **27 种** 最先进（SOTA）的方法进行对比，包括传统的 CNN 模型（如 MIDD, CGFNet）和基于 Transformer 的模型（如 SwinNet, CAVER, Samba, SAMSOD）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 4080 GPU**。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：输入图像缩放至 384×384，使用 RMSprop 优化器，学习率设为 1e-4，动量为 0.9。
*   **运行速度**：检测速度约为 **9.4 FPS**（由于采用了双阶段设计和复杂的模块，速度较一些轻量化模型稍慢）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（3个数据集 × 27个对比方法）和定性可视化分析。
*   **消融实验**：针对 SO 模块、DDE 模块、MIS 模块以及不同的骨干网络（ResNet 系列、SAM、DINO 等）进行了详细的消融研究。
*   **充分性评价**：实验设计非常充分且客观。通过对比不同融合策略（加法、乘法、拼接、像素级门控）证明了“选择性优化”的优越性。同时，对失败案例的展示也体现了研究的诚实性和客观性。

### 6. 主要结论与发现
*   **性能领先**：RSONet 在 VT5000 数据集上达到了最优性能，在 VT1000 和 VT821 上也极具竞争力。
*   **模态主导性**：实验证明，动态选择更可靠的模态进行引导，比盲目融合双模态特征更能有效抑制噪声。
*   **结构优化**：引入 VSS 块（Mamba 结构）能显著提升模型对不规则物体边缘和细节的建模能力。

### 7. 优点与亮点
*   **创新的引导机制**：通过预先计算模态相似度来决定融合策略，解决了 RGB-T 领域核心的“模态不一致”痛点。
*   **先进的模块设计**：DDE 模块引入了最新的视觉状态空间（VSS）技术，MIS 模块强化了高层语义的相互作用。
*   **鲁棒性强**：在低光照、热交叉（Thermal Crossover）等极端挑战场景下表现稳健。

### 8. 不足与局限
*   **计算开销与速度**：由于采用了双阶段架构和三分支引导，模型的参数量（88M）和计算量（143.8G FLOPs）相对较高，9.4 FPS 的速度难以满足超实时应用需求。
*   **极端场景失效**：当显著目标极小或极细长时，或者当 RGB 和 Thermal 模态同时处于极低质量时，模型仍可能出现漏检或误检。
*   **大模型适配**：消融实验显示，直接替换更强大的骨干网络（如 SAM 或 DINO）反而导致性能下降，说明模型对特定特征提取器有依赖，需要专门的适配器。

（完）
