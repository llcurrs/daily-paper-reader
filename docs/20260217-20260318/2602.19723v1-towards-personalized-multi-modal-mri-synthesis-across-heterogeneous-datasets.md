---
title: Towards Personalized Multi-Modal MRI Synthesis across Heterogeneous Datasets
title_zh: 迈向跨异构数据集的个性化多模态 MRI 合成
authors: "Yue Zhang, Zhizheng Zhuo, Siyao Xu, Shan Lv, Zhaoxi Liu, Jun Qiu, Qiuli Wang, Yaou Liu, S. Kevin Zhou"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19723v1"
tags: ["query:mm-fus"]
score: 9.0
evidence: 多模态MRI中缺失模态的合成
tldr: 针对多模态MRI缺失合成在跨异构数据集时泛化性差的问题，本文提出PMM-Synth框架。该框架通过个性化特征调制、模态一致性批调度和选择性监督损失，实现了在不同疾病类型和强度分布的数据集上的联合训练与高效合成。实验证明，该方法在多种合成任务中优于现有SOTA模型，并能有效支持下游临床诊断任务。
selection_source: fresh_fetch
motivation: 现有的多模态MRI合成模型通常局限于单一数据集，难以应对临床应用中不同数据集间的分布偏移和模态不一致问题。
method: 提出PMM-Synth框架，包含个性化特征调制模块、模态一致性批调度器以及选择性监督损失函数。
result: 在四个临床数据集上的评估显示，PMM-Synth在PSNR和SSIM指标上均优于现有最先进方法，且能更好地保留解剖结构和病理细节。
conclusion: PMM-Synth通过跨数据集的个性化合成，为真实世界中模态缺失情况下的可靠诊断提供了有效的技术支持。
---

## 摘要
在多模态磁共振成像（MRI）中合成缺失模态对于确保诊断完整性至关重要，特别是在由于时间限制、运动伪影和患者耐受性而无法进行完整采集的情况下。最近的统一合成模型通过适应各种输入输出配置，实现了灵活的合成任务。然而，它们的训练和评估通常局限于单一数据集，限制了它们在不同临床数据集上的泛化能力，并阻碍了实际部署。为了解决这一局限性，我们提出了 PMM-Synth，这是一个个性化的 MRI 合成框架，它不仅支持各种合成任务，而且能有效地在异构数据集之间进行泛化。PMM-Synth 在多个多模态 MRI 数据集上进行联合训练，这些数据集在模态覆盖范围、疾病类型和强度分布方面存在差异。它通过三项核心创新实现了跨数据集泛化：一个个性化特征调制模块，根据数据集标识符动态调整特征表示，以减轻分布偏移的影响；一个模态一致批调度器，在模态不一致的情况下促进稳定高效的批训练；以及一个选择性监督损失，以确保在地面真值模态部分缺失时进行有效学习。在四个临床多模态 MRI 数据集上的评估结果表明，PMM-Synth 在一对一和多对一合成任务中均持续优于现有最先进方法，获得了更高的 PSNR 和 SSIM 分数。定性结果进一步证明了其在保留解剖结构和病理细节方面的改进。此外，下游肿瘤分割和放射学报告研究表明，PMM-Synth 具有在现实世界模态缺失场景下支持可靠诊断的潜力。

## Abstract
Synthesizing missing modalities in multi-modal magnetic resonance imaging (MRI) is vital for ensuring diagnostic completeness, particularly when full acquisitions are infeasible due to time constraints, motion artifacts, and patient tolerance. Recent unified synthesis models have enabled flexible synthesis tasks by accommodating various input-output configurations. However, their training and evaluation are typically restricted to a single dataset, limiting their generalizability across diverse clinical datasets and impeding practical deployment. To address this limitation, we propose PMM-Synth, a personalized MRI synthesis framework that not only supports various synthesis tasks but also generalizes effectively across heterogeneous datasets. PMM-Synth is jointly trained on multiple multi-modal MRI datasets that differ in modality coverage, disease types, and intensity distributions. It achieves cross-dataset generalization through three core innovations: a Personalized Feature Modulation module that dynamically adapts feature representations based on dataset identifier to mitigate the impact of distributional shifts; a Modality-Consistent Batch Scheduler that facilitates stable and efficient batch training under inconsistent modality conditions; and a selective supervision loss to ensure effective learning when ground truth modalities are partially missing. Evaluated on four clinical multi-modal MRI datasets, PMM-Synth consistently outperforms state-of-the-art methods in both one-to-one and many-to-one synthesis tasks, achieving superior PSNR and SSIM scores. Qualitative results further demonstrate improved preservation of anatomical structures and pathological details. Additionally, downstream tumor segmentation and radiological reporting studies suggest that PMM-Synth holds potential for supporting reliable diagnosis under real-world modality-missing scenarios.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：PMM-Synth 跨异构数据集个性化多模态 MRI 合成

#### 1. 核心问题与整体含义（研究动机和背景）
在临床实践中，多模态 MRI（如 T1, T2, FLAIR 等）对于神经系统疾病的诊断至关重要。然而，受限于扫描时间、患者耐受性或运动伪影，往往难以获取完整的模态序列。
*   **现有挑战**：目前的合成模型大多在单一、受控的数据集（如 BraTS）上训练，难以应对真实世界中的**异构性**。这种异构性体现在：
    1.  **分布偏移**：不同医院的设备、协议导致同一模态的强度分布和视觉特征差异显著。
    2.  **模态不一致**：不同数据集覆盖的模态组合不同，甚至同一数据集内不同病例的可用模态也存在缺失。
*   **研究目标**：开发一个既能支持灵活输入输出配置（任务通用性），又能跨多个异构临床数据集有效泛化（数据集通用性）的统一合成框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PMM-Synth** 框架，其核心思想是通过“个性化”机制来吸收多源数据的共性并保留其特性。
*   **个性化特征调制 (PFM)**：
    *   引入数据集标识符（ID），通过正弦位置编码和多层感知机（MLP）生成数据集嵌入（Embedding）。
    *   在生成器的卷积块中应用仿射变换（Scale 和 Shift），动态调整特征图，使模型能够根据数据来源自适应地匹配特定的强度分布。
*   **模态一致性批调度器 (MCBS)**：
    *   针对多流网络在 Batch 训练时要求输入模态一致的限制，MCBS 将具有相同可用模态和数据集 ID 的样本归为一组。
    *   通过组内填充和随机打乱，确保每个 Mini-batch 内的样本模态配置完全一致，从而支持大 Batch 训练。
*   **选择性监督损失 (Selective Supervision Loss)**：
    *   在训练过程中，仅对那些在 Ground Truth 中实际存在的模态计算 L1 损失和对抗损失。这种机制允许模型在标签不完整的异构数据集上进行联合训练。

#### 3. 实验设计
*   **数据集**：使用了 4 个异构数据集，总计 1753 个病例。
    *   **TTG (私有)**：胶质瘤，6 模态（T1, T2, T1C, FLAIR, DWI, ADC），模态完整。
    *   **TTI (私有)**：免疫相关疾病，4 模态，模态不一致。
    *   **BraTS (公开)**：胶质瘤，4 模态，模态完整。
    *   **ISLES (公开)**：中风，3 模态（FLAIR, DWI, ADC），模态完整。
*   **对比方法 (Benchmark)**：
    *   **单数据集模型**：MM-GAN, MM-Synth, ResViT（每个数据集单独训练一个模型）。
    *   **多数据集/联邦学习变体**：pFLSynth, FTN（适配为集中式训练）。
*   **评估场景**：涵盖 1-to-1（如 T1→T1C）和 Many-to-1（如 T1+T2→FLAIR）合成任务。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU**。
*   **训练参数**：训练 200 个 epoch，初始学习率为 $2 \times 10^{-4}$，50 个 epoch 后线性衰减。
*   **效率提升**：引入 MCBS 后，单 epoch 训练时间从 8724 秒（Batch Size=1）显著降至 2078 秒（Batch Size=16），大幅提升了算力利用率。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同疾病领域的数据集上进行了广泛测试，涵盖了从基础的图像质量指标（PSNR/SSIM）到复杂的下游临床任务。
*   **消融实验**：分别验证了 PFM 模块对处理分布偏移的有效性，以及 MCBS 对训练稳定性和效率的贡献。
*   **客观性与公平性**：
    *   采用了**双盲放射学报告研究**：邀请两位资深放射科医生对真实序列与合成序列（1-to-5, 2-to-4, 3-to-3 场景）进行诊断报告编写，并计算文本相似度和诊断准确率。这种临床视角的评估比单纯的像素指标更具说服力。

#### 6. 主要结论与发现
*   **性能领先**：PMM-Synth 在所有数据集和合成任务中均取得了最优的 PSNR 和 SSIM 结果。
*   **细节保留**：相比对比方法，PMM-Synth 能产生更清晰的肿瘤增强效果和更锐利的解剖边界，有效避免了多数据集训练常见的“平均化”模糊问题。
*   **临床价值**：
    *   **下游分割**：使用合成模态辅助分割，显著提升了胶质瘤和中风病灶的 Dice 分数。
    *   **诊断一致性**：放射科医生的盲测显示，即使在仅有 T1 输入（1-to-5 合成）的极端情况下，合成图像仍能支持准确的肿瘤分级和初步诊断。

#### 7. 优点（亮点）
*   **架构创新**：PFM 模块以极小的参数代价实现了跨数据集的个性化适配。
*   **训练策略**：MCBS 解决了异构数据联合训练时的工程瓶颈（Batch 组建问题）。
*   **临床深度**：不仅关注图像指标，还通过医生盲测和下游任务验证了合成图像的“诊断可用性”。

#### 8. 不足与局限
*   **持续学习能力**：目前模型仍属于静态联合训练。在实际临床中，新数据集会不断加入，模型尚不支持在不遗忘旧知识的情况下进行增量学习（Continual Learning）。
*   **模态扩展性**：虽然支持 6 模态，但对于更高级的序列（如 DTI, PWI）或不同场强（1.5T vs 3T）的精细建模仍有待进一步探索。
*   **偏差风险**：私有数据集（TTG/TTI）占比重较大，虽然增加了真实性，但其他研究者难以完全复现其在特定私有分布上的表现。

（完）
